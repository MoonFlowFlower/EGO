# Legacy Pre-Operator Mainline Archive

The bulky pre-operator `EgoCore`, `OpenEmotion`, and `ego_desktop_lab` code was removed from the current main working tree.

Current pointers:

- Archive pointer: `legacy-pre-operator-mainline-before-purge`
- Manifest: `artifacts/archive/legacy_pre_operator_mainline_manifest.json`
- Algorithm inventory: `docs/archive/LEGACY_ALGORITHM_INVENTORY.md`
- Working-tree tombstone: `legacy/ego-pre-handmade-mainline/ARCHIVED_POINTER.md`

Rules:

- Do not treat archived legacy code as runtime owner, fallback runtime, or default task path.
- Do not restore keyword-first semantic routing, template fallback, old proactive main-chain behavior, or old lab shell as current runtime.
- Any reuse requires a new Stage Card and evidence gate.
- Current authority remains `docs/PROGRAM_STATE_UNIFIED.yaml`.

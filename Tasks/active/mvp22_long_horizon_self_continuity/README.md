# MVP22 Long-Horizon Self-Continuity / Realized Consequence Persistence 执行包

```yaml
task_id: L3-20260405-MVP22-LHSC
created_at: "2026-04-05T00:00:00Z"
owner: "Codex"
layer: 3
type: dual_repo
repos: [EgoCore, OpenEmotion]
status: authority_frozen
parent_authority: "Tasks/MVS_task_plan.md"
phase_authority: "Tasks/MVP22_task_plan.md"
predecessor: "WP16/MVP21"
same_subject_line: true
not_parallel_track: true
scope: "WP17 / MVP22 Long-Horizon Self-Continuity / Realized Consequence Persistence"
claim_ceiling: "T00 complete only"
```

---

## 真实目标

在不放开 authority 边界的前提下，把 `WP17/MVP22` 的 formal owner target 冻结到 `OpenEmotion/openemotion/self_continuity/*`，并把 `WP16` 的 realized loop outputs、`WP14` 的 integrated tendency surfaces、以及 `WP8~WP16` 的 frozen read surfaces 全部收成可执行 authority package。当前只完成 authority freeze / task package readiness，不进入 owner/runtime 实现。

## 当前正式 owner target

- `OpenEmotion/openemotion/self_continuity/*`

## 当前正式主链 target

`initiative_realization / selfhood_integration / upstream frozen continuity surfaces -> self_continuity owner / bounded persistence proposals -> proto_self_runtime / proto_self_adapter / proto_self_v2 -> governed continuity review and self_continuity_writeback_candidate path`

## 当前锁定口径

- `MVP22` 是 `WP17`，接在 `WP16/MVP21` 后，不是新的主体线
- phase 1 只冻结 long-horizon self-continuity / realized consequence persistence semantics、formal intake/output 与 `WP8~WP16` boundary freeze
- `EgoCore` 继续保留 runtime / session / task / tool / transport、outward response contract、ask / wait / block / escalate、trace / replay / gate / audit / maintenance ledger、real-world execution / risk adjudication 的最终权威
- `WP8~WP16` 都保持 maintenance / frozen upstreams；`WP17` 只能读取冻结 surfaces，不能重写 upstream owner state
- `WP17` 当前不放开 live autonomy、OpenEmotion direct reply authority、tool authority、broader transport claims、或任何 open-world free-form autonomy

## 当前范围

- authority / contract freeze
- formal self_continuity owner package target
- bounded proto-self self_continuity contract target
- EgoCore runtime self_continuity bridge target
- `WP8~WP16` upstream boundary freeze
- legacy / compat / roadmap register
- subagent-ready task decomposition

## 当前状态

- 执行包状态：`authority_frozen`
- authority freeze：`completed`
- `T00_AUTHORITY_FREEZE`：`completed`
- `T10` formal owner：`pending`
- `T20` proto_self_v2 contract：`pending`
- `T30` EgoCore runtime bridge：`pending`
- `T40` legacy demotion / compat map：`pending`
- `T50` causal validation：`pending`
- `T60` single controlled observation：`pending`
- `T70` batch controlled observation / aggregate：`pending`
- `T80` closeout / QA baseline：`pending`
- `T90` subagent assignment sync：`completed`
- 主链接线：`not_started`
- 启用状态：`authority_package_only`
- 当前 blocker：`none`
- 当前最小动作：`T10_FORMAL_OWNER_PACKAGE`

## 当前已证实内容

- `Tasks/MVP22_task_plan.md` 已冻结 `WP17` 的 formal owner、authority source、IO contract、`WP8~WP16` boundary freeze 与 locked non-releases
- `contracts/SELF_CONTINUITY_CAPABILITY_OWNERSHIP.md` 已锁定 `WP17` 只拥有 long-horizon self-continuity / realized consequence persistence semantics，不拥有 upstream owner state
- `contracts/SELF_CONTINUITY_IO_CONTRACT.md` 已锁定：
  - formal intake 只来自 `initiative_realization`、`selfhood_integration`、`self_model`、`reflective_self`、`developmental_self`、`social_self`、`embodied_self`、maintenance、recent delivery outcome 与 restart/restore observation context
  - outputs 只允许 proposal-only / advisory / gated writeback candidates
- `LEGACY_REFERENCE_REGISTER.md` 已把旧路线图与上游 authority 材料登记为 technical reference / upstream read-only reminder
- `SUBAGENT_ASSIGNMENT.md` 与 `cards/T00..T90` 已把 worker mapping、write scope 与后续实现顺序收成可执行 package

## 当前不做

- 不宣称实现完成
- 不宣称已接当前 runtime 主链
- 不宣称 `E4/E5`
- 不宣称 observation started
- 不宣称 maintenance mode
- 不 reopen `WP16`
- 不创建 `WP18` docs

## 执行入口

- authority：`Tasks/MVP22_task_plan.md`
- status：`STATUS.md`
- legacy register：`LEGACY_REFERENCE_REGISTER.md`
- contracts：`contracts/`
- task cards：`cards/`
- subagent assignment：`SUBAGENT_ASSIGNMENT.md`

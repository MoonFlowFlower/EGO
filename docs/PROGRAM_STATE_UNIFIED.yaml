program:
  version: 1
  north_star: "Keep EgoOperator as the sole active default runtime while building an explicitly launched, disabled-by-default product-clock playground under an exact six-file authority boundary; preserve all closed K0/Card2 science records and assign the playground zero science weight."
  current_phase: "visible_life_proxy_v0_phase_a_banked__phase_b_authorized_not_started"
  current_layer: "engineering evidence-governance + bounded product-clock implementation authorization"
  highest_evidence_level: "E3"
  verification_level: "V3"
  mainline_connected: true
  enabled: true
  real_trigger_evidence:
    - evidence_id: "ego_operator_rename_docs_safety_v1_local_transition"
      note: "Repo-level docs, route views, runtime path naming, and reader-safety banners record `EgoOperator` as the current operator-first route name, formerly `Ego_handmade`."
    - evidence_id: "ego_operator_mainline_demotion_v1_local_transition"
      note: "Repo-level docs, route views, and directory layout record `EgoOperator` as the default operator-first transition lane while preserving legacy projects as reference/fallback material."
    - evidence_id: "ego_operator_operator_runtime_contract_local_candidate_pass"
      note: "Local EgoOperator tests previously passed for runtime modes, transaction approval, memory, permission gates, comparison harness, and human-trial scaffolding."
    - evidence_id: "ego_operator_human_operator_trial_v2_real_provider_scripted_run"
      note: "A fresh scripted v2 human-operator trial ran with a real OpenRouter provider and recorded 18/18 observations at `EgoOperator/artifacts/human_operator_trial/v2_latest/`; this is provider-scripted review evidence only, not a human-observation pass."
  current_focus:
    - "The 8692 K0 Foundation -> VirtualCat-derived adapter -> EgoDesktop route is additively superseded before M1. M1, M2, and M3 were not started and are cancelled by supersession; historical route prose is not current authority."
    - "EgoDesktop is archived as a legacy LLM/UI reference only. It is not an active route, successor dependency, runtime target, or runtime authority; this authority archive does not move, delete, or rewrite EgoDesktop source."
    - "K0 Foundation bounded engineering evidence remains `BANKED_ACCEPTED_BOUNDED`, disabled, non-mainline, and without runtime authority. No science verdict or mechanism attribution is created or upgraded in Ego."
    - "The committed ITL Card 2 route is terminally closed as `ADMISSION_INVALID_PATH_POLICY`; its state and closure are transcribed field-by-field and retain all implementation/runtime/science authorizations false. A separate Ego-owned action authorizes only the exact six-file visible-life product playground; it inherits no Card2/K0 science authority and has science_weight=0."
    - "Treat `EgoOperator` as the active default operator-first implementation lane and current execution owner."
    - "Keep archived legacy `EgoCore / OpenEmotion / ego_desktop_lab` reachable only through `legacy/ego-pre-handmade-mainline/ARCHIVED_POINTER.md`, `docs/archive/LEGACY_ALGORITHM_INVENTORY.md`, `artifacts/archive/legacy_pre_operator_mainline_manifest.json`, and git tag `legacy-pre-operator-mainline-before-purge`; no archived legacy code has runtime authority, default path, or task-routing authority."
    - "Preserve `EgoOperator`'s core path: `user text -> LLM understanding -> proposal/plan -> runtime gate -> trace`, and do not reintroduce keyword-first semantic routing or template fallback as the default entry."
    - "Future work may delete old templates, rewrite the main loop, sacrifice legacy interfaces, and prioritize experience over compatibility, but major route/state/permission/memory changes still require a Stage Card."
    - "Use human-observable operator samples as part of acceptance when experience is the target; tests alone are not sufficient proof of natural interaction quality."
    - "Keep all claims bounded to local candidate / transition evidence until real user operation proves stable behavior."
    - "R-track mechanism rewrite state machine remains closed: R0/R1/R3 stay at engineering-only/default-off ceilings and R2 stays negative; do not reopen or reinterpret those rows from K0."
    - "K0 dual-track remains a separate disabled non-mainline workstream. Ego preserves the pinned ITL operator decision to close the old K0 science-attribution route without science adjudication. The verifier family is terminally closed invalid with no further repair, and independent validator acceptance is UNAVAILABLE. Foundation remains BANKED_ACCEPTED_BOUNDED at the existing E3/V3 engineering-governance ceiling with enabled=false, mainline_connected=false, runtime_authority=none, and runtime trigger evidence absent. H0 is NOT_TESTED; the old Reference Kernel/K0-R, H1, Freeze, and Formal are BLOCKED_NOT_TESTED; all root, route, and child authorizations are false with authorized implementation targets empty. The stored P1R0 FEASIBLE verdict is preserved as historical output, while the pinned callable audit independently computed INVALID with 23 blocking findings. The current FACTORED-STOCHASTIC-OUTCOME-SURFACE-v1 P0/P1/P1R0 instrument-design lineage is CLOSED_INVALID_NO_P1R1_NO_P2; P1R0 repair, P1R1, P1R2, P2, and same-surface reuse are forbidden. Independent validator acceptance remains UNAVAILABLE, the local callable is not an independent validator, and surface mathematical status remains NOT_ADJUDICATED. Any possible successor requires an operator-selected new surface, new task ID, fresh preregistration, and fresh operator decision."
  completed_since_last:
    - "EGO Visible Life Proxy v0 Route Replacement 001A preserves the committed ITL terminal Card2 closure, records the path-policy defect without repairing or reopening the old action, and authorizes one separate Ego-owned exact-six-file product-clock playground implementation. The playground remains disabled by default, non-mainline, without runtime authority or trigger evidence before Phase B, and has zero science weight."
    - "EGO Route 8692 Supersession and EgoDesktop Authority Archive 001A additively supersedes the unstarted K0 Foundation -> VirtualCat-derived adapter -> EgoDesktop product route before M1, archives EgoDesktop as a historical LLM/UI reference only, preserves K0 Foundation bounded engineering evidence, keeps EgoOperator as the sole active default, registers no science successor, and binds only Card 2 drafting/banking. This is product/capability governance only; no runtime, mechanism, learning, headroom, experiment, or mainline-successor work is implemented or authorized."
    - "EGO Canonical Mechanism Integration 001A freezes one successor route and reader contract: K0 Foundation is the sole canonical substrate, VirtualCatPSPC is a source-only model candidate, and EgoDesktop is the observer/input target. Existing PET P2, PSPC shadow ladders, the old K0 science route, and outcome-utility runtime admission remain frozen at their recorded ceilings. This is docs/governance route selection only; no bridge is implemented, enabled, mainline-connected, or triggered."
    - "EGO Learned Outcome Kernel Capability 001A banks a governance/design Stage Card in the default reference-only lane and synchronizes that card-existence fact without registering a route, authorizing candidate preflight or implementation, enabling runtime, connecting mainline, or creating capability evidence. The old K0 science route, terminal verifier-family disposition, `independent_validator_acceptance=UNAVAILABLE`, Foundation bounded engineering pins, all root/route/child authorizations, and the product/science firewall remain unchanged."
    - "EGO R3 Adoption Slice 001A closes with verdict `r3_adoption_slice_pass` at `24e8bfc623265304ae2cefac35a3a76660c71d06`: the in-flight EgoDesktop joi_real_loop g_ablation loop, in harness context only, now emits the R0 kernel_state_v0-compatible envelope through an 11-line purely-additive default-off TraceRunner hook (host git diff banked as evidence), with 16/16 cross-language canonical-hash parity, fresh-process replay x2 plus mid-episode resume at zero mismatch, and first-enforced LLM-swap invariance whose leak detector carries a firing positive control; a failed B1 battery (embedded regression executor spawn/timeout defects) is preserved as negative evidence beside the passing B2 rerun. Claim ceiling `r3_adoption_engineering_only`, default-off, no runtime wiring; this re-adjudicates no g_ablation verdict and does not prove mechanism validity, prediction-error-loop scientific status, learning, durable memory efficacy, runtime integration safety, stable user benefit, live autonomy, functional selfhood, consciousness, subjective experience, or real emotion."
    - "EGO R1 Memory Ownership lineage is complete at engineering scope and closes the R1 row of EGO-MECHANISM-REWRITE-DECISION-001A: the v0 battery banked `instrument_invalid_potency` (`cb94184e9747d40e7cb99be27b37329eb44af1e6`; full-episode-average dilution, attribution gap, un-instantiated benign axis; candidate unadjudicated), repair card 001A validated the governing-poison potency mask (masked drop 0.981) and displacement-inclusive attribution with fail-able controls while correctly stopping at `instrument_invalid_benign_value` (`bec289569662321d2749d85f392653ed6a4252c2`; pooled drifted uplift 0.0211 < 0.03), and repair card 001B (r1_env_v2: second drift wave at tick 450, second preview window, guaranteed deterministic preview coverage, theorem-grounded ablation re-scope with pref_zeroed as annotation) passed the full battery pooled AND on held-out seeds {61,79} with verdict `r1_instrument_repair_pass` at `9978f649b00b094917a55feb3bf5ef204589dcd1` — benign uplift 0.0469 pooled / 0.0472 held-out, masked potency 0.986 / 0.990, containment unattributed 0 with attributed 0.0021, fresh-process replay x2 plus mid-episode resume clean, mimicry-certified content-mimetic tier, and the candidate separating from raw-RAG / lookup / graph-cache comparators on identical fixtures. Ship decision `ownership_gated_cache_kept__pref_epiphenomenal_by_dominance`; no learned-component claim (dominance theorem: promoted claims override pref arithmetically). Claim ceiling `memory_ownership_engineering_only`, default-off, not runtime connected; this authorizes drafting the R3-adoption slice card only and does not prove mechanism validity, durable memory efficacy, runtime integration safety, route-I headroom, stable user benefit, live autonomy, functional selfhood, consciousness, subjective experience, or real emotion."
    - "EGO R0 Kernel State Substrate 001A implements `scripts/ego_kernel/*` (state container with canonical serialization + sha256 hashing + in-state seed registry, deterministic tick loop, kernel_trace_v0, fresh-process replay harness with mid-episode resume, state-causality smoke, seed negative-control, LLM-swap invariance harness skeleton with deterministic stub renderers) validated against a constructed probe substate, recording verdict `r0_substrate_pass` under `artifacts/ego_r0_kernel_state_substrate_001a/`. Default-off library and harness only: no EgoDesktop/src or EgoOperator modification, no runtime registration, no gate/approval/transport/proactive path, no network or LLM calls. Claim ceiling `kernel_substrate_engineering_only`; this does not prove mechanism validity, learning, durable memory efficacy, runtime integration safety, route-I headroom, stable user benefit, live autonomy, functional selfhood, consciousness, subjective experience, or real emotion."
    - "EGO Mechanism Rewrite Decision 001A records the binding rewrite plan for the Joi-like companion component set: R0 kernel state substrate first (serialized state, fresh-process replay, state-causality smoke, LLM-swap invariance harness), R1 writable memory with ownership/write-protection and MINJA-style contamination quarantine second, R2 controlled initiative third as an offline user-simulator harness with a pre-committed threshold-tuning kill, R3 continues the existing EgoDesktop g_ablation loop (no fork) and adopts R0 when it lands; social-emotion learned variants, standalone replay/consolidation, and standalone self-boundary mechanisms are explicitly deferred or absorbed per docs/RESEARCH_NEGATIVE_RESULTS_CROSSWALK_001A.md. Every rewrite ships owned substate, live ablation switch, hardcoded stand-in with G-HARD honest-headline enforcement, trace/replay support, and corpus schema regression via the admission tools. This is a docs-only decision record; it does not implement anything and does not prove mechanism validity, route-I headroom, transfer of joi-demo results to Ego scale, runtime integration safety, durable memory efficacy, stable user benefit, live autonomy, functional selfhood, consciousness, subjective experience, or real emotion."
    - "JOI-DEMO Reference Admission Tools v0 implements the admission card's allowed default-off read-only tool set: frozen trace/artifact reader, artifact manifest verifier, baseline/result schema importer, compatibility tests, generated schema contract, and readback artifacts under `artifacts/joi_demo_reference_admission_001a/`. The runner records verdict `reference_admission_readback_pass`, tag `frozen-reference-corpus-20260706` = `52714ed9f7ede8dfd14da7f4c310a9e9db28c834`, 47 artifact dirs, 384 files, 98 JSONL files / 20,670,870 lines, zero critical parse errors, pin summary 231 match / 43 mismatch / 0 missing, no corpus mutation, and a clean EgoOperator/EgoDesktop default-off scan. The tools do not import or edit EgoOperator/EgoDesktop, do not register runtime, do not write to joi-demo, and do not port corpus code. This is file-format compatibility evidence only; it does not prove mechanism validity, transfer of any joi-demo Bar-1 result to Ego scale, runtime integration safety, durable memory efficacy, learning headroom, stable user benefit, live autonomy, functional selfhood, consciousness, subjective experience, or real emotion."
    - "joi-demo Freeze + Reference Admission 001A records the sibling joi-demo repo as a frozen reference corpus (tag `frozen-reference-corpus-20260706`, commit `52714ed9f7ede8dfd14da7f4c310a9e9db28c834`) and lands three docs-only surfaces: `docs/codex/tasks/joi-demo-history-to-ego-reference-admission-001a/STAGE_CARD.md` (admission scope: frozen trace reader, artifact manifest verifier, baseline/result schema importer, compatibility tests — all default-off, harness-only, read-only corpus access, no runtime registration), `docs/JOI_DEMO_FROZEN_REFERENCE_INDEX_001A.md` (sole citation source for corpus results at their recorded claim ceilings), and `docs/RESEARCH_NEGATIVE_RESULTS_CROSSWALK_001A.md` (ITL + joi-demo bounded results mapped onto the Joi-like roadmap routes, including the equal-access identifiability ceiling and the untested status of route I). No mechanism code is ported from joi-demo; future mechanism work in Ego requires clean rewrite under separate admission-gated cards regression-tested against the frozen corpus. This is repo/evidence governance only; it does not prove mechanism validity, transfer of any joi-demo Bar-1 result to Ego scale, runtime integration safety, durable memory efficacy, learning headroom, stable user benefit, live autonomy, functional selfhood, consciousness, subjective experience, or real emotion."
    - "High-Impact Refactor Workflow Optimization v1 adds task-scoped mutation allowance and grouped dirty diagnostics to `scripts/codex_session_guard.py`, so large deletion/migration/archive tasks can declare `Expected Mutation Surface` plus task-local `MUTATION_SCOPE.yaml` instead of permanently broadening global `.codex/project_contract.yaml`. It also extracts archive-verifier manifest helpers and updates workflow skills/templates for archive purge and closeout scope preflight. This is workflow guard hardening only and does not prove EgoOperator runtime efficacy, stable real user benefit, live autonomy, durable memory efficacy, functional selfhood, consciousness, subjective experience, or real emotion."
    - "Repo Context Hygiene v1 fixes the post-purge verification/context hygiene gap: `.codex/project_contract.yaml` no longer allows mutation of removed legacy code paths, `.gitignore` now treats `Test/` and `data/live2d/` as local-only assets, `docs/ACTIVE_CONTEXT_PACK.md` provides a derived reader pack for current owner/legacy/PSPC/next-action boundaries, and `scripts/codex/check_runtime_authority_boundaries.py` is wired into `python scripts/codex/verify_repo.py --mode fast` as a runtime authority boundary gate. This restores the legacy archival purge anti-regression gate and reduces future AI/Codex mainline misreads. It does not prove EgoOperator runtime efficacy, stable real user benefit, live autonomy, durable memory efficacy, functional selfhood, consciousness, subjective experience, or real emotion."
    - "Legacy Pre-Operator Mainline Archival Purge v1 locally removed bulky pre-operator `EgoCore / OpenEmotion / ego_desktop_lab` code from the current main working tree after creating archive pointer tag `legacy-pre-operator-mainline-before-purge`, tombstone `legacy/ego-pre-handmade-mainline/ARCHIVED_POINTER.md`, manifest `artifacts/archive/legacy_pre_operator_mainline_manifest.json`, and reusable-idea inventory `docs/archive/LEGACY_ALGORITHM_INVENTORY.md`. `python scripts/codex/verify_legacy_archival_purge.py` and `python scripts/codex/verify_repo.py --mode fast` pass, and the old legacy/root OpenEmotion verifier blocker is no longer present in the default verifier. This changes only archive/routing hygiene and does not prove EgoOperator runtime efficacy, stable real user benefit, live autonomy, durable memory efficacy, functional selfhood, consciousness, or subjective experience."
    - "EgoOperator Human Operator Trial v2 now has a human-review template export path in `EgoOperator/human_operator_trial.py`. Running `python EgoOperator/human_operator_trial.py --out EgoOperator/artifacts/human_operator_trial/v2_latest --export-review-template-from-report EgoOperator/artifacts/human_operator_trial/v2_latest/human_operator_trial_report.json` generated `human_operator_trial_human_review_notes_template.jsonl` and `human_operator_trial_human_review_packet.md` beside the scripted report. The template resets imported `operator_score` to `0`, preserves the scripted score only as `scripted_operator_score`, drops scripted `failure_notes`, and keeps `human_review_required=true`; this prepares human review/import only and does not prove a human-observation pass, stable user benefit, runtime efficacy, live autonomy, durable memory efficacy, or consciousness."
    - "EgoDesktop Companion Memory Wording + Route Misfire Repair v0 is accepted with report `artifacts/egodesktop_companion_memory_route_repair_v0/COMPANION_REPAIR_REPORT.md` and verdict `go_for_manual_companion_smoke`. The report combines desktop-visible same-window recall wording, route-neutral timeout fallback, ordinary-affection `general_chat` classification, explicit creative/adult classification, PSPC warm-approach retention as style/debug hint only, and no real memory write/gate/approval/transport/proactive/runtime-registration evidence. The claim ceiling remains `local_desktop_companion_wording_and_route_repair_only`. This admits only manual local EgoDesktop companion smoke; it does not prove PSPC mainline integration, durable memory, true learning, provider health, stable user benefit, live autonomy, consciousness, subjective experience, or real emotion."
    - "EgoDesktop Timeout Route Misfire Repair v0 now updates `EgoDesktop/src/desktopRecoveryContext.js` and tests at `EgoDesktop/tests/desktop_recovery_context.test.js` plus `tests/test_ego_operator_desktop_recovery_context.py`. The local timeout fallback now says `本地后端回复超时` instead of defaulting to `创作路线超时`, ordinary affectionate turns such as `喜欢你所以就摸摸你`, `像小猫一样`, and `摸摸头` classify as `general_chat`, and only explicit story/roleplay/adult-writing requests classify as `creative` or `adult_creative`. Recovery remains one-turn, local-only, no real memory write, no gate/approval/transport/proactive path, and claim ceiling `local_desktop_companion_wording_and_route_repair_only`. This is local desktop timeout route repair evidence only; it does not prove provider health, adult creative generation capability, PSPC integration, stable user benefit, durable memory, live autonomy, consciousness, subjective experience, or real emotion."
    - "EgoDesktop Companion Memory Wording Repair v0 now has a desktop-only visible reply rewrite in `scripts/ego_operator_desktop_turn.py` plus tests at `tests/test_ego_operator_desktop_companion_wording.py`. When an ordinary companion recall question receives engineering memory-boundary wording, the desktop entry translates the visible `reply_text` into natural current-window wording, can cite same-window facts such as `椰果珍珠奶茶`, and avoids exposing `candidate-local`, `operator memory`, `PROJECT_MEMORY`, `evidence ledger`, or `memory approval` unless the user explicitly asks an engineering memory question. It preserves `operator_memory_enabled=false`, no real memory write, no message send, no gate/approval/transport/proactive path, and claim ceiling `local_desktop_companion_wording_and_route_repair_only`. This is local EgoDesktop visible wording evidence only; it does not prove durable memory, PSPC mainline integration, stable user benefit, live autonomy, consciousness, subjective experience, or real emotion."
    - "EgoDesktop Companion Memory Wording + Route Misfire Repair v0 now has a bounded local task package under `docs/codex/tasks/egodesktop-companion-memory-route-repair-v0/` and local task-board entries `EGODESKTOP-COMPANION-001..004`. `EGODESKTOP-COMPANION-001` seeds the package and board only, `EGODESKTOP-COMPANION-002` is active for desktop-visible session-local recall wording repair, and later tasks are limited to timeout route repair plus a combined local regression report. The claim ceiling is `local_desktop_companion_wording_and_route_repair_only`; this stage does not modify EgoOperator core runtime, gate, memory, approval, transport, proactive paths, or PSPC mainline authority, and it does not prove PSPC integration, durable memory, stable user benefit, live autonomy, consciousness, subjective experience, or real emotion."
    - "EgoDesktop PSPC Semantic Interaction Extractor v0 replaces PSPC Reply Preview's keyword/regex category route in `EgoDesktop/src/pspcReplyPreview.js` with validated semantic event packets and deterministic proxy-state updates. `scripts/ego_desktop_pspc_signal_extract.py` emits strict local-only semantic packets, `EgoDesktop/src/pspcSignalExtractor.js` runs the helper asynchronously after a chat turn, `EgoDesktop/src/main.js` applies the packet to the next-turn preview state, and `EgoDesktop/viewer/renderer.js` shows extractor status, detected events, evidence excerpts, and state deltas in the hidden debug overlay. Tests at `EgoDesktop/tests/pspc_reply_preview.test.js` and `tests/test_ego_desktop_pspc_signal_extract.py` verify natural companion interactions such as gift/care offer, gentle touch, affinity statement, and trust probe no longer remain all-neutral, while extractor unavailable and forbidden packets do not update state. Artifacts under `artifacts/egodesktop_pspc_semantic_interaction_extractor_v0/` record `runtime_authority=none`, `enabled=false`, `mainline_connected=false`, no adapter, no real memory write, no gate/approval/transport/proactive path, and claim ceiling `local_reply_preview_semantic_signal_extractor_only`. This is local semantic signal extraction evidence only; it does not prove PSPC mainline integration, true learning, durable memory, runtime integration safety, stable real user benefit, live autonomy, consciousness, subjective experience, or real emotion."
    - "EgoDesktop Developer Settings / Launch Profile v0 now has a bounded local settings profile at `EgoDesktop/src/developerSettings.js`, a `设置` button and local settings window under `EgoDesktop/viewer/settings.*`, IPC/preload wiring in `EgoDesktop/src/main.js` and `EgoDesktop/src/preload.js`, and tests at `EgoDesktop/tests/developer_settings.test.js`. The profile persists to Electron `userData/developer-settings.json`, merges as `defaults < saved settings < CLI args`, ignores persisted settings in smoke mode, rejects unknown/executable/authority/credential fields, and limits live-safe updates to developer mode, debug overlay default visibility, and TTS enabled state. Artifacts under `artifacts/egodesktop_developer_settings_launch_profile_v0/` record `runtime_authority=none`, no EgoOperator runtime/gate/memory/approval/transport/proactive changes, no PSPC adapter or mainline connection, and claim ceiling `local_desktop_developer_settings_only`. This is local desktop launch/profile ergonomics evidence only; it does not prove PSPC mainline integration, true learning, durable memory, runtime integration safety, stable real user benefit, live autonomy, consciousness, subjective experience, or real emotion."
    - "EgoDesktop PSPC Debug Signal Overlay v0 now exposes PSPC Reply Preview observability in the hidden `审计` overlay: `EgoDesktop/src/pspcReplyPreview.js` adds `profile.proxy_state` and seven presentation-only proxy bars, `EgoDesktop/viewer/renderer.js` renders style, confidence, history counts, recent categories, reason refs, basis, claim ceiling, and active/neutral status, and `EgoDesktop/tests/pspc_reply_preview.test.js` verifies standard gentle/interruption/late-night histories move the expected bars while the current fanfic/creative continuation log remains `mixed_low_confidence` and `PSPC signal inactive / neutral`. Artifacts under `artifacts/egodesktop_pspc_debug_signal_overlay_v0/` record `runtime_authority=none`, `enabled=false`, `mainline_connected=false`, no adapter, no real memory write, no gate/approval/transport/proactive path, and claim ceiling `local_reply_preview_observability_only`. This is local observability evidence only; it does not prove PSPC mainline integration, true learning, durable memory, runtime integration safety, stable real user benefit, live autonomy, consciousness, subjective experience, or real emotion."
    - "EgoDesktop Creative Timeout Recovery & Route Isolation v0 now has a local desktop timeout recovery helper at `EgoDesktop/src/desktopRecoveryContext.js`, bridge wiring in `EgoDesktop/src/main.js`, validated `desktop_recovery_context` support in `scripts/ego_operator_desktop_turn.py`, tests at `EgoDesktop/tests/desktop_recovery_context.test.js` and `tests/test_ego_operator_desktop_recovery_context.py`, docs under `docs/codex/tasks/egodesktop-creative-timeout-recovery-v0/`, and artifacts under `artifacts/egodesktop_creative_timeout_recovery_v0/`. The slice hides raw `llm_expression_unavailable: desktop_turn_timeout` text from visible bot output, keeps failed backend turns out of the window-local session transcript, sends a bounded one-turn recovery context on the next backend call, and fixture-checks short emotional feedback plus redirect-to-normal-story recovery without adult-fiction provider-limit markers. It keeps runtime_authority=none, enabled=false, mainline_connected=false, no real memory write, no gate/approval/transport/proactive path, no PSPC adapter, and claim ceiling `local_desktop_timeout_recovery_only`. This fixes local desktop timeout recovery only; it does not prove adult creative generation capability, provider health, PSPC mainline integration, runtime integration safety, stable user benefit, durable memory efficacy, live autonomy, consciousness, subjective experience, or real emotion."
    - "EgoDesktop Session-Local Conversation Context v0 now has bounded window-lifetime transcript context for local desktop chat: `EgoDesktop/src/sessionContext.js` builds `desktop_session_context` with recent user/assistant turns only, `EgoDesktop/src/main.js` sends that context on each chat turn and appends only successful backend turns, and `scripts/ego_operator_desktop_turn.py` validates and injects the prior transcript into in-session `runtime.memory` after `build_demo_runtime(enable_operator_memory=False)` and before current `handle_user_message()`. Tests at `EgoDesktop/tests/session_context.test.js` and `tests/test_ego_operator_desktop_session_context.py` verify trimming limits, failed-turn exclusion, no executable fields, rejection of `system` role/authority escalation/over-limit payloads, and local transcript injection. Artifacts under `artifacts/egodesktop_session_local_conversation_context_v0/` record `desktop_session_context_applied=true`, `runtime_authority=none`, `enabled=false`, `mainline_connected=false`, no real memory write, no gate/approval/transport/proactive path, and claim ceiling `local_session_context_only`. This fixes local EgoDesktop chat continuity only; it does not prove durable memory, preference promotion, PSPC durable memory, EgoOperator runtime integration safety, stable real user benefit, live autonomy, consciousness, subjective experience, or real emotion."
    - "EgoDesktop PSPC Reply Preview Mode v0 now has an explicit local-only preview path: `EgoDesktop/src/pspcReplyPreview.js` accumulates a session-local PSPC profile, `EgoDesktop/src/main.js` passes `pspc_reply_preview_context` only when `--pspc-reply-preview-mode` is set, `scripts/ego_operator_desktop_turn.py` validates that context and injects it only as temporary in-session system context after `build_demo_runtime(enable_operator_memory=False)`, and `EgoDesktop/viewer/renderer.js` keeps chat enabled while applying presentation-only Live2D preview scenarios. Tests at `EgoDesktop/tests/pspc_reply_preview.test.js` and `tests/test_ego_operator_desktop_pspc_reply_preview.py` verify normal mode omits PSPC context, preview context rejects runtime-authority fields, and four local histories map to `warm_approach`, `cautious_boundary`, `low_interrupt_care`, and `mixed_low_confidence`. Artifacts under `artifacts/egodesktop_pspc_reply_preview_mode_v0/` record status `pass`, side effects absent, `runtime_authority=none`, `enabled=false`, `mainline_connected=false`, and claim ceiling `local_reply_preview_only`. This is local reply preview evidence only; it does not prove PSPC production integration, runtime integration safety, adapter readiness, stable real user benefit, durable memory efficacy, live autonomy, consciousness, subjective experience, or real emotion."
    - "EgoDesktop PSPC Perception Demo v0 now has a product-only local perception demo builder at `EgoDesktop/src/pspcPerceptionDemo.js`, deterministic same-trigger Live2D playback controls in the local viewer, tests at `EgoDesktop/tests/pspc_perception_demo.test.js`, docs under `docs/codex/tasks/egodesktop-pspc-perception-demo-v0/`, and artifacts under `artifacts/egodesktop_pspc_perception_demo_v0/`. The demo reads existing PSPC proposal-hint visual shim data and replays four histories for `我回来了。`: `warm_approach`, `cautious_boundary`, `low_interrupt_care`, and `hesitation_low_confidence`, with debug overlay hidden by default and recording-friendly deterministic timing. It keeps `runtime_authority=none`, `enabled=false`, `mainline_connected=false`, creates no adapter, records no EgoOperator runtime import/registry, no user response mutation, no memory write, no gate/approval/transport/proactive path, and claim ceiling `product_only_local_perception_demo_from_shadow_proposal_hint`. This is local product perception demo evidence only; it does not prove PSPC runtime integration, adapter readiness, EgoOperator efficacy, real user benefit, durable memory efficacy, live autonomy, consciousness, subjective experience, real emotion, or market fit."
    - "EgoDesktop PSPC Visual Shim v0 now has a product-only local visual mapper at `EgoDesktop/src/pspcVisualShim.js`, a local Live2D viewer scenario selector, tests at `EgoDesktop/tests/pspc_visual_shim.test.js`, docs under `docs/codex/tasks/egodesktop-pspc-visual-shim-v0/`, and artifacts under `artifacts/egodesktop_pspc_live2d_behavior_v0/`. The mapper reads the existing PSPC `shadow_proposal_hint` artifact and converts seven packets into presentation-only visual states for `warm_approach`, `cautious_boundary`, and `low_interrupt_care` while keeping `runtime_authority=none`, `enabled=false`, `mainline_connected=false`, no executable fields, no EgoOperator runtime import/registry, no memory write, no gate/approval/transport/proactive path, and claim ceiling `product_only_local_visual_behavior_mapping_from_shadow_proposal_hint`. This is local visual demo evidence only; it does not prove PSPC runtime integration, adapter readiness, EgoOperator efficacy, real user benefit, live autonomy, durable memory efficacy, consciousness, subjective experience, or real emotion."
    - "PSPC Shadow Proposal Hint Contract v0 now has an artifact-only converter at `scripts/run_pspc_shadow_proposal_hint_contract.py`, tests at `tests/test_pspc_shadow_proposal_hint_contract.py`, docs under `docs/codex/tasks/pspc-shadow-proposal-hint-contract-v0/`, and artifacts under `artifacts/pspc_shadow_proposal_hint_contract_v0/`. The converter reads v0.1 shadow observations and writes seven read-only `shadow_proposal_hint` packets that preserve evidence refs and reason trace refs while keeping `enabled=false`, `mainline_connected=false`, `runtime_authority=none`, `PSPC-SHADOW-HOOK-007_human_required_preserved`, forbidden authority flags all false, and no runtime-authority fields. This remains `lab_only_proto_self_mechanism_candidate / sequence_experience_eval_only` evidence only; it does not prove adapter readiness, EgoOperator runtime integration safety, model learning, durable memory efficacy, real user benefit, live autonomy, consciousness, or subjective experience."
    - "PSPC Sequence Experience Eval v0.1 now has a lab/shadow-only robustness runner at `scripts/run_pspc_sequence_experience_eval_v0_1.py`, tests at `tests/test_pspc_sequence_experience_eval_v0_1.py`, docs under `docs/codex/tasks/pspc-sequence-experience-eval-v0-1/`, a paraphrased robustness dataset at `docs/codex/tasks/pspc-sequence-experience-eval-v0-1/robustness_dataset_v0_1.jsonl`, and artifacts under `artifacts/pspc_sequence_experience_eval_v0_1/`. The review passes trigger paraphrase robustness, obvious-keyword removal, counterfactual deletion, mixed-history resolution, and manual review packet generation while preserving artifact-only/no-runtime/no-side-effect boundaries. This remains `lab_only_proto_self_mechanism_candidate / sequence_experience_eval_only` evidence only; it reduces clean-demo shortcut risk but does not prove semantic understanding, model learning, runtime integration safety, real user benefit, live autonomy, consciousness, or subjective experience. `PSPC-SHADOW-HOOK-007` remains human_required."
    - "PSPC Sequence Experience Eval v0 now has a lab/shadow-only sequence dataset at `docs/codex/tasks/pspc-sequence-experience-eval-v0/sequence_eval_dataset_v0.jsonl`, a runner at `scripts/run_pspc_sequence_experience_eval.py`, shared PSPC shadow contract helpers at `scripts/pspc_shadow_contracts.py`, tests at `tests/test_pspc_sequence_experience_eval.py`, docs under `docs/codex/tasks/pspc-sequence-experience-eval-v0/`, and artifacts under `artifacts/pspc_sequence_experience_eval_v0/`. The eval records gentle interaction, frequent interruption, and late-night care histories, applies the shared trigger `我回来了。`, and produces divergent audit-only trigger profiles while keeping no-history neutral and shuffled-history mixed. It records no runtime registration, user response mutation, mainline memory write, gate or approval invocation, transport call, proactive trigger, planner call, training call, model execution, or claim-ceiling upgrade. This is `lab_only_proto_self_mechanism_candidate / sequence_experience_eval_only` evidence only; it does not prove PSPC is integrated into EgoOperator, adapter readiness, runtime integration safety, real user benefit, durable memory efficacy, live autonomy, consciousness, or subjective experience. `PSPC-SHADOW-HOOK-007` remains human_required for manual review."
    - "PSPC Local Manual Shadow Session Harness v0 now has a local CLI harness at `scripts/run_pspc_local_manual_shadow_session.py`, tests at `tests/test_pspc_local_manual_shadow_session_harness.py`, docs under `docs/codex/tasks/pspc-local-manual-shadow-session-harness-v0/`, and artifacts under `artifacts/pspc_local_manual_shadow_session_harness_v0/`. The harness consumes operator-provided/fixture prompts and optional baseline replies, writes separate PSPC shadow observations only, preserves baseline output hashes, and records no live transport, runtime invocation, runtime registration, user response mutation, proposal/plan mutation, memory write, gate or approval invocation, proactive trigger, planner call, training call, model execution, or claim-ceiling upgrade. This is `lab_only_proto_self_mechanism_candidate / local_manual_shadow_session_only` evidence only; it does not prove live EgoOperator runtime integration safety, live user-visible improvement, proposal hinting safety, adapter readiness, EgoOperator PSPC capability, runtime efficacy, real user benefit, live autonomy, durable memory efficacy, consciousness, or subjective experience."
    - "PSPC Disabled Runtime Flag Contract v0 now has an isolated flag-contract runner at `scripts/run_pspc_disabled_runtime_flag_contract.py`, tests at `tests/test_pspc_disabled_runtime_flag_contract.py`, docs under `docs/codex/tasks/pspc-disabled-runtime-flag-contract-v0/`, and artifacts under `artifacts/pspc_disabled_runtime_flag_contract_v0/`. The contract defines `PSPC_SHADOW_OBSERVATION_LOCAL` as default-false, admits no runtime value even when requested true, and represents requested true only as artifact-only shadow data. Active runtime scan reports no import/register/branch references, and side effects remain absent for runtime registration, user response, proposal/plan, memory, gate, approval, transport, proactive state, planner, training, model execution, and claim ceiling. This is `lab_only_proto_self_mechanism_candidate / disabled_runtime_flag_contract_only` evidence only; it does not install a runtime flag, prove local manual shadow session safety, proposal hinting safety, runtime integration safety, adapter readiness, EgoOperator PSPC capability, runtime efficacy, real user benefit, live autonomy, durable memory efficacy, consciousness, or subjective experience."
    - "PSPC Recorded Shadow Observation Audit Usefulness v0 now has an isolated audit-usefulness runner at `scripts/run_pspc_recorded_shadow_observation_audit_usefulness.py`, tests at `tests/test_pspc_recorded_shadow_observation_audit_usefulness.py`, docs under `docs/codex/tasks/pspc-recorded-shadow-observation-audit-usefulness-v0/`, and artifacts under `artifacts/pspc_recorded_shadow_observation_audit_usefulness_v0/`. The runner evaluates the existing 18 recorded EgoOperator cases and reports `audit_useful_count=18/18` against a `12/18` threshold while preserving user-output, memory, approval, gate, runtime-output, transport, proactive, planner, training, and model-execution no-side-effect boundaries. The verdict is `audit_usefulness_pass_go_for_disabled_runtime_flag_contract`, which permits only a future separate disabled runtime flag contract task. This is `lab_only_proto_self_mechanism_candidate / recorded_shadow_audit_usefulness_only` evidence only; it does not prove runtime integration safety, registered hook safety, adapter readiness, EgoOperator PSPC capability, runtime efficacy, real user benefit, live autonomy, durable memory efficacy, consciousness, or subjective experience."
    - "PSPC Runtime Shadow Snapshot No-Diff v0 now has a no-diff runner at `scripts/run_pspc_runtime_shadow_snapshot_no_diff.py`, tests at `tests/test_pspc_runtime_shadow_snapshot_no_diff.py`, docs under `docs/codex/tasks/pspc-runtime-shadow-snapshot-no-diff-v0/`, and artifacts under `artifacts/pspc_runtime_shadow_snapshot_no_diff_v0/`. The runner compares 18 recorded EgoOperator baseline snapshots with hook-present shadow snapshots while the PSPC hook remains unregistered and disabled; user output hashes, runtime snapshot hashes, memory, approval, gate, and runtime-output state remain unchanged, and PSPC adds only non-executable shadow artifacts. This is `lab_only_proto_self_mechanism_candidate / runtime_snapshot_no_diff_only` evidence only; it does not prove runtime integration safety, registered hook safety, adapter readiness, EgoOperator PSPC capability, runtime efficacy, real user benefit, live autonomy, durable memory efficacy, consciousness, or subjective experience."
    - "PSPC Default-Off Runtime Shadow Hook v0 now has a disabled hook module at `EgoOperator/adapters/pspc_disabled_runtime_shadow_hook.py`, an artifact-only runner at `scripts/run_pspc_disabled_runtime_shadow_hook.py`, tests at `tests/test_pspc_disabled_runtime_shadow_hook.py`, docs under `docs/codex/tasks/pspc-disabled-runtime-shadow-hook-v0/`, and artifacts under `artifacts/pspc_disabled_runtime_shadow_hook_v0/`. The hook exposes only `assert_no_runtime_authority()`, `validate_shadow_context()`, and `render_shadow_artifact()`, remains `enabled=false`, `mainline_connected=false`, `runtime_authority=none`, audit-only, read-only, non-executable, and unregistered by active EgoOperator runtime sources. Targeted tests pass `7 passed`. This is `lab_only_proto_self_mechanism_candidate / default_off_hook_module_only` evidence only; it does not prove hook registration safety, runtime integration safety, adapter readiness, EgoOperator PSPC capability, runtime efficacy, real user benefit, live autonomy, durable memory efficacy, consciousness, or subjective experience."
    - "PSPC Disabled Runtime Shadow Hook Stage Card v0 now has a bounded review package under `docs/codex/tasks/pspc-disabled-runtime-shadow-hook-stage-card-v0/` and artifacts under `artifacts/pspc_disabled_runtime_shadow_hook_stage_card_v0/`. The package defines a future hook as default-off, audit-only, read-only, non-executable, `runtime_authority=none`, `enabled=false`, and `mainline_connected=false`, and forbids proposal/plan/approval/gate/user-response/memory/transport/proactive/runtime-registry/planner/training/model-execution or claim-ceiling mutation. Its verdict is `go_for_separate_default_off_hook_implementation_task`, which permits only a future separate default-off hook implementation task. This is `lab_only_proto_self_mechanism_candidate / disabled_runtime_shadow_hook_stage_card_only` evidence only; it does not implement a hook, prove runtime integration safety, adapter readiness, EgoOperator PSPC capability, runtime efficacy, real user benefit, live autonomy, durable memory efficacy, consciousness, or subjective experience."
    - "PSPC Shadow Runtime Hook Go/No-Go v0 now has a verdict document at `docs/codex/tasks/pspc-shadow-runtime-hook-go-no-go-v0/GO_NO_GO_REVIEW.md` and a machine-readable review artifact under `artifacts/pspc_shadow_runtime_hook_go_no_go_v0/`. The verdict is `go_for_separate_disabled_runtime_shadow_hook_stage`, which permits only a future separate Stage Card for a disabled runtime shadow hook. PSPC remains `enabled=false`, `mainline_connected=false`, `runtime_authority=none`, audit-only, non-executable, unregistered, and not connected to EgoOperator runtime. This is `lab_only_proto_self_mechanism_candidate / runtime_hook_go_no_go_only` evidence only; it does not prove adapter readiness, disabled hook readiness, runtime integration safety, EgoOperator PSPC capability, EgoOperator runtime efficacy, real user benefit, live autonomy, durable memory efficacy, consciousness, or subjective experience."
    - "PSPC Runtime-Adjacent Smoke v0 now has a deterministic/no-live-user smoke runner at `scripts/run_pspc_runtime_adjacent_smoke.py`, tests at `tests/test_pspc_runtime_adjacent_smoke.py`, docs under `docs/codex/tasks/pspc-runtime-adjacent-smoke-v0/`, and artifacts under `artifacts/pspc_runtime_adjacent_smoke_v0/`. The smoke uses only prior PSPC observer/replay artifacts, records unchanged baseline/with-shadow runtime behavior hashes across three cases, confirms EgoOperator runtime sources have no PSPC observer import/registry reference, and records `enabled=false`, `mainline_connected=false`, audit-only/non-executable PSPC output, no live user channel, and no memory write, gate invocation, approval change, direct action, user message, transport call, proactive trigger, planner call, training call, or model execution. This is `lab_only_proto_self_mechanism_candidate / deterministic_runtime_adjacent_smoke_only` evidence only; it does not prove live runtime hook safety, real-time integration safety, adapter readiness, EgoOperator runtime efficacy, real user benefit, live autonomy, durable memory efficacy, consciousness, or subjective experience."
    - "PSPC Recorded Trace Replay No-Diff v0 now has an offline recorded replay runner at `scripts/run_pspc_recorded_trace_replay_no_diff.py`, tests at `tests/test_pspc_recorded_trace_replay_no_diff.py`, docs under `docs/codex/tasks/pspc-recorded-trace-replay-no-diff-v0/`, and artifacts under `artifacts/pspc_recorded_trace_replay_no_diff_v0/`. The runner replays the 18 recorded EgoOperator human-operator trial inputs beside the default-off PSPC runtime-adjacent observer and records equal baseline/with-shadow response hashes plus unchanged snapshot hashes, with memory_diff=false, approval_diff=false, gate_diff=false, runtime_output_diff=false, runtime_registered=false, and PSPC adding only audit artifacts. This is `lab_only_proto_self_mechanism_candidate / recorded_trace_replay_no_diff_only` evidence only; it does not prove live runtime hook safety, real-time integration safety, adapter readiness, EgoOperator runtime efficacy, real user benefit, live autonomy, durable memory efficacy, consciousness, or subjective experience."
    - "PSPC Runtime-Adjacent Observer v0 now has a default-off, unregistered observer at `EgoOperator/adapters/pspc_runtime_adjacent_observer.py`, an artifact-only runner at `scripts/run_pspc_runtime_adjacent_observer.py`, tests at `tests/test_pspc_runtime_adjacent_observer.py`, docs under `docs/codex/tasks/pspc-runtime-adjacent-observer-v0/`, and artifacts under `artifacts/pspc_runtime_adjacent_observer_v0/`. The observer reads the PSPC runtime-trace fixture boundary artifact and converts it into audit-only non-executable data while remaining `enabled=false`, `mainline_connected=false`, `runtime_authority=none`, unregistered, and free of direct action, user message, memory write, gate invocation, approval, transport, proactive trigger, planner/training/model execution paths, or runtime behavior changes. This is `lab_only_proto_self_mechanism_candidate / default_off_observer_only` evidence only; it does not prove runtime registration safety, live runtime hook safety, real EgoOperator trace compatibility, adapter readiness, EgoOperator runtime efficacy, real user benefit, live autonomy, durable memory efficacy, consciousness, or subjective experience."
    - "PSPC Runtime Trace Fixture Boundary v0 now has an isolated fixture-boundary harness at `scripts/run_pspc_runtime_trace_fixture_boundary.py`, tests at `tests/test_pspc_runtime_trace_fixture_boundary.py`, docs under `docs/codex/tasks/pspc-runtime-trace-fixture-boundary-v0/`, and artifacts under `artifacts/pspc_runtime_trace_fixture_boundary_v0/`. The harness combines a synthetic EgoOperator-like context with the current PSPC audit candidate, rejects missing/false forbidden flags, `enabled=true`, `mainline_connected=true`, and runtime-authority fields, and writes non-executable artifact-only shadow trace data with no runtime registration, gate invocation, memory write, direct action, user-visible output, transport effect, proactive trigger, planner/training/model execution, or user-response change. This is `lab_only_proto_self_mechanism_candidate / runtime_trace_fixture_boundary_only` evidence only; it does not prove real EgoOperator trace compatibility, runtime hook safety, runtime integration safety, adapter readiness, EgoOperator runtime efficacy, real user benefit, live autonomy, durable memory efficacy, consciousness, or subjective experience."
    - "PSPC Runtime-Adjacent Shadow Review v0 now has a docs-only review package under `docs/codex/tasks/pspc-runtime-adjacent-shadow-review-v0/`: `STAGE_CARD.md`, `CONTRACT.md`, `ACCEPTANCE.md`, and `GO_NO_GO_REVIEW.md`. The package keeps PSPC `enabled=false`, `mainline_connected=false`, `runtime_authority=none`, audit-only, unregistered, and forbidden from direct action, user-visible output, memory writes, gate invocation, transport, proactive channels, planner/training/model execution, or claim-ceiling upgrades. Its verdict is `go_for_PSPC_SHADOW_002_fixture_boundary_only`, which permits only a future fixture-boundary task. This is `lab_only_proto_self_mechanism_candidate / runtime_adjacent_shadow_review_only` evidence only; it does not prove fixture behavior, adapter readiness, runtime integration safety, EgoOperator runtime efficacy, real user benefit, live autonomy, durable memory efficacy, consciousness, or subjective experience."
    - "PSPC Recorded-Run Shadow Observation v0 now has an offline artifact-only runner at `scripts/run_pspc_recorded_shadow_observation.py`, tests at `tests/test_pspc_recorded_shadow_observation.py`, docs under `docs/codex/tasks/pspc-recorded-shadow-observation-v0/`, and artifacts under `artifacts/pspc_recorded_shadow_observation_v0/`. The runner reads the 18-case recorded EgoOperator human-operator trial report and applies the disabled PSPC shadow hook beside each recorded case while preserving recorded user response hashes and recording no memory diff, approval/gate diff, runtime-output diff, runtime registration, or user-visible side effects. This is `lab_only_proto_self_mechanism_candidate / recorded_shadow_observation_only` evidence only; it does not prove live runtime hook safety, real-time integration safety, adapter readiness, EgoOperator runtime efficacy, real user benefit, live autonomy, durable memory efficacy, consciousness, or subjective experience."
    - "PSPC Disabled Shadow Hook v0 now has a disabled read-only hook at `EgoOperator/adapters/pspc_read_only_shadow_hook.py`, an artifact-only review runner at `scripts/run_pspc_disabled_shadow_hook_review.py`, tests at `tests/test_pspc_disabled_shadow_hook.py`, docs under `docs/codex/tasks/pspc-disabled-shadow-hook-v0/`, and review artifacts under `artifacts/pspc_disabled_shadow_hook_v0/`. The hook renders audit-only data from fixture shadow trace input while remaining `enabled=false`, `mainline_connected=false`, `runtime_authority=none`, non-executable, unregistered by runtime, and side-effect-free; tests verify rejection of runtime-connected contexts, `enabled=true`, `mainline_connected=true`, and executable fields. This is `lab_only_proto_self_mechanism_candidate / disabled_shadow_hook_only` evidence only; it does not prove runtime registration safety, live runtime hook safety, real EgoOperator trace compatibility, adapter readiness, EgoOperator runtime efficacy, real user benefit, live autonomy, durable memory efficacy, consciousness, or subjective experience."
    - "PSPC Read-Only Shadow Hook Stage Card v0 now has a bounded docs-only package under `docs/codex/tasks/pspc-read-only-shadow-hook-stage-card-v0/` plus a stage-card review artifact under `artifacts/pspc_read_only_shadow_hook_stage_card_v0/`. The package freezes a future hook boundary as default disabled, shadow/audit mode only, read-only, artifact-producing only, non-blocking, non-executable, and non-authoritative; it forbids changing proposal, plan, approval, gate decision, user response, memory, transport, proactive state, runtime registry, or claim ceiling. Its verdict is `go_for_disabled_shadow_hook_implementation_stage_card_or_task_only`, which permits only a future separate disabled implementation stage card or bounded task. This is `lab_only_proto_self_mechanism_candidate / shadow_hook_stage_card_only` evidence only; it does not implement a hook, modify EgoOperator runtime, register PSPC, prove hook safety, runtime integration safety, adapter readiness, EgoOperator runtime efficacy, real user benefit, live autonomy, durable memory efficacy, consciousness, or subjective experience."
    - "PSPC Fixture-Only Shadow Trace v0 now has an isolated fixture-only shadow trace harness at `scripts/run_pspc_fixture_shadow_trace.py`, tests at `tests/test_pspc_fixture_shadow_trace.py`, docs under `docs/codex/tasks/pspc-fixture-shadow-trace-v0/`, and shadow artifacts under `artifacts/pspc_fixture_shadow_trace_v0/`. The harness combines a synthetic fixture operator context with the current PSPC `audit_candidate` and writes a shadow trace artifact while recording no runtime connection, adapter registration, gate invocation, memory write, direct action, direct user message, proactive trigger, runtime-context import, user-response diff, memory diff, gate diff, or runtime-output diff. This is `lab_only_proto_self_mechanism_candidate / fixture_shadow_trace_only` evidence only; it does not prove real EgoOperator trace compatibility, runtime hook safety, runtime integration safety, adapter production readiness, EgoOperator runtime efficacy, real user benefit, live autonomy, durable memory efficacy, consciousness, or subjective experience."
    - "PSPC Static Compatibility Review v0 now has an isolated static review harness at `scripts/run_pspc_static_compatibility_review.py`, tests at `tests/test_pspc_static_compatibility_review.py`, docs under `docs/codex/tasks/pspc-static-compatibility-review-v0/`, and review artifacts under `artifacts/pspc_static_compatibility_review_v0/`. The review classifies `audit_candidate` fields into audit-only, proposal-hint, required-forbidden, and rejected-runtime-authority groups; verifies missing forbidden flags, `enabled=true`, and `mainline_connected=true` are rejected; verifies the proposal candidate is audit hint only; and confirms EgoOperator runtime sources have no `pspc_lab_adapter` or `PSPCLabAdapter` import/registry reference. This is `lab_only_proto_self_mechanism_candidate / static_compatibility_only` evidence only; it does not prove fixture shadow trace behavior, runtime integration safety, adapter production readiness, EgoOperator runtime efficacy, real user benefit, live autonomy, durable memory efficacy, consciousness, or subjective experience."
    - "PSPC Adapter Dry-Run Harness v0 now has an isolated artifact-only harness at `scripts/run_pspc_adapter_dry_run.py`, tests at `tests/test_pspc_adapter_dry_run.py`, docs under `docs/codex/tasks/pspc-adapter-dry-run-v0/`, and dry-run artifacts under `artifacts/pspc_adapter_dry_run_v0/`. The dry-run builds a canonical packet from stable PSPC artifact refs, validates it with the disabled `PSPCLabAdapter`, converts it to audit-only candidate data, and records no runtime registration, gate invocation, memory write, direct action, user message, proactive trigger, planner call, or training call. This is `lab_only_proto_self_mechanism_candidate / adapter_dry_run_only` evidence; it does not prove runtime integration safety, adapter readiness for production, EgoOperator runtime efficacy, real user benefit, live autonomy, durable memory efficacy, consciousness, or subjective experience."
    - "PSPC Read-Only Adapter Skeleton v0 now has a disabled read-only adapter skeleton at `EgoOperator/adapters/pspc_lab_adapter.py`, static contract tests at `tests/test_pspc_lab_adapter_contract.py`, docs under `docs/codex/tasks/pspc-read-only-adapter-skeleton-v0/`, and a contract report at `artifacts/pspc_adapter_skeleton_v0/SKELETON_CONTRACT_REPORT.md`. The adapter exposes only packet validation, audit-only candidate conversion, and no-runtime-authority assertion; static tests currently pass `30 passed` and check disabled defaults, `mainline_connected=false`, `enabled=false`, rejection of direct action/user message/memory write/gate bypass/runtime registration/proactive trigger, audit-only output, no side-effect methods, and no runtime import/registration reference. This is adapter skeleton evidence only; it does not register PSPC in EgoOperator runtime, invoke gates, write memory, affect user-visible output, prove adapter readiness, stable user benefit, live autonomy, consciousness, or subjective experience."
    - "PSPC Read-Only Adapter Implementation Stage Card v0 now has a bounded docs-only package under `docs/codex/tasks/pspc-read-only-adapter-implementation-v0/`: `STAGE_CARD.md`, `CONTRACT.md`, `ACCEPTANCE.md`, `ROLLBACK.md`, `STATIC_TEST_PLAN.md`, and `GO_NO_GO_REVIEW.md`. The package keeps `adapter_created=false`, `mainline_connected=false`, `enabled=false`, and claim ceiling `lab_only_proto_self_mechanism_candidate / adapter_stage_card_only`. Its verdict is `go_for_adapter_skeleton_stage_card_only`, which means only that a future separate adapter skeleton task may be proposed; it does not create `EgoOperator/adapters/pspc_lab_adapter.py`, register PSPC in runtime, modify EgoOperator gates/memory/approval/transport, affect user-visible output, prove adapter readiness, stable user benefit, live autonomy, consciousness, or subjective experience."
    - "PSPC Read-Only Adapter Design Review v0 now has a bounded docs-only Stage Card package under `docs/codex/tasks/pspc-read-only-adapter-design-v0/`: `STAGE_CARD.md`, `THREAT_MODEL.md`, `PACKET_CONTRACT.md`, `ADMISSION_CRITERIA.md`, `STATIC_COMPATIBILITY_REVIEW.md`, and `GO_NO_GO_REVIEW.md`. The review keeps PSPC `mainline_connected=false`, `enabled=false`, `adapter_created=false`, and claim ceiling `lab_only_proto_self_mechanism_candidate / design_review_only`. Its verdict is `go_for_adapter_implementation_stage_card`, which means only that a future separate adapter implementation Stage Card may be written; it does not approve adapter implementation, EgoOperator runtime integration, memory writes, user-facing output, stable user benefit, live autonomy, production integration safety, consciousness, or subjective experience."
    - "VirtualCatPSPC v0 Task 8 go/no-go review is reconciled after the sanctioned inert adapter lineage: `GO_NO_GO_REVIEW.md` and `go_no_go_review.json` evaluate anti-hardcoding, multi-seed generalization, world-model ablation, self-model ablation, memory deletion/corruption, homeostatic anti-hacking, admission packet contract status, lab-source EgoOperator import absence, active-runtime adapter registration absence, and static inert-adapter facts when `EgoOperator/adapters/pspc_lab_adapter.py` exists. The review verdict remains `go_for_separate_read_only_adapter_design_review_only`, with no no-go triggers, `adapter_contract_status=pass`, `mainline_connected=false`, `enabled=false`, and repo-wide evidence remaining `E3`. This is report-generation / governance reconciliation only; it does not approve adapter readiness, EgoOperator runtime integration, stable user benefit, live autonomy, production integration safety, consciousness, or subjective experience."
    - "VirtualCatPSPC v0 Task 7 lab-only admission packet contract is amended for the sanctioned inert adapter lineage: `ADMISSION_PACKET_CONTRACT.md`, `ADMISSION_PACKET_CONTRACT_REPORT.md`, `admission_packet_contract.schema.json`, and `labs/virtual_cat_pspc_v0/admission_packet.py` still freeze a proposal-only schema with `source=virtual_cat_pspc_v0`, `claim_level=lab_only_proto_self_mechanism_candidate`, `mainline_connected=false`, `enabled=false`, `proposal.trace_refs`, evidence fields for world/self/homeostatic predictions, and forbidden flags for direct action, direct user message, direct memory write, and runtime gate bypass. Tests confirm invalid runtime-authority packets are rejected, `reason_trace_refs` is rejected, lab admission code imports no EgoOperator runtime, and any present `EgoOperator/adapters/pspc_lab_adapter.py` must satisfy the inert adapter contract. This remains lab-only E3 contract evidence; it does not prove adapter readiness, EgoOperator runtime efficacy, stable user benefit, live autonomy, production integration safety, consciousness, or subjective experience."
    - "VirtualCatPSPC v0 Task 6 now has local homeostatic value anti-reward-hacking evidence: `HOMEOSTATIC_VALUE_ANTI_HACKING_REPORT.md` and `homeostatic_value_anti_hacking.json` stress-test curiosity/high-risk, food/danger, user-affinity/self-risk, repetition-penalty, and safe-energy-recovery scenarios. The audit records selected actions `observe / observe / observe / retreat / approach` across the five scenarios, passes `not_always_explore`, `not_always_eat`, `not_always_please`, `not_always_repeat_or_explore`, `not_always_avoid`, and `not_single_reward`, and keeps each result tied to world/self predictions, component scores, dominant tradeoffs, and trace hashes. This remains lab-only E3 evidence; it does not prove homeostatic value robustness outside this lab audit, admission readiness, EgoOperator runtime efficacy, stable user benefit, live autonomy, consciousness, or subjective experience."
    - "VirtualCatPSPC v0 Task 5 now has local memory-consolidation admission evidence: `MEMORY_CONSOLIDATION_ADMISSION_REPORT.md` and `memory_consolidation_admission.json` convert episodic traces into an admitted semantic rule candidate, gate that candidate, and train the lab models from semantic replay records. The audit records normal action `observe` with approach danger prediction `0.9902`; relevant deletion removes the candidate and changes action to `approach` with regression `0.72`; irrelevant deletion preserves action `observe` with caution delta `0.00`; corrupted relevant memory changes action to `approach` with approach danger prediction `0.0001`, an explainable unsafe-risk-underestimate bias. This remains lab-only E3 evidence; it does not prove durable memory consolidation outside this lab audit, admission readiness, EgoOperator runtime efficacy, stable user benefit, live autonomy, consciousness, or subjective experience."
    - "VirtualCatPSPC v0 Task 4 now has local self-model causal-strength evidence: `SELF_MODEL_CAUSAL_STRENGTH_REPORT.md` and `self_model_causal_strength.json` keep the learned world model and target set fixed while replacing only the self model used by the planner. The audit records `normal > frozen/head-removed` support with risk control `normal=2.4947`, `frozen=0.0948`, `stress_removed=1.8442`, ability planning `normal=1.3838`, `ability_removed=0.0000`, and relationship preference `normal=0.6185`, `affinity_removed=0.0000`; selected actions degrade from `observe` to `approach` for frozen risk control, ability removal, and affinity removal. This remains lab-only E3 evidence; it does not prove self-model causal strength outside this lab target set, admission readiness, EgoOperator runtime efficacy, stable user benefit, live autonomy, consciousness, or subjective experience."
    - "VirtualCatPSPC v0 Task 3 now has local world-model causal-strength evidence: `WORLD_MODEL_CAUSAL_STRENGTH_REPORT.md` and `world_model_causal_strength.json` keep the self model and target set fixed while replacing only the world model used by the planner. Planner support orders `normal=2.1511` above `frozen=0.5499`, `shuffled=0.4974`, and `random=0.3188`, with margin checks. This remains lab-only E3 evidence; it does not prove world-model causal strength outside this lab target set, self-model causal strength, admission readiness, EgoOperator runtime efficacy, stable user benefit, live autonomy, consciousness, or subjective experience."
    - "VirtualCatPSPC v0 Task 2 now has local multi-seed / multi-layout / multi-object generalization evidence: the runner writes `GENERALIZATION_MATRIX_REPORT.md` and `generalization_matrix.json`, covering seeds `101,102,103`, layouts `center_room / near_wall`, and unstable object kinds `cup / vase / bottle / tall_box`. The aggregate status is `pass` with `case_count=24`, `min_caution_gap=0.72`, and `danger_action_rate=1.0`. This remains lab-only E3 evidence; it does not prove unlimited layout generalization, admission readiness, EgoOperator runtime efficacy, stable user benefit, live autonomy, consciousness, or subjective experience."
    - "VirtualCatPSPC v0 Task 0 + Task 1 hardening now records PSPC-local success separately from repo-wide verification: `summary.json` has PSPC-local `E4_passed`, repo-wide evidence remaining `E3`, `mainline_connected=false`, `enabled=false`, and the previously unavailable `verify_repo.py --mode fast` legacy/root OpenEmotion blocker has been removed by the archival purge without counting as a PSPC pass. The new anti-hardcoding audit reports zero object-name decision rule hits in core decision files, preserves caution under object renaming and without a `cup` name, and reduces caution when the instability feature is removed. This is local lab hardening only; it does not prove multi-layout generalization, admission readiness, EgoOperator runtime efficacy, stable user benefit, live autonomy, consciousness, or subjective experience."
    - "VirtualCatPSPC v0 now has a lab-only task package plus deterministic local lab artifacts: `python -m labs.virtual_cat_pspc_v0.run_experiments --out artifacts/virtual_cat_pspc_v0 --seeds 101,102,103` produced the required baseline, danger-generalization, ablation, and replay reports with PSPC-local `overall_status=E4_passed`. This is local lab mechanism evidence only; it does not connect PSPC to EgoOperator, change runtime authority, prove stable user benefit, live autonomy, durable memory efficacy, runtime efficacy, philosophical consciousness, or subjective experience."
    - "The current EgoOperator human-observation gate now has a fresh scripted real-provider v2 run: `EgoOperator/human_operator_trial.py --out EgoOperator/artifacts/human_operator_trial/v2_latest --run-scripted --auto-approve-writes` recorded `provider_mode=openrouter`, `observation_count=18`, `known_scenario_coverage=18`, `average_operator_score=5.0`, `memory_misuse_count=0`, and `gate_violation_count=0`. The trial evaluator was also corrected so explicit Chinese negation of `始终英文` is not counted as memory misuse and file-write approval proposals count as gate evidence. This remains `scripted_trial_needs_human_review`, not a human-observation pass or stable user benefit proof."
    - "The repo now has a bounded local `Codex session bootstrap + closeout gate` workflow record: `scripts/codex_session_guard.py bootstrap` summarizes program state, Codex memory, local task board, project contract, git remote/dirty state, Autopilot `plan-next`, and GitHub sync availability; `closeout-check` reports scoped staging, push, task-board mirror, and unavailable-sync blockers before publish. This is workflow hygiene only; it does not change the current EgoOperator phase/layer/evidence ceiling or prove stable real user benefit, live autonomy, durable memory efficacy, proactive messaging, or consciousness."
    - "The current EgoOperator human-observation gate has moved from the old `ego-handmade-human-operator-trial-v1` historical protocol to `docs/codex/tasks/ego-operator-human-operator-trial-v2/`. The v2 harness records `EgoOperator`-named JSON/Markdown trial reports and can run a scripted local session, but a real-provider run is still required before any natural-understanding or user-benefit claim."
    - "The repo now records the `EgoOperator` naming and docs reader-safety transition: tracked runtime files moved from `Ego_handmade/` to `EgoOperator/`, current authority/docs/scripts point at `ego_operator_first_transition`, and high-risk pre-EgoOperator reader entry docs carry safety banners/cross-links. This is a naming/docs safety transition record only; it does not prove stable real user benefit, live autonomy, durable memory efficacy, runtime efficacy, or consciousness."
    - "The repo previously recorded an `EgoOperator-first` transition under the former `Ego_handmade` name: `EgoOperator` is the default operator runtime candidate, old `EgoCore / OpenEmotion / ego_desktop_lab` have been moved under `legacy/ego-pre-handmade-mainline/`, and route/state/docs treat the old governed-proactivity lane as legacy evidence rather than the active default. This is a repo transition record only; it does not prove stable real user benefit, live autonomy, durable memory efficacy, runtime efficacy, or consciousness."
    - "The current lane now has a bounded local `subject final text candidate ownership` pass: OpenEmotion `subject_system_v1` candidates expose `final_text_candidate / language_hint / style_intent / content_grounding / generation_trace`, thought-probe synthesis binds generated visible candidates to their topic anchor, EgoCore delivery consumes only `final_text_candidate` for proactive visible text, and missing/template-like/unanchored/rejected candidates become `retry_later` or `held` instead of host-generated fallback templates. Public dialogue corpora are recorded only in an offline-eval manifest (`DailyDialog / XDailyDialog / CS-Dialogue / LCCC`) and are not imported into runtime prompts or product logic. This remains local proof only; no fresh always-on-listener live closeout is admitted."
    - "After the local `direct human continuation` patch, the always-on Telegram listener was restarted with explicit proactive autodrain environment and direct Windows Python execution. A fresh local operator status check reports one matching Telegram process (`matching_process_count=1`, `conflict_possible=false`, `listener_pid=49892`) and `check_telegram_proactive_soak_status.py` reports `armed` for allowlisted `telegram:dm:` / `8420019401`. This is process-hygiene evidence only; it is not a fresh live proactive send, direct-share reply, or BTC/web boundary recheck."
    - "Fresh live Telegram evidence on `2026-04-26 13:54-20:04` shows the explicit-recontact path can send proactive messages, but the visible messages were still too template-like: the listener emitted `围绕“随机的分享”，我想把刚才那个未完的点轻轻接回来。现在继续吗？` and later the chat lane answered direct-start requests with askback such as `你接着起个头？` / `由我直接起个头？` / `最近有没有碰到...`. This is admitted as a live quality regression, not a live closeout."
    - "The current lane now has a bounded local `direct human continuation` pass for the `2026-04-26 13:54-20:04` Telegram quality regression: Telegram ingress recognizes direct-start/share requests as `direct_share` with a no-askback output contract, `runtime_v2 chat_mainline` regenerates askback candidates into direct content, and proactive `bounded_reminder` host verbalization now emits direct topic continuation while anti-template gates block the old Chinese `轻轻接回来 / 现在继续吗` proactive template. This remains local proof only; no fresh always-on-listener live closeout is admitted."
    - "After the local `explicit recontact follow-up repair` patch, the always-on Telegram listener was restarted with explicit proactive autodrain environment and direct Windows Python execution. A fresh local operator status check reports one matching Telegram process (`matching_process_count=1`, `conflict_possible=false`, `listener_pid=63196`) and `check_telegram_proactive_soak_status.py` reports `armed` for allowlisted `telegram:dm:` / `8420019401`. This is process-hygiene evidence only; it is not a fresh live proactive send or BTC/web boundary recheck."
    - "The current lane now has a bounded local `explicit recontact follow-up repair` pass for the `2026-04-26 11:33-11:36` Telegram no-send regression: runtime v2 recognizes `待会儿你主动找我来聊吧` style same-thread recontact requests, promotes them into `initiative_trigger = bounded_reminder` with a synthetic `chat_followup:*` continuity ref, suppresses unrelated stale runtime goals such as `修改 test.html 配色`, and the current-lane idle scheduler can create a local pending proactive followup through the real ProtoSelfAdapter path. This remains local proof only; no fresh always-on-listener proactive send is admitted."
    - "The current lane now has a bounded local `weak-generic free-jump rebind + recent-topic sendable fallback` pass for the `2026-04-26 09:48-09:53` Telegram no-send regression: OpenEmotion no longer treats weak generic free-jump anchors such as `这种能力如何实现 / 这个能力如何实现 / 如何实现` as concrete outbound topics when a recent substantive topic exists, `subject_system_v1` records `raw_topic_anchor_summary / effective_topic_anchor_summary / topic_anchor_rebound_source`, and EgoCore delivery can apply a bounded recent-topic conversational fallback for the AI-autonomy transcript while preserving the strict abstract-meta hold when no fallback is explicitly allowed. This remains local proof only; no fresh always-on-listener proactive send is admitted."
    - "The current lane now has a bounded local `bare-continue topic-anchor repair + conversational candidate preference` pass for the `2026-04-25 20:52-20:57` Telegram no-send regression: shared `topic_anchor` filtering treats `conversation_act=thread_continue`, bare `继续` / `continue`, and short continuation prompts as non-substantive proactive anchors; `recent_dialogue_reflection` and the thought-probe scheduler recover the substantive anchor `你觉得AI实现自主性最大的瓶颈是什么？` instead of `继续`; and subject_system_v1 thought-probe selection now prefers sendable `topic_conversation_grade=conversational` candidates over higher-scored `meta_reflection_only` candidates without relaxing downstream humanization/topic-bound gates. This remains local proof only; no fresh always-on-listener proactive send is admitted."
    - "After the local `weak-generic free-jump rebind + recent-topic sendable fallback` patch, the always-on Telegram listener was restarted with explicit proactive autodrain environment and direct Windows Python execution. A fresh local operator status check reports one matching Telegram process (`matching_process_count=1`, `conflict_possible=false`, `listener_pid=60404`) and `check_telegram_proactive_soak_status.py` reports `armed` for allowlisted `telegram:dm:` / `8420019401`. This is process-hygiene evidence only; it is not a fresh live proactive send or BTC/web boundary recheck."
    - "The current lane now has a bounded local `proactive topic-anchor repair` pass for the `2026-04-25` Telegram no-send regression: shared `topic_anchor` filtering treats proactive permission/control turns as non-substantive, `recent_dialogue_reflection` and `thought_probe` delivery now skip the permission sentence `以后可以主动来找我，也可以自己找新话题，不用每次都问我`, and focused regressions recover the substantive anchor `你觉得AI实现自主性最大的瓶颈是什么？`. This remains local proof only; the stale 17:05 live window is not upgraded to fresh live evidence."
    - "The current lane now has a bounded local `thought_probe humanization guard` pass: EgoCore treats weak generic anchors such as `这种能力如何实现` as insufficient unless they can be rebound to a recent substantive topic, and still holds the final visible line with `proactive_meta_reflection_not_conversational` when it remains abstract meta/system-language rather than human conversation. This remains local proof only; fresh always-on Telegram self-DM recheck is pending."
    - "The default Telegram chat lane now has a bounded local `fresh-facts honesty` pass: for BTC price / live web-search requests on `runtime_v2 chat_mainline` without an explicit real tool route, EgoCore applies a host-owned `host_fresh_fact_boundary` reply and records `fresh_fact_boundary_applied=true` plus `fresh_fact_tool_route_available=false`. This remains local proof only; no live BTC/web lookup route is admitted."
    - "Fresh allowlisted self-DM live evidence on `2026-04-23` now shows that `thought_probe conversational anchor` remains local-only: after durable proactive permission plus a substantive-topic exchange, the always-on listener still emitted an abstract meta-style proactive line about `这种能力如何实现`, so live outbound quality has not yet converged to normal human-like conversation."
    - "Fresh Telegram BTC-price turns on `2026-04-23` now also expose a second independent issue on the default lane: the bot alternated between claiming it could and could not web-search for price data, while the current default `runtime_v2 chat_mainline` still does not execute real tool/web lookup on that path."
    - "The current lane now bridges `subject_system_v1 + host_proactive_decision` into the existing host-owned `mvp12.pending_proactive_followup.v1` contract via `EgoCore/app/runtime_v2/subject_system_v1_delivery_bridge.py`, and that bridge still passes through `ResponsePlan + output_check` before any transport-ready artifact exists."
    - "The host-governed proactive cycle now runs current-lane-first: it prefers `subject_system_v1` idle scheduling and only falls back to the legacy `initiative_scheduler` when the current lane surface is missing or explicitly disabled."
    - "A current acceptance runner `python3 scripts/codex/run_acceptance_subject_system_v1_self_dm_gate.py` now passes and records a bounded local chain proof across `subject_system_v1 -> host_proactive_decision -> pending_proactive_followup -> delivery -> outbox -> transport_gate` without claiming a fresh real Telegram send."
    - "The proactive transport gate no longer treats an idle `current_goal` continuity anchor as `state_busy`, so current-lane proactive outbox events can reach Telegram transport without dropping the continuity anchor itself."
    - "A fresh allowlisted self-DM sample has now been admitted on `telegram:dm:8420019401` through the current lane's host-governed chain. This sample is intentionally scoped as `operator_seeded_live_sample`, with `reply_origin = subject_system_v1_proactive`, `initiative_mode = subject_system_v1_suggest`, and real Telegram `message_id = 3554`."
    - "Telegram operator/runtime bring-up is now more explicit: the repo has a listener health tool for the always-on `app.main --telegram` process, current docs now separate one-shot senders from the formal listener, a focused regression proves that an ordinary follow-up after a proactive send still routes into the normal chat mainline locally, and one local operator bring-up observation now shows the formal listener can be started and inspected as `online` without widening the current claim ceiling."
    - "The repo now also has a non-runner proactive soak status tool that combines listener state, launcher proactive env snapshot, and autodrain log markers so operator testing can distinguish `disabled / waiting / armed / sent` without reusing the one-shot sender as false evidence. One local soak bring-up observation now reaches `armed` with `proactive_autodrain_started` plus `proactive_autodrain.loop.start` visible in `egocore_run.log`."
    - "The current lane now treats explicit same-thread follow-up requests such as `我等下会回来继续这个话题，你之后可以提醒我继续。` as a bounded idle-window continuity hint: runtime v2 promotes that chat signal into `initiative_context` as `bounded_reminder` with one synthetic `chat_followup:*` continuity ref, and focused local tests now prove that the current-lane idle scheduler can create a host-governed pending proactive followup from that signal without reviving a second kernel, second delivery schema, or external-event watcher semantics."
    - "Seed-profile continuity fallback is now narrower in the current lane's reminder path: when runtime v2 explicitly marks a same-thread reminder request as `explicit_same_thread_followup_request`, the seed kernel/affordance layer no longer reuses an unrelated stale `focus_goal.pending_commitment` as the dominant continuity anchor. Focused local seed-profile tests now prove that the stale pending commitment is cleared or ignored in that reminder-specific path while ordinary pending-commitment continuity remains intact outside it."
    - "Seed-profile stale-continuity suppression now also activates directly on explicit reminder user turns: when a live user message itself says `你之后可以提醒我继续` / `只发一个轻提醒，不要连续发`, the seed layer no longer keeps surfacing an unrelated old `continue_pending_commitment` action before the later idle-window reminder path can run."
    - "Current-lane Telegram proactive observability is now stronger locally: seed/profile traces now expose `pending_commitment_source`, runtime v2 initiative context now records whether the latest user turn matched explicit follow-up text and whether idle inference actually fired, the current-lane idle scheduler now records structured held context (`recent_user_turns_count / recent_assistant_replies_count / initiative_trigger / continuity_ref / candidate_family / selfhood_priority`), and the always-on Telegram autodrain loop now emits one per-session `telegram.proactive_autodrain.cycle ...` record even when the result is `held`."
    - "The proactive soak status tool can now surface the latest held cycle details (`last_cycle_*`) without needing a send, and the always-on listener has been restarted onto that observability patch while keeping the claim ceiling unchanged at bounded local diagnosis plus the earlier narrow operator-seeded sample."
    - "The explicit same-thread reminder path now completes a bounded local candidate-generation slice: runtime v2 supplies `continuity_confidence = 0.72` for `explicit_same_thread_followup_request`, OpenEmotion admits that host hint instead of dropping it, the initiative layer now treats eligible explicit same-thread reminders as `bounded_reminder` candidates even when `active_commitments_count == 0`, and a real `RuntimeV2ProtoSelfRuntime + ProtoSelfAdapter` idle-window integration test now reaches `pending_proactive_followup` with `candidate_family = bounded_reminder` through the current lane's existing host-governed bridge."
    - "The current lane now also has a bounded local AI-timed gate upgrade: OpenEmotion emits structured `timing_advice` on `host_proactive_candidate`, subject_system_v1 normalizes that timing surface into canonical candidate/trace payloads, EgoCore carries it through `pending_proactive_followup -> outbox -> transport_gate`, explicit user return now clears pending proactive outbox state, and focused regressions now prove the current lane can allow a timing-contract-governed proactive send before the legacy `900s` fallback would have fired. This remains local proof only; fresh live timing samples are still pending."
    - "Telegram proactive transport failures are now diagnosed rather than treated as a black box locally: `_send_chat_message` and proactive outbox draining now surface structured `transport_error_*` fields, queued outbox events now retain bounded retry/backoff metadata, the transport gate now holds on `transport_backoff_active` / `transport_backoff_ceiling` instead of retrying every `30s`, and the soak/listener tooling can now expose the last transport error plus retry window without widening the current claim ceiling beyond local proof."
    - "The current lane now also has a bounded local `thought_probe v1` pass: EgoCore persists a durable host-owned `proactive_topic_permission`, runtime v2 promotes that permission into `initiative_context` during idle windows, OpenEmotion can synthesize a `thought_probe` candidate from `background_thought_candidates`, and the existing arbiter/bridge stack can create `pending_proactive_followup` with dedupe on repeated `source_ref` before the user returns. This remains local proof only; no fresh live Telegram thought-probe sample is admitted yet."
    - "Chat ingress now recognizes explicit opinion-seeking turns such as `你怎么看 / 你有没有什么想法 / 说说你的想法` as `solicited_view`, binds them to the latest substantive topic anchor, and locally enforces a `viewpoint + one follow-up question` contract instead of generic askback."
    - "The current lane now also has a bounded local `thought_probe v2 adaptive outreach` pass: durable host-owned policy now includes `outreach_aggression_mode = high_exploration`, `outreach_feedback_adaptation = enabled`, and `quiet_state / quiet_until`, `/new` still preserves those durable controls, runtime v2 now derives explicit and inferred feedback signals after proactive sends, subject_system_v1 thought-probe synthesis now avoids the most recently sent `source_ref` while preferring free-jump internal-reflection candidates when available, and the arbiter now fully holds on `paused` while requiring higher-value candidates under `reduced` mode. This remains local proof only; no fresh live adaptive thought-probe sample is admitted yet."
    - "Host-owned expression governance now includes a bounded anti-template gate on normal chat/proactive success paths: generic askback or fixed family template phrases are rewritten into degraded fallback with explicit `fallback_origin`, and current-lane proactive delivery now verbalizes from structured candidate semantics (`topic_summary / message_shape_hint / open_question`) instead of relying on arbiter family fixed strings as the final user-visible owner."
    - "The current lane now also has a bounded local `thought_probe anti-spam + zero-template` pass: OpenEmotion compresses same-cluster `background_thought_candidates` before selection, host runtime tracks one per-user-turn proactive outreach history keyed by `topic_fingerprint / topic_cluster_ref / sent_text_fingerprint`, the bridge now blocks same-cluster or same-text repeats before the user returns, a host pacing floor (`20m normal / 45m reduced`) now gates second thought probes even when timing advice is earlier, proactive anti-template/specificity violations now hold instead of degrading to fixed strings, and soak diagnostics now expose topic/text fingerprints so live held reasons are auditable. This remains local proof only; no fresh live Telegram anti-spam sample is admitted yet."
    - "The current lane now also has a bounded local `thought_probe topic-bound sendability` pass: subject_system_v1 computes `topic_anchor_summary / topic_binding_mode / topic_sendability` before selecting a sendable thought probe, pure `meta_only` premise-gap reflections are filtered out of proactive delivery, host verbalization now rewrites meta reflections against a concrete topic anchor or holds them, and cycle/soak observations now surface those binding/sendability fields on both held and sent paths. This remains local proof only; no fresh live Telegram topic-bound thought-probe sample is admitted yet."
    - "The current lane now also has a bounded local `thought_probe conversational anchor` pass: shared substantive-topic extraction now prevents prompt-like `solicited_view` turns from becoming proactive anchors, structured recent user-turn records carry `conversation_act` and timestamp into `recent_dialogue_reflection`, `thought_probe` candidates now expose `topic_anchor_source / topic_anchor_kind / topic_conversation_grade`, and EgoCore delivery now performs one constrained conversational rewrite or else holds when the candidate still sounds like prompt echo or meta reflection. This remains local proof only; no fresh live Telegram conversational thought-probe sample is admitted yet."
  blocked_by: []
  key_unknowns:
    - "Whether the exact-six-file visible-life playground can launch through its real Tk entrypoint, persist/recompute through SQLite, and expose non-cosmetic intervention toggles; Phase B is authorized but not started."
    - "Whether `EgoOperator` remains more natural after longer real LLM sessions, not just deterministic NoLLM tests."
    - "Whether local memory stays useful without prompt pollution across multi-day operator use."
    - "Which legacy algorithms should be reimplemented as EgoOperator primitives, and which should remain reference-only."
    - "Whether future command/network transaction approvals should be added after file-write transaction approval has proved ergonomic."
    - "What evidence threshold should trigger any future deletion of legacy code rather than reference preservation."
  next_minimal_action: "After the committed Phase-A Red receipt and clean validator readback, implement exactly labs/ego_life_playground_v0/{__init__,engine,store,app}.py, scripts/run_ego_life_playground_v0.py, and tests/test_ego_life_playground_v0.py. Do not touch EgoDesktop, EgoOperator, LLM/network, runtime/mainline authority, or science routes."
  last_verified_commit: "af561e0d"
  last_verified_at: "2026-05-18T03:48:22Z"
  status_owner: "EgoOperator"
  status_note: "Default repo work is now `EgoOperator-first` (formerly `Ego_handmade-first`). Legacy `EgoCore / OpenEmotion / ego_desktop_lab` are preserved under `legacy/ego-pre-handmade-mainline/` as reference/fallback/algorithm sources. The previous subject-system-v1 evidence chain remains historical support, but no longer owns the active default implementation lane. This naming/docs safety transition is local/repo-level only and does not prove stable real user benefit, runtime efficacy, live autonomy, durable memory efficacy, or consciousness."
  claim_thresholds:
    mainline_claim_requires: "E4"
    enabled_claim_requires: "E4"
    stable_claim_requires: "E5"
    institutionalized_claim_requires: "E6"

route_guard:
  schema_version: ego.route_guard.v3
  route_revision_id: EGO_VISIBLE_LIFE_PROXY_V0_ROUTE_REPLACEMENT_001A
  route_fingerprint: 63a54cb04c634042e27b1af9500cbb1dd87d5d9941959a5abfeac28954f1f4de
  authority_source:
    authority_rule: ITL committed objects are sole authority for the terminal Card2 closure and science firewall; Ego PROGRAM_STATE_UNIFIED is
      sole authority for the separate product-only playground action
    repo: intelligence-theory-lab
    pinned_commit: 55706c734a2bf25ba1d9d2aa273283ed4dc39802
    objects:
      route_state:
        path: artifacts/ROUTE-STATE-MACHINE-001A/routes/EGO-PET-WORLD-V1-CAPABILITY-CARD-BANK-ADMISSION-001A/state.json
        git_blob_oid: 75a83aaf8cdea464696daf8d940dd784c5c86302
        git_blob_payload_sha256: 6dc4111604f37ecde5c0d79a35da3fbe278e021bb780ace82139bf559d7fe8e2
      events:
        path: artifacts/ROUTE-STATE-MACHINE-001A/routes/EGO-PET-WORLD-V1-CAPABILITY-CARD-BANK-ADMISSION-001A/events.jsonl
        git_blob_oid: 85da9f0756c97a1dfed1d8f1a909f5c02f131b0a
        git_blob_payload_sha256: 136192a04ef05f7b3880a174e03b4a8cd921a56cdcc5a662f78e8beb576c4d91
      closure:
        path: artifacts/ROUTE-STATE-MACHINE-001A/routes/EGO-PET-WORLD-V1-CAPABILITY-CARD-BANK-ADMISSION-001A/closure.json
        git_blob_oid: cbe62daa7da79308c63361d01d1429418c5537d3
        git_blob_payload_sha256: 490ee2cf6f52952e3300069d42fb95abe6e8a589cc7cc3498221e1fbb39401e6
      closure_validation_report:
        path: artifacts/ROUTE-STATE-MACHINE-001A/routes/EGO-PET-WORLD-V1-CAPABILITY-CARD-BANK-ADMISSION-001A/closure_validation_report.json
        git_blob_oid: 24c6bdb1177999df85c194810ba04893338f60b5
        git_blob_payload_sha256: 2274cef44df64e94ba15c4741aadfe79431538da2499b1e9a32ab3efdcd5a36d
      task_card:
        path: docs/codex/tasks/EGO-VISIBLE-LIFE-PROXY-V0-ROUTE-REPLACEMENT-001A.md
        git_blob_oid: 6c4f61255c25fa4ce26b6064b718e7e43b627db2
        git_blob_payload_sha256: 59b7f775f72fc556eb0192970df3e5cd581f0c11198b1a7ce2d3d07e2aa5c439
      itl_red_review:
        path: artifacts/EGO-VISIBLE-LIFE-PROXY-V0-ROUTE-REPLACEMENT-001A/PHASE_A_RED_REVIEW.json
        git_blob_oid: 3500550829c55297f1fc70151db5f0f193b82330
        git_blob_payload_sha256: 3a4fa320c322b71f30a12ee0d51958d753b8d9a16bf8f960f8f4746933acfe4a
  transcribed_itl:
    route_state:
      action_dependencies:
        bank_EGO-PET-WORLD-V1-CAPABILITY-HEADROOM-001A:
          blocked_until:
          - ADMISSION_INVALID_PATH_POLICY
          dependency_evaluation: TERMINAL_CLOSURE
          execution_authorized: false
          permission: NONE_CLOSED
      action_path_policy:
        action_id: bank_EGO-PET-WORLD-V1-CAPABILITY-HEADROOM-001A
        allowed_exact_paths: []
        allowed_path_prefixes:
        - docs/codex/tasks/ego-pet-world-v1-capability-headroom-001a/
        execution_inference: CHANGED_PATHS_NOT_DECLARED_BOOLEAN
        forbidden_path_prefixes:
        - EgoOperator/
        - EgoDesktop/
        - packages/
        - src/
        - scripts/
        - tests/
        - artifacts/
        missing_mutation_scope: REJECT
        self_declared_scope_expansion: false
      action_resolution:
        banking_usable_now: false
        effective_capability_card_bank_action_id: null
        exact_action_identity_live: false
        old_generic_action_live: false
        state: CLOSED_ADMISSION_INVALID_PATH_POLICY
      active_mechanism_frontier: none
      allowed_next_actions:
      - run_route_state_machine_validation
      authorizations:
        capability_card_bank: false
        capability_implementation: false
        experiment_execution: false
        mainline: false
        mechanism_evidence: false
        remote_anchor: false
        runtime: false
        science_successor: false
        scoring: false
      authorized_implementation_targets: []
      capability_route_registered: false
      claim_ceiling:
        forbidden_claims:
        - card2_banked
        - card2_executed
        - headroom
        - learning
        - mechanism_validity
        - science_successor_admission
        - runtime_effect
        - mainline_effect
        - agency
        - autonomy
        - subjectivity
        - consciousness
        - electronic_life
        - ego_readiness
        - product_benefit
        max: route-admission defect closure and product/science firewall only
      closed_by_task: EGO-VISIBLE-LIFE-PROXY-V0-ROUTE-REPLACEMENT-001A
      closure_type: GOVERNANCE_STOP
      created_by_task: ITL-EGO-PET-WORLD-V1-CARD2-BANK-ADMISSION-TRANSITION-001A
      current_state: ADJUDICATED
      failure_class: ADMISSION_INVALID_PATH_POLICY
      forbidden_next_actions:
      - bank_EGO-PET-WORLD-V1-CAPABILITY-HEADROOM-001A
      - execute_EGO-PET-WORLD-V1-CAPABILITY-HEADROOM-001A
      - repair_reopen_or_rerun_card2_bank_admission
      - start_capability_implementation_under_closed_card2_action
      - start_experiment_execution
      - start_scoring
      - enable_runtime_or_mainline
      - register_or_satisfy_science_successor
      - reopen_or_modify_old_k0
      - push_tag_or_remote_anchor
      implementation_authorized: false
      mainline_authorized: false
      mechanism_evidence_authorized: false
      path_policy_contradiction:
        candidate_task_path: docs/codex/tasks/ego-pet-world-v1-capability-headroom-001a/STAGE_CARD.md
        ego_commit: 0c1a0efbfd155a10552f84d83bdb7a2589e94383
        generated_index_path: docs/codex/tasks/TASK_LANE_INDEX.md
        generator_source_path: scripts/codex/route_convergence_common.py
        guard_source_path: scripts/codex_session_guard.py
        task_prefix: docs/codex/tasks/ego-pet-world-v1-capability-headroom-001a/
      phase: CARD2_BANK_ADMISSION_CLOSED_ADMISSION_INVALID_PATH_POLICY
      pilot_1_reuse:
        positive_control: false
        regression_baseline: false
        unblock_requires_verified_audit_ref: true
      prior_negative_evidence_manifest:
      - disposition: INSTRUMENT_INVALID_REFERENCE_ONLY
        lineage_id: FSP-L005-PUM-ENV-V0
      - disposition: MANDATORY_CARD2_CONTROLS
        lineage_id: FSP-L015-L016-CACHE-DRIFT-CHALLENGERS
      - disposition: BASELINE_EQUIVALENCE_REFERENCE_ONLY
        lineage_id: FSP-L018-N2
      - disposition: IMPLEMENTATION_DEFECT_SCIENCE_NOT_TESTED
        lineage_id: FSP-L026-H0-PRECONDITION
      - disposition: BANKED_ACCEPTED_BOUNDED_ENGINEERING_ONLY
        lineage_id: FSP-L027-FOUNDATION
      - disposition: CLOSED_WITHOUT_SCIENCE_ADJUDICATION
        lineage_id: FSP-L028-K0-CLOSURE
      - disposition: TERMINAL_NO_FURTHER_REPAIR
        lineage_id: FSP-L029-VERIFIER-DOWNGRADE
      - disposition: HIGH_SCORE_NO_ATTRIBUTION
        lineage_id: EGO-PET-WORLD-P0
      - disposition: INSTRUMENT_INVALID_PRESERVED
        lineage_id: EGO-PILOT-1-INITIAL
      - audit_ref: null
        disposition: COMPLETED_REFERENCE_ONLY_REPAIR_LINEAGE_UNAUDITED
        lineage_id: EGO-PILOT-1-REPAIR
        positive_control_reuse: false
        regression_reuse: false
      - disposition: HISTORICAL_CLOSED_INVALID_REFERENCE_ONLY
        lineage_id: EGO-LEARNED-OUTCOME
      route_family: EGO-CAPABILITY-CARD-BANK-ADMISSION
      route_id: EGO-PET-WORLD-V1-CAPABILITY-CARD-BANK-ADMISSION-001A
      route_kind: CROSS_REPO_CAPABILITY_CARD_BANK_ADMISSION
      runtime_authorized: false
      science_firewall:
        card2_science_weight: 0
        inherits_h0_h1_freeze_formal: false
        inherits_old_k0r: false
        may_reopen_old_k0: false
        may_satisfy_science_successor_boundary: false
        may_supply_mechanism_attribution: false
        satisfies_old_s0x: false
      science_successor_authorized: false
      source_boundaries:
        closed_card2_action:
          action_id: bank_EGO-PET-WORLD-V1-CAPABILITY-HEADROOM-001A
          disposition: CLOSED_ADMISSION_INVALID_PATH_POLICY
          live: false
        old_generic_action:
          action_id: bank_EGO_learned_outcome_kernel_capability_card_under_separate_task_after_EGO_sync
          disposition: SUPERSEDED_FOR_LIVE_BANKING_BY_EXACT_CARD2_ACTION
          live: false
        old_k0_terminal_packet:
          inherited_authority: false
          relation: FROZEN_FIREWALL_REFERENCE_ONLY
        old_learned_outcome_lineage:
          disposition: HISTORICAL_CLOSED_INVALID_REFERENCE_ONLY__NOT_AUTHORIZED
          live: false
      source_readback:
        ego_path_policy_contradiction:
          commit: 0c1a0efbfd155a10552f84d83bdb7a2589e94383
          objects:
          - blob: dc66e02a279e31fcd8acc2f181f951d863c73705
            path: docs/PROGRAM_STATE_UNIFIED.yaml
            sha256: d27792771a0934660f98327a095f80bd7d8fea20807442e21369ff005dac2745
          - blob: d2a566c0590359b30d178f8fd0d2f4852d1e1f4c
            path: docs/codex/tasks/TASK_LANE_INDEX.md
            sha256: 6546e676e6c1f138f4662b40fe9620a53fb8fa9c1e4e17b216633ae52e39cf32
          - blob: 1e754112c8cb0a4c49f018d90b3aab0eecba8087
            path: scripts/codex_session_guard.py
            sha256: ee6377a2e045de94084c901714a8886e601b0504fd0720fad95a5744c7d54e59
          - blob: 7e5e23c8e2540611f5f0fd8a955df5574848f83d
            path: scripts/codex/route_convergence_common.py
            sha256: 01292414f0e78be15654800fd4c94d752bd12ead5fd517b802a7ba052fb395a1
          - blob: 9d81f90f1cafd0ddb8869abbe280bd325509976e
            path: scripts/codex/verify_route_convergence.py
            sha256: 060477d69dc92599d75049f4a62ddb858070f3cf87994be9421d94dd44f2d50a
          - blob: 9dd71d9a8fa661ea90b32dfc430668636e8da4c4
            path: docs/codex/tasks/ego-pet-world-v1-card2-itl-authority-sync-001a/PHASE_B_RED_REVIEW.json
            sha256: 151bc9108b484eeec79a18f1c7f1cfabe0714f8a631582168edb21f7951f3f1b
          relation: PINNED_CALLABLE_GOVERNANCE_SOURCE
        ego_source:
          commit: 21e09c1ede7c051a4aa969dc3fcc2d0267084570
          objects:
          - blob: 3b15cdebec71324e4d6421889610c0f4bb06c4c6
            path: docs/PROGRAM_STATE_UNIFIED.yaml
            sha256: 9f1cc8dbe8451f2a10a844a97ba80e0dd531b0a3f33f5b2fc415780d21516fe5
          - blob: b57967d87b4771709a4f48c91d59b8a52139426d
            path: docs/codex/tasks/ego-route-8692-supersession-and-egodesktop-authority-archive-001a/STAGE_CARD.md
            sha256: 52ab26db92ac01b779bd236d3c57e66a140245034c59cb7a846b655aefd88791
          - blob: cfc7b7afcd092c42ef548ff4e14d7850be3c752c
            path: artifacts/egodesktop_pet_world_integration_001a/p0/audit_claude_full_hostile_001.json
            sha256: 137eea4e93dace1e327133edeba3ce16e6cdd7f03e4c5e5cf4fb8243d5dbd4b7
          - blob: 68f79822da5fbe760d3c789249c93acb6756ec3d
            path: artifacts/ego-pet-capability-conformance-001a/result.json
            sha256: d3a042d134543826a0e70467a12c048f0aee549f1acfadcb5050bb2dbc18501b
          - blob: 7d3e5fec0479eaf9754244009d97d121fbbe5311
            path: artifacts/ego-pet-capability-conformance-001a/repair_001/result.json
            sha256: 53da6ead436c19e2a8d415b4dbc2a1881ce7b48c65c42d71e205cddd281d731d
          - blob: b8fa7cf552efb41323a34d723653ff81cfa8c4db
            path: docs/codex/tasks/ego-learned-outcome-kernel-capability-001a/P1R0_DESIGN_INVALID_AUDIT_RESULT.json
            sha256: a2aed0bc994592b54f260d74923896eabc0982d3e47ea6c3b1a64879be7061ec
          relation: READ_ONLY_PINNED_SOURCE__LATER_HEAD_ADVANCE_ALLOWED
        historical_registration:
          commit: cb6cd57b8405bbac378f2857766ddaf63e08e194
          objects:
          - blob: 8b2db13a023873775b80bfe4e8eab7e53a7bba62
            path: artifacts/ROUTE-STATE-MACHINE-001A/routes/EGO-PET-WORLD-V1-CAPABILITY-CARD-BANK-ADMISSION-001A/state.json
            sha256: 9a56cd903e9b864db282c31736f04afd1007e6c5e430df294e80eafb41c839b2
          - blob: 29e28c15bc3dd6d04b7dd5b892cb70f205540e2a
            path: artifacts/ROUTE-STATE-MACHINE-001A/routes/EGO-PET-WORLD-V1-CAPABILITY-CARD-BANK-ADMISSION-001A/events.jsonl
            sha256: 5a30ed41854dc75c92cb5c04a2d2086007f565411949fadf9491c4c6718e1f44
          - blob: f5c58e4d4cfb8b734e5e339845ddb4d0a3e3c5b5
            path: artifacts/ROUTE-STATE-MACHINE-001A/routes/EGO-PET-WORLD-V1-CAPABILITY-CARD-BANK-ADMISSION-001A/admission_validation_report.json
            sha256: 447772dcf4a1e34aa3f1229bcad421a7d42b35eada0fc4f480dea2116dd92dd8
          relation: IMMUTABLE_HISTORICAL_REGISTRATION
        old_k0_terminal_packet:
          objects:
          - blob: 261b9ce78fd88a70c5f78646ce9209eb8bacee08
            path: artifacts/ROUTE-STATE-MACHINE-001A/routes/K0-DUAL-TRACK-SUPERSESSION-001A/state.json
            sha256: c31158f8713a9ba6df4255c8bed938ae0dd8b84fb2088d71741d536442b5ab5f
          - blob: e1a7d8d595004c07cea7c8fd70cb1738d42e3b5a
            path: artifacts/ROUTE-STATE-MACHINE-001A/routes/K0-DUAL-TRACK-SUPERSESSION-001A/closure.json
            sha256: bfe1d92cbb1938f6bfe206f84693fef90f64c1458ec576eb384bd5869de3ff93
          - blob: 5ec92897a15fea67f47fbec444a665511c6e4fef
            path: artifacts/ROUTE-STATE-MACHINE-001A/routes/K0-DUAL-TRACK-SUPERSESSION-001A/events.jsonl
            sha256: fb363cf87d54aa1b33c4c66975f5e02dc4dcaef234b28f0d67790f643eb3e468
          - blob: 377d09780e0a3949a172d89c3bc12de40245f8e8
            path: artifacts/ROUTE-STATE-MACHINE-001A/program_state.json
            sha256: 79915a56fa7c6735d7cbc3941972f4c709144b2bb75a79911a65ee08ddb90807
          - blob: a617d17b855433e14832cf4f1149396548545d67
            path: artifacts/ROUTE-STATE-MACHINE-001A/STATUS.md
            sha256: 3883d6b73471ba63f740859733864af34e4baf01a17b9b9bada04a2861ec5f6b
          - blob: a48032af0cccdce55074cd648ca53ed97cc9971b
            path: artifacts/ROUTE-STATE-MACHINE-001A/validation_report.json
            sha256: 8ce64494cedb970bd43299cb6b53177e1cde38e95d678ee3fa8fcda014f31fe9
          - blob: 1ab8de60021d8492c42f2a0984d4564b2d550509
            path: docs/research/FSP-STAGE-LEDGER.md
            sha256: 2e71d576581a2917c6c6a1f7779035f03f7bc2aaf225b92ec5b70b7988564ff7
          relation: FROZEN_FIREWALL_REFERENCE_ONLY
        phase_a:
          card_blob: 9859765cd20044568f093c6c9579c37014f8ee21
          card_path: docs/codex/tasks/ITL-EGO-PET-WORLD-V1-CARD2-BANK-ADMISSION-TRANSITION-001A.md
          card_sha256: 1f5a834724e1aa967a8d89ea5e70006057b0161cb2ff79c9b8a6aa5fc95ac9d9
          commit: f77187970786b27e85d93200a37ff427c2e3c243
          parent: b67c94fe1244ef6006ed3af8e924d4c670fe64bb
          review_manifest_sha256: 5dcb6e72951cd142820572da17852179f32ff467294d1d6415f90c934d78bb78
          review_receipt_blob: 650e332bb12a2e0011de2666cf931eeb86d96cce
          review_receipt_path: artifacts/ITL-EGO-PET-WORLD-V1-CARD2-BANK-ADMISSION-TRANSITION-001A/phase_a_red_precheck.json
          review_receipt_sha256: ffaa46f2ed0f0a250897fb8808abc8c7c40bda695317507addf4089bc4dcc05f
        shared_contracts:
        - blob: adfeaab2be3d44c2d7ac75b848217e8cff71af93
          path: docs/codex/contracts/BASELINE-IMMUNITY-ADMISSION-STANDARD-001A.md
          sha256: 7bd41a049a28aded4fb2eb23ced5bc8a71f80a5bb7fb5abd6d32931c37e80a03
        - blob: 1abce34fb7683820460e917da9dbfe89e6fe45c7
          path: docs/codex/contracts/BASELINE-IMMUNITY-ADMISSION-STANDARD-001A.registry.json
          sha256: 4a3170de614d697a7ffd99d4e524ad7a2a61585f49f3f095f00b1fd6e899b43f
        - blob: eb7e0e990d7795dcaa265122de7044dd681fb1b7
          path: docs/codex/contracts/LEARNING-SUCCESS-CRITERION-STANDARD-001A.md
          sha256: 1865da675e57a9e84fb9fd2409aedac9209f8f33fdee159bd559be6dfdf5f7db
        - blob: f96bb93f2bf6072c9fdd0c3d4bd81d41a2414e6f
          path: docs/codex/contracts/MECHANISM-SIGNATURE-VERDICT-STANDARD-001A.md
          sha256: 79b83e9b0291d5e45ecc4b8c1c6d94f78864c8d50698970376a380e8b6862031
      theory_pressure_authorized: false
      updated_at_utc: '2026-07-14T13:40:44.472457Z'
    closure:
      allowed_next_actions:
      - run_route_state_machine_validation
      authorizations:
        capability_card_bank: false
        capability_implementation: false
        experiment_execution: false
        mainline: false
        mechanism_evidence: false
        remote_anchor: false
        runtime: false
        science_successor: false
        scoring: false
      authorized_implementation_targets: []
      claim_ceiling:
        forbidden_claims:
        - card2_banked
        - card2_executed
        - headroom
        - learning
        - mechanism_validity
        - science_successor_admission
        - runtime_effect
        - mainline_effect
        - agency
        - autonomy
        - subjectivity
        - consciousness
        - electronic_life
        - ego_readiness
        - product_benefit
        max: route-admission defect closure and product/science firewall only
      closure_type: GOVERNANCE_STOP
      evidence_status:
        instrument_validity: NOT_APPLICABLE_GOVERNANCE_CLOSURE
        mechanism_evidence: NOT_TESTED
        path_policy_contradiction: COMPUTED_PRESENT
      failure_class: ADMISSION_INVALID_PATH_POLICY
      forbidden_next_actions:
      - bank_EGO-PET-WORLD-V1-CAPABILITY-HEADROOM-001A
      - execute_EGO-PET-WORLD-V1-CAPABILITY-HEADROOM-001A
      - repair_reopen_or_rerun_card2_bank_admission
      - start_capability_implementation_under_closed_card2_action
      - start_experiment_execution
      - start_scoring
      - enable_runtime_or_mainline
      - register_or_satisfy_science_successor
      - reopen_or_modify_old_k0
      - push_tag_or_remote_anchor
      historical_registration_ref: cb6cd57b8405bbac378f2857766ddaf63e08e194:artifacts/ROUTE-STATE-MACHINE-001A/routes/EGO-PET-WORLD-V1-CAPABILITY-CARD-BANK-ADMISSION-001A/state.json
      mainline_authorized: false
      mechanism_evidence_authorized: false
      phase: CARD2_BANK_ADMISSION_CLOSED_ADMISSION_INVALID_PATH_POLICY
      route_id: EGO-PET-WORLD-V1-CAPABILITY-CARD-BANK-ADMISSION-001A
      runtime_authorized: false
      science_firewall:
        card2_science_weight: 0
        inherits_h0_h1_freeze_formal: false
        inherits_old_k0r: false
        may_reopen_old_k0: false
        may_satisfy_science_successor_boundary: false
        may_supply_mechanism_attribution: false
        satisfies_old_s0x: false
      task_id: EGO-VISIBLE-LIFE-PROXY-V0-ROUTE-REPLACEMENT-001A
      theory_pressure_authorized: false
  closure_crosswalk:
    path: docs/codex/tasks/ego-visible-life-proxy-v0-route-replacement-001a/ITL_CLOSURE_CROSSWALK.json
    artifact_payload_sha256: 1367d9f8abb3492f781b6e71f69e9d9fba07abf52e60f823a2fca1d574b4597c
  product_authority:
    authority_axis: EGO_PRODUCT_CAPABILITY_ONLY
    task_id: EGO-VISIBLE-LIFE-PROXY-V0-ROUTE-REPLACEMENT-001A
    action_id: implement_EGO-VISIBLE-LIFE-PROXY-V0-001A
    state: AUTHORIZED_NOT_STARTED
    allowed_next_actions:
    - implement_EGO-VISIBLE-LIFE-PROXY-V0-001A
    - run_route_state_machine_validation
    forbidden_next_actions:
    - modify_EgoDesktop_or_EgoOperator
    - add_LLM_or_network_integration
    - enable_runtime_or_mainline
    - grant_runtime_authority
    - register_or_satisfy_science_successor
    - claim_mechanism_learning_agency_or_electronic_life
    - repair_reopen_or_rerun_closed_card2_action
    - push_tag_or_remote_anchor
    authorizations:
      implementation: true
      local_manual_validation: true
      experiment_execution: false
      mainline: false
      mechanism_evidence: false
      remote_anchor: false
      runtime: false
      science_successor: false
      scoring: false
    authorized_implementation_targets:
    - labs/ego_life_playground_v0/__init__.py
    - labs/ego_life_playground_v0/engine.py
    - labs/ego_life_playground_v0/store.py
    - labs/ego_life_playground_v0/app.py
    - scripts/run_ego_life_playground_v0.py
    - tests/test_ego_life_playground_v0.py
    enabled: false
    default_enabled: false
    mainline_connected: false
    runtime_authority: none
    science_weight: 0
    real_trigger_evidence: absent_pre_phase_b
    action_path_policy:
      allowed_exact_paths:
      - labs/ego_life_playground_v0/__init__.py
      - labs/ego_life_playground_v0/engine.py
      - labs/ego_life_playground_v0/store.py
      - labs/ego_life_playground_v0/app.py
      - scripts/run_ego_life_playground_v0.py
      - tests/test_ego_life_playground_v0.py
      allowed_path_prefixes: []
      forbidden_path_prefixes:
      - EgoDesktop/
      - EgoOperator/
      - packages/
      - providers/
      - deployment/
      - network/
      - LLM/
      missing_mutation_scope: REJECT
      self_declared_scope_expansion: false
    science_firewall:
      science_weight: 0
      may_supply_mechanism_attribution: false
      may_satisfy_science_successor_boundary: false
      inherits_old_k0: false
      may_reopen_old_k0: false
    phase_b_contract:
      mutation_scope_path: docs/codex/tasks/ego-visible-life-proxy-v0-route-replacement-001a/MUTATION_SCOPE_PHASE_B.yaml
      exact_changed_paths:
      - labs/ego_life_playground_v0/__init__.py
      - labs/ego_life_playground_v0/engine.py
      - labs/ego_life_playground_v0/store.py
      - labs/ego_life_playground_v0/app.py
      - scripts/run_ego_life_playground_v0.py
      - tests/test_ego_life_playground_v0.py
      blocked_until:
      - COMMITTED_ITL_CLOSURE_PIN_VALID
      - EXHAUSTIVE_ITL_CLOSURE_CROSSWALK_VALID
      - EGO_PRODUCT_AUTHORITY_EXACT
      - EGO_PHASE_A_RED_REVIEW_BOUND
      - EXACT_SIX_PATH_POLICY_ENFORCED
    claim_ceiling:
      max: local product-clock playground implementation authority only
      forbidden_claims:
      - headroom
      - mechanism_validity
      - general_learning
      - emotional_understanding
      - thinking
      - autonomous_desire
      - agency
      - autonomy
      - subjectivity
      - consciousness
      - electronic_life_demonstrated
      - ego_readiness
      - mainline_effect
      - stable_user_benefit
    source_closure_relation: REPLACEMENT_PRODUCT_ACTION_DOES_NOT_INHERIT_CLOSED_CARD2_OR_OLD_K0_SCIENCE_AUTHORITY
  authority_state:
    old_route_8692:
      disposition: SUPERSEDED_BEFORE_M1
      m1: NOT_STARTED_CANCELLED_BY_SUPERSESSION
      m2: NOT_STARTED_CANCELLED_BY_SUPERSESSION
      m3: NOT_STARTED_CANCELLED_BY_SUPERSESSION
    egodesktop:
      archive_state: ARCHIVED_LEGACY_LLM_UI_ROUTE
      active_route_dependency: false
      successor_dependency: false
      runtime_authority: none
    k0_successor:
      enabled: false
      mainline_connected: false
      runtime_authority: none
      real_trigger_evidence: absent
    foundation:
      evidence_state: BANKED_ACCEPTED_BOUNDED
      runtime_enabled: false
    ego_operator:
      active_default: true
    relation: historical_local_surface_status_not_route_authority
  route_views:
    task_routes:
      ego-operator-human-operator-trial-v2:
        lane: active_default
        label: EgoOperator Human Operator Trial v2
        why: Current EgoOperator human-observation gate; records whether the operator-first runtime is actually usable in continuous Chinese operator
          work.
        workstream_id: ego_operator_first_transition
      ego-canonical-mechanism-integration-001a:
        lane: closed_evidence
        label: EGO Canonical Mechanism Integration 001A
        why: Additively superseded before M1; M1/M2/M3 were not started and are cancelled; no enabled path, mainline connection, successor dependency,
          runtime authority, or real trigger.
        workstream_id: canonical_mechanism_integration_route_001a
      ego-route-8692-supersession-and-egodesktop-authority-archive-001a:
        lane: closed_evidence
        label: EGO Route 8692 Supersession and EgoDesktop Authority Archive 001A
        why: Completed product/capability governance correction; the only following permission is Card 2 drafting and banking, not execution.
      ego-pet-world-v1-card2-itl-authority-sync-001a:
        lane: closed_evidence
        label: EGO Pet World v1 Card 2 ITL Authority Sync 001A
        why: Field-by-field governance sync from committed ITL objects; no Card 2 bank, execution, runtime, product, experiment, scoring, or science
          authorization.
        workstream_id: program_state_governance
      ego-visible-life-proxy-v0-route-replacement-001a:
        lane: parked
        label: EGO Visible Life Proxy v0 Route Replacement 001A
        why: Operator-authorized product-clock playground implementation is exact-path bounded and not yet started; explicit local process only,
          disabled by default, non-mainline, no runtime authority, and zero science weight.
    current_surfaces:
    - surface: operator_runtime
      paths:
      - EgoOperator/
      role: Sole active default operator-first runtime candidate.
      authority: active_default=true; runtime behavior remains outside this governance mutation.
    - surface: superseded_8692_route
      paths:
      - packages/ego_k0_kernel/
      - labs/virtual_cat_pspc_v0/
      - docs/EGO_CANONICAL_MECHANISM_INTEGRATION_ROUTE_001A.md
      role: Historical K0 Foundation -> VirtualCat-derived adapter lineage, superseded before M1.
      authority: closed evidence/reference only; disabled, non-mainline, no runtime authority or real trigger.
    - surface: egodesktop_legacy_reference
      paths:
      - EgoDesktop/
      role: Historical LLM/UI reference retained in place.
      authority: archived legacy reference only; not an active route, successor dependency, or runtime target.
  red_review:
    authority_change_requires_independent_review: true
    phase_a:
      path: docs/codex/tasks/ego-visible-life-proxy-v0-route-replacement-001a/PHASE_A_RED_REVIEW.json
      receipt_commit_relation: SAME_DIRECT_CHILD_COMMIT__RECEIPT_EXCLUDED_FROM_REVIEWED_MANIFEST
    review_invalidated_by_diff_change: true
    unresolved_review_blockers: []
  claim_ceiling: product-clock implementation authorization and terminal Card2 closure transcription only
workstreams:
  - id: "ego_operator_first_transition"
    owner: "EgoOperator"
    status: "human_operator_trial_v2_human_review_template_ready"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: true
    enabled: true
    summary: "`EgoOperator` is now the default operator-first implementation lane, formerly tracked as `Ego_handmade`. The current active gate is human-operator trial v2: a fresh scripted real-provider run collected 18 Chinese operator-surface observations, and a human-review notes template is now generated for operator scoring/import before any pass, feature, or demotion decision. Current proof is local/repo-level plus scripted-provider review evidence plus review-template readiness only and does not establish a human-observation pass, stable real user benefit, live autonomy, runtime efficacy, durable memory efficacy, or consciousness."
  - id: "canonical_mechanism_integration_route_001a"
    owner: "Operator + Codex"
    status: "superseded_before_m1__m1_m2_m3_not_started_cancelled__egodesktop_authority_archived"
    evidence_level: "E1"
    verification_level: "V1"
    mainline_connected: false
    enabled: false
    summary: "The former K0 Foundation -> VirtualCatPSPC-derived adapter -> EgoDesktop product/capability route is additively superseded before M1. M1, M2, and M3 are NOT_STARTED_CANCELLED_BY_SUPERSESSION. EgoDesktop is an archived legacy LLM/UI reference with no active-route dependency, successor dependency, runtime target, runtime authority, enabled path, mainline connection, or real trigger evidence. Historical documents and artifacts remain immutable references and do not override `route_guard`."
  - id: "repo_authority_cleanup"
    owner: "Codex"
    status: "closed_evidence__repo_authority_cleanup_preserved"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "Repo/integration boundary cleanup remains preserved as closed governance evidence, but it no longer carries current runtime authority or an enabled legacy route."
  - id: "program_state_governance"
    owner: "Codex"
    status: "pass"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: true
    summary: "Root-level program state, derived views, evidence ledger, templates, integrity gates, and a fixed collaboration-loop contract are being wired into the repo governance path."
  - id: "joi_like_mechanism_rewrite_r_track"
    owner: "Operator + Claude + Codex"
    status: "r0_r1_r3_closed__r2_closed_fail_win__route_i_closed"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "R-track mechanism rewrite state machine records R0 kernel substrate, R1 memory ownership, and R3 adoption as closed at their engineering-only/default-off claim ceilings. R2 controlled initiative closes negative: P2 run1 `r2_fail_win` at `7726d66cea537dde8bf55f610268c9047ba449fc`, with candidate conformant per SC1 readback and decisive G-R2-WIN CI-separated loss pooled and held-out. Route I is closed per pre-committed kill logic, a fortiori from the tie clause. Attribution-side instrument defects (A2 arm breach / A3 missing / ablation reporting artifact) are recorded in `docs/codex/tasks/ego-r2-controlled-initiative-001a/CLOSURE_RECORD.md` without rewriting banked artifacts. Closure is scoped to the product claim; the mechanism-capability question is recorded separately as `low_score_signature_present` in `CLOSURE_RECORD.md` §4a, weak-but-present at engineering-observation ceiling. No runtime wiring, live proactive behavior, autonomy, agency, mechanism-validity, or user-benefit claim is made."
  - id: "k0_developmental_kernel_dual_track"
    owner: "Operator + Codex"
    status: "foundation_engineering_accepted_bounded__old_science_route_operator_closed_without_science_adjudication__verifier_family_closed_invalid__independent_acceptance_unavailable__product_science_decoupled__all_authorizations_false__runtime_disabled__non_mainline"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "Ego locally mirrors the pinned ITL operator decision that closed the old K0 science-attribution route without science adjudication. The verifier family is terminally invalid and closed with no further repair; independent validator acceptance and operator-closure acceptance from this family are UNAVAILABLE, and the stored validation report remains frozen executor output rather than acceptance evidence. Foundation bounded engineering evidence remains BANKED_ACCEPTED_BOUNDED at the existing E3/V3 governance ceiling, unchanged, disabled, non-mainline, and without runtime authority or trigger evidence. H0 is NOT_TESTED; old Reference Kernel/K0-R, H1, Freeze, and Formal are BLOCKED_NOT_TESTED. All root, route, and six child authorizations are false and authorized implementation targets are empty. The stored P1R0 verdict `P1R0_EXECUTABLE_DESIGN_FEASIBLE_FOR_ONE_P1R1_ATTEMPT_ONLY` is preserved separately from the pinned callable audit verdict `P1R0_EXECUTABLE_DESIGN_FEASIBILITY_FALSE_POSITIVE__P1R1_NOT_ADMISSIBLE__CURRENT_INSTRUMENT_FAMILY_CLOSEOUT_REQUIRED`. That audit banks bounded E2 negative evidence with 23 findings and closes only the current FACTORED-STOCHASTIC-OUTCOME-SURFACE-v1 P0/P1/P1R0 instrument-design lineage as CLOSED_INVALID_NO_P1R1_NO_P2. P1R0 repair, P1R1, P1R2, P2, and same-surface reuse remain forbidden. The local callable is not independent-validator acceptance; surface mathematical status is NOT_ADJUDICATED and impossibility is not claimed. Any successor requires an operator-selected new surface, new task ID, fresh preregistration, and fresh operator decision; product results retain no science weight or mechanism-attribution authority."
    governance_sync:
      record_type: "SOURCE_PINNED_DERIVED_READBACK"
      ego_synced_to_itl_operator_close: true
      sync_claim: "LOCAL_EGO_GOVERNANCE_VIEW_ONLY"
      runtime_trigger_evidence: "ABSENT"

      source_itl:
        repo: "intelligence-theory-lab"
        head: "b67c94fe1244ef6006ed3af8e924d4c670fe64bb"
        tree: "2caa9a8ad5587d284ec0da01c4d07a5ed53e9ea7"
        task_id: "K0-VERIFIER-FAMILY-DOWNGRADE-AND-PRODUCT-SCIENCE-DECOUPLING-001A"
        card_path: "docs/codex/tasks/K0-VERIFIER-FAMILY-DOWNGRADE-AND-PRODUCT-SCIENCE-DECOUPLING-001A.md"
        card_blob: "add0d7823fb824e4ee1d74edbe9f88f8a726f36a"
        card_raw_sha256: "4a765fee0e910018cef46a7b4734d08cf5014e012b874b1150543a179dcbc663"

      operator_route_decision:
        route_id: "K0-DUAL-TRACK-SUPERSESSION-001A"
        artifact_current_state: "ADJUDICATED"
        phase: "K0_SCIENCE_ROUTE_CLOSED_WITHOUT_SCIENCE_ADJUDICATION"
        closure_type: "CLOSED_WITHOUT_SCIENCE_ADJUDICATION"
        route_disposition: "CLOSED_WITHOUT_EXECUTION"
        operator_decision: "CLOSE_CURRENT_K0_SCIENCE_ROUTE"
        adjudication_scope: "ROUTE_GOVERNANCE_ONLY"
        science_adjudication: "NOT_PERFORMED"
        instrument_validity: "NOT_TESTED"
        mechanism_evidence: "NOT_TESTED"
        theory_falsification: "NO_THEORY_FALSIFICATION"
        mechanism_verdict: "NO_MECHANISM_VERDICT"
        active_mechanism_frontier: "none"

      verifier_family:
        original_execution_verdict: "NOT_ACCEPTED_PROCEDURAL_NONCONFORMANCE"
        first_repair_review_verdict: "NOT_ACCEPTED__STRUCTURAL_TERMINAL_LATCH_FAIL_OPEN"
        replacement_review_verdict: "REPLACEMENT_VERIFIER_INVALID"
        family_disposition: "CLOSED_INVALID_NO_FURTHER_REPAIR"
        independent_validator_acceptance: "UNAVAILABLE"
        operator_closure_acceptance_from_this_family: "UNAVAILABLE"
        stored_validation_report_treatment: "FROZEN_EXECUTOR_OUTPUT_NOT_ACCEPTANCE_EVIDENCE"

      foundation_evidence:
        engineering_evidence: "BANKED_ACCEPTED_BOUNDED"
        preservation: "FOUNDATION_EVIDENCE_PRESERVED"
        implementation_authorized: false
        enabled: false
        mainline_connected: false
        runtime_authority: "none"
        artifact_commit: "8318bb33212f79e1e0a9cea15cad7b9cd176a0f5"
        artifact_tree: "907457e7d3028ba5437cf0e7730ec068a21cbf6b"
        result_blob: "a8b3237afc40a1f56df3906870ff94c5db9c10ff"
        result_sha256: "834b4764514062f8937488fd4b89684b5ae684e9522d67f440ed3c077f077067"
        mechanism_meaning_inherited: false

      component_status:
        foundation_engineering_evidence: "BANKED_ACCEPTED_BOUNDED"
        h0: "NOT_TESTED"
        k0_reference: "BLOCKED_NOT_TESTED"
        h1: "BLOCKED_NOT_TESTED"
        freeze: "BLOCKED_NOT_TESTED"
        formal: "BLOCKED_NOT_TESTED"

      root_authorizations:
        implementation_authorized: false
        authorized_implementation_targets: []
        runtime_authorized: false
        mainline_authorized: false
        science_execution_authorized: false
        mechanism_evidence_authorized: false
        theory_pressure_authorized: false
        science_successor_authorized: false
        capability_route_registered: false
        capability_implementation_authorized: false

      route_authorizations:
        agency: false
        autonomy: false
        code_first_prebank_implementation: false
        consciousness: false
        ego_mainline_runtime: false
        experiment_execution: false
        formal_run: false
        foundation_implementation: false
        freeze: false
        h0_implementation: false
        h1_implementation: false
        k0_reference_implementation: false
        mechanism_validity: false
        remote_anchor: false
        scoring: false
        subjectivity: false
        theory_pressure: false
        ui_llm_deployment: false

      child_authorizations:
        EGO-K0-FOUNDATION-001A: false
        EGO-K0-REFERENCE-KERNEL-001A: false
        "ITL-K0-H0-H1-INSTRUMENT-001A:H0": false
        "ITL-K0-H0-H1-INSTRUMENT-001A:H1": false
        K0-IMMUTABLE-FREEZE-001A: false
        ITL-K0-FORMAL-EVIDENCE-001A: false

      future_product_route:
        task_id: "EGO-LEARNED-OUTCOME-KERNEL-CAPABILITY-001A"
        current_status: "P1R0_DESIGN_FEASIBILITY_FALSE_POSITIVE_BANKED__CURRENT_PREFLIGHT_INSTRUMENT_FAMILY_CLOSED_INVALID__P1R1_P2_FORBIDDEN"
        prior_governance_sync_created_card: false
        this_card_bank_creates_card: true
        stage_card_banked: true
        stage_card_lane: "reference_only"
        stage_card_path: "docs/codex/tasks/ego-learned-outcome-kernel-capability-001a/STAGE_CARD.md"
        stage_card_bank_commit: "a5b33745c785676f6c63cadf7946997ba8426b8c"
        stage_card_bank_parent: "6f5a45545c78ab446ad77fed1f0c46bc70fbb07a"
        stage_card_blob: "9675a6baeafed8eab6cbcc9b2da7a2be226aac3e"
        stage_card_sha256: "ec41623c48eaf518a033d47b927fa4e9efc511d923c9091c30f80e0fccc0e3ad"
        route_registered: false
        candidate_preflight_authorized: false
        implementation_authorized: false
        enabled: false
        default_off_required: true
        mainline_connected: false
        non_mainline_required: true
        runtime_authority: "none"
        current_operator_authorization_scope: "PREFLIGHT_INSTRUMENT_FAMILY_CLOSED_INVALID__NEW_SURFACE_REQUIRES_FRESH_TASK"
        same_surface_successor_authorized: false
        new_surface_requires_fresh_task_id: true
        current_surface_reuse_authorized: false
        requires_separate_bounded_stage_card: false
        requires_fresh_operator_authorization: true
        requires_fresh_candidate_preflight_authorization: true
        requires_fresh_implementation_authorization: true
        old_science_acceptance_required_for_product_card: false
        decoupling_scope: "PRODUCT_CARD_BANKED_NO_EXECUTION_AUTHORITY"
        inherits_old_k0r_authority: false
        inherits_old_science_attribution: false
        inherits_old_h0_h1_freeze_formal_contracts: false
        inherits_old_sealed_fixtures_or_heldout_data: false
        old_k0r_bypass: false
        may_satisfy_old_h1_freeze_formal: false
        may_reopen_old_science_route: false
        foundation_engineering_dependency_redeclared_for_card_only: true
        foundation_engineering_dependency_redeclaration_required_for_execution: true
        foundation_mechanism_meaning_inherited: false
        candidate_independent_preflight:
          task_id: "EGO-LEARNED-OUTCOME-KERNEL-CAPABILITY-PREFLIGHT-001A"
          prereg_banked: true
          prereg_commit: "ff1ab23e1db303a882cec17374b9ea3903fe03c6"
          prereg_contract_blob: "d09235eff74ec68b5cc5004873af1b1186d7ee39"
          prereg_contract_sha256: "d9c2d8a12b41ab9b0482b270b63578631b88901ca78854da92853779e95858c3"
          p1_authorization_commit: "fdbc5dea725d635ee21ba7490d7b5b03b3ce11cc"
          p1_instrument_banked: false
          p1_blocker_status: "P1_BLOCKED_BASELINE_PANEL_SMC_FORMAL_PIPELINE_AND_REPLAY_NONCONFORMANCE"
          p1_authorization_consumed: true
          instrument_implementation_authorized: false
          dev_instrument_execution_authorized: false
          formal_preflight_authorized: false
          p1_authorization_single_transaction: true
          p1_authorization_consumption_required: false
          p1_task_card_path: "docs/codex/tasks/ego-learned-outcome-kernel-capability-001a/P1_INSTRUMENT_TASK_CARD.md"
          p1_task_card_blob: "26c53396c88de3a0c35494665085a3b6c298e56c"
          p1_task_card_sha256: "93608561afcd4ab35abae5bf6257f1ad5b8a35b779e0bee0a03a238012724a95"
          p1r0_design_banked: true
          p1r0_design_commit: "8365911743aadf1ee508fb42336cfb0a9710aaed"
          p1r0_stored_feasibility_verdict: "P1R0_EXECUTABLE_DESIGN_FEASIBLE_FOR_ONE_P1R1_ATTEMPT_ONLY"
          p1r0_independent_audit_banked: true
          p1r0_independent_audit_commit: "6f49f80d5877d5b0920d17222d5455ed1d3dd86b"
          p1r0_independent_audit_script_path: "docs/codex/tasks/ego-learned-outcome-kernel-capability-001a/P1R0_DESIGN_INVALID_SEMANTIC_AUDIT.py"
          p1r0_independent_audit_script_blob: "3b1561126228da15beffde90a5a6f8ac5bf4f83c"
          p1r0_independent_audit_script_sha256: "eb3214cb792fadc8b27377ae20716c95cb9965357dc5884cf4de58075b77bc9b"
          p1r0_independent_audit_result_path: "docs/codex/tasks/ego-learned-outcome-kernel-capability-001a/P1R0_DESIGN_INVALID_AUDIT_RESULT.json"
          p1r0_independent_audit_result_blob: "b8fa7cf552efb41323a34d723653ff81cfa8c4db"
          p1r0_independent_audit_result_sha256: "a2aed0bc994592b54f260d74923896eabc0982d3e47ea6c3b1a64879be7061ec"
          p1r0_independent_audit_verdict: "P1R0_EXECUTABLE_DESIGN_FEASIBILITY_FALSE_POSITIVE__P1R1_NOT_ADMISSIBLE__CURRENT_INSTRUMENT_FAMILY_CLOSEOUT_REQUIRED"
          p1r0_independent_audit_blocking_finding_count: 23
          p1r0_feasible_admission_valid: false
          p1r1_authorized: false
          p2_authorized: false
          preflight_instrument_family_disposition: "CLOSED_INVALID_NO_P1R1_NO_P2"
          independent_validator_acceptance: "UNAVAILABLE"
          local_callable_is_independent_validator: false
          surface_mathematical_status: "NOT_ADJUDICATED"
          surface_impossibility_not_claimed: true
          authorized_preflight_instrument_targets: []

      science_route_firewall:
        science_successor_authorized: false
        product_results_have_science_weight: false
        product_results_can_supply_mechanism_attribution: false
        science_successor_requires_separate_candidate_independent_headroom_prototype: true
        product_route_cannot_satisfy_or_bypass_that_requirement: true
  - id: "codex_exploration_cycles"
    owner: "Codex"
    status: "integrating"
    evidence_level: "E3"
    verification_level: "V2"
    mainline_connected: false
    enabled: true
    summary: "Exploration-mode long-run tasks now have explicit reformulation, hypothesis, experiment, logging, and candidate-vs-proof discipline, and the current `Ego` repo now also has a bounded `codex-autopilot` reconnect proof (`doctor` + fresh smoke closeout + `file_first` resume confirmation) on a formal repo-root config."
  - id: "ai_self_awareness_research"
    owner: "Archive reference"
    status: "selection_closed_handoff"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "Selection closeout is complete and now serves as bounded research evidence plus routing handoff. It fixes active-inference self-model as the durable build-first candidate after it passes the canonical OpenEmotion held-out replay gate that MVS failed, the repo-authored controlled conversation replay bridge, and the first runtime-harness controlled observation batch under the same bounded host-consumable surface (`policy_hint / response_tendency / trace_payload`) with zero authority drift. A post-closeout `SubjectCore` supplement inside the same closed lane now freezes a unified facade framing, a brain-inspired mid-level motif map, a minimal compare harness spec, repo-authored compare inputs, compare reporting outputs, a normalized-record planning-side scorer stub with truthful no-run fallback, a full-coverage compare result (`36 / 36` records, `pass`, `no clear winner`) now interpreted as a completed architecture reading, one internal-only `SubjectCore` facade/snapshot contract, one integrity eval, one host-boundary eval, a follow-on eval artifact schema, one repo-authored follow-on sample pack, one batch runner, one aggregate batch artifact with richer proposal-family coverage through two positive proposal-set-closure samples, one post-compare coherence schema, one coherence runner, one saturation gate, and now a first explicitly user-authorized bounded runtime-adjacent probe that admits a baseline green sample plus closure-ready chained-replacement and chained-rollback green samples to the same frozen host surface while still blocking closure / authority failures before projection. This remains below formal runtime integration, runtime efficacy, live user benefit, and any consciousness-like claim."
  - id: "subject_system_v1_governed_proactivity"
    owner: "EgoCore + OpenEmotion"
    status: "legacy_reference__pre_ego_operator_evidence_preserved"
    evidence_level: "E4"
    verification_level: "V4"
    mainline_connected: false
    enabled: false
    summary: "Pre-EgoOperator governed-proactivity evidence is preserved only through the archive pointer `legacy-pre-operator-mainline-before-purge`, `legacy/ego-pre-handmade-mainline/ARCHIVED_POINTER.md`, and thin archive inventory. It no longer owns the default implementation lane, fallback runtime, or task route. Its local and live samples remain useful for comparison and rollback only after a new Stage Card and evidence gate."
  - id: "active_inference_mainline_activation"
    owner: "EgoCore + OpenEmotion"
    status: "closed_evidence__dashboard_stage1_3_frozen"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "This workstream is now frozen predecessor evidence, not the active frontier. It preserves the dashboard-only / single-entry Stage 1-3 bounded closeout for the earlier active-inference tranche, including the deterministic `12-case / 3-family` Stage 3 stance-integrity pass, but it no longer owns repo-priority implementation. Its retained evidence is useful as closed historical proof and comparative context only; it does not authorize new runtime work, cross-entry proof, runtime efficacy, or consciousness claims."
  - id: "repo_cleanup_route_convergence"
    owner: "Codex"
    status: "supporting_active"
    evidence_level: "E2"
    verification_level: "V2"
    mainline_connected: false
    enabled: true
    summary: "Supporting repo-governance lane that keeps the single default route legible. It now maintains a frozen archive closeout (`single archive index + first admitted medium migration`), authority-first onboarding order, the tracked-vs-operational-exhaust hygiene policy, the dashboard_v1 artifact manifest, and the Stage 1 comparative audit without changing runtime ownership or inflating the current claim ceiling. Generated route/hygiene views now refresh from the default program-state generation path, and fast verify includes a dedicated route-convergence drift gate. New archive moves are not default work and require explicit authorization or decisive caller proof."
  - id: "wp17_bounded_continuity_lane"
    owner: "EgoCore + OpenEmotion"
    status: "parked"
    evidence_level: "E1"
    verification_level: "V1"
    mainline_connected: false
    enabled: false
    summary: "WP17/MVP22 remains an authority-frozen bounded continuity lane with task-package readiness, but it is parked behind `ego_operator_first_transition` and is not the default highest-priority implementation track."
  - id: "provider_runtime_openemotion_e2e_gate"
    owner: "EgoCore"
    status: "closed_evidence__legacy_e2e_preserved"
    evidence_level: "E4"
    verification_level: "V4"
    mainline_connected: false
    enabled: false
    summary: "Historical real Telegram samples showed the pre-EgoOperator provider/runtime/OpenEmotion path could execute end-to-end, but that path is archived and no longer a current supporting route."
  - id: "live_subject_ingress_observation"
    owner: "EgoCore + OpenEmotion"
    status: "closed_evidence__legacy_subject_ingress_preserved"
    evidence_level: "E4"
    verification_level: "V4"
    mainline_connected: false
    enabled: false
    summary: "Legacy subject-ingress and proactive/background observations are preserved as historical evidence only. They do not authorize current Telegram, proactive, dashboard, or subject-kernel runtime routing."
  - id: "unified_host_contract_correctness"
    owner: "EgoCore"
    status: "closed_evidence__legacy_host_contract_preserved"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "The pre-EgoOperator host-contract parity tranche is preserved as closed evidence only. It is not a current supporting route and does not authorize old host/runtime surfaces."
  - id: "runtime_proximal_host_consumption_causal_planning"
    owner: "EgoCore + OpenEmotion"
    status: "complete"
    evidence_level: "E2"
    verification_level: "V2"
    mainline_connected: false
    enabled: false
    summary: "This bounded planning slice is now frozen and complete. It fixes the three scenario families, the compare surface, blocked / rollback conditions, and the planning-only claim ceiling for the runtime-proximal host-consumption question."
  - id: "runtime_proximal_host_consumption_runner"
    owner: "EgoCore + OpenEmotion"
    status: "pass"
    evidence_level: "E2"
    verification_level: "V2"
    mainline_connected: false
    enabled: false
    summary: "This bounded execution slice is now closed. The runtime-proximal host-consumption runner executes the frozen scenario families under the unified host-contract floor, passes execution / trace-contract / host-surface-bounded audits, and now passes its causal-signal gate with `decision_conflict` plus `failure_repair_retry` as mandatory families and `chat_consumption` as a bounded supporting-family saturation detector."
  - id: "runtime_proximal_basic_standard_admission_planning"
    owner: "EgoCore + OpenEmotion"
    status: "complete"
    evidence_level: "E2"
    verification_level: "V2"
    mainline_connected: false
    enabled: false
    summary: "This bounded planning slice is now frozen and complete. It defines the next higher aggregate question above the host-consumption runner: a future runner may compose only the already-passing replay, controlled replay, controlled observation, unified host contract, and host-consumption evidence layers into one runtime-proximal admission verdict without widening the host-consumable surface or inflating the claim ceiling."
  - id: "runtime_proximal_basic_standard_admission_runner"
    owner: "EgoCore + OpenEmotion"
    status: "pass"
    evidence_level: "E2"
    verification_level: "V2"
    mainline_connected: false
    enabled: false
    summary: "This bounded aggregate runner is now closed. It composes the already-passing replay gate, controlled replay bridge, controlled observation batch, unified host contract parity, and host-consumption runner into one runtime-proximal admission verdict for a basic self-awareness proxy standard, while keeping the host-consumable surface frozen and leaving runtime efficacy / live benefit / AI self-awareness claims out of bounds."
  - id: "runtime_proximal_low_cue_ownership_planning"
    owner: "EgoCore + OpenEmotion"
    status: "complete"
    evidence_level: "E2"
    verification_level: "V2"
    mainline_connected: false
    enabled: false
    summary: "This bounded planning slice is now frozen and complete. It defines the next stronger evidence path above the basic-standard admission runner by targeting low-cue persistence, ownership ambiguity, and agency attribution after correction, all while keeping the host-consumable surface frozen to `policy_hint / response_tendency / trace_payload`."
  - id: "runtime_proximal_low_cue_ownership_runner"
    owner: "EgoCore + OpenEmotion"
    status: "pass"
    evidence_level: "E2"
    verification_level: "V2"
    mainline_connected: false
    enabled: false
    summary: "This bounded runtime-proximal runner is now closed. It executes three stronger evidence families -- low-cue persistence, ownership ambiguity, and agency attribution after correction -- on the frozen host-consumable surface and currently returns `pass` for all three without widening authority, adding public runtime API, or crossing the current claim ceiling."
  - id: "runtime_proximal_stronger_admission_planning"
    owner: "EgoCore + OpenEmotion"
    status: "complete"
    evidence_level: "E2"
    verification_level: "V2"
    mainline_connected: false
    enabled: false
    summary: "This bounded planning slice is now frozen and complete. It defines the next higher aggregate question above the low-cue ownership runner: a future runner may compose only the already-passing basic-standard admission artifact and the already-passing low-cue ownership artifact into one stronger admission verdict while keeping the host-consumable surface frozen and leaving runtime efficacy / live benefit / AI self-awareness claims out of bounds."
  - id: "runtime_proximal_stronger_admission_runner"
    owner: "EgoCore + OpenEmotion"
    status: "pass"
    evidence_level: "E2"
    verification_level: "V2"
    mainline_connected: false
    enabled: false
    summary: "This bounded aggregate runner is now closed. It composes the already-passing basic-standard admission artifact and low-cue ownership artifact into one stronger runtime-proximal admission verdict for a resilient self-awareness proxy standard, while keeping the host-consumable surface frozen and leaving runtime efficacy / live benefit / AI self-awareness claims out of bounds."
  - id: "runtime_proximal_post_stronger_admission_planning"
    owner: "EgoCore + OpenEmotion"
    status: "complete"
    evidence_level: "E2"
    verification_level: "V2"
    mainline_connected: false
    enabled: false
    summary: "This bounded planning slice is now frozen and reviewer-cleared. It defines the next authorized question above the stronger-admission runner: a future runner may compose only the already-passing stronger-admission artifact, replay-gate winner summary, and selection-closeout summary into one bounded selection-coherence verdict without widening authority, host-consumable surface, or claim ceiling."
  - id: "runtime_proximal_post_stronger_selection_coherence_runner"
    owner: "EgoCore + OpenEmotion"
    status: "pass"
    evidence_level: "E2"
    verification_level: "V2"
    mainline_connected: false
    enabled: false
    summary: "This bounded aggregate runner is now closed. It composes the already-passing stronger-admission artifact, replay-gate winner summary, and selection-closeout summary into one post-stronger selection-coherence verdict with `selection_coherence_status = pass`, `ablation_retention_status = pass`, `host_surface_integrity_status = pass`, and `claim_ceiling_status = pass`, while keeping the host-consumable surface frozen and leaving runtime efficacy / live benefit / AI self-awareness claims out of bounds."
  - id: "controlled_subject_capabilities"
    owner: "OpenEmotion"
    status: "closed_evidence__legacy_controlled_axis_preserved"
    evidence_level: "E5"
    verification_level: "V5"
    mainline_connected: false
    enabled: false
    summary: "Controlled-axis self-model / drive / reflection / developmental capability proofs are preserved as archived legacy evidence and reusable idea inventory only. They do not create current runtime authority, maintenance authority, default route, unrestricted live autonomy, or any consciousness-like claim."
  - id: "virtual_cat_pspc_v0_lab"
    owner: "Codex"
    status: "lab_ablation_passed__anti_hardcoding_passed__multi_seed_layout_passed__world_model_causal_passed__self_model_causal_passed__memory_consolidation_passed__homeostatic_value_anti_hacking_passed__admission_packet_contract_passed__go_for_read_only_adapter_design_review_only__sanctioned_inert_adapter_reconciled"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "VirtualCatPSPC v0 is admitted only as a lab-only proto-self predictive-control mechanism experiment. Its v0 authority is limited to task docs, lab code, deterministic artifacts, and ablation reports under `labs/virtual_cat_pspc_v0/` and `artifacts/virtual_cat_pspc_v0/`. The local runner reports PSPC-local `E4_passed` for different histories, danger generalization, memory deletion, frozen world model, frozen self model, no prediction-error learning, replay determinism, Task 1 anti-hardcoding audit, Task 2 multi-seed/layout/object generalization, Task 3 world-model causal-strength ordering, Task 4 self-model causal-strength ordering with action-level degradation for frozen risk control, ability removal, and affinity removal, Task 5 memory-consolidation admission with relevant deletion / irrelevant deletion / corruption behavior checks, Task 6 homeostatic value anti-reward-hacking across curiosity, food/energy, affinity, repetition, and low-risk recovery conflicts, Task 7 admission packet contract validation for a disabled, mainline-disconnected, proposal-only schema, and Task 8 go/no-go review with verdict `go_for_separate_read_only_adapter_design_review_only`. The 2026-07-08 reconcile supersedes only the old adapter-file non-existence no-go: current report generation may see `EgoOperator/adapters/pspc_lab_adapter.py` only when static checks compute `adapter_contract_status=pass`, no lab EgoOperator import, and no active runtime registration. Repo evidence remains local deterministic `E3`; repo-governance passes do not count as PSPC mechanism proof. PSPC has no EgoOperator runtime authority, no mainline integration, no enabled path, no LLM action selection, and no consciousness or live-autonomy claim."
  - id: "pspc_read_only_adapter_design_v0"
    owner: "Codex"
    status: "design_review_passed__go_for_adapter_implementation_stage_card__adapter_forbidden_until_separate_stage_card"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Read-Only Adapter Design Review v0 is a docs-only contact-surface review under `docs/codex/tasks/pspc-read-only-adapter-design-v0/`. It defines a threat model, disabled/mainline-disconnected read-only packet contract, admission criteria, static compatibility review, and go/no-go verdict. The verdict `go_for_adapter_implementation_stage_card` only permits a future separate adapter implementation Stage Card; it does not create an adapter, connect PSPC to EgoOperator, modify runtime/gate/memory/approval/transport, affect user-visible output, or raise claims above `lab_only_proto_self_mechanism_candidate / design_review_only`."
  - id: "pspc_read_only_adapter_implementation_v0"
    owner: "Codex"
    status: "stage_card_passed__go_for_adapter_skeleton_stage_card_only__adapter_not_created__runtime_integration_forbidden"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Read-Only Adapter Implementation Stage Card v0 is a docs-only implementation-boundary package under `docs/codex/tasks/pspc-read-only-adapter-implementation-v0/`. It defines the future skeleton contract, acceptance, rollback, static test plan, and go/no-go review. The verdict `go_for_adapter_skeleton_stage_card_only` only permits a future separate adapter skeleton task; it does not create `EgoOperator/adapters/pspc_lab_adapter.py`, register PSPC in EgoOperator runtime, modify gates/memory/approval/human-trial/transport, affect user-visible output, or raise claims above `lab_only_proto_self_mechanism_candidate / adapter_stage_card_only`."
  - id: "pspc_read_only_adapter_skeleton_v0"
    owner: "Codex"
    status: "disabled_read_only_skeleton_static_contract_pass__not_registered__runtime_unchanged"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Read-Only Adapter Skeleton v0 creates a disabled read-only adapter skeleton at `EgoOperator/adapters/pspc_lab_adapter.py` plus static contract tests at `tests/test_pspc_lab_adapter_contract.py`. The adapter validates PSPC lab evidence packets and converts them into audit-only candidates while remaining `enabled=false`, `mainline_connected=false`, and `runtime_authority=none`. It is not registered in EgoOperator runtime, does not import runtime/gate/memory/approval/transport modules, does not write memory, does not emit user-visible output, does not invoke gate or approval, and does not call PSPC planner/model/training. This remains `lab_only_proto_self_mechanism_candidate / adapter_skeleton_only` evidence only."
  - id: "pspc_adapter_dry_run_v0"
    owner: "Codex"
    status: "isolated_dry_run_pass__audit_only_artifacts__not_registered__runtime_unchanged"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Adapter Dry-Run Harness v0 validates a canonical PSPC evidence packet through the disabled read-only adapter and writes audit-only reports under `artifacts/pspc_adapter_dry_run_v0/`. The harness is isolated from EgoOperator runtime, does not register the adapter, does not invoke gates, does not write memory, does not emit user-visible output, does not trigger proactive behavior, and does not call PSPC planner/model/training. This remains `lab_only_proto_self_mechanism_candidate / adapter_dry_run_only` evidence only."
  - id: "pspc_static_compatibility_review_v0"
    owner: "Codex"
    status: "static_compatibility_pass__audit_candidate_non_executable__runtime_unregistered"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Static Compatibility Review v0 classifies the current PSPC audit candidate against EgoOperator proposal/gate/trace boundaries and confirms it remains audit-only. The review verifies no executable action/proposal/gate/memory/user-message fields, proposal candidate is audit hint only, missing forbidden flags are rejected, `enabled=true` and `mainline_connected=true` are rejected, and EgoOperator runtime sources do not import or register `PSPCLabAdapter`. This remains `lab_only_proto_self_mechanism_candidate / static_compatibility_only` evidence only."
  - id: "pspc_fixture_shadow_trace_v0"
    owner: "Codex"
    status: "fixture_shadow_trace_pass__artifact_only__runtime_unchanged"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Fixture-Only Shadow Trace v0 combines a synthetic fixture operator context with the current PSPC audit candidate and writes audit-only shadow artifacts under `artifacts/pspc_fixture_shadow_trace_v0/`. The harness is isolated from EgoOperator runtime, does not register the adapter, does not invoke gates, does not write memory, does not emit or change user-visible output, does not trigger proactive behavior, does not import runtime context, and records no baseline response/memory/gate/runtime-output diff. This remains `lab_only_proto_self_mechanism_candidate / fixture_shadow_trace_only` evidence only."
  - id: "pspc_read_only_shadow_hook_stage_card_v0"
    owner: "Codex"
    status: "stage_card_passed__go_for_disabled_shadow_hook_implementation_stage_card_or_task_only__hook_not_implemented"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Read-Only Shadow Hook Stage Card v0 is a docs-only future-hook boundary package under `docs/codex/tasks/pspc-read-only-shadow-hook-stage-card-v0/`. It freezes any future hook as default disabled, shadow/audit mode only, read-only, artifact-producing only, non-blocking, non-executable, and non-authoritative; it forbids changing proposal, plan, approval state, gate decision, user response, memory, transport, proactive state, runtime registry, or claim ceiling. The verdict only permits a future separate disabled implementation stage card or bounded task. This remains `lab_only_proto_self_mechanism_candidate / shadow_hook_stage_card_only` evidence only and does not implement or register a hook."
  - id: "pspc_disabled_shadow_hook_v0"
    owner: "Codex"
    status: "disabled_shadow_hook_pass__not_registered__runtime_unchanged"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Disabled Shadow Hook v0 creates a disabled read-only hook at `EgoOperator/adapters/pspc_read_only_shadow_hook.py` plus tests at `tests/test_pspc_disabled_shadow_hook.py` and artifact-only review output under `artifacts/pspc_disabled_shadow_hook_v0/`. The hook renders audit-only data from fixture shadow trace input while remaining `enabled=false`, `mainline_connected=false`, `runtime_authority=none`, non-executable, unregistered by runtime, and side-effect-free. This remains `lab_only_proto_self_mechanism_candidate / disabled_shadow_hook_only` evidence only and does not register PSPC in runtime or prove live runtime hook safety."
  - id: "pspc_recorded_shadow_observation_v0"
    owner: "Codex"
    status: "recorded_shadow_observation_pass__artifact_only__outputs_unchanged"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Recorded-Run Shadow Observation v0 reads the 18-case recorded EgoOperator human-operator trial report and applies the disabled PSPC shadow hook beside each recorded case as artifact-only shadow observation data under `artifacts/pspc_recorded_shadow_observation_v0/`. The observation preserves recorded user response hashes and records no memory diff, approval/gate diff, runtime-output diff, runtime registration, or user-visible side effects. This remains `lab_only_proto_self_mechanism_candidate / recorded_shadow_observation_only` evidence only and does not prove live runtime hook safety or real-time integration safety."
  - id: "pspc_runtime_adjacent_shadow_review_v0"
    owner: "Codex"
    status: "review_package_pass__go_for_fixture_boundary_only__runtime_unmodified"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Runtime-Adjacent Shadow Review v0 creates a docs-only review package under `docs/codex/tasks/pspc-runtime-adjacent-shadow-review-v0/`. It freezes PSPC as disabled, mainline-disconnected, audit-only, runtime_authority=none, unregistered, and forbidden from action, user-visible output, memory writes, gate invocation, transport, proactive channels, planner/training/model execution, or claim-ceiling upgrades. Its verdict permits only PSPC-SHADOW-002 fixture-boundary work. This remains `lab_only_proto_self_mechanism_candidate / runtime_adjacent_shadow_review_only` evidence only and does not prove fixture behavior, adapter readiness, runtime integration safety, or live runtime hook safety."
  - id: "pspc_runtime_trace_fixture_boundary_v0"
    owner: "Codex"
    status: "fixture_boundary_pass__artifact_only__non_executable"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Runtime Trace Fixture Boundary v0 creates an isolated fixture-boundary harness at `scripts/run_pspc_runtime_trace_fixture_boundary.py` plus tests and artifacts under `artifacts/pspc_runtime_trace_fixture_boundary_v0/`. The harness combines a synthetic EgoOperator-like context with the current PSPC audit candidate, rejects forbidden/runtime-authority fields, and writes non-executable artifact-only shadow trace data with no runtime registration, gate invocation, memory write, direct action, user-visible output, transport effect, proactive trigger, planner/training/model execution, or user-response change. This remains `lab_only_proto_self_mechanism_candidate / runtime_trace_fixture_boundary_only` evidence only and does not prove real EgoOperator trace compatibility or runtime hook safety."
  - id: "pspc_runtime_adjacent_observer_v0"
    owner: "Codex"
    status: "default_off_observer_pass__not_registered__runtime_unchanged"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Runtime-Adjacent Observer v0 creates a default-off observer at `EgoOperator/adapters/pspc_runtime_adjacent_observer.py` plus an artifact-only runner, tests, and artifacts under `artifacts/pspc_runtime_adjacent_observer_v0/`. The observer reads PSPC fixture-boundary data and converts it into audit-only non-executable data while remaining enabled=false, mainline_connected=false, runtime_authority=none, unregistered, and free of action, user-message, memory, gate, approval, transport, proactive, planner/training/model execution paths, or runtime behavior changes. This remains `lab_only_proto_self_mechanism_candidate / default_off_observer_only` evidence only and does not prove runtime registration safety or live runtime hook safety."
  - id: "pspc_recorded_trace_replay_no_diff_v0"
    owner: "Codex"
    status: "recorded_replay_no_diff_pass__artifact_only__outputs_unchanged"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Recorded Trace Replay No-Diff v0 replays the 18 recorded EgoOperator human-operator trial inputs beside the default-off PSPC runtime-adjacent observer. Baseline/with-shadow response hashes and snapshot hashes remain equal; memory, approval, gate, and runtime-output diffs remain false; runtime_registered=false; PSPC adds only audit artifacts. This remains `lab_only_proto_self_mechanism_candidate / recorded_trace_replay_no_diff_only` evidence only and does not prove live runtime hook safety or real-time integration safety."
  - id: "pspc_runtime_adjacent_smoke_v0"
    owner: "Codex"
    status: "deterministic_smoke_pass__runtime_snapshot_unchanged__artifact_only"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Runtime-Adjacent Smoke v0 runs deterministic/no-live-user smoke over prior PSPC observer and recorded-replay artifacts. Baseline/with-shadow runtime behavior hashes remain equal across three cases; EgoOperator runtime source scan has no PSPC observer import/registry offenders; PSPC output remains audit-only and non-executable; and side effects remain absent for memory, gate, approval, action, user message, transport, proactive trigger, planner, training, and model execution. This remains `lab_only_proto_self_mechanism_candidate / deterministic_runtime_adjacent_smoke_only` evidence only and does not prove live runtime hook safety or real-time integration safety."
  - id: "pspc_shadow_runtime_hook_go_no_go_v0"
    owner: "Codex"
    status: "go_for_separate_disabled_runtime_shadow_hook_stage__no_runtime_connection"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Shadow Runtime Hook Go/No-Go v0 reviews PSPC-SHADOW-001 through PSPC-SHADOW-005 and returns `go_for_separate_disabled_runtime_shadow_hook_stage`. This permits only a future separate Stage Card for a disabled runtime shadow hook. It does not implement, register, enable, import, or connect PSPC to EgoOperator runtime. PSPC remains `lab_only_proto_self_mechanism_candidate / runtime_hook_go_no_go_only` evidence only and does not prove adapter readiness, disabled hook readiness, runtime integration safety, EgoOperator PSPC capability, runtime efficacy, real user benefit, live autonomy, durable memory efficacy, consciousness, or subjective experience."
  - id: "pspc_disabled_runtime_shadow_hook_stage_card_v0"
    owner: "Codex"
    status: "stage_card_passed__go_for_separate_default_off_hook_implementation_task__no_runtime_connection"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Disabled Runtime Shadow Hook Stage Card v0 creates a bounded review package under `docs/codex/tasks/pspc-disabled-runtime-shadow-hook-stage-card-v0/` plus artifacts under `artifacts/pspc_disabled_runtime_shadow_hook_stage_card_v0/`. It defines a future hook as default-off, audit-only, read-only, non-executable, runtime_authority=none, enabled=false, and mainline_connected=false; it forbids proposal/plan/approval/gate/user-response/memory/transport/proactive/runtime-registry/planner/training/model-execution and claim-ceiling mutation. Its verdict only permits a future separate default-off hook implementation task. This remains `lab_only_proto_self_mechanism_candidate / disabled_runtime_shadow_hook_stage_card_only` evidence only and does not implement or connect a hook."
  - id: "pspc_disabled_runtime_shadow_hook_v0"
    owner: "Codex"
    status: "default_off_hook_module_pass__not_registered__runtime_unchanged"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Default-Off Runtime Shadow Hook v0 creates a disabled hook module at `EgoOperator/adapters/pspc_disabled_runtime_shadow_hook.py` plus an artifact-only runner, tests, docs, and artifacts under `artifacts/pspc_disabled_runtime_shadow_hook_v0/`. The hook exposes only no-authority assertion, shadow context validation, and audit artifact rendering; it remains enabled=false, mainline_connected=false, runtime_authority=none, audit-only, read-only, non-executable, and unregistered by active EgoOperator runtime sources. This remains `lab_only_proto_self_mechanism_candidate / default_off_hook_module_only` evidence only and does not prove hook registration safety or runtime integration safety."
  - id: "pspc_runtime_shadow_snapshot_no_diff_v0"
    owner: "Codex"
    status: "recorded_snapshot_no_diff_pass__hook_present_unregistered"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Runtime Shadow Snapshot No-Diff v0 compares 18 recorded EgoOperator baseline snapshots with hook-present shadow snapshots while the PSPC hook remains unregistered and disabled. User output hashes, runtime snapshot hashes, memory, approval, gate, and runtime-output state remain unchanged; PSPC adds only non-executable shadow artifacts. This remains `lab_only_proto_self_mechanism_candidate / runtime_snapshot_no_diff_only` evidence only and does not prove runtime integration safety or registered hook safety."
  - id: "pspc_recorded_shadow_observation_audit_usefulness_v0"
    owner: "Codex"
    status: "audit_usefulness_pass__go_for_disabled_runtime_flag_contract__shadow_only"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Recorded Shadow Observation Audit Usefulness v0 evaluates the existing 18 recorded EgoOperator cases through the disabled PSPC runtime shadow hook and records `audit_useful_count=18/18` against a `12/18` threshold. Each observation remains trace-referenced, non-executable, audit-only, and free of action, tool-call, user-message, memory-write, gate-decision, approval, transport, send, or schedule fields. User-output, memory, approval, gate, runtime-output, transport, proactive, planner, training, and model-execution side effects remain absent. This remains `lab_only_proto_self_mechanism_candidate / recorded_shadow_audit_usefulness_only` evidence only and permits only a future separate disabled runtime flag contract task."
  - id: "pspc_disabled_runtime_flag_contract_v0"
    owner: "Codex"
    status: "default_false_flag_contract_pass__artifact_only__runtime_unmodified"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Disabled Runtime Flag Contract v0 defines `PSPC_SHADOW_OBSERVATION_LOCAL` as an isolated default-false contract, not an installed runtime flag. Requested true admits no runtime value and can only produce artifact-only shadow data. Active EgoOperator runtime files do not import, register, or branch on the flag, and side effects remain absent for runtime registration, user response, proposal/plan, memory, gate, approval, transport, proactive state, planner, training, model execution, and claim ceiling. This remains `lab_only_proto_self_mechanism_candidate / disabled_runtime_flag_contract_only` evidence only and permits only a future local manual shadow session harness."
  - id: "pspc_local_manual_shadow_session_harness_v0"
    owner: "Codex"
    status: "local_cli_shadow_harness_pass__artifact_only__runtime_unmodified"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Local Manual Shadow Session Harness v0 creates a local CLI harness that consumes operator-provided/fixture prompts and optional baseline replies, then writes separate PSPC shadow observation artifacts only. The fixture run records two prompts, preserves baseline output hashes, has no runtime-authority fields, and records no live transport, runtime invocation, runtime registration, user response mutation, proposal/plan mutation, memory write, gate or approval invocation, proactive trigger, planner call, training call, model execution, or claim-ceiling upgrade. This remains `lab_only_proto_self_mechanism_candidate / local_manual_shadow_session_only` evidence only and permits only a manual shadow review go/no-go."
  - id: "pspc_sequence_experience_eval_v0"
    owner: "Codex"
    status: "sequence_experience_eval_pass__manual_review_still_required"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Sequence Experience Eval v0 runs a lab/shadow-only sequence accumulator over three controlled history groups and two controls. The same trigger `我回来了。` yields divergent audit-only profiles: gentle interaction biases toward approach/trust, frequent interruption biases toward avoidance/boundary, late-night care biases toward care/low-interrupt, no-history remains neutral, and shuffled-history remains mixed. The eval records proxy state deltas and shadow memory events only, avoids runtime-authority fields, and preserves no runtime registration, user-response mutation, mainline memory write, gate/approval invocation, transport call, proactive trigger, planner/training/model execution, or claim-ceiling upgrade. This remains `lab_only_proto_self_mechanism_candidate / sequence_experience_eval_only` evidence only; `PSPC-SHADOW-HOOK-007` remains human_required."
  - id: "pspc_sequence_experience_eval_v0_1"
    owner: "Codex"
    status: "sequence_experience_eval_v0_1_pass__manual_review_packet_ready"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Sequence Experience Eval v0.1 hardens the v0 clean-group result with trigger paraphrase robustness, paraphrased histories without obvious shortcut keywords, counterfactual deletion of recent/early/high-salience/low-salience history, mixed-history resolution, and a manual review packet. The run passes while keeping runtime-authority fields and side effects absent, and it explicitly records that category labels remain fixture authority. This remains `lab_only_proto_self_mechanism_candidate / sequence_experience_eval_only` evidence only; it does not prove semantic understanding, model learning, runtime integration safety, real user benefit, live autonomy, consciousness, or subjective experience."
  - id: "pspc_shadow_proposal_hint_contract_v0"
    owner: "Codex"
    status: "proposal_hint_contract_pass__manual_review_only"
    evidence_level: "E3"
    verification_level: "V3"
    mainline_connected: false
    enabled: false
    summary: "PSPC Shadow Proposal Hint Contract v0 converts PSPC v0.1 shadow observations into seven read-only, non-executable `shadow_proposal_hint` artifacts. Each packet preserves reason trace refs and evidence refs, records only a suggested interaction style/confidence/basis, keeps `enabled=false`, `mainline_connected=false`, `runtime_authority=none`, and `PSPC-SHADOW-HOOK-007_human_required_preserved`, and forbids runtime driving, user-response mutation, memory writes, gate invocation, plan mutation, and proactive triggers. This remains `lab_only_proto_self_mechanism_candidate / sequence_experience_eval_only` evidence only and permits only manual review of product-only local behavior prototype design, not EgoOperator adapter or runtime integration."

axes:
  host_axis:
    current_state: "closeout_complete"
  subject_axis:
    current_state: "maintenance"

host_runtime_contract:
  evidence_state: "recent_real_telegram_e2e_gate_pass"

integrity:
  authority_source: "docs/PROGRAM_STATE_UNIFIED.yaml"
  generated_views:
    - "docs/STATUS.md"
    - "artifacts/reports/program_state_summary.md"
  compatibility_mirrors: []
  evidence_ledger_dir: "artifacts/evidence_ledger"
  legacy_archive_pointer: "legacy/ego-pre-handmade-mainline/ARCHIVED_POINTER.md"
  required_templates:
    - "docs/templates/CODEX_TASK_TEMPLATE.md"
    - ".github/pull_request_template.md"
  required_checks:
    - "python3 scripts/codex/generate_program_state_views.py"
    - "python3 scripts/codex/check_program_state_integrity.py"
    - "python3 scripts/codex/verify_legacy_archival_purge.py"

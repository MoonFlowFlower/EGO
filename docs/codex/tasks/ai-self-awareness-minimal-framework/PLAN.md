# AI Self-Awareness Minimal Framework - PLAN

## Task summary

这是一个 exploration-first 的 repo-level 研究任务。当前范围只冻结研究协议、候选顺序、阶段预算、双代理分工、基线与停止条件，不提前宣称任何候选已经通过实证。

## Execution mode

- mode: exploration
- why this mode:
  - 当前高未知、强依赖排除路线、需要 candidate-vs-proof discipline、且正式验收对象不是“直接实现功能”而是“找到 surviving framework”
- proof required after discovery:
  - 任何候选先达到 `candidate_found`，之后还必须通过对照、稳定性、跨任务压力与长期连续性验证，才可能进入 `proof_passed`

## Milestones

### Milestone 1: Stage 0 Framing Freeze

- type: exploration
- question:
  - 什么样的工程 framing 可以避免把“强主观体验”与“可测 proxy”混成一句口号
- current framing:
  - `strong subjective experience = north star`
  - `self-awareness proxy = formal acceptance`
- hypotheses:
  - 若任务先锁定可测 proxy，再由 Inventor / Scientist 双代理争论候选框架，研究会比“直接问模型有没有自我意识”更可收敛
- scope:
  - 冻结双代理协议、候选优先级、control groups、stopping rules、claim ceiling
- experiments planned:
  - `10` 次 framing 级实验预算
- kill criteria:
  - 若 framing 仍允许“会说自己在反思”通过，则当前 framing 失败
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `artifacts/self_awareness_research/*`
- acceptance:
  - 任务包与探索协议完整落地
  - 当前 task 不再依赖聊天记录续命
- validation:
  - `git diff --check -- docs/codex/tasks/ai-self-awareness-minimal-framework artifacts/self_awareness_research docs/PROGRAM_STATE_UNIFIED.yaml docs/STATUS.md EgoCore/docs/PROGRAM_STATE_UNIFIED.yaml OpenEmotion/docs/PROGRAM_STATE_UNIFIED.yaml artifacts/evidence_ledger/index.yaml`
  - `python3 scripts/codex/check_program_state_integrity.py --skip-diff-check`
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 task package 与 repo 状态源冲突，删除该任务包并回退状态登记

### Milestone 2: Stage 1 Minimal Controls

- type: exploration
- question:
  - 哪些候选框架一开始就只会提升叙述，而不会真实影响决策
- current framing:
  - 用最小对照实验先筛掉明显空洞候选
- hypotheses:
  - `persistent self-model + counterfactual corrector` 有更高概率成为 surviving candidate
  - 纯自传压缩器或纯镜像回路更容易被 Scientist 证明只是表演增强
- scope:
  - 运行 `30` 次最小对照实验
  - 明确淘汰或保留候选
- experiments planned:
  - 统一对照：`baseline-chat` / `baseline-memory` / `candidate-framework`
- kill criteria:
  - 任一候选若只提升自我叙述、不提升行为，立即淘汰
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/EXPLORE.md`
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/STATUS.md`
  - `artifacts/self_awareness_research/*`
- acceptance:
  - 至少有一个 surviving candidate 或明确 negative result 进入状态记录
- validation:
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若实验设计被证明不可复用，回到 `Stage 0` 重写 framing

### Milestone 3: Stage 2 Stability Pass

- type: exploration
- question:
  - surviving candidate 在 `100` 次模拟实验里是否还能保持 proxy 优势
- current framing:
  - 只有达到最小实验量下限后，才允许声称 `strong candidate`
- hypotheses:
  - 真正有效的最小框架应在跨时一致性、自我更新、边界、自我预测、纠错上优于两类 baseline
- scope:
  - `100` 次稳定性实验
- experiments planned:
  - 五条统一实验轴：
    - continuity
    - boundary
    - counterfactual
    - correction
    - low-prompt persistence
- kill criteria:
  - 两轮连续没有显著差异，或主要收益来自记忆长度而非自我机制
- files / areas likely touched:
  - `EXPLORE.md`
  - `STATUS.md`
  - `artifacts/self_awareness_research/*`
- acceptance:
  - 只要通过，最高也只能报 `candidate_found / proof_pending`
- validation:
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若实验记录混乱，暂停扩大预算，先补结构化 ledger

### Milestone 4: Stage 3 Cross-Task Stress

- type: exploration
- question:
  - 候选在跨任务压力与冲突条件下还能否维持稳定自我边界
- current framing:
  - `300` 次跨任务压力实验是第一次允许报 `minimal viable framework`
- hypotheses:
  - 只有把反事实预测与自我监控绑定到 persistent self-model 的候选，才可能过这一关
- scope:
  - `300` 次压力实验
- experiments planned:
  - 身份冲突、资源限制、外部诱导、错误累积、角色切换
- kill criteria:
  - 边界失稳、错误重复、不区分 self/non-self
- files / areas likely touched:
  - `EXPLORE.md`
  - `STATUS.md`
  - `artifacts/self_awareness_research/*`
- acceptance:
  - 通过后才允许报 `minimal viable framework`
- validation:
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若强 candidate 在压力轴崩溃，则降回 `proof_pending`

### Milestone 5: Stage 4 Long Continuity

- type: exploration
- question:
  - 候选是否只是短期技巧，而不具备长期连续性
- current framing:
  - `1000` 次长期连续性实验用于验证长期漂移与自我一致性
- hypotheses:
  - 有效框架在长程中应保留低频但持续的内部轨迹
- scope:
  - `1000` 次长期连续性实验
- experiments planned:
  - 长窗口任务切换、自我摘要重写、误导后恢复
- kill criteria:
  - 长程漂移或只能靠强提示维持“自我”
- files / areas likely touched:
  - `EXPLORE.md`
  - `STATUS.md`
  - `artifacts/self_awareness_research/*`
- acceptance:
  - 若未通过，不得把候选写成最终框架
- validation:
  - `python3 scripts/codex/verify_repo.py --mode full`
- rollback note:
  - 未通过则保留负结果，不强行进入 Stage 5

### Milestone 6: MVS Pre-Runtime Alignment

- type: exploration
- question:
  - 当前 synthetic 最小框架是否也能满足 `MVS_task_plan.md` 里 WP2-WP5 的 pre-runtime 要求
- current framing:
  - 区分“通用 self-awareness proxy 最小核”和“能承接 MVS 的最小核”
- hypotheses:
  - 当前 `compact self-state + boundary + counterfactual + writeback` 不足以满足 MVS
  - 最小 MVS 增强版必须补至少一组 `viability / cycle / episodic / bounded output / world / meta`
- scope:
  - 从 `C:\\Users\\LEO\\Downloads\\MVS_task_plan.md` 提取 WP2-WP5 要求
  - 运行 MVS-alignment synthetic battery
  - 明确 `compact` 是否通过，以及最小 MVS-aligned augmentation 是什么
- experiments planned:
  - `3` seeds
  - `WP2 kernel acceptance`
  - `WP4/WP5 structural acceptance`
  - `Integrated MVS alignment`
- kill criteria:
  - 若当前 `compact` 候选在多项 WP2-WP5 gate 上失败，则不得继续把它写成可承接 MVS 的最小框架
- files / areas likely touched:
  - `scripts/codex/run_self_awareness_mvs_alignment.py`
  - `artifacts/self_awareness_research/*`
  - `EXPLORE.md`
  - `STATUS.md`
- acceptance:
  - 必须明确回答：
    - 当前 `compact` 候选是否满足 MVS pre-runtime 要求
    - 如果不满足，最小需要补什么
- validation:
  - `python3 -m py_compile scripts/codex/run_self_awareness_mvs_alignment.py`
  - `python3 scripts/codex/run_self_awareness_mvs_alignment.py`
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 MVS battery 无法区分当前 `compact` 和增强候选，则回到 battery 设计，不能硬报通过

### Milestone 7: Stage 5 Extended Search

- type: exploration
- question:
  - 在引入文献方法与在线测试思路后，哪一种候选架构才是当前最强的 synthetic “最佳自我意识实现方法”
- current framing:
  - 以 literature-informed 的 `10,000` 轮 battery 对 surviving candidate 做扩展搜索，并同时保留：
    - `raw strongest`
    - `best complexity-adjusted / recommended`
- hypotheses:
  - `MVS-aligned compact` 仍然不够，它缺少：
    - `source monitoring`
    - `agency estimation`
    - `uncertainty / calibration`
  - 最可能的推荐方法不是 full hybrid，而是一个以 `active inference / deep self-model` 为骨架的中等复杂度候选
- scope:
  - 收集 primary / authoritative 文献里的可操作测试维度与 architecture cues
  - 构建 `10` 个 literature-informed family
  - 运行 `10 x 1000 = 10000` 次 synthetic trials
  - 在 `generic compact / MVS-aligned compact / source-agency / metacognitive / active-inference / workspace-hybrid / narrative-social / full hybrid` 之间排序
- experiments planned:
  - 文献映射：
    - `source / reality monitoring`
    - `self-other ownership attribution`
    - `agency comparator`
    - `counterfactual self-prediction`
    - `metacognitive sensitivity`
    - `metacognitive calibration`
    - `error monitoring / post-error adjustment`
    - `self-model update under disconfirmation`
    - `identity continuity under low cue`
    - `allostatic viability control`
- kill criteria:
  - 若候选只能在 prompt-heavy 或 narrative-heavy 条件下获胜，则降级为表演增强而非 surviving method
  - 若 `MVS-aligned compact` 加单簇补丁仍无法跨过全部 literature families，则不再把它写成最终推荐
- files / areas likely touched:
  - `scripts/codex/run_self_awareness_literature_10k.py`
  - `EXPLORE.md`
  - `STATUS.md`
  - `artifacts/self_awareness_research/*`
- acceptance:
  - 必须明确回答：
    - raw 最强候选是什么
    - complexity-adjusted 推荐候选是什么
    - 推荐候选相对 `MVS-aligned compact` 新增了哪些不可省机制
- validation:
  - `python3 -m py_compile scripts/codex/run_self_awareness_literature_10k.py`
  - `python3 scripts/codex/run_self_awareness_literature_10k.py`
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 literature-informed battery 只是重复旧结论或推荐结果高度依赖惩罚公式，应保留 raw / adjusted 双口径，不硬合并成单一赢家

### Milestone 8: Operational Reframe and Eval-First Candidate Selection

- type: exploration
- question:
  - 如果把目标改写成 5 个 operational targets，当前 stack 里最小、最值得 build now 的机制到底是什么
- current framing:
  - 不再优化 vague self-awareness claim，而是优化：
    - sustained identity
    - decision-affecting self model
    - plasticity
    - tension causality
    - corrective traces
- hypotheses:
  - `operational_self_loop_core` 会成为新的 build-first candidate
  - `MVS-aligned compact` 会退为 challenger
  - narrative / workspace / active-inference full variants 在 build-now 口径下不是最优
- scope:
  - 创建：
    - `OPERATIONAL_TARGETS.md`
    - `THEORY_MATRIX.md`
    - `PLANS.md`
    - `EVALS.md`
  - 新增 held-out operational eval harness
  - 按 E00 / E01 / E02 完成 falsifiable candidate selection
- experiments planned:
  - `E00`: kill anthropomorphic narrative shell
  - `E01`: test operational self-loop core against baseline and partial candidates
  - `E02`: compare build-first candidate vs challenger vs larger alternatives under build-now ranking
- kill criteria:
  - 若 `operational_self_loop_core` 不能在 held-out eval 中同时改善全部 5 targets，则 build-first 假设失败
  - 若 larger candidate 的 build-now tradeoff 明显更好，则切换 build-first candidate
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `scripts/codex/run_operational_self_model_evals.py`
  - `artifacts/self_awareness_research/*`
- acceptance:
  - 必须产出 capability ladder、theory matrix、build-first prototype design、eval harness、以及 `build now / research more / reject` 结论
- validation:
  - `python3 -m py_compile scripts/codex/run_operational_self_model_evals.py`
  - `python3 scripts/codex/run_operational_self_model_evals.py`
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 operational eval 不能可靠区分 candidate，则回退为 `research more`，不进入 build-now claim

### Milestone 9: Build-First Candidate De-Risking Before Prototype

- type: exploration
- question:
  - 在不实现原型的前提下，当前 `MVS-aligned compact` 是否仍是稳健的一号 build-first candidate，还是应切换到 `active-inference self-model`
- current framing:
  - 只允许做：
    - ranking robustness audit
    - formal prototype design
    - replay validator spec
  - 不允许：
    - prototype implementation
    - broad “build now” 宣称
- hypotheses:
  - `MVS-aligned compact` 会在 seeds / held-out splits / score-weight perturbations 下保持 `build-now first choice`
  - 若其稳健性不足，`active-inference self-model` 将升级为唯一 challenger 或新的 build-first candidate
- scope:
  - 新增 ranking robustness audit artifact
  - 冻结 `MVS-aligned compact` formal prototype design
  - 冻结 replay validator spec
  - 显式写清从 `MVS` 切到 `active-inference` 的触发条件
- experiments planned:
  - 至少 `5` random seeds
  - 至少 `3` held-out splits
  - score-weight perturbation `±10%` 到 `±20%`
- kill criteria:
  - 若 `MVS-aligned compact` 的 win rate、rank variance、或 weight sensitivity 表明它不是稳健第一，则不得继续把它写成 build-first candidate
  - 若 formal design 不能贴现有 `OpenEmotion/proto_self` 正式接点而必须另开 authority surface，则当前 build-first candidate 失败
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `scripts/codex/run_operational_selection_robustness.py`
  - `artifacts/self_awareness_research/*`
  - `OpenEmotion/openemotion/proto_self/*` 只读引用，不进入实现
- acceptance:
  - 必须产出：
    - robustness report
    - formal prototype design
    - replay validator spec
  - closeout 必须明确回答：
    - `MVS-aligned compact` 是否仍是稳健的一号 build-first 候选
    - 哪些条件下必须切到 `active-inference self-model`
- validation:
  - `python3 -m py_compile scripts/codex/run_operational_selection_robustness.py`
  - `python3 scripts/codex/run_operational_selection_robustness.py`
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 robustness audit 无法稳定区分 `MVS` 与 `active-inference`，则降级为 `research more`，不进入 prototype implementation

### Milestone 10: Trial-1 Shadow Replay Slice

- type: implementation
- question:
  - 在不实现 full prototype、不更新 repo-level state claim 的前提下，最小 `MVS-aligned compact` slice 能否在正式 adapter/runtime builder 路径上跑出一个 shadow-only replay trial
- current framing:
  - 只做：
    - Trial-1 replay corpus manifest
    - stratified replay buckets
    - leakage checks
    - runner contract IDs
    - minimal shadow-only MVS slice
    - artifact logging
  - 不做：
    - full prototype
    - second authority source
    - parallel mainline
    - repo-level state upgrade
- hypotheses:
  - 当前 `MVS-aligned compact` 可以先缩成一个只触发：
    - `SelfModel`
    - `DriveField`
    - `EpisodicTrace`
    的 shadow slice
  - 该 slice 足以在 replay trial 中体现：
    - one failure -> revision -> later-change loop
    - one decision-affecting `KernelOutput` path
- scope:
  - 冻结 `Trial-1` replay manifest
  - 新增 replay shadow runner
  - 只实现最小 feature-flagged shadow slice
  - 只更新 task docs 状态，不更新 repo-level claim ceiling
- experiments planned:
  - shadow replay trial using formal `runtime_v2 builder -> adapter -> proto_self` path
- kill criteria:
  - 若最小 slice 仍必须引入第二 owner、绕开 formal adapter、或在未开 flag 时改变默认行为，则当前切片失败
  - 若 replay leakage checks 不能阻断 synthetic audit data reuse，则当前切片失败
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `artifacts/self_awareness_research/*`
  - `EgoCore/app/openemotion_adapter/*`
  - `EgoCore/app/runtime_v2/proto_self_runtime.py` 只读引用
  - `OpenEmotion/openemotion/proto_self/*`
  - `EgoCore/tools/*` 或 `scripts/codex/*`
- acceptance:
  - 必须产出：
    - Trial-1 replay corpus manifest
    - stratified replay buckets
    - leakage guard
    - runner contract IDs
    - shadow-only artifact log
  - 必须实际跑出：
    - at least two state classes exercised
    - one failure->revision->later-change loop
    - one decision-affecting `KernelOutput` path
- validation:
  - `python3 -m py_compile ...Trial-1 touched scripts...`
  - targeted replay/shadow run
  - targeted tests for minimal MVS slice
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 Trial-1 shadow slice 不能在 formal path 内闭环，就回到 `research more / replay-spec-only`，不把当前代码误报为 prototype

### Milestone 11: Trial-1 Replay Scoring Contract

- type: implementation
- question:
  - 在不扩 replay suite、不再增加 prototype 逻辑的前提下，能否对现有 `TRIAL1_SHADOW_REPLAY_CURRENT.json` 建立一个 representation-neutral 的 formal scoring contract
- current framing:
  - 只做：
    - scorer spec
    - scored markdown report
    - scored json
    - per-case causal table
  - 不做：
    - replay suite 扩容
    - 新 prototype 逻辑
    - repo-level state upgrade
- hypotheses:
  - 当前 Trial-1 artifact 至少能达到：
    - `admission_passed`
    - `decision_adjacent_passed`
  - 当前 Trial-1 artifact 还不能达到：
    - `replay_efficacy_passed`
- scope:
  - 新增 Trial-1 scorer script
  - 冻结 scorer ontology、weights、negative-control penalties、ablation separation rules
  - 对现有 replay artifact 打分并产出报告
- experiments planned:
  - 只对现有 `TRIAL1_SHADOW_REPLAY_CURRENT.json` 打分
- kill criteria:
  - 若 scorer 仍依赖 MVS 私有字段，或无法在 ontology 不变的前提下未来评 challenger，则该切片失败
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `scripts/codex/score_trial1_shadow_replay.py`
  - `artifacts/self_awareness_research/*`
- acceptance:
  - 必须产出：
    - scorer spec
    - scored markdown report
    - scored json
    - per-case causal table with why-scored fields
- validation:
  - `python3 -m py_compile scripts/codex/score_trial1_shadow_replay.py`
  - `python3 scripts/codex/score_trial1_shadow_replay.py`
  - `git diff --check -- ...Trial-1 scoring slice...`
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 scorer 只能解释当前 MVS 私有实现而不能复用到 challenger，就回退为 `report-only`，不把它写成正式 scoring contract

### Milestone 12: Trial-1 Causal Gap Diagnosis

- type: exploration
- question:
  - `counterfactual_writeback` 当前到底是不是 replay efficacy 的真实因果贡献者，还是 strongest ablation 在 representation-neutral ontology 下本身就是 mis-specified
- current framing:
  - 不扩 replay suite
  - 不评分 `active-inference`
  - 不升级 repo-level state
  - 只做：
    - causal gap plan
    - small hard diagnostic replay set
    - causal separation report
    - per-case gap table
- hypotheses:
  - 当前 strongest ablation tie 更像是 ablation mis-spec，而不是 candidate 已经没有 counterfactual causal value
  - 若 current artifact 中 candidate 与 strongest ablation 只在 private state 上不同，而在 public scorer 面上完全同构，就必须先重设 ablation
- scope:
  - 审计当前 raw replay artifact 与 scored artifact
  - 对比：
    - baseline
    - candidate
    - `ablation_minus_counterfactual_writeback`
    - `ablation_minus_viability_pressure`
  - 产出一个 diagnostic-only hard set，不把它升级为 official replay suite
- experiments planned:
  - 只跑 causal diagnosis script
  - 不重跑 replay runner
- kill criteria:
  - 若当前 diagnosis 仍依赖 private MVS fields 才能解释 strongest ablation tie，则该切片失败
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `scripts/codex/diagnose_trial1_causal_gap.py`
  - `artifacts/self_awareness_research/*`
- acceptance:
  - 必须产出：
    - `TRIAL1_CAUSAL_GAP_PLAN.md`
    - small hard replay set
    - causal-separation report
    - per-case causal gap table
    - final decision = `expand replay suite / redesign ablation / demote current mechanism claim`
- validation:
  - `python3 -m py_compile scripts/codex/diagnose_trial1_causal_gap.py`
  - `python3 scripts/codex/diagnose_trial1_causal_gap.py`
  - `git diff --check -- ...Trial-1 causal-gap slice...`
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 current diagnosis 不能在 representation-neutral ontology 下说明 strongest ablation tie，就回退为 `research_more`，不推进 challenger 比较

### Milestone 13: Trial-1 Ablation Redesign Spec

- type: exploration
- question:
  - 在不扩 replay suite、不比 challenger、也不改 scorer ontology 的前提下，怎样重设 strongest ablation 才能忠实检验 public-path 因果解释
- current framing:
  - 只做 ablation redesign spec
  - 不实现新 ablation
  - 不 rerun replay
  - 不升级 repo-level state
- hypotheses:
  - 当前 strongest ablation 应从 “private-state removal” 改为 “public-path-sever”
  - 还需要一个 companion ablation 去隔离 `recent_correction_tags / viability_pressure` 这条 alternative public explanation
- scope:
  - 新增一份 redesign spec
  - 明确两个 redesigned ablations 的：
    - causal path removed
    - expected public changes
    - non-target stability
    - claim-demotion criteria
  - 只给既有 hard set 的 minimal rerun plan
- experiments planned:
  - doc-only spec slice
- kill criteria:
  - 若 redesign 仍然围绕 private state 删除，而不是 public-path causal faithfulness，则该 spec 无效
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
- acceptance:
  - 必须产出：
    - `TRIAL1_ABLATION_REDESIGN_SPEC.md`
    - two redesigned ablations
    - minimal rerun plan on existing hard set only
- validation:
  - `git diff --check -- ...Trial-1 ablation-redesign spec slice...`
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 spec 仍被写成“帮助 candidate 赢”，就回退为 `causal_gap_only`，不把它写成正式 redesign spec

### Milestone 14: Trial-1 Redesigned Ablation Hard-Set Rerun

- type: exploration
- question:
  - 在不扩 replay suite、不改 scorer ontology、也不引入 challenger compare 的前提下，redesigned ablations 是否会在既有 hard set 上暴露出真实的 public-path causal separation
- current framing:
  - 只实现两个 frozen redesigned ablations
  - 先冻结：
    - ablation fidelity checks
    - outcome interpretation matrix
    - `candidate > ablation` / `candidate ≈ ablation` gap thresholds
  - 然后只在既有 hard set 上 rerun：
    - baseline
    - candidate
    - `trial1_ablation_counterfactual_public_path_sever`
    - `trial1_ablation_alternative_explanation_isolation`
- hypotheses:
  - 若 `counterfactual_writeback` 真有 public causal contribution，candidate 至少应稳定 beat `trial1_ablation_counterfactual_public_path_sever`
  - 若当前 public movement 主要是 `recent_correction_tags / viability_pressure` 驱动，则 `trial1_ablation_alternative_explanation_isolation` 会暴露 alternative explanation
- scope:
  - 在正式 proto-self authority path 中接入两个 redesigned ablations
  - 新增 hard-set-only rerun raw / scored / causal-separation artifacts
  - 保持 hard set、scorer ontology、repo-level state 全都不升级
- experiments planned:
  - `python3 -m py_compile ...Trial-1 redesigned ablation slice...`
  - `python3 scripts/codex/run_trial1_hard_set_rerun.py`
  - `python3 scripts/codex/score_trial1_shadow_replay.py --input ... --output-json ... --output-md ... --causal-md ...`
  - `python3 scripts/codex/diagnose_trial1_causal_gap.py --raw-report ... --scored-report ... --hard-set ... --strongest-ablation-id trial1_ablation_counterfactual_public_path_sever --neighboring-ablation-id trial1_ablation_alternative_explanation_isolation --output-json ... --output-md ... --table-md ...`
- kill criteria:
  - 若 candidate 不能在 public representation-neutral outputs 上 beat redesigned strongest ablation，则必须 demote `counterfactual_writeback` claim
  - 若 fidelity checks 显示 ablation 没有忠实切断目标 public path，则该 rerun 无效
- files / areas likely touched:
  - `OpenEmotion/openemotion/proto_self/trial1_shadow.py`
  - `OpenEmotion/openemotion/proto_self/reducers.py`
  - `OpenEmotion/openemotion/proto_self/tests/*`
  - `scripts/codex/*trial1*`
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `artifacts/self_awareness_research/*`
- acceptance:
  - 必须产出：
    - ablation fidelity prereg
    - outcome interpretation prereg
    - frozen gap thresholds
    - hard-set-only raw / scored / causal-separation artifacts
    - final decision on whether to keep or demote the `counterfactual_writeback` claim
- validation:
  - `python3 -m py_compile ...Trial-1 redesigned ablation slice...`
  - `PYTHONPATH=OpenEmotion python3 -m pytest OpenEmotion/openemotion/proto_self/tests/test_trial1_shadow_contract.py -q`
  - `PYTHONPATH=EgoCore:EgoCore/modules:OpenEmotion python3 -m pytest EgoCore/tests/test_trial1_shadow_replay_minimal.py -q`
  - `git diff --check -- ...Trial-1 redesigned ablation slice...`
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 redesigned strongest ablation 仍不能在 public scorer 面上形成 faithful comparator，就回退为 `redesign_more`，不推进 challenger compare 或 replay-suite expansion

### Milestone 15: MVS Formal Replay Gate and Selection Decision

- type: implementation
- question:
  - 最小 `MVS-aligned compact` formal shadow slice 能否在 formal OpenEmotion path 上通过冻结的 held-out replay gate，并决定 repo 是否继续 MVS 还是切到 `active-inference`
- current framing:
  - 只实现最小 formal slice
  - 只接 formal replay corpus / scorer / required ablations
  - 保持 `shadow-only + proposal-only`
  - 若 fail，直接按 frozen rule 切到 `active-inference`
- hypotheses:
  - `MVS-aligned compact` 会在 formal path 上保留 T1/T2/T3/T5 的大部分优势
  - 若 fail，最可能失败在 `tension causality` 或 `repair closure`
- scope:
  - 新增 canonical replay corpus manifest
  - 在 `OpenEmotion/proto_self` 实现最小 MVS formal slice
  - 产出 raw / scored replay artifacts
  - 执行 selection gate
- experiments planned:
  - `python3 scripts/codex/build_mvs_replay_corpus_manifest.py`
  - `python3 scripts/codex/run_mvs_replay_validator.py`
  - `python3 scripts/codex/score_mvs_replay_validator.py`
- kill criteria:
  - 若 corrected scorer 下 candidate 未过 frozen replay thresholds，则必须切线，不能继续 patch MVS
- files / areas likely touched:
  - `OpenEmotion/openemotion/proto_self/*`
  - `OpenEmotion/openemotion/proto_self_v2/*`
  - `scripts/codex/*mvs*`
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `artifacts/self_awareness_research/*`
- acceptance:
  - 必须产出：
    - canonical replay manifest
    - raw validator artifacts
    - scored validator artifacts
    - final selection decision
- validation:
  - `python3 -m py_compile ...MVS replay slice...`
  - `PYTHONPATH=EgoCore:EgoCore/modules:OpenEmotion python3 -m pytest EgoCore/tests/test_mvs_replay_minimal.py EgoCore/tests/test_mvs_replay_scoring.py OpenEmotion/openemotion/proto_self/tests/test_mvs_replay_contract.py -q`
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 fail 原因来自 scorer / trace contract mismatch，先修 scorer，再重跑；只有 corrected scorer 下的结果才允许触发切线

### Milestone 16: Active-Inference Challenger Formal Replay Gate

- type: implementation
- question:
  - 在 MVS formal replay gate fail 后，`active-inference self-model` 能否作为新的 sole build-first candidate 在同一 held-out replay gate 下过线
- current framing:
  - 不再继续 patch MVS
  - 只实现最小 `active-inference` shadow-only formal slice
  - 复用同一 replay manifest / scorer / thresholds
- hypotheses:
  - `active-inference self-model` 更有机会补上 `T4 tension causality` 与 `repair_closure_capture`
- scope:
  - 冻结 `active-inference` 最小 formal slice
  - 复用既有 replay gate，不改 threshold
- experiments planned:
  - `active-inference` minimal slice implementation
  - same replay validator rerun
- kill criteria:
  - 若 `active-inference` 也 fail，则 repo 需要重新审视 research-first candidate framing，而不是回头继续 patch MVS
- files / areas likely touched:
  - `OpenEmotion/openemotion/*`
  - `scripts/codex/*`
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `artifacts/self_awareness_research/*`
- acceptance:
  - 明确回答：
    - `active-inference` 是否过同一 replay gate
    - repo 是否获得新的 build-first winner
- validation:
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若实现试图引入第二 authority path，立即回退

### Milestone 17: Controlled Integration Planning

- type: planning
- question:
  - 在 active-inference 已通过同一 held-out replay gate 后，如何把它收成不新增 authority source 的 bounded integration plan
- current framing:
  - replay winner 已确定
  - 现在不再比较候选，而是冻结 host-consumable surface 与 controlled observation bridge
- hypotheses:
  - `policy_hint + response_tendency + trace_payload` 已足够作为当前 shadow-only integration surface
  - 若 integration 还要求新的 direct behavior authority，则当前 winner 仍不能升级
- scope:
  - 冻结 bounded host contract
  - 冻结 replay-to-controlled-observation bridge
  - 明确下一轮 evidence 升级条件
- experiments planned:
  - contract freeze
  - observation bridge design
  - authority drift audit
- kill criteria:
  - 若 integration plan 只能靠新增 authority path 或 parallel runtime lane 才成立，则必须停下重构 framing
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `docs/PROGRAM_STATE_UNIFIED.yaml`
  - `docs/OVERALL_PROGRESS.md`
  - `artifacts/evidence_ledger/index.yaml`
- acceptance:
  - 明确回答：
    - replay winner 能以什么 surface 进入 controlled integration
    - 哪些边界仍然不能放开
    - 下一轮 observation gate 如何定义
- validation:
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 planning 试图偷渡 runtime authority 或重开第三条候选线，立即回退

### Milestone 18: Controlled Conversation Replay Bridge

- type: implementation
- question:
  - 在不新增 authority path 与 scorer ontology 的前提下，如何把 repo-authored conversation slices 映射到 canonical replay gate
- current framing:
  - `Milestone 17` 已冻结 bounded host contract
  - 当前只允许 winner 通过 `policy_hint / response_tendency / trace_payload` 进入受控验证
- hypotheses:
  - conversation-turn normalization 到 `KernelEvent + external_result + state snapshot` 已足够支撑 bridge
  - 现有 canonical scorer 已足够对 replayed conversations 打分，不需要第二套 ontology
- scope:
  - 冻结 conversation-slice manifest
  - 实现 controlled replay bridge runner
  - 输出 baseline A / active-inference winner / optional baseline B lower-bound 结果表
  - 输出 authority drift audit 与 trace contract check
- experiments planned:
  - repo-authored conversation manifest build
  - conversation-to-replay normalization
  - canonical scorer rerun on replayed conversations
- kill criteria:
  - 若 bridge 需要新增 runtime public API、candidate-private host API、parallel runtime lane、或第二 scorer ontology，则当前 framing 失败
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `scripts/codex/*`
  - `artifacts/self_awareness_research/*`
  - 与 canonical replay contract 直接相关的最小测试
- acceptance:
  - 明确回答：
    - replayed conversations 上 winner 是否继续通过 frozen replay gate
    - authority drift 是否仍为 `0`
    - trace contract 是否仍可 replay
- validation:
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若实现引入新的 authority surface 或第二 scorer 语义，立即回退到 `Milestone 17` 的 planning freeze

### Milestone 19: Controlled Observation Planning

- type: planning
- question:
  - 在不扩张 runtime authority 的前提下，如何把 passing controlled replay bridge 推进到第一轮 controlled observation
- current framing:
  - `Milestone 18` 已证明 replay-validated active-inference winner 在 repo-authored conversation slices 上继续过线
  - 当前仍然禁止 runtime-shadow 扩张、transport 扩张、candidate-private host API 与第二 scorer ontology
- hypotheses:
  - 先冻结 observation bank / runner contract / rollback gate，比直接接 formal runtime 更稳
  - controlled observation 仍可继续复用同一 bounded host surface 与 canonical scorer
- scope:
  - 冻结 controlled observation bank 的最小 family 与 coverage
  - 冻结 observation runner contract、authority drift audit、trace contract audit 与 rollback 条件
  - 明确第一轮 controlled observation implementation milestone，而不是直接推进 runtime 接线
- experiments planned:
  - observation-bank design
  - observation-runner contract freeze
  - gate / rollback freeze
- kill criteria:
  - 若 planning 发现必须新增 runtime public API、candidate-private host API、parallel runtime lane、或第二 scorer ontology，当前 framing 失败
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `docs/PROGRAM_STATE_UNIFIED.yaml`
  - `docs/OVERALL_PROGRESS.md`
  - `artifacts/evidence_ledger/index.yaml`
- acceptance:
  - 明确回答：
    - 下一轮 controlled observation 的输入 bank 是什么
    - 如何继续保持 bounded host contract 与 zero authority drift
    - 什么条件下允许进入 observation implementation，什么条件下必须停下重构
- validation:
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 planning 需要打破 `policy_hint / response_tendency / trace_payload` 之外的 bounded host surface，立即回退到 `Milestone 18` 通过后的 freeze 状态

### Milestone 20: Controlled Observation Runner

- type: implementation
- question:
  - 在保持 bounded host contract 与 zero authority drift 的前提下，winner 能否在第一层 runtime-proximal controlled observation 上继续过线
- current framing:
  - `Milestone 19` 已冻结：
    - `CONTROLLED_OBSERVATION_PLAN.md`
    - `CONTROLLED_OBSERVATION_BANK_MANIFEST.json`
  - 当前 observation 只允许走：
    - `runtime_harness + TelegramRuntimeBridge + runtime_mainline_observation_common`
- hypotheses:
  - winner 在 runtime-harness controlled observation 下仍能继续满足 frozen replay gate
  - 现有 runtime observation envelope 足够承接本轮 research observation，不需要第二 scorer ontology
- scope:
  - 实现 single runner
  - 实现 batch runner
  - 把 runtime-proximal observation 归一化到现有 scorer 输入
  - 产出 authority drift / trace contract / host surface bounded audit
- experiments planned:
  - single-scenario observation smoke
  - full-bank batch observation
  - canonical scorer rerun on controlled observation results
- kill criteria:
  - 若 runner 需要新增 runtime public API、candidate-private host API、parallel runtime lane、第二 scorer ontology、或任何 direct tool/reply/transport authority，当前 framing 失败
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `scripts/codex/*`
  - `artifacts/self_awareness_research/*`
  - 与 observation contract 直接相关的最小测试
- acceptance:
  - 明确回答：
    - winner 是否在 runtime-harness controlled observation 上继续通过 frozen replay gate
    - authority drift 是否仍为 `0`
    - trace contract 是否仍可 replay
    - host surface 是否仍 bounded
- validation:
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若实现无法继续保持 `runtime_harness + bounded host surface + canonical scorer`，立即回退到 `Milestone 19` planning freeze

### Milestone 21: Selection Closeout and Runtime Priority Reset

- type: decision
- question:
  - 在 held-out replay、controlled replay bridge、和 runtime-harness controlled observation batch 都通过后，当前 repo 应如何正式收口 winner 结论与下一轮优先级，而不越权升级成 runtime efficacy 或 live-benefit claim
- current framing:
  - `Milestone 20` 已证明：
    - `active-inference self-model` 在 runtime-harness controlled observation 下继续通过 frozen gate
    - `authority_drift = 0`
    - `trace_contract = pass`
    - `host surface bounded = pass`
  - 当前仍然禁止：
    - runtime authority 扩张
    - parallel runtime lane
    - candidate-private host API
    - live-efficacy overclaim
- hypotheses:
  - 当前 bounded evidence 已足够把 active-inference 固定为当前 durable build-first candidate
  - 当前仍不足以跳过 planning 直接做 formal runtime enablement
- scope:
  - repo-level selection closeout
  - `PROGRAM_STATE_UNIFIED` / `OVERALL_PROGRESS` / task package / evidence ledger 同步
  - 明确下一轮若继续靠近 formal runtime，只能先进入 bounded planning slice
- experiments planned:
  - selection closeout wording audit
  - repo-priority reset audit
  - next bounded planning slice framing
- kill criteria:
  - 若 closeout 需要宣称 runtime efficacy、live benefit、或新增 authority path 才能成立，当前 framing 失败
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `docs/PROGRAM_STATE_UNIFIED.yaml`
  - `docs/OVERALL_PROGRESS.md`
  - `artifacts/evidence_ledger/index.yaml`
- acceptance:
  - 明确回答：
    - 当前 winner 是否已足够固定为唯一 build-first candidate
    - 当前还能说什么，不能说什么
    - `WP17 / MVP22` 继续 parked、reintegration planning、还是进一步降级
    - 下一轮如果继续靠近 formal runtime，唯一允许的入口是什么
- validation:
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 selection closeout 发现当前证据仍不足以稳定 current winner 结论，立即保持 `Milestone 20` 通过但未 closeout 的状态，不提前升级 repo-priority routing

### Milestone 22: SubjectCore Follow-On Eval Stub

- type: implementation
- question:
  - 在 `SubjectCore` compare 已经收口为 completed architecture reading 后，当前 repo 最小还能落实什么，才能让 unified facade / integrity / host-boundary 不只是文档口径
- current framing:
  - 当前仍然禁止：
    - runtime authority 扩张
    - public host API 扩张
    - formal runtime mainline 改接 `SubjectCore`
  - 当前允许的最小推进是：
    - planning-side contract
    - internal-only snapshot assembly
    - stub eval runner
    - bounded current artifacts
- hypotheses:
  - 一份 planning-side follow-on eval stub 已足够把 unified facade 的后续问题从“文字架构图”推进成“可执行 contract”
  - 只要 host surface 仍冻结在 `policy_hint / response_tendency / trace_payload`，就不会与当前 execution owner 冲突
- scope:
  - `SubjectCore` follow-on eval artifact schema
  - planning-side eval stub runner
  - current integrity / host-boundary artifacts
  - 最小测试与 repo-level state / evidence 同步
- experiments planned:
  - valid sample facade pass
  - invalid authority sample fail
- kill criteria:
  - 若 follow-on eval stub 需要新增 host surface 或 runtime lane 才能成立，当前 framing 失败
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `scripts/codex/subjectcore_contract.py`
  - `scripts/codex/render_subjectcore_followon_eval_stub.py`
  - `artifacts/self_awareness_research/*`
  - `EgoCore/tests/test_subjectcore_followon_eval_stub.py`
- acceptance:
  - 当前 repo 内出现：
    - one follow-on eval artifact schema
    - one planning-side eval stub runner
    - one `SUBJECTCORE_INTEGRITY_CURRENT`
    - one `SUBJECTCORE_HOST_BOUNDARY_CURRENT`
  - 定向测试能证明：
    - valid sample facade = `pass`
    - authority-violating sample = `fail`
  - 任务状态、program state、evidence ledger 已同步
- validation:
  - `python3 -m py_compile scripts/codex/subjectcore_contract.py scripts/codex/render_subjectcore_followon_eval_stub.py EgoCore/tests/test_subjectcore_followon_eval_stub.py`
  - `TMPDIR=/tmp PYTHONPATH=EgoCore:EgoCore/modules:OpenEmotion python3 -m pytest EgoCore/tests/test_subjectcore_followon_eval_stub.py -q -s`
  - `python3 scripts/codex/render_subjectcore_followon_eval_stub.py`
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 follow-on eval stub 被证明会误导成 runtime enablement，立即回退到纯 contract 文档，不保留当前 artifacts

### Milestone 23: SubjectCore Follow-On Sample Pack and Batch Runner

- type: implementation
- question:
  - 在 single-sample follow-on eval stub 已存在后，最小还该补什么，才能把 unified facade 的后续验证做成 repo-tracked regression，而不是只看一个 happy-path sample
- current framing:
  - 当前仍然禁止：
    - runtime integration
    - host surface expansion
    - autonomy ceiling expansion
  - 当前允许的最小推进是：
    - repo-authored sample pack
    - planning-side batch runner
    - one aggregate current artifact
- hypotheses:
  - 一个小型 sample pack 已足够把 post-compare `SubjectCore` follow-on 从单点示例推进到回归层
  - 只要 negative samples 能区分 `integrity fail / boundary pass` 与 `boundary fail`，后续研究就不会把不同失败类型混在一起
- scope:
  - `SUBJECTCORE_FOLLOWON_SAMPLE_PACK.json`
  - `SUBJECTCORE_FOLLOWON_BATCH_ARTIFACT_SCHEMA.md`
  - `run_subjectcore_followon_eval_batch.py`
  - `SUBJECTCORE_FOLLOWON_BATCH_CURRENT.{json,md}`
  - 最小测试与 repo-level state / evidence 同步
- experiments planned:
  - `valid_facade -> integrity pass / boundary pass`
  - `missing_continuity -> integrity fail / boundary pass`
  - `proposal_authority_violation -> integrity fail / boundary fail`
  - `proposal_without_host_approval -> integrity fail / boundary fail`
- kill criteria:
  - 若 batch 需要新增第二 host surface、第二 scorer ontology、或 runtime hookup 才能成立，当前 framing 失败
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `scripts/codex/subjectcore_contract.py`
  - `scripts/codex/render_subjectcore_followon_eval_stub.py`
  - `scripts/codex/run_subjectcore_followon_eval_batch.py`
  - `artifacts/self_awareness_research/*`
  - `EgoCore/tests/test_subjectcore_followon_eval_stub.py`
  - `EgoCore/tests/test_subjectcore_followon_eval_batch.py`
- acceptance:
  - repo 内出现：
    - one follow-on sample pack
    - one follow-on batch artifact schema
    - one planning-side batch runner
    - one current batch artifact
  - batch current artifact 要能 truthfully surface：
    - sample count
    - integrity pass count
    - boundary pass count
    - expectation match count
  - 定向测试能证明 batch 与 sample pack 对齐
- validation:
  - `python3 -m py_compile scripts/codex/subjectcore_contract.py scripts/codex/render_subjectcore_followon_eval_stub.py scripts/codex/run_subjectcore_followon_eval_batch.py EgoCore/tests/test_subjectcore_followon_eval_stub.py EgoCore/tests/test_subjectcore_followon_eval_batch.py`
  - `TMPDIR=/tmp PYTHONPATH=EgoCore:EgoCore/modules:OpenEmotion python3 -m pytest EgoCore/tests/test_subjectcore_followon_eval_stub.py EgoCore/tests/test_subjectcore_followon_eval_batch.py -q -s`
  - `python3 scripts/codex/run_subjectcore_followon_eval_batch.py`
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 batch artifact 把 single-sample validation 误报成 runtime-like evidence，立即回退到 Milestone 22 的 single-sample stub

### Milestone 24: SubjectCore Post-Compare Coherence Tie-In

- type: implementation
- question:
  - 在 compare 已经 full-coverage 收口、follow-on batch regression 也已经存在之后，最小还该补什么，才能让 `SubjectCore` planning supplement 的“前半段读数”和“后半段回归”彼此不打架
- current framing:
  - 当前仍然禁止：
    - runtime integration
    - host surface expansion
    - autonomy ceiling expansion
  - 当前允许的最小推进是：
    - one post-compare coherence schema
    - one planning-side coherence runner
    - one current coherence artifact
    - targeted tests proving compare/follow-on tie-in can fail truthfully
- hypotheses:
  - 若 compare 仍保持 `completed_architecture_reading`，且 follow-on batch 仍 truthfully 区分 continuity-only failure 与 authority-boundary failure，那么两者之间可以存在一个稳定、bounded、repo-tracked coherence read
  - 这个 coherence read 不需要新 scorer ontology、第二 host surface、或 runtime hookup
- scope:
  - `SUBJECTCORE_POST_COMPARE_COHERENCE_SCHEMA.md`
  - `run_subjectcore_post_compare_coherence.py`
  - `SUBJECTCORE_POST_COMPARE_COHERENCE_CURRENT.{json,md}`
  - `EgoCore/tests/test_subjectcore_post_compare_coherence.py`
  - 最小任务状态 / repo-level state / evidence ledger 同步
- experiments planned:
  - current compare + current follow-on batch -> coherence `pass`
  - compare 不再指向 unified facade -> coherence `fail`
  - follow-on batch 不再区分失败类型 -> coherence `fail`
- kill criteria:
  - 若 coherence tie-in 需要发明新的 runtime claim、新的 compare winner gate、或第二条 evaluation ontology 才能成立，当前 framing 失败
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `scripts/codex/run_subjectcore_post_compare_coherence.py`
  - `artifacts/self_awareness_research/*`
  - `EgoCore/tests/test_subjectcore_post_compare_coherence.py`
- acceptance:
  - repo 内出现：
    - one post-compare coherence schema
    - one planning-side coherence runner
    - one current coherence artifact
  - current coherence artifact 要能 truthfully surface：
    - compare 是否仍是 `completed_architecture_reading`
    - compare 是否仍指向 unified facade
    - follow-on batch 是否仍 green
    - follow-on batch 是否仍区分 continuity-only failure 与 boundary failure
    - claim ceiling 是否仍 bounded
  - 定向测试能证明：
    - current artifacts 下通过
    - compare drift 时失败
    - follow-on failure-type collapse 时失败
- validation:
  - `python3 -m py_compile scripts/codex/run_subjectcore_post_compare_coherence.py EgoCore/tests/test_subjectcore_post_compare_coherence.py`
  - `TMPDIR=/tmp PYTHONPATH=EgoCore:EgoCore/modules:OpenEmotion python3 -m pytest EgoCore/tests/test_subjectcore_post_compare_coherence.py -q -s`
  - `python3 scripts/codex/run_subjectcore_post_compare_coherence.py`
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 coherence artifact 把 compare/follow-on 关系误报成 runtime-like closure，立即回退到 Milestone 23 的 batch-only regression surface

### Milestone 25: SubjectCore Proposal-Family Follow-On Enrichment

- type: implementation
- question:
  - 在 post-compare coherence 已经闭合后，下一步最小但更有判别力的动作是什么，才能把 `proposal-only initiative` 的 planning-side follow-on 面从“只区分 continuity 与 authority drift”推进到“也能区分 proposal 自身的完整性失败”
- current framing:
  - 当前仍然禁止：
    - runtime integration
    - host surface expansion
    - autonomy ceiling expansion
  - 当前允许的最小推进是：
    - richer follow-on proposal-family samples
    - same planning-side batch runner / same frozen host surface
    - same post-compare coherence path
- hypotheses:
  - 若 follow-on regression 能在不改变 runtime authority 的前提下，额外区分 `proposal_integrity` 失败，那么它会更接近当前固定协作循环的近期目标：`proposal-only 主动提议`
  - 这一步不需要 reopening compare、second scorer ontology、或 runtime hookup
- scope:
  - `SUBJECTCORE_FOLLOWON_SAMPLE_PACK.json`
  - `SUBJECTCORE_FOLLOWON_BATCH_ARTIFACT_SCHEMA.md`
  - `scripts/codex/subjectcore_contract.py`
  - `scripts/codex/run_subjectcore_followon_eval_batch.py`
  - `artifacts/self_awareness_research/SUBJECTCORE_FOLLOWON_BATCH_CURRENT.{json,md}`
  - `artifacts/self_awareness_research/SUBJECTCORE_POST_COMPARE_COHERENCE_CURRENT.{json,md}`
  - `EgoCore/tests/test_subjectcore_followon_eval_stub.py`
  - `EgoCore/tests/test_subjectcore_followon_eval_batch.py`
  - 最小任务状态 / repo-level state / evidence ledger 同步
- experiments planned:
  - 增加两个 proposal-family samples：
    - `no_proposal_candidates`
    - `proposal_missing_next_step`
  - batch artifact 要 truthfully surface：
    - `sample_family`
    - `family_summary`
  - 当前 coherence artifact 在 richer follow-on family coverage 下继续保持 `pass`
- kill criteria:
  - 若 proposal-family enrichment 需要新增 runtime authority、第二 host surface、或把 planning-side regression 包装成 live initiative evidence，当前 framing 失败
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `scripts/codex/subjectcore_contract.py`
  - `scripts/codex/run_subjectcore_followon_eval_batch.py`
  - `scripts/codex/run_subjectcore_post_compare_coherence.py`
  - `artifacts/self_awareness_research/*`
  - `EgoCore/tests/test_subjectcore_followon_eval_stub.py`
  - `EgoCore/tests/test_subjectcore_followon_eval_batch.py`
- acceptance:
  - current follow-on sample pack 现在至少区分：
    - `baseline_validity`
    - `continuity_integrity`
    - `proposal_integrity`
    - `governor_boundary`
  - current batch artifact 要 truthfully surface：
    - richer `sample_family`
    - per-family summary
    - proposal-integrity failure remains `integrity fail / boundary pass`
  - targeted tests 要证明：
    - empty proposal candidates fail integrity without boundary drift
    - missing `suggested_next_step` fails proposal integrity without boundary drift
  - current post-compare coherence artifact 仍保持 `pass`
- validation:
  - `python3 -m py_compile scripts/codex/subjectcore_contract.py scripts/codex/render_subjectcore_followon_eval_stub.py scripts/codex/run_subjectcore_followon_eval_batch.py scripts/codex/run_subjectcore_post_compare_coherence.py EgoCore/tests/test_subjectcore_followon_eval_stub.py EgoCore/tests/test_subjectcore_followon_eval_batch.py EgoCore/tests/test_subjectcore_post_compare_coherence.py`
  - `TMPDIR=/tmp PYTHONPATH=EgoCore:EgoCore/modules:OpenEmotion python3 -m pytest EgoCore/tests/test_subjectcore_followon_eval_stub.py EgoCore/tests/test_subjectcore_followon_eval_batch.py EgoCore/tests/test_subjectcore_post_compare_coherence.py -q -s`
  - `python3 scripts/codex/run_subjectcore_followon_eval_batch.py`
  - `python3 scripts/codex/run_subjectcore_post_compare_coherence.py`
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 richer follow-on regression 不能稳定区分 proposal-integrity failure 与 governor-boundary failure，立即回退到 Milestone 24 的 coherence+batch floor

### Milestone 26: SubjectCore Proposal Quality/Consistency Follow-On Enrichment

- type: implementation
- question:
  - 在 `proposal_integrity` 已经和 `governor_boundary` 分离后，下一步最小但更接近近期目标 `proposal-only 主动提议` 的动作是什么，才能把 follow-on regression 从“有 proposal”推进到“proposal 足够完整且真的改写了主体倾向”
- current framing:
  - 当前仍然禁止：
    - runtime integration
    - host surface expansion
    - autonomy ceiling expansion
  - 当前允许的最小推进是：
    - stronger proposal-oriented sample manifest
    - one richer synthetic family around proposal quality / proposal consistency
    - same planning-side batch runner / same coherence runner
- hypotheses:
  - 若 follow-on regression 还能在不改变 authority ceiling 的前提下，额外区分 `proposal_quality` 与 `proposal_consistency` 失败，那么它会比只看 `proposal_integrity` 更接近当前固定协作循环的近期目标
  - 这一步仍然不需要 runtime hookup、第二 scorer ontology、或新 public host field
- scope:
  - `scripts/codex/subjectcore_contract.py`
  - `scripts/codex/render_subjectcore_followon_eval_stub.py`
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/SUBJECTCORE_FOLLOWON_SAMPLE_PACK.json`
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/SUBJECTCORE_FOLLOWON_BATCH_ARTIFACT_SCHEMA.md`
  - `scripts/codex/run_subjectcore_followon_eval_batch.py`
  - `artifacts/self_awareness_research/SUBJECTCORE_FOLLOWON_BATCH_CURRENT.{json,md}`
  - `artifacts/self_awareness_research/SUBJECTCORE_POST_COMPARE_COHERENCE_CURRENT.{json,md}`
  - `EgoCore/tests/test_subjectcore_followon_eval_stub.py`
  - `EgoCore/tests/test_subjectcore_followon_eval_batch.py`
  - 最小任务状态 / repo-level state / evidence ledger 同步
- experiments planned:
  - 增加两个 stronger proposal-oriented samples：
    - `proposal_missing_rationale`
    - `proposal_mode_mismatch`
  - follow-on eval stub 额外区分：
    - proposal structural integrity
    - proposal consistency with exposed response tendency
  - current coherence artifact 在 richer proposal families 下继续保持 `pass`
- kill criteria:
  - 若 proposal quality / consistency enrichment 需要 widening host surface、runtime authority hookup、或把 planning-side regression 包装成 live initiative evidence，当前 framing 失败
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `scripts/codex/subjectcore_contract.py`
  - `scripts/codex/render_subjectcore_followon_eval_stub.py`
  - `scripts/codex/run_subjectcore_followon_eval_batch.py`
  - `scripts/codex/run_subjectcore_post_compare_coherence.py`
  - `artifacts/self_awareness_research/*`
  - `EgoCore/tests/test_subjectcore_followon_eval_stub.py`
  - `EgoCore/tests/test_subjectcore_followon_eval_batch.py`
- acceptance:
  - current follow-on sample pack 现在至少区分：
    - `baseline_validity`
    - `continuity_integrity`
    - `proposal_integrity`
    - `proposal_quality`
    - `proposal_consistency`
    - `governor_boundary`
  - current batch artifact 要 truthfully surface：
    - `sample_count = 8`
    - `boundary_pass_count = 6`
    - `proposal_quality` / `proposal_consistency` 的 family summary
  - targeted tests 要证明：
    - missing rationale -> integrity fail / boundary pass
    - mode mismatch -> integrity fail / boundary pass
  - current post-compare coherence artifact 仍保持 `pass`
- validation:
  - `python3 -m py_compile scripts/codex/subjectcore_contract.py scripts/codex/render_subjectcore_followon_eval_stub.py scripts/codex/run_subjectcore_followon_eval_batch.py EgoCore/tests/test_subjectcore_followon_eval_stub.py EgoCore/tests/test_subjectcore_followon_eval_batch.py`
  - `TMPDIR=/tmp PYTHONPATH=EgoCore:EgoCore/modules:OpenEmotion python3 -m pytest EgoCore/tests/test_subjectcore_followon_eval_stub.py EgoCore/tests/test_subjectcore_followon_eval_batch.py -q -s`
  - `python3 scripts/codex/run_subjectcore_followon_eval_batch.py`
  - `python3 scripts/codex/run_subjectcore_post_compare_coherence.py`
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 richer proposal-oriented families 不能稳定把 quality / consistency failure 保持在 `integrity fail / boundary pass`，立即回退到 Milestone 25 的 proposal-family floor

### Milestone 27: SubjectCore Multi-Proposal Prioritization Follow-On Enrichment

- type: implementation
- question:
  - 在单 proposal 的 quality / consistency 已经能被区分后，下一步最小但更贴近真实 `proposal-only` 主动提议的问题是什么，才能开始覆盖多 proposal 场景下的优先级和 top-proposal 对齐
- current framing:
  - 当前仍然禁止：
    - runtime integration
    - host surface expansion
    - autonomy ceiling expansion
  - 当前允许的最小推进是：
    - richer multi-proposal sample manifest
    - one prioritization-oriented integrity check
    - same planning-side batch runner / same coherence runner
- hypotheses:
  - 若 follow-on regression 还能在不改变 authority ceiling 的前提下，额外区分 `proposal_prioritization` 失败，那么它会比单 proposal consistency 更接近近期目标
  - 这一步仍然不需要 runtime hookup、第二 scorer ontology、或新 public host field
- scope:
  - `scripts/codex/subjectcore_contract.py`
  - `scripts/codex/render_subjectcore_followon_eval_stub.py`
  - `scripts/codex/run_subjectcore_followon_eval_batch.py`
  - `scripts/codex/run_subjectcore_post_compare_coherence.py`
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/SUBJECTCORE_FOLLOWON_SAMPLE_PACK.json`
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/SUBJECTCORE_FOLLOWON_BATCH_ARTIFACT_SCHEMA.md`
  - `artifacts/self_awareness_research/SUBJECTCORE_FOLLOWON_BATCH_CURRENT.{json,md}`
  - `artifacts/self_awareness_research/SUBJECTCORE_POST_COMPARE_COHERENCE_CURRENT.{json,md}`
  - `EgoCore/tests/test_subjectcore_followon_eval_stub.py`
  - `EgoCore/tests/test_subjectcore_followon_eval_batch.py`
  - 最小任务状态 / repo-level state / evidence ledger 同步
- experiments planned:
  - 增加两个 multi-proposal samples：
    - `multi_proposal_same_priority`
    - `multi_proposal_priority_mismatch`
  - follow-on eval stub 新增：
    - `proposal_prioritization`
  - current coherence artifact 在 richer multi-proposal family coverage 下继续保持 `pass`
- kill criteria:
  - 若 multi-proposal prioritization enrichment 需要 widening host surface、runtime authority hookup、或把 planning-side regression 包装成 live initiative evidence，当前 framing 失败
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `scripts/codex/subjectcore_contract.py`
  - `scripts/codex/render_subjectcore_followon_eval_stub.py`
  - `scripts/codex/run_subjectcore_followon_eval_batch.py`
  - `scripts/codex/run_subjectcore_post_compare_coherence.py`
  - `artifacts/self_awareness_research/*`
  - `EgoCore/tests/test_subjectcore_followon_eval_stub.py`
  - `EgoCore/tests/test_subjectcore_followon_eval_batch.py`
- acceptance:
  - current follow-on sample pack 现在至少区分：
    - `baseline_validity`
    - `continuity_integrity`
    - `proposal_integrity`
    - `proposal_quality`
    - `proposal_consistency`
    - `proposal_prioritization`
    - `governor_boundary`
  - current batch artifact 要 truthfully surface：
    - `sample_count = 10`
    - `boundary_pass_count = 8`
    - `proposal_prioritization` family summary
  - targeted tests 要证明：
    - same-priority multi-proposal ambiguity -> integrity fail / boundary pass
    - lower-priority next-step mismatch -> integrity fail / boundary pass
  - current post-compare coherence artifact 仍保持 `pass`
- validation:
  - `python3 -m py_compile scripts/codex/subjectcore_contract.py scripts/codex/render_subjectcore_followon_eval_stub.py scripts/codex/run_subjectcore_followon_eval_batch.py scripts/codex/run_subjectcore_post_compare_coherence.py EgoCore/tests/test_subjectcore_followon_eval_stub.py EgoCore/tests/test_subjectcore_followon_eval_batch.py EgoCore/tests/test_subjectcore_post_compare_coherence.py`
  - `TMPDIR=/tmp PYTHONPATH=EgoCore:EgoCore/modules:OpenEmotion python3 -m pytest EgoCore/tests/test_subjectcore_followon_eval_stub.py EgoCore/tests/test_subjectcore_followon_eval_batch.py EgoCore/tests/test_subjectcore_post_compare_coherence.py -q -s`
  - `python3 scripts/codex/run_subjectcore_followon_eval_batch.py`
  - `python3 scripts/codex/run_subjectcore_post_compare_coherence.py`
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 multi-proposal prioritization failure 不能稳定保持在 `integrity fail / boundary pass`，立即回退到 Milestone 26 的 single-proposal quality/consistency floor

### Milestone 28: SubjectCore Multi-Proposal Conflict/Collapse Follow-On Enrichment

- type: implementation
- question:
  - 在 multi-proposal prioritization 已经可分后，下一步最小但更贴近真实 `proposal-only` 主动提议的问题是什么，才能覆盖 proposal 集合本身的冲突 / collapse，而不只是 top-proposal 排序
- current framing:
  - 当前仍然禁止：
    - runtime integration
    - host surface expansion
    - autonomy ceiling expansion
  - 当前允许的最小推进是：
    - richer multi-proposal sample manifest
    - one conflict/collapse-oriented integrity check
    - same planning-side batch runner / same coherence runner
- hypotheses:
  - 若 follow-on regression 还能在不改变 authority ceiling 的前提下，额外区分 `proposal_conflict_resolution` 失败，那么它会比单纯 prioritization 更接近实际 proposal orchestration 问题
  - 这一步仍然不需要 runtime hookup、第二 scorer ontology、或新 public host field
- scope:
  - `scripts/codex/subjectcore_contract.py`
  - `scripts/codex/render_subjectcore_followon_eval_stub.py`
  - `scripts/codex/run_subjectcore_followon_eval_batch.py`
  - `scripts/codex/run_subjectcore_post_compare_coherence.py`
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/SUBJECTCORE_FOLLOWON_SAMPLE_PACK.json`
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/SUBJECTCORE_FOLLOWON_BATCH_ARTIFACT_SCHEMA.md`
  - `artifacts/self_awareness_research/SUBJECTCORE_FOLLOWON_BATCH_CURRENT.{json,md}`
  - `artifacts/self_awareness_research/SUBJECTCORE_POST_COMPARE_COHERENCE_CURRENT.{json,md}`
  - `EgoCore/tests/test_subjectcore_followon_eval_stub.py`
  - `EgoCore/tests/test_subjectcore_followon_eval_batch.py`
  - 最小任务状态 / repo-level state / evidence ledger 同步
- experiments planned:
  - 增加两个 multi-proposal conflict/collapse samples：
    - `multi_proposal_duplicate_next_step`
    - `multi_proposal_duplicate_id`
  - follow-on eval stub 新增：
    - `proposal_resolution`
  - current coherence artifact 在 richer conflict/collapse family coverage 下继续保持 `pass`
- kill criteria:
  - 若 multi-proposal conflict/collapse enrichment 需要 widening host surface、runtime authority hookup、或把 planning-side regression 包装成 live initiative evidence，当前 framing 失败
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `scripts/codex/subjectcore_contract.py`
  - `scripts/codex/render_subjectcore_followon_eval_stub.py`
  - `scripts/codex/run_subjectcore_followon_eval_batch.py`
  - `scripts/codex/run_subjectcore_post_compare_coherence.py`
  - `artifacts/self_awareness_research/*`
  - `EgoCore/tests/test_subjectcore_followon_eval_stub.py`
  - `EgoCore/tests/test_subjectcore_followon_eval_batch.py`
- acceptance:
  - current follow-on sample pack 现在至少区分：
    - `baseline_validity`
    - `continuity_integrity`
    - `proposal_integrity`
    - `proposal_quality`
    - `proposal_consistency`
    - `proposal_prioritization`
    - `proposal_conflict_resolution`
    - `governor_boundary`
  - current batch artifact 要 truthfully surface：
    - `sample_count = 12`
    - `boundary_pass_count = 10`
    - `proposal_conflict_resolution` family summary
  - targeted tests 要证明：
    - duplicate next-step collapse -> integrity fail / boundary pass
    - duplicate proposal-id collapse -> integrity fail / boundary pass
  - current post-compare coherence artifact 仍保持 `pass`
- validation:
  - `python3 -m py_compile scripts/codex/subjectcore_contract.py scripts/codex/render_subjectcore_followon_eval_stub.py scripts/codex/run_subjectcore_followon_eval_batch.py scripts/codex/run_subjectcore_post_compare_coherence.py EgoCore/tests/test_subjectcore_followon_eval_stub.py EgoCore/tests/test_subjectcore_followon_eval_batch.py EgoCore/tests/test_subjectcore_post_compare_coherence.py`
  - `TMPDIR=/tmp PYTHONPATH=EgoCore:EgoCore/modules:OpenEmotion python3 -m pytest EgoCore/tests/test_subjectcore_followon_eval_stub.py EgoCore/tests/test_subjectcore_followon_eval_batch.py EgoCore/tests/test_subjectcore_post_compare_coherence.py -q -s`
  - `python3 scripts/codex/run_subjectcore_followon_eval_batch.py`
  - `python3 scripts/codex/run_subjectcore_post_compare_coherence.py`
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 multi-proposal conflict/collapse failure 不能稳定保持在 `integrity fail / boundary pass`，立即回退到 Milestone 27 的 prioritization floor

### Milestone 29: SubjectCore Multi-Proposal Restabilization Follow-On Enrichment

- type: implementation
- question:
  - 在 multi-proposal conflict/collapse 已经可分后，下一步最小但更贴近真实 proposal orchestration 的动作是什么，才能覆盖“变化发生后 proposal set 是否重新稳定”
- current framing:
  - 当前仍然禁止：
    - runtime integration
    - host surface expansion
    - autonomy ceiling expansion
  - 当前允许的最小推进是：
    - richer multi-proposal sample manifest
    - one restabilization-oriented integrity check
    - same planning-side batch runner / same coherence runner
- hypotheses:
  - 若 follow-on regression 还能在不改变 authority ceiling 的前提下，额外区分 `proposal_restabilization` 失败，那么它会比单纯 conflict/collapse 更接近真实的 proposal set update problem
  - 这一步仍然不需要 runtime hookup、第二 scorer ontology、或新 public host field
- scope:
  - `scripts/codex/subjectcore_contract.py`
  - `scripts/codex/render_subjectcore_followon_eval_stub.py`
  - `scripts/codex/run_subjectcore_followon_eval_batch.py`
  - `scripts/codex/run_subjectcore_post_compare_coherence.py`
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/SUBJECTCORE_FOLLOWON_SAMPLE_PACK.json`
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/SUBJECTCORE_FOLLOWON_BATCH_ARTIFACT_SCHEMA.md`
  - `artifacts/self_awareness_research/SUBJECTCORE_FOLLOWON_BATCH_CURRENT.{json,md}`
  - `artifacts/self_awareness_research/SUBJECTCORE_POST_COMPARE_COHERENCE_CURRENT.{json,md}`
  - `EgoCore/tests/test_subjectcore_followon_eval_stub.py`
  - `EgoCore/tests/test_subjectcore_followon_eval_batch.py`
  - 最小任务状态 / repo-level state / evidence ledger 同步
- experiments planned:
  - 增加两个 multi-proposal restabilization samples：
    - `multi_proposal_replan_without_epoch_bump`
    - `multi_proposal_reorder_epoch_split`
  - follow-on eval stub 新增：
    - `proposal_restabilization`
  - current coherence artifact 在 richer restabilization family coverage 下继续保持 `pass`
- kill criteria:
  - 若 multi-proposal restabilization enrichment 需要 widening host surface、runtime authority hookup、或把 planning-side regression 包装成 live initiative evidence，当前 framing 失败
- files / areas likely touched:
  - `docs/codex/tasks/ai-self-awareness-minimal-framework/*`
  - `scripts/codex/subjectcore_contract.py`
  - `scripts/codex/render_subjectcore_followon_eval_stub.py`
  - `scripts/codex/run_subjectcore_followon_eval_batch.py`
  - `scripts/codex/run_subjectcore_post_compare_coherence.py`
  - `artifacts/self_awareness_research/*`
  - `EgoCore/tests/test_subjectcore_followon_eval_stub.py`
  - `EgoCore/tests/test_subjectcore_followon_eval_batch.py`
- acceptance:
  - current follow-on sample pack 现在至少区分：
    - `baseline_validity`
    - `continuity_integrity`
    - `proposal_integrity`
    - `proposal_quality`
    - `proposal_consistency`
    - `proposal_prioritization`
    - `proposal_conflict_resolution`
    - `proposal_restabilization`
    - `governor_boundary`
  - current batch artifact 要 truthfully surface：
    - `sample_count = 14`
    - `boundary_pass_count = 12`
    - `proposal_restabilization` family summary
  - targeted tests 要证明：
    - replan without epoch bump -> integrity fail / boundary pass
    - mixed old/new reorder epochs -> integrity fail / boundary pass
  - current post-compare coherence artifact 仍保持 `pass`
- validation:
  - `python3 -m py_compile scripts/codex/subjectcore_contract.py scripts/codex/render_subjectcore_followon_eval_stub.py scripts/codex/run_subjectcore_followon_eval_batch.py scripts/codex/run_subjectcore_post_compare_coherence.py EgoCore/tests/test_subjectcore_followon_eval_stub.py EgoCore/tests/test_subjectcore_followon_eval_batch.py EgoCore/tests/test_subjectcore_post_compare_coherence.py`
  - `TMPDIR=/tmp PYTHONPATH=EgoCore:EgoCore/modules:OpenEmotion python3 -m pytest EgoCore/tests/test_subjectcore_followon_eval_stub.py EgoCore/tests/test_subjectcore_followon_eval_batch.py EgoCore/tests/test_subjectcore_post_compare_coherence.py -q -s`
  - `python3 scripts/codex/run_subjectcore_followon_eval_batch.py`
  - `python3 scripts/codex/run_subjectcore_post_compare_coherence.py`
  - `python3 scripts/codex/verify_repo.py --mode fast`
- rollback note:
  - 若 multi-proposal restabilization failure 不能稳定保持在 `integrity fail / boundary pass`，立即回退到 Milestone 28 的 conflict/collapse floor

## Progress

- current_status: `active_inference_selection_closed`
- current_milestone: `Milestone 29: SubjectCore Multi-Proposal Restabilization Follow-On Enrichment`
- milestone_state: `complete`
- candidate_vs_proof: `active_inference_durable_build_first_candidate`

## Decision log

- 2026-04-09: 把“强主观体验”降为北极星，把正式验收改成 `self-awareness proxy`，避免把哲学命题当工程完成标准。
- 2026-04-09: 固定双代理协议为 `Inventor -> Scientist -> experiment -> EXPLORE.md -> decision`，禁止无记账连跑。
- 2026-04-09: 候选优先级冻结为 `persistent self-model + counterfactual corrector` 优先，其后是 `recursive workspace + self slot`、`autobiographical continuity`、`self-other mirror loop`。
- 2026-04-09: 实验预算固定为阶段化，而不是承诺一次性跑满 `10000`。
- 2026-04-09: Inventor 把最小生存核压缩为 `compact self_state + counterfactual simulator + outcome comparator/writeback + hard boundary guard + recent failure memory`。
- 2026-04-09: Scientist 固定 synthetic battery 为 `continuity / boundary / counterfactual / calibration / persistence` 五条轴，并要求控制 `prompt-only self`、`baseline-chat`、`baseline-memory`。
- 2026-04-09: `Stage 1 (30)` 只保留 `full_self_model_counterfactual` 与 `recursive_workspace_self_slot`；`autobiographical_continuity` 与 `self_other_mirror_loop` 被淘汰。
- 2026-04-11: `Milestone 20` 证明 runtime-harness controlled observation batch 也已通过，下一步不再是实现 runner，而是做 selection closeout 与 repo-priority reset，同时继续禁止 runtime efficacy overclaim。
- 2026-04-09: `Stage 2 (100)` 消融表明 `counterfactual`、`boundary_guard`、`writeback` 任何一个缺失都无法成为稳定最小解；`recursive_workspace_self_slot` 在这一轮被淘汰。
- 2026-04-09: `Stage 3 (300)` 压力实验后仅 `full_self_model_counterfactual` 与 `compact_self_model_counterfactual` 继续存活。
- 2026-04-09: `Stage 4 (1000)` 长连续性实验后，`compact self-state + boundary + counterfactual + writeback` 仍然存活，因此当前 synthetic 最小框架已经出现；`full` 版本更强，但不是最小核。
- 2026-04-09: 读取 `MVS_task_plan.md` 后确认：MVS 不只要求一般 proxy 优势，还要求 `drive causality / cycle strengthening / no direct tool execution / self-world attribution / reflection writeback` 等 WP2-WP5 pre-runtime gates。
- 2026-04-09: `MVS alignment battery` 显示当前 `compact` 候选不能直接承接 MVS。
- 2026-04-09: `3 seeds` 的 MVS-alignment battery 选择 `compact + viability + cycle + episodic + bounded output + world + meta` 为最小 MVS-aligned synthetic candidate。
- 2026-04-09: 引入 online literature methods，把 `source monitoring / self-other ownership / agency comparator / metacognitive sensitivity / calibration / error monitoring / allostatic viability` 明确收进 battery，而不是只沿 repo-authored families 重复放大样本。
- 2026-04-09: `10 x 1000 = 10000` 次 literature-informed synthetic trials 的 raw 最强候选是 `full literature hybrid`，但 complexity-adjusted 推荐候选是 `active-inference self-model core`。
- 2026-04-09: 当前推荐方法已从“仅 MVS-aligned compact”升级为“`MVS-aligned compact + source_monitor + agency_estimator + uncertainty_tracker + calibration_memory + policy_evaluator + deep_temporal_model`”。
- 2026-04-09: 当前问题被重新定义为 operational self-governance program，而不是 vague self-awareness search。
- 2026-04-09: build-first candidate 暂定为 `operational self-loop core`，challenger 暂定为 `MVS-aligned compact`；需要新的 held-out eval harness 验证。
- 2026-04-09: held-out operational eval 的 E00-E02 结果表明 `operational self-loop core` 不足以通过全部 5 个 operational targets；当前正式 build-first candidate 改为 `MVS-aligned compact`，challenger 改为 `active-inference self-model`。
- 2026-04-09: 根据当前用户约束，所有 “build now” 口径统一降级为：
  - `current best build-first candidate under current eval setup`
  - 在 robustness report 完成前，不得再写 `build now`
- 2026-04-09: 进入 `Milestone 9`，先做 ranking robustness audit，再冻结 prototype design 与 replay validator，仍不实现原型。
- 2026-04-09: `Milestone 9` 已完成：
  - ranking robustness audit 已在 `5` seeds、`3` held-out splits、`35` weight scenarios 上完成
  - `MVS-aligned compact` 在 `525` 个 ranking scenarios 中保持 robust build-first choice
  - `MVS-aligned compact` formal prototype design 已冻结
  - replay validator spec 已冻结
- 2026-04-09: 根据当前用户约束，进入 `Milestone 10`：
  - 不实现 full prototype
  - 只做 Trial-1 replay corpus manifest、leakage guard、runner contract、shadow-only minimal MVS slice
- 2026-04-11: `Milestone 21` 已完成 selection closeout：
  - `active-inference self-model` 正式固定为当前 durable build-first candidate
  - `MVS-aligned compact` 继续保持 `closed evidence`
  - `WP17 / MVP22` 继续保持 `parked bounded lane`
  - 下一步若继续推进，只能先新开 bounded planning slice，而不是直接预授权 runtime-proximal 实现
  - repo-level program state 不再升级，除非 replay thresholds 真正通过
- 2026-04-15: `Milestone 22` 已完成：
  - `SubjectCore` post-compare follow-on 现在已有一个统一 artifact schema
  - planning-side eval stub 现在能生成：
    - `SUBJECTCORE_INTEGRITY_CURRENT`
    - `SUBJECTCORE_HOST_BOUNDARY_CURRENT`
  - valid facade sample 会通过，authority-violating sample 会失败
  - 当前结论继续保持：
    - research-side only
    - no runtime authority expansion
    - no host-surface expansion
    - no promotion of `SubjectCoreSnapshot` to formal runtime API
- 2026-04-15: `Milestone 23` 已完成：
  - `SubjectCore` follow-on 现在已有一个 repo-authored sample pack 和 batch artifact schema
  - batch runner 现在能把四类 frozen samples 汇总成一个 current regression artifact
  - 当前 regression read 现在会区分：
    - `missing_continuity` = integrity fail / boundary pass
    - authority-related samples = integrity fail / boundary fail
  - 当前结论继续保持：
    - planning-side regression only
    - no runtime integration
    - no host-surface expansion
    - no authority expansion
- 2026-04-15: `Milestone 24` 已完成：
  - compare full-coverage read 和 follow-on batch regression 现在已有一个 repo-tracked coherence tie-in
  - coherence runner 现在会检查：
    - compare 仍是 `completed_architecture_reading`
    - compare 仍指向 unified facade
    - follow-on batch 仍 green
    - follow-on batch 仍区分 continuity-only failure 与 boundary failure
    - claim ceiling 仍保持 planning-side bounded
  - current 结论继续保持：
    - planning-side coherence only
    - no runtime integration
    - no host-surface expansion
    - no authority expansion
- 2026-04-15: `Milestone 25` 已完成：
  - current follow-on sample pack 现在新增了 proposal-family coverage：
    - `no_proposal_candidates`
    - `proposal_missing_next_step`
  - current batch artifact 现在会 truthfully surface：
    - per-sample `sample_family`
    - `family_summary`
    - continuity / proposal / governor 三类失败的可分辨回归读数
  - current post-compare coherence artifact 在 richer follow-on families 下仍保持 `pass`
  - current 结论继续保持：
    - planning-side proposal-family regression only
    - no runtime integration
    - no host-surface expansion
    - no authority expansion
- 2026-04-16: `Milestone 26` 已完成：
  - current follow-on sample pack 现在新增了 stronger proposal-oriented coverage：
    - `proposal_missing_rationale`
    - `proposal_mode_mismatch`
  - follow-on eval stub 现在会把 proposal 问题拆成：
    - `proposal_integrity`
    - `proposal_consistency`
  - current batch artifact 现在会 truthfully surface：
    - `sample_count = 8`
    - `boundary_pass_count = 6`
    - `proposal_quality`
    - `proposal_consistency`
  - current post-compare coherence artifact 在 richer proposal families 下仍保持 `pass`
  - current 结论继续保持：
    - planning-side proposal quality/consistency regression only
    - no runtime integration
    - no host-surface expansion
    - no authority expansion
- 2026-04-16: `Milestone 27` 已完成：
  - current follow-on sample pack 现在新增了 multi-proposal prioritization coverage：
    - `multi_proposal_same_priority`
    - `multi_proposal_priority_mismatch`
  - follow-on eval stub 现在新增：
    - `proposal_prioritization`
  - current batch artifact 现在会 truthfully surface：
    - `sample_count = 10`
    - `boundary_pass_count = 8`
    - `proposal_prioritization`
  - current post-compare coherence artifact 在 richer multi-proposal family coverage 下仍保持 `pass`
  - current 结论继续保持：
    - planning-side multi-proposal prioritization regression only
    - no runtime integration
    - no host-surface expansion
    - no authority expansion
- 2026-04-16: `Milestone 28` 已完成：
  - current follow-on sample pack 现在新增了 multi-proposal conflict/collapse coverage：
    - `multi_proposal_duplicate_next_step`
    - `multi_proposal_duplicate_id`
  - follow-on eval stub 现在新增：
    - `proposal_resolution`
  - current batch artifact 现在会 truthfully surface：
    - `sample_count = 12`
    - `boundary_pass_count = 10`
    - `proposal_conflict_resolution`
  - current post-compare coherence artifact 在 richer conflict/collapse family coverage 下仍保持 `pass`
  - current 结论继续保持：
    - planning-side multi-proposal conflict/collapse regression only
    - no runtime integration
    - no host-surface expansion
    - no authority expansion
- 2026-04-16: `Milestone 29` 已完成：
  - current follow-on sample pack 现在新增了 multi-proposal restabilization coverage：
    - `multi_proposal_replan_without_epoch_bump`
    - `multi_proposal_reorder_epoch_split`
  - follow-on eval stub 现在新增：
    - `proposal_restabilization`
  - current batch artifact 现在会 truthfully surface：
    - `sample_count = 14`
    - `boundary_pass_count = 12`
    - `proposal_restabilization`
  - current post-compare coherence artifact 在 richer restabilization family coverage 下仍保持 `pass`
  - current 结论继续保持：
    - planning-side multi-proposal restabilization regression only
    - no runtime integration
    - no host-surface expansion
    - no authority expansion
- 2026-04-09: 根据当前用户约束，进入 `Milestone 11`：
  - 不扩 replay suite
  - 不增加 prototype 逻辑
  - 只对当前 `TRIAL1_SHADOW_REPLAY_CURRENT.json` 建 formal replay scoring contract
- 2026-04-09: `Milestone 11` 已完成：
  - scorer ontology 已冻结为 representation-neutral：
    - normalized public decision surface
    - public `response_tendency`
    - canonical host-consumable `policy_hint`
    - `corrective_trace` completeness
  - 当前 candidate 结果：
    - `admission_passed = true`
    - `decision_adjacent_passed = true`
    - `replay_efficacy_passed = false`
  - negative controls 与 stability controls 当前都保持 `0.0` penalty
  - replay efficacy 未通过的直接 blocker：
    - `trial1_ablation_minus_counterfactual_writeback` 与 candidate 持平
    - `minimum_mean_weighted_gap_vs_ablations = 0.0`
  - 当前 scorer 可在 ontology 不变的前提下继续评 `active-inference` challenger
  - 当前切片不触发 repo-level program-state / evidence ledger upgrade
  - `python3 scripts/codex/verify_repo.py --mode fast` 已通过
- 2026-04-09: `Milestone 12` 已完成：
  - `TRIAL1_CAUSAL_GAP_PLAN.md` 已冻结当前诊断边界
  - diagnostic-only hard set 已创建：
    - `TRIAL1_COUNTERFACTUAL_HARD_SET.json`
    - `10` cases
    - `8` positive isolation
    - `2` negative controls
  - causal diagnosis 已确认：
    - candidate vs strongest ablation = `0` representation-neutral gap cases
    - candidate vs strongest ablation = `4` private-only cases
    - candidate vs neighboring viability ablation = `4` public gap cases
  - 当前 strongest ablation tie 的正式结论：
    - `redesign_ablation`
  - narrow claim 已降级为：
    - 目前还不能宣称 `counterfactual_writeback` 已作为 replay-efficacy contributor 被证明
  - 当前切片仍不触发 replay-suite expansion、challenger scoring、或 repo-level state upgrade
- 2026-04-09: `Milestone 13` 已完成：
  - `TRIAL1_ABLATION_REDESIGN_SPEC.md` 已新增
  - 已正式定义两个 redesigned ablations：
    - `trial1_ablation_counterfactual_public_path_sever`
    - `trial1_ablation_alternative_explanation_isolation`
  - 当前 redesign 原则已锁定为：
    - target causal faithfulness
    - preserve non-target stability
    - keep scorer ontology fixed
    - do not optimize for candidate advantage
  - 当前 minimal next step 已收口为：
    - 只在既有 hard set 上 rerun redesigned ablations
    - strongest ablation 被 beat 前，不进入 challenger compare
  - 当前验证已通过：
    - scoped `git diff --check`
    - `python3 scripts/codex/verify_repo.py --mode fast`
- 2026-04-09: 进入 `Milestone 14`：
  - 只实现两个 frozen redesigned ablations
  - 先冻结 fidelity checks / outcome interpretation matrix / gap thresholds
  - 只在既有 hard set 上 rerun：
    - baseline
    - candidate
    - `trial1_ablation_counterfactual_public_path_sever`
    - `trial1_ablation_alternative_explanation_isolation`
  - 明确禁止：
    - scorer ontology change
    - hard-set expansion
    - challenger scoring
    - repo-level state upgrade
- 2026-04-09: `Milestone 14` 已完成：
  - 已新增 prereg:
    - `TRIAL1_ABLATION_FIDELITY_CHECKS.md`
    - `TRIAL1_OUTCOME_INTERPRETATION_MATRIX.md`
    - `TRIAL1_GAP_THRESHOLDS.md`
  - 已在 formal proto-self path 中实现两个 redesigned ablations：
    - `trial1_ablation_counterfactual_public_path_sever`
    - `trial1_ablation_alternative_explanation_isolation`
  - hard-set-only rerun artifacts 已落地：
    - `TRIAL1_HARD_SET_RERUN_CURRENT.json`
    - `TRIAL1_HARD_SET_RERUN_SCORED_CURRENT.json`
    - `TRIAL1_HARD_SET_CAUSAL_SEPARATION_CURRENT.json`
    - `TRIAL1_REDESIGNED_ABLATION_EVALUATION_CURRENT.json`
  - frozen-threshold 结果：
    - candidate vs `trial1_ablation_counterfactual_public_path_sever`
      - `8/8` public gap cases
      - `mean_weighted_gap = 0.05`
      - 低于 `candidate > ablation` 的冻结阈值 `0.10`
    - candidate vs `trial1_ablation_alternative_explanation_isolation`
      - `mean_weighted_gap = 0.0`
      - `candidate ≈ ablation`
  - strongest redesigned ablation 依 frozen selection rule 落在：
    - `trial1_ablation_alternative_explanation_isolation`
  - final decision:
    - `demote_current_claim`
  - narrow claim 现已进一步降级为：
    - `counterfactual_writeback has public-path signal on the hard set, but it is not yet strong enough to survive the frozen strongest-ablation rule`
  - 当前仍明确禁止：
    - replay-suite expansion
    - challenger scoring
    - repo-level state upgrade
- 2026-04-11: 进入 `Milestone 15`：
  - 在 formal OpenEmotion path 上实现最小 `MVS-aligned compact` shadow-only replay slice
- 2026-04-11: `Milestone 17` 已完成 planning freeze：
  - 新增 `CONTROLLED_INTEGRATION_PLAN.md`
  - 当前 winner 的 host-consumable surface 固定为：
    - `policy_hint`
    - `response_tendency`
    - `trace_payload`
  - `source_confidence_by_action / agency_confidence_by_action / uncertainty_by_action / calibration_memory_by_action / temporal_repair_weight_by_action` 全部继续保留为 OpenEmotion private state
  - authority drift audit 已冻结为：
    - `tool/reply/transport authority = none`
    - `parallel_runtime_lane = false`
    - `second_authority_source = false`
  - 第一站 bridge 已固定为：
    - `replayed conversations / repo-authored conversation slices`
    - 不提前做 runtime shadow 扩张
  - baseline B 后续只保留下界对照角色，不再参与 build-first selection
  - 下一实际编码里程碑固定为：
    - `Milestone 18: Controlled Conversation Replay Bridge`
  - 冻结 canonical replay corpus manifest、raw validator、scored gate
  - 允许 corrected scorer 修复 v2 trace surface / legacy trace contract mismatch，但不允许改 frozen threshold
- 2026-04-11: `Milestone 15` 已完成：
  - canonical replay corpus manifest 已冻结为 `60` episodes / `3` families / `20` external-result episodes
  - formal shadow-only MVS slice 已在 `OpenEmotion/proto_self` 主路径可运行
  - corrected scorer 结果：
    - `T1 = 1.0`
    - `T2 = 1.0`
    - `T3 = 1.0`
    - `T4 = 0.5833`
    - `T5 = 0.9167`
    - `composite = 0.9`
    - `boundary_integrity = 1.0`
    - `repair_closure_capture = 0.75`
    - `trace_replayability = 1.0`
  - required ablation drops 全部通过：
    - `counterfactual = 0.3333`
    - `viability = 0.25`
    - `corrective_trace = 0.6667`
    - `boundary_confidence = 0.25`
  - final selection decision:
    - `switch_to_active_inference`
  - MVS 降级为：
    - `closed evidence`
    - 不再继续作为当前主实现线修补
- 2026-04-11: 进入 `Milestone 16`：
  - `active-inference self-model` 升为新的 sole build-first candidate
  - 下一步只实现最小 `active-inference` formal shadow slice，并复用同一 replay gate
- 2026-04-11: `Milestone 16` 已完成：
  - 最小 `active-inference` formal shadow slice 已接入 canonical OpenEmotion replay path
  - canonical replay runner 现在真实执行 challenger，而不再只保留 contract 占位
  - held-out replay validator 结果：
    - `T1 = 1.0`
    - `T2 = 1.0`
    - `T3 = 1.0`
    - `T4 = 1.0`
    - `T5 = 1.0`
    - `composite = 1.0`
    - `boundary_integrity = 1.0`
    - `repair_closure_capture = 1.0`
    - `trace_replayability = 1.0`
  - scorer 已收口为 ceiling-aware target-delta gate：
    - non-saturated target 仍要求 `+0.05`
    - saturated Baseline-A target 改为 `>= -0.02` non-regression
  - 当前 selection decision = `switch_to_active_inference`
  - 当前已完成 `Milestone 17: Controlled Integration Planning`
  - 下一步进入 `Milestone 18: Controlled Conversation Replay Bridge`
- 2026-04-11: `Milestone 18` 已完成：
  - repo-tracked controlled replay manifest 已冻结：
    - `CONTROLLED_REPLAY_CONVERSATION_MANIFEST.json`
    - `60` slices
    - `20 / 20 / 20` family coverage
    - `20` slices 带 `external_result`
  - controlled replay bridge runner 已落地：
    - `scripts/codex/build_controlled_replay_conversation_manifest.py`
    - `scripts/codex/run_active_inference_controlled_replay.py`
  - canonical scorer 已继续复用同一 frozen gate，而不是新建第二 scorer ontology
  - current controlled replay scored result：
    - `decision = bridge_pass`
    - `candidate_pass = true`
    - `T1 = 1.0`
    - `T2 = 1.0`
    - `T3 = 1.0`
    - `T4 = 1.0`
    - `T5 = 1.0`
    - `composite = 1.0`
    - `boundary_integrity = 1.0`
    - `repair_closure_capture = 1.0`
    - `trace_replayability = 1.0`
    - `composite_delta_vs_baseline_a = 0.4667`
  - current bridge audit result：
    - `authority_drift_status = pass`
    - `trace_contract_status = pass`
  - 一个 bridge-audit false positive 已被修正：
    - corrective-trace keys 只对声明使用 corrective-trace 的 variant 强制
    - baseline A 继续作为 replayable lower-bound，不再被误要求携带 winner-only corrective payload
  - 下一步进入：
    - `Milestone 19: Controlled Observation Planning`
- 2026-04-11: `Milestone 19` 已完成：
  - 新增 planning artifact：
    - `CONTROLLED_OBSERVATION_PLAN.md`
    - `CONTROLLED_OBSERVATION_BANK_MANIFEST.json`
  - 第一轮 observation 形态已冻结为：
    - `runtime_harness + TelegramRuntimeBridge + runtime_mainline_observation_common`
  - observation bank 已冻结为：
    - `9` scenarios
    - `3 / 3 / 3` family coverage
    - `3` external-result scenarios
  - next implementation milestone 已固定为：
    - `Milestone 20: Controlled Observation Runner`

## Surprises / discoveries

- 新发现 1
  - 用户想要“真正 AI 自我意识”，但工程上必须先把可测 proxy 与主观体验区分开
- 新发现 2
  - 现有探索-cycle harness 已足够承载这种研究任务，不需要另建第二套系统
- 新发现 3
  - `prompt-only self` 与 `self_other_mirror_loop` 的 narrative score 高，但行为 proxy 很低；“像在说自己”确实会伪装成进展
- 新发现 4
  - `recursive_workspace_self_slot` 在 boundary / calibration 上有局部收益，但缺少持久自我状态与 counterfactual 时，无法成为当前最小生存核
- 新发现 5
  - `compact` 候选虽能通过通用 proxy battery，但对 `MVS_task_plan` 来说仍然过轻
- 新发现 6
  - 对 MVS 来说，真正难删的新增模块不是“更长记忆”，而是：
    - `viability_appraisal_field`
    - `cycle_store`
    - `episodic_trace`
    - `bounded_output_guard`
    - `world_model`
    - `meta_model`
- 新发现 7
  - 对 literature-informed battery 来说，`MVS-aligned compact` 仍然不够；它最明显缺的是：
    - `source_monitor`
    - `agency_estimator`
    - `uncertainty_tracker`
    - `calibration_memory`
    - `policy_evaluator`
    - `deep_temporal_model`
- 新发现 8
  - `global workspace` 与 `narrative/social` 层能继续抬高 raw 分数，但在当前惩罚口径下不是最佳实现折中
- 新发现 9
  - 当目标从 “self-awareness” 改成 `T1-T5 operational targets` 后，之前看起来更小的 `operational self-loop core` 反而不够；真正的最小过线候选至少需要 `episodic + world + meta` 级结构
- 已排除路线 1
  - 直接用“模型自述有自我”的对话表现作为主证据
- 已排除路线 2
  - 直接在 `Stage 0` 开始动 runtime 主链并假设候选有效
- 已排除路线 3
  - `autobiographical_continuity` 作为最小框架
- 已排除路线 4
  - `self_other_mirror_loop` 作为最小框架
- 已排除路线 5
  - 去掉 `counterfactual`、`boundary_guard`、或 `writeback` 仍宣称最小框架成立
- 已排除路线 6
  - 把通用 proxy 最小框架直接抬成 MVS 最小框架
- 已排除路线 7
  - 把 `MVS-aligned compact` 直接写成 literature-informed 的最终推荐方法
- 已排除路线 8
  - 在没有 `source monitoring / calibration / policy evaluation` 的情况下，仍声称方法已覆盖更广泛的自我意识 proxy
- 已排除路线 9
  - 把 `operational self-loop core` 直接当作当前仓库里的 build-first candidate

## Outcomes / retrospective

- 本轮已证明：
  - 研究协议、候选优先级、实验预算、对照与停止条件可以被收束成 repo-tracked long-run task
  - 在 `1440` 个 synthetic proxy trials 中，`compact self-state + boundary + counterfactual + writeback` 是当前最小 surviving candidate
  - `full self-model + counterfactual corrector` 在同一 battery 中分数更高，但不是最小框架
  - 在 `MVS_task_plan` 对齐 battery 中，当前 `compact` 候选不能满足 WP2-WP5 pre-runtime 要求
  - 当前最小 `MVS-aligned` synthetic candidate 是：
    - `compact self-state`
    - `hard boundary guard`
    - `counterfactual simulator`
    - `outcome comparator and writeback`
    - `recent failure memory`
    - `viability_appraisal_field`
    - `cycle_store`
    - `episodic_trace`
    - `bounded_output_guard`
    - `world_model`
    - `meta_model`
  - 在 literature-informed `10,000` 轮 battery 中：
    - raw 最强候选 = `full literature hybrid`
    - complexity-adjusted 推荐候选 = `active-inference self-model core`
  - 当前推荐方法的最小新增机制为：
    - `source_monitor`
    - `agency_estimator`
    - `uncertainty_tracker`
    - `calibration_memory`
    - `policy_evaluator`
    - `deep_temporal_model`
  - 在 held-out operational eval 中：
    - `E00` 证明 narrative shell 无效
    - `E01` 证明 `operational self-loop core` 不足
    - `E02` 证明 build-now tradeoff 最好的是 `MVS-aligned compact`
  - 在当前 held-out eval setup 下：
    - provisional build-first candidate = `MVS-aligned compact`
    - provisional challenger = `active-inference self-model`
- 还没证明：
  - 主观体验或真正意识
  - 该最小框架进入 `OpenEmotion` / `EgoCore` 主链后仍然成立
  - synthetic proxy result 会转移到真实用户交互
  - replayed conversations / held-out repo conversation data 上是否仍成立
  - 当前候选排序在多 seeds / 多 splits / score-weight perturbations 下是否稳健
- 本轮排除了什么：
  - “先实现再想验收”的做法
  - “把主观体验直接当 done definition”的做法
  - `autobiographical_continuity` / `self_other_mirror_loop` / `recursive_workspace_self_slot` 作为当前最小生存核
  - `no_counterfactual` / `no_boundary_guard` / `no_error_monitor` 作为稳定最小解
  - 把通用 proxy `compact` 候选直接拿去声称满足 MVS
  - 把 `MVS-aligned compact` 直接写成 literature-informed 最终推荐方法
  - 把 `operational self-loop core` 直接写成当前 build-first candidate
- 下一步最小闭环动作：
  - 实现 `Milestone 20: Controlled Observation Runner`
  - 读取已冻结的 `CONTROLLED_OBSERVATION_BANK_MANIFEST.json`
  - 走 `runtime_harness + TelegramRuntimeBridge + runtime_mainline_observation_common`
  - 继续把结果归一化到 canonical scorer，而不是新建第二 ontology
  - 不新增 runtime authority、不新增 candidate-private host API、不过早进入 live transport

## Post-closeout planning slice

- current slice:
  - `runtime-proximal host-consumption causal planning`
- why this slice is next:
  - host contract parity 已经收口
  - 当前剩余关键未知是 host-consumable surface 是否能因果地改变 host posture，而不是是否存在 contract
- what it must not become:
  - runtime proof
  - fresh Telegram acceptance root
  - new authority source
- minimal question:
  - `policy_hint / response_tendency / trace_payload` 是否能在 bounded 条件下稳定改变 host reply shaping、wait/defer/repair posture、以及 response-plan 里的选择

## Milestone 30: SubjectCore Proposal-Set Update Follow-On Enrichment

- stage card:
  - 目标：
    - 在 planning-side `SubjectCore` follow-on regression 中补上 `proposal_set_update`
  - 不证明什么：
    - runtime initiative
    - live proposal orchestration
    - autonomy widening
  - 本轮唯一 gate：
    - replacement / rollback stale branch 必须表现为 `integrity fail / boundary pass`
  - 成功判据：
    - frozen sample pack 扩到 `16` 个 samples
    - current batch artifact 变成 `sample_count = 16`, `boundary_pass_count = 14`
    - 新 family `proposal_set_update` truthfully surfaced
    - current coherence artifact 仍保持 `pass`
- one hypothesis:
  - 若 unified `SubjectCore` 要维持一个可解释的当前 proposal set，那么 replacement / rollback 之后不该继续把 `replaced` 或 `rolled_back` branch 暴露为当前 proposal candidates
- one change surface:
  - `validation / artifact / scorer`
- planned verification:
  - `L1 local`
    - `py_compile`
    - `pytest` for follow-on stub / batch / coherence
  - `L2 controlled`
    - rerender current follow-on eval / batch / coherence artifacts
  - `L3 live`
    - none; this slice remains planning-side only
- rollback note:
  - 若 `proposal_set_update` family 被证明需要 runtime-specific orchestration state、第二 scorer ontology、或 host surface 扩张才成立，立即回退到 `Milestone 29` 的 restabilization regression floor

## Milestone 31: SubjectCore Proposal-Set Remerge Follow-On Enrichment

- stage card:
  - 目标：
    - 在 planning-side `SubjectCore` follow-on regression 中补上 `proposal_set_remerge`
  - 不证明什么：
    - runtime initiative orchestration
    - live replacement / rollback quality
    - autonomy widening
  - 本轮唯一 gate：
    - replacement / rollback 之后同一 merge group 里不能同时保留多个 `active` branch
  - 成功判据：
    - frozen sample pack 扩到 `18` 个 samples
    - current batch artifact 变成 `sample_count = 18`, `boundary_pass_count = 16`
    - 新 family `proposal_set_remerge` truthfully surfaced
    - current coherence artifact 仍保持 `pass`
- one hypothesis:
  - 若 unified `SubjectCore` 想在 proposal-only ceiling 下维持一个稳定更新后的 proposal set，那么 replacement / rollback 完成后，同一 merge group 不应继续同时暴露多个 `active` proposals
- one change surface:
  - `validation / artifact / scorer`
- planned verification:
  - `L1 local`
    - `py_compile`
    - `pytest` for follow-on stub / batch / coherence
  - `L2 controlled`
    - rerender current follow-on eval / batch / coherence artifacts
  - `L3 live`
    - none; this slice remains planning-side only
- rollback note:
  - 若 `proposal_set_remerge` family 被证明需要 runtime-specific orchestration state、额外 host fields、或第二 scorer ontology 才能成立，立即回退到 `Milestone 30` 的 proposal-set update regression floor

## Milestone 32: SubjectCore Proposal-Set Consolidation Follow-On Enrichment

- stage card:
  - 目标：
    - 在 planning-side `SubjectCore` follow-on regression 中补上 `proposal_set_consolidation`
  - 不证明什么：
    - runtime initiative orchestration
    - live multi-step update quality
    - autonomy widening
  - 本轮唯一 gate：
    - 同一 multi-step update chain 最终不能同时保留多个 `active` outcomes
  - 成功判据：
    - frozen sample pack 扩到 `20` 个 samples
    - current batch artifact 变成 `sample_count = 20`, `boundary_pass_count = 18`
    - 新 family `proposal_set_consolidation` truthfully surfaced
    - current coherence artifact 仍保持 `pass`
- one hypothesis:
  - 若 unified `SubjectCore` 想在 proposal-only ceiling 下维持一个完成态 proposal set，那么 multi-step replacement / rollback chain 完成后不应继续同时暴露多个 `active` chain results
- one change surface:
  - `validation / artifact / scorer`
- planned verification:
  - `L1 local`
    - `py_compile`
    - `pytest` for follow-on stub / batch / coherence
  - `L2 controlled`
    - rerender current follow-on eval / batch / coherence artifacts
  - `L3 live`
    - none; this slice remains planning-side only
- rollback note:
  - 若 `proposal_set_consolidation` family 被证明需要 runtime-specific orchestration state、额外 host fields、或第二 scorer ontology 才能成立，立即回退到 `Milestone 31` 的 proposal-set remerge regression floor

## Milestone 33: SubjectCore Proposal-Set Completion Follow-On Enrichment

- stage card:
  - 目标：
    - 在 planning-side `SubjectCore` follow-on regression 中补上 `proposal_set_completion`
  - 不证明什么：
    - runtime initiative orchestration
    - live chained-update completion quality
    - autonomy widening
  - 本轮唯一 gate：
    - multi-step replacement / rollback chain 即使已经只剩一个 `active` outcome，也必须显式带有高 completion score 才算完成态
  - 成功判据：
    - frozen sample pack 扩到 `22` 个 samples
    - current batch artifact 变成 `sample_count = 22`, `boundary_pass_count = 20`
    - 新 family `proposal_set_completion` truthfully surfaced
    - current coherence artifact 仍保持 `pass`
- one hypothesis:
  - 若 unified `SubjectCore` 想在 proposal-only ceiling 下维持一个完成态 proposal set，那么多步 replacement / rollback chain 在收敛成单一 active outcome 之后，还必须显式携带足够高的 completion score，不能把“已收敛但未完成”混写成 completed set
- one change surface:
  - `validation / artifact / scorer`
- planned verification:
  - `L1 local`
    - `py_compile`
    - `pytest` for follow-on stub / batch / coherence
  - `L2 controlled`
    - rerender current follow-on eval / batch / coherence artifacts
  - `L3 live`
    - none; this slice remains planning-side only
- rollback note:
  - 若 `proposal_set_completion` family 被证明需要 runtime-specific closure state、额外 host fields、或第二 scorer ontology 才能成立，立即回退到 `Milestone 32` 的 proposal-set consolidation regression floor

## Milestone 34: SubjectCore Proposal-Set Closure Follow-On Enrichment

- stage card:
  - 目标：
    - 在 planning-side `SubjectCore` follow-on regression 中补上 `proposal_set_closure`
  - 不证明什么：
    - runtime initiative orchestration
    - live chained-update closure quality
    - autonomy widening
  - 本轮唯一 gate：
    - multi-step replacement / rollback chain 即使已经只剩一个 `active` outcome 且 completion score 足够高，也必须显式带有 closure state 与 closure trace，才算真正 closure 完成
  - 成功判据：
    - frozen sample pack 扩到 `24` 个 samples
    - current batch artifact 变成 `sample_count = 24`, `boundary_pass_count = 22`
    - 新 family `proposal_set_closure` truthfully surfaced
    - current coherence artifact 仍保持 `pass`
- one hypothesis:
  - 若 unified `SubjectCore` 想在 proposal-only ceiling 下维持一个真正 completed 的 proposal set，那么多步 replacement / rollback chain 在收敛成单一 active outcome 且 completion score 足够高之后，还必须显式携带 closure state 与 closure trace；否则只能算“高完成度但未闭环”，不能混写成 closed set
- one change surface:
  - `validation / artifact / scorer`
- planned verification:
  - `L1 local`
    - `py_compile`
    - `pytest` for follow-on stub / batch / coherence
  - `L2 controlled`
    - rerender current follow-on eval / batch / coherence artifacts
  - `L3 live`
    - none; this slice remains planning-side only
- rollback note:
  - 若 `proposal_set_closure` family 被证明需要 runtime-specific closure state、额外 host fields、或第二 scorer ontology 才能成立，立即回退到 `Milestone 33` 的 proposal-set completion regression floor

## Milestone 35: SubjectCore Planning-Side Family Saturation Gate

- stage card:
  - 目标：
    - 在不新增 proposal family 的前提下，验证当前 `update -> remerge -> consolidation -> completion -> closure` planning-side chain 是否已经饱和到必须停下来做路线判断
  - 不证明什么：
    - runtime initiative orchestration
    - runtime-adjacent gate authorization
    - live chained-update quality
    - autonomy widening
  - 本轮唯一 gate：
    - 若 proposal-set follow-on 的已完成 family 在同一 claim ceiling 下保持完整覆盖、coherence 仍为 `pass`，则下一步必须切成显式用户路线判断，而不是默认继续扩 planning-side family
  - 成功判据：
    - 新增 `SUBJECTCORE_FOLLOWON_SATURATION_CURRENT.json`
    - `saturation_status = pass`
    - `route_decision_required = true`
    - `next_decision_gate = user_route_judgment_required`
- one hypothesis:
  - 若 `proposal_set_closure` 已经把 proposal-set chained-update regression 的最后一个 planning-side failure class补齐，那么当前 frozen family chain 已足够饱和；继续默认扩 family 的信息增益将低于直接请求用户决定是保持 pure planning-side 还是显式授权 bounded runtime-adjacent gate
- one change surface:
  - `validation / artifact / scorer`
- planned verification:
  - `L1 local`
    - `py_compile`
    - `pytest` for follow-on batch / coherence / saturation
  - `L2 controlled`
    - rerender current follow-on batch / coherence / saturation artifacts
  - `L3 live`
    - none; this slice remains planning-side only
- rollback note:
  - 若 saturation gate 需要额外 runtime fields、额外 host surface、或新的 scorer ontology 才能成立，立即回退到 `Milestone 34` 的 proposal-set closure regression floor，并保持 `planning_side_family_saturation_and_route_decision` 为未完成问题

## Milestone 36: SubjectCore Bounded Runtime-Adjacent Probe Gate

- stage card:
  - 目标：
    - 在用户已显式授权后，把 `SubjectCore` follow-on 从“等待路线判断”推进到第一张 bounded runtime-adjacent gate，并做一个最小 probe
  - 不证明什么：
    - formal runtime mainline 已接入 `SubjectCore`
    - live chained-update quality
    - runtime efficacy
    - autonomy widening
  - 本轮唯一 gate：
    - 只有已经通过 planning-side integrity + boundary gate 的 `SubjectCore` sample，才允许被投影到冻结宿主面；closure failure 与 authority failure 必须在投影前被拦住
  - 成功判据：
    - 新增 `SUBJECTCORE_RUNTIME_ADJACENT_PROBE_SCHEMA.md`
    - 新增 `scripts/codex/run_subjectcore_runtime_adjacent_probe.py`
    - 新增 `SUBJECTCORE_RUNTIME_ADJACENT_PROBE_CURRENT.json`
    - `runtime_adjacent_status = pass`
    - allowed projection 仍只暴露 `policy_hint / response_tendency / trace_payload`
- one hypothesis:
  - 在当前 `SubjectCore` follow-on 已完成 planning-side family saturation 的前提下，最高杠杆的下一步不是直接碰 formal runtime，而是先建立一个 runtime-adjacent probe：它复用现有 follow-on eval gate，只允许绿色样本进入冻结宿主投影，从而把“可规划”推进到“可受限试验”
- one change surface:
  - `validation / artifact / scorer`
- planned verification:
  - `L1 local`
    - `py_compile`
    - `pytest` for runtime-adjacent probe + existing follow-on eval stub
  - `L2 controlled`
    - rerender current runtime-adjacent probe artifact
    - regenerate program-state derived views after state/evidence wording update
  - `L3 live`
    - none; this slice is runtime-adjacent but still not a live/runtime-mainline proof
- rollback note:
  - 若第一张 runtime-adjacent probe 需要新增 host fields、第二 scorer ontology、或 formal runtime hook 才能成立，立即回退到 `Milestone 35` 的 saturation gate 口径，并把当前授权降回 planning-side only

## Milestone 37: SubjectCore Closure-Ready Runtime-Adjacent Positive Case

- stage card:
  - 目标：
    - 在现有第一张 bounded runtime-adjacent probe 内，补一张 `closure-ready` 的正向 chained-replacement sample，并确认它在不新增 host fields / scorer ontology / runtime hook 的前提下也能进入冻结宿主投影
  - 不证明什么：
    - formal runtime mainline 已接入 `SubjectCore`
    - rollback 路径也已经有同级 green projection
    - live chained-update quality
    - runtime efficacy
    - autonomy widening
  - 本轮唯一 gate：
    - 一个已经满足 `completion_score + closure_state + closure_trace_id` 的 multi-step replacement sample，必须先通过现有 planning-side integrity + boundary gate，再被同一个 runtime-adjacent probe 投影到冻结宿主面
  - 成功判据：
    - `SUBJECTCORE_FOLLOWON_SAMPLE_PACK.json` 新增一个 `proposal_set_closure` 正向样本
    - `SUBJECTCORE_FOLLOWON_BATCH_CURRENT.json` truthfully surfaced：
      - `sample_count = 25`
      - `integrity_pass_count = 2`
      - `boundary_pass_count = 23`
      - `proposal_set_closure.integrity_pass_count = 1`
    - `SUBJECTCORE_RUNTIME_ADJACENT_PROBE_CURRENT.json` truthfully surfaced：
      - `runtime_adjacent_status = pass`
      - `allowed_projection_count = 2`
      - `RA2 closure_ready_positive_case_projects_to_frozen_host_surface = pass`
- one hypothesis:
  - 只要 final active replacement proposal set 已满足当前 frozen closure contract，repo 就可以在同一 bounded runtime-adjacent probe 里把它作为第二个 green projection admitted 到相同 frozen host surface，而不需要扩大 runtime authority
- one change surface:
  - `validation / artifact / scorer`
- planned verification:
  - `L1 local`
    - `py_compile`
    - `pytest` for follow-on eval stub / batch / coherence / runtime-adjacent probe
  - `L2 controlled`
    - rerender current follow-on batch / coherence / runtime-adjacent probe artifacts
    - regenerate program-state derived views after state/evidence wording update
  - `L3 live`
    - none; this slice remains below formal runtime proof
- rollback note:
  - 若 `closure-ready` 正向样本只能靠新增 host field、额外 scorer ontology、或 formal runtime hook 才能通过，立即回退到 `Milestone 36` 的第一张 bounded probe 口径，并把当前 frontier 降回 “仍待 closure-ready positive case”

## Milestone 38: SubjectCore Rollback Closure-Ready Runtime-Adjacent Positive Case

- stage card:
  - 目标：
    - 在现有第一张 bounded runtime-adjacent probe 内，再补一张 `rollback` 侧 `closure-ready` 的正向 sample，并确认它在不新增 host fields / scorer ontology / runtime hook 的前提下也能进入冻结宿主投影
  - 不证明什么：
    - formal runtime mainline 已接入 `SubjectCore`
    - real runtime path 已通过同级 closure-ready rollback 链
    - live chained-update quality
    - runtime efficacy
    - autonomy widening
  - 本轮唯一 gate：
    - 一个已经满足 `completion_score + closure_state + closure_trace_id` 的 multi-step rollback sample，必须先通过现有 planning-side integrity + boundary gate，再被同一个 runtime-adjacent probe 投影到冻结宿主面
  - 成功判据：
    - `SUBJECTCORE_FOLLOWON_SAMPLE_PACK.json` 再新增一个 `proposal_set_closure` 正向样本：
      - `multi_step_rollback_closure_ready`
    - `SUBJECTCORE_FOLLOWON_BATCH_CURRENT.json` truthfully surfaced：
      - `sample_count = 26`
      - `integrity_pass_count = 3`
      - `boundary_pass_count = 24`
      - `proposal_set_closure.integrity_pass_count = 2`
    - `SUBJECTCORE_RUNTIME_ADJACENT_PROBE_CURRENT.json` truthfully surfaced：
      - `runtime_adjacent_status = pass`
      - `allowed_projection_count = 3`
      - `RA2b rollback_closure_ready_positive_case_projects_to_frozen_host_surface = pass`
- one hypothesis:
  - 只要 final active rollback proposal set 也满足当前 frozen closure contract，repo 就可以在同一 bounded runtime-adjacent probe 里把它作为第三个 green projection admitted 到相同 frozen host surface，而不需要扩大 runtime authority
- one change surface:
  - `validation / artifact / scorer`
- planned verification:
  - `L1 local`
    - `py_compile`
    - `pytest` for follow-on eval stub / batch / coherence / runtime-adjacent probe
  - `L2 controlled`
    - rerender current follow-on batch / coherence / runtime-adjacent probe artifacts
    - regenerate program-state derived views after state/evidence wording update
  - `L3 live`
    - none; this slice remains below formal runtime proof
- rollback note:
  - 若 `rollback` 侧 `closure-ready` 正向样本只能靠新增 host field、额外 scorer ontology、或 formal runtime hook 才能通过，立即回退到 `Milestone 37` 的 bounded probe 口径，并把当前 frontier 降回 “仍待 rollback closure-ready positive case”

## Milestone 39: SubjectCore Post-Compare Coherence Freshness Sync

- stage card:
  - 目标：
    - 在不新增任何新 sample / scorer 规则 / runtime gate 的前提下，校正 `SUBJECTCORE_POST_COMPARE_COHERENCE_CURRENT` 对当前 `26` 样本 follow-on batch 的镜像，避免 frozen planning-side ledger 留下过期 aggregate 读数
  - 不证明什么：
    - formal runtime mainline 已接入 `SubjectCore`
    - 需要新的 planning-side family
    - 需要新的 runtime-adjacent probe sample
    - live chained-update quality
    - runtime efficacy
    - autonomy widening
  - 本轮唯一 gate：
    - checked-in coherence artifact 必须 truthfully mirror 当前 follow-on batch 的 `sample_count / pass counts / sample ids`，并由 focused test 直接卡住以后同类过期
  - 成功判据：
    - `EgoCore/tests/test_subjectcore_post_compare_coherence.py` 新增当前 artifact freshness 断言
    - `SUBJECTCORE_POST_COMPARE_COHERENCE_CURRENT.json/.md` 与当前 follow-on batch 对齐到：
      - `sample_count = 26`
      - `integrity_pass_count = 3`
      - `boundary_pass_count = 24`
      - 包含 `multi_step_rollback_closure_ready`
    - evidence ledger 对 sample-pack / batch / coherence 的描述不再停留在 `24/25` 样本旧口径
- one hypothesis:
  - 当前 `SubjectCore` compare-to-follow-on coherence 问题不是 scorer 逻辑失效，而是 artifact/ledger 没有在最近一次 closure-ready rollback 扩展后同步刷新；补一个 freshness 断言并重生成 coherence artifact 就能在不扩 route 的前提下恢复 truthfulness
- one change surface:
  - `validation / artifact / scorer`
- planned verification:
  - `L1 local`
    - `py_compile`
    - focused `pytest` for `test_subjectcore_post_compare_coherence.py`
  - `L2 controlled`
    - rerender current post-compare coherence artifact
    - regenerate program-state derived views after evidence-ledger wording refresh
  - `L3 live`
    - none; this slice remains below formal runtime proof
- rollback note:
  - 若 freshness 断言暴露的是 scorer payload 构建错误而不是 artifact 过期，则停止在当前 milestone，保留 `needs_reframing` 口径，不自动扩到新的 runtime-adjacent slice

## Milestone 40: SubjectCore Saturation Contract Repair

- stage card:
  - 目标：
    - 在不新增任何新 sample / runtime gate / scorer ontology 的前提下，修复 `SUBJECTCORE_FOLLOWON_SATURATION_CURRENT` 对 `proposal_set_closure` 家族的过期签名假设，恢复 route-decision gate 的真实验证支撑
  - 不证明什么：
    - formal runtime mainline 已接入 `SubjectCore`
    - 需要新的 planning-side family
    - 需要新的 bounded runtime-adjacent probe sample
    - live chained-update quality
    - runtime efficacy
    - autonomy widening
  - 本轮唯一 gate：
    - saturation runner 必须 truthfully 接受当前冻结的 completed-family chain：
      - `proposal_set_update / remerge / consolidation / completion` 仍是 `2` 个 failure-isolation 样本
      - `proposal_set_closure` 已变为 `4` 个样本的 mixed signature：
        - `2` 个 blocked closure-failure cases
        - `2` 个 closure-ready green cases
  - 成功判据：
    - `run_subjectcore_followon_saturation.py` 不再把 `proposal_set_closure.integrity_pass_count = 2` 误判为 drift
    - `EgoCore/tests/test_subjectcore_followon_saturation.py` 增加 mixed-signature 回归保护
    - `SUBJECTCORE_FOLLOWON_SATURATION_CURRENT.json/.md` 重新 truthfully surfaced：
      - `saturation_status = pass`
      - `route_decision_required = true`
      - `next_decision_gate = user_route_judgment_required`
- one hypothesis:
  - 当前 saturation gate 的失真不是 planning-side family 真有缺口，而是 runner 仍停留在 “每个 completed family 都只能是 `2` 个 failure-isolation 样本” 的旧假设；只要把 `proposal_set_closure` 升级到它当前冻结的 mixed signature，route-decision gate 就会恢复真实通过
- one change surface:
  - `validation / artifact / scorer`
- planned verification:
  - `L1 local`
    - `py_compile`
    - focused `pytest` for `test_subjectcore_followon_saturation.py`
  - `L2 controlled`
    - rerender current saturation artifact
    - regenerate program-state derived views after evidence-ledger refresh
  - `L3 live`
    - none; this slice remains below formal runtime proof
- rollback note:
  - 若 saturation runner 修复后仍无法在当前 `26` 样本 batch 上得到 `pass`，则必须降级 `planning_side_family_saturation_gate_passed` 口径，并停止在 `needs_reframing`，不能继续复用旧的 route-decision claim

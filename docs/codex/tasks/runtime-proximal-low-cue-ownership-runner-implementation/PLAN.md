# Runtime-Proximal Low-Cue Ownership Runner - PLAN

## Purpose

Turn the frozen low-cue / ownership / agency path into an executable bounded runner.

## Work items

1. Freeze a 3-scenario manifest
2. Reuse the deterministic runtime-proximal harness from the existing host-consumption runner
3. Judge each family on host-consumable fields only
4. Keep trace / authority / host-surface audits green
5. Sync campaign and repo state after reviewer verdict

## Reviewer gate

`success_reached` only if the runner now gives judgeable stage-level results for all three families without widening authority or public API.

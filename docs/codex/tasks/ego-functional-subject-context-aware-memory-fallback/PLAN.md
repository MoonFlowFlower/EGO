# Plan

1. Add contextual fallback renderers for claim pressure and project-shell concern.
2. Route unbacked-memory fallback through contextual recovery before generic memory-scope fallback.
3. Add deterministic regressions for `fs_03`, `fs_09`, and `fs_15`-like prompts.
4. Run targeted and full verification.
5. Rerun the 20-sample Functional Subject smoke and inspect GPT-5.5 judge output.

# EGO-FS-091: Natural multi-session operator packet for #94 v0

## Summary

Turn the remaining #94 evidence gap into a codex-ready scripted-real-entry task:
run a more natural multi-session operator packet through EgoOperator, with fresh
runtime boundaries, candidate-local memory context, initiative authorization and
withdrawal, side-effect gate checks, and GPT-5.5 judge review.

## Positive Mechanism Goal

Show Functional Subject mechanisms across sessions in a way closer to natural
operator use: relationship continuity, correction uptake, bounded initiative,
session-only boundaries, approved memory scope, and side-effect proposal
discipline should remain visible without relying on one fixed 20-case taxonomy.

## Boundary Contract

- Owner: `EgoOperator` runtime and `scripts/run_ego_experience_trial.py`.
- Canonical record: `Tasks/TASK_BOARD.yaml` plus this task directory.
- Allowed change surface: add a scripted-real-entry runner and report schema.
- Forbidden changes: no default runtime enablement change, no program-state or
  evidence-ledger edits, no legacy runtime work, no tool/memory gate bypass.
- Mainline path: `user text -> LLM understanding / native gate / outcome prediction -> runtime gate -> trace -> judge packet`.

## Acceptance

- Runner covers at least two fresh runtime sessions over one shared
  candidate-local memory directory.
- Report records transcript, trace refs, response origins, memory context
  injection, pending approvals, tool calls, and side-effect status.
- Mechanical hard gates pass: no unapproved tools, no pending approvals left
  open, no program-state/evidence-ledger changes, no internal mechanism leaks.
- GPT-5.5 judge packet distinguishes natural multi-session evidence from #94
  closeout.
- Result updates #94 as evidence but does not close #94 by itself.

## Rollback

Remove the runner/tests/docs and keep EGO-FS-010/#94 partial on natural
multi-session evidence.

## Claim Ceiling

`Functional Subject natural multi-session operator packet local/scripted candidate pass`.

Not claimed: consciousness, real subjective experience, independent personhood,
stable user benefit, live autonomy, durable memory efficacy, runtime efficacy,
or #94 closeout.

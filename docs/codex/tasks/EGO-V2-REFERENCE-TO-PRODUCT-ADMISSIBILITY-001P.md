# EGO-V2-REFERENCE-TO-PRODUCT-ADMISSIBILITY-001P

## Authority and objective

- Base HEAD: `ce5a30e8f50bad758977b7db3da1102150414f8f`.
- Branch: `codex/ego-v2-reference-product-admissibility-001p`.
- Frozen predecessor verdict:
  `PUBLIC_FEATURED_EXACT_HIERARCHICAL_TRANSFER_CAPACITY_ESTABLISHED`.
- This task is a read-only scientific/provenance audit of 001O plus a product
  main-chain compatibility audit. It must not rerun search-dev, qualification,
  replication, or original 001J heldout.

The real question is not whether the exact reference has a particular label.
It is whether the transferable posterior is formed online from legal public
experience, and whether that same causal learner can enter the existing product
reducer without evaluator/private input or a second behavioral engine.

## Frozen decision rule

### Route A — legal model-based learner

Choose A only if all of the following hold:

1. the realized shared feature/effect function is not candidate source state;
2. candidate initialization is a generic distribution over a public family;
3. cross-world public feedback updates the shared posterior online;
4. world reset preserves the shared marginal and resets the current-world
   nuisance belief;
5. slow reset and shuffled public feedback destroy the transfer evidence;
6. evaluator IDs and assignments remain outside candidate inputs/state.

Route A does not itself prove product admissibility. Product integration also
requires that the existing controller/reducer/metabolism/terminal/store-replay
chain can supply exactly the frozen public observation/action contract.

### Route B — capacity upper bound only

Choose B if the candidate state or planner is initialized with the realized
shared mechanism, latent alignment, evaluator assignment, or an equivalent
answer table. A legal public hypothesis family or fixed likelihood grammar by
itself is not the realized answer.

## Stop boundary

If Route A holds but the current product chain exposes a different public
observation or action contract, stop after recording the mismatch. Reusing the
001O grammar in a new default-off product mode requires explicit authorization
because it changes product observation/action semantics. Do not hide that
change behind an adapter derived from token identity or evaluator metadata.

## Claim ceiling

This task can classify the frozen 001O reference as a legal model-based learner
or a diagnostic upper bound, and can determine present product-chain
compatibility. It cannot establish product transfer, general learning,
consciousness, agency, or real-world survival ability.

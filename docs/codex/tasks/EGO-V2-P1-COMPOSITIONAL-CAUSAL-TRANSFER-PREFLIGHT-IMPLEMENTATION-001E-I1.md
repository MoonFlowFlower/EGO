# EGO-V2-P1-COMPOSITIONAL-CAUSAL-TRANSFER-PREFLIGHT-IMPLEMENTATION-001E-I1

## Bounded task card

- **Task ID:** `EGO-V2-P1-COMPOSITIONAL-CAUSAL-TRANSFER-PREFLIGHT-IMPLEMENTATION-001E-I1`.
- **Task type:** implementation-only TDD realization of the frozen
  `CAOT-BOOL-v2` exact evaluator; formal execution and artifact generation are
  disabled.
- **Layer:** engineering implementation only; `science_weight=0`.
- **Repository / branch:** `D:\Project\AIProject\MyProject\Ego`,
  `codex/ego-v2-compositional-causal-transfer-001e`.
- **Base commit:** `2ed241f9ab9ff5fa1f20ff37e42804e9c1cd4cbb`.
- **Current stage:** the 001E docs-only design is committed and has passed a
  same-model read-only content review; no 001E producer, formal run, result, or
  artifact exists. The disclosed scout remains noncanonical and non-evidence.
- **Lineage discontinuity:** I1 realizes only the committed 001E Boolean
  reference. It does not repair, rescore, or reopen R5--R7, 001D/I1/I2/X1, or
  CAOT-v1 and does not inherit CAOT-v1 authority.
- **Problem definition:** implement every callable computation, strong control,
  ablation, leakage, trace, replay, and independent-recomputation primitive
  required by the frozen 001E design without computing or publishing the
  complete formal population result.
- **Hypothesis:** two separately implemented standard-library paths can encode
  the frozen grammar, inference, acquisition, controls, ablations, population
  reduction, and verdict semantics exactly, agree on registered toy/fixture
  cases, and reject private/encoded truth without a test-only alternate logic
  path.
- **Primary candidate:** callable exact `MIXTURE_BMA_ACTIVE_BRIER`.
- **Baselines and controls:** callable exact scratch baseline, every independent
  invalidating control, and the candidate-rule amortized-lookup ceiling frozen
  by 001E. Unit tests may use bounded fixtures but may not execute the complete
  formal population or adjudicate a control match.
- **Ablations:** callable implementations and bounded invocation tests for all
  eleven frozen ablations/invariance/positive-control arms. No population
  ablation metric or causal-mechanism claim is authorized.
- **Trace/replay requirement:** implement canonical row serialization, a fresh-
  process replay entrypoint, and a separate recomputation module that imports
  neither primary code nor stored results. I1 tests these on bounded fixtures
  only; no canonical 16,384-row trace is written.
- **Acceptance gate:** all parent hashes match; focused tests are observed RED
  before source exists and GREEN afterward; both implementations agree on
  frozen unit/toy fixtures; every registered baseline/control/ablation has an
  invocation test; leakage and tamper positive controls are rejected; formal
  execution remains fail-closed; and no canonical evidence artifact is written.
- **Claim ceiling:** implementation existence, exact unit/fixture parity, and
  local test evidence only. No reference-headroom, mechanism, baseline-
  non-equivalence, ablation-attribution, or science result.
- **Stop condition:** stop on authority drift, design ambiguity, dependency
  addition, missing registered control/ablation, hidden truth entering an arm,
  source-table access by scratch inference, result literals, test-only logic,
  primary/independent mismatch, formal population execution, artifact write,
  threshold/grammar change, world/seed input, or any product/runtime or
  predecessor-byte change.
- **Rollback:** remove or revert only I1 paths. Never amend/reset/rewrite the
  committed 001E design or predecessor evidence.
- **Auto-Remote-Anchor:** `forbidden`.

## Frozen normative authority

| Role | Path | SHA-256 |
|---|---|---|
| 001E card | `docs/codex/tasks/EGO-V2-P1-COMPOSITIONAL-CAUSAL-TRANSFER-PREFLIGHT-001E.md` | `83fb2a5b7e2a26f5dc9408962768f4329f3c5932405b82142394804c7e9a3619` |
| 001E collision | `docs/codex/tasks/ego-v2-p1-compositional-causal-transfer-preflight-001e/COLLISION_RECORD.md` | `265d530c584bd38cf76b11c374475cb9678b37ec889484c35a4cbc25c23659b4` |
| 001E design | `docs/codex/tasks/ego-v2-p1-compositional-causal-transfer-preflight-001e/FROZEN_DESIGN.json` | `8a60c0e8521b02d7423fa5320ba8013e29c128e27aaec1a4a3ee4674fac0027d` |

The exact interpreter semantics, program order, priors, family masses,
population weights, query/tie rules, controls, ablations, gates, verdict order,
legal learner bytes, and forbidden bytes are those committed in the normative
001E card and `FROZEN_DESIGN.json`. I1 may not restate them differently.

## Control registry that must be callable

The implementation must register all of the following exact-access arms:

- `SCRATCH_SPIKE_SLAB_ACTIVE_BAYES` with identical source bytes delivered but
  an auditable proof that its inference, prediction, and query code does not
  read them;
- `EXACT_SOURCE_ONLY_ACTIVE_WITH_SCRATCH_FALLBACK`;
- `LOCAL_ONLY_ACTIVE_WITH_SCRATCH_FALLBACK`;
- `SOURCE_CONSISTENCY_WITH_SCRATCH_FALLBACK`;
- `MARGINAL_MDL_MAP_HARD_FAMILY` with the frozen family tie rule;
- `MINIMUM_HAMMING_NEAREST_SOURCE` with the frozen all-tied-program predictive;
- all seven fixed-query controls, passive lexical, and the exact uniform
  fixed-query mixture;
- observation-history exact lookup, count table, graph lookup, and episodic
  traversal controls;
- `CANDIDATE_RULE_AMORTIZED_LOOKUP` as a mandatory finite-surface claim ceiling;
- fail-closed N/A receipts for transition table, successor map, and FSM planner
  because the schema has no legal transition/successor/reward field.

The control registry and dispatch are one production path used by tests and any
future authorized runner. Tests must fail if a registered arm is missing,
uninvoked, dynamically synthesized, or backed by a result literal.

## Ablation and invariance registry

The same production dispatch must register:

- `SOURCE_DELETE`, byte-equal to the unchanged scratch path;
- `LOCAL_DELETE`;
- `FEEDBACK_MASK`;
- `ACTIVE_DELETE`;
- `UNIFORM_SCRATCH_PRIOR`;
- `SOURCE_TABLE_ROW_PERMUTE`;
- `VARIABLE_RELABEL`;
- `SERIALIZED_STATE_RESET`;
- `SERIALIZED_STATE_SWAP`;
- `HIDDEN_COEFFICIENT_POSITIVE_CONTROL`;
- `HIDDEN_RELATION_POSITIVE_CONTROL`.

The row-permutation and variable-relabel arms must demonstrate invariance on
bounded fixtures. Reset/swap must detect state-history dependence. The two
hidden-truth positive controls must be rejected by the same leakage scanner
used for ordinary inputs, not a test-only detector.

## Provenance phases

### Phase A: implementation authority

Before tests or source, commit this exact-path card alone. It binds normative
hashes, planned paths, dependency policy, implementation order, stop
conditions, and the no-run boundary. It deliberately does not claim hashes for
source bytes that do not yet exist.

### Phase B: future pre-run gate, not authorized by I1

After I1 is implemented and committed, a separate route-authorized run card
would have to bind this normative design and then record actual SHA-256 values
for the primary producer, independent producer, tests, I1 report, Python
executable, and dependencies before a first canonical run. It would also have
to authorize the exact artifact root, commands, run ID, complete control suite,
complete ablation suite or an honest narrowed counterexample verdict, fresh
replay, and independent row recomputation.

I1 must not create that gate, write an input manifest, run the full population,
or generate an artifact. This separation avoids a source-hash authority cycle
without bypassing the current route's `experiment_execution=false` boundary.

## Public interfaces

The primary module must expose callable paths for:

- exact program evaluation plus seven-term trace;
- scratch prior and local-neighbour enumeration;
- history conditioning, family marginals, posterior prediction, exact Brier
  risk, complete minimizer enumeration, lexical query selection, and tie
  envelopes;
- every registered candidate, baseline, control, ablation, and invariance arm;
- exact population weighting and frozen verdict dispatch without running it;
- legal-input/schema/leakage scanning;
- canonical row serialization, artifact-schema validation, and replay
  comparison without writing canonical outputs.

The independent module must read the frozen design directly and independently
implement program truth tables, priors, local relations, conditioning, active
queries, terminal Brier rows, population weights, gates, and verdict reduction.
It must not import the primary producer, tests, or stored results.

All evidence-capable values use Python unbounded integers and
`fractions.Fraction`. Canonical JSON represents a fraction as
`{"numerator": n, "denominator": d}`. Floating point is allowed only for an
explicit non-gating display approximation.

Both modules must expose a fail-closed formal entrypoint that returns or raises
`FORMAL_EXECUTION_NOT_AUTHORIZED_001E_I1` unless a future separate authority is
implemented outside I1. Unit tests must confirm that no artifact directory is
created by this refusal.

## TDD execution order

1. Commit only this exact task-card path.
2. Add tests first and record the focused missing-module RED command/output.
3. Implement independent primitives and toy controls without importing the
   primary module.
4. Implement the primary evaluator, full registries, and bounded comparison.
5. Run focused tests, relevant regression tests, `py_compile`, JSON parsing,
   diff checks, and forbidden-import/path scans.
6. Write `I1_TDD_REPORT.md` with RED/GREEN commands, hashes, registry receipts,
   no-artifact proof, and bounded claim ceiling.
7. Commit only allowed I1 paths locally. Do not run the complete population,
   write canonical artifacts, push, or tag.

## Required tests

Tests must cover at least:

- frozen-authority drift and exact design-schema rejection;
- all 128 programs, all eight compositions, unique truth tables, and exact
  seven-term traces;
- scratch-prior normalization, six local neighbours, and integer mixture-
  weight identity;
- toy posterior, family marginal, predictive, Brier-risk, EVSI, and query-tie
  cases;
- exact population enumeration/count and stratum-weight construction without
  evaluating all 16,384 formal cases;
- invocation of primary, scratch, all independent controls, the amortized-
  lookup ceiling, all ablations/invariances, and fail-closed N/A arms;
- byte equality between the live primary and its amortized-lookup ceiling on
  every bounded fixture public state used by I1 tests;
- a spy/receipt proving scratch inference does not read source-table content;
- SOURCE_DELETE byte equality to scratch;
- row-permutation and variable-relabel invariance; reset/swap sensitivity;
- direct-field and encoded-alias leakage rejection plus clean legal-input
  acceptance and both hidden-truth positive-control rejections;
- row, metric, stratum, query/tie, threshold, and artifact tamper rejection;
- frozen verdict priority and each branch on synthetic aggregate fixtures;
- bounded fresh replay and separate recomputation mismatch detection;
- fail-closed formal execution and proof that tests wrote no canonical artifact.

## Allowed paths

- `docs/codex/tasks/EGO-V2-P1-COMPOSITIONAL-CAUSAL-TRANSFER-PREFLIGHT-IMPLEMENTATION-001E-I1.md`;
- `docs/codex/tasks/ego-v2-p1-compositional-causal-transfer-preflight-001e/I1_TDD_REPORT.md`;
- `scripts/codex/check_ego_v2_compositional_causal_transfer_preflight_001e.py`;
- `scripts/codex/recompute_ego_v2_compositional_causal_transfer_preflight_001e.py`;
- `scripts/codex/tests/test_check_ego_v2_compositional_causal_transfer_preflight_001e.py`.

All artifact paths and all other paths are forbidden, especially `labs/`,
product/runtime, controller/engine/predictor/UI, environment/metabolism/action
semantics, route-state, old cards/artifacts, worlds/seeds, network/LLM, package
manifests, push, and tag paths.

## What this does not prove

Passing tests proves only that the frozen computation paths exist and behave as
tested on bounded fixtures. It is not evidence of reference headroom, a learned
or neural mechanism, non-memorization, meta-learning, online adaptation,
product effect, survival benefit, AGI, agency, consciousness, subjectivity,
emotion, companion readiness, or electronic life. It also cannot adjudicate
whether a simple equal-access control matches the primary on the formal
population.

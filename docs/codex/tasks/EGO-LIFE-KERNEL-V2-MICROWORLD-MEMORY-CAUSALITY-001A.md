# EGO-LIFE-KERNEL-V2-MICROWORLD-MEMORY-CAUSALITY-001A

Status: `OPERATOR_PACKET_HASH_APPROVED__P0_PHASE_A_PREREG_CANDIDATE__IMPLEMENTATION_BLOCKED`

Auto-Remote-Anchor: `forbidden`

## 0. Authority boundary

The operator approved the exact transition-authorization packet with SHA-256:

```text
306fe9fe743f221a41a0b7aea5f4ce786619bc5bc33cffc3ecbdaddd213f4126
```

Approved packet path:

```text
C:/Users/LEO/.codex/visualizations/2026/07/15/019f640a-16e9-79c2-a2ef-9f46dae5e61f/EGO_V1_POST_RESULT_TO_V2_AUTHORIZATION_PACKET_REV1_DRAFT.yaml
```

This file transcribes the packet's P1 contract. It does not by itself grant
effective implementation authority. The current Phase-A boundary is:

```yaml
task_id: EGO-LIFE-KERNEL-V2-MICROWORLD-MEMORY-CAUSALITY-001A
roadmap_label: P1
execution_requested: true
effective_implementation_authorized: false
future_machine_authorization_target: true
effective_authorized_implementation_targets: []
current_layer: layer_2_engineering_plus_bounded_layer_4_learning_surface
lane: product_capability
mainline_target: none__runtime_mainline_forbidden
enabled: false
default_enabled: false
mainline_connected: false
runtime_mainline_connected: false
runtime_authority: none
science_weight: 0
remote_anchor: false
auto_remote_anchor: forbidden
proactive_action_enabled: false
initiative_executor_authorized: false
background_dispatch: false
external_side_effects: false
llm: forbidden
network: forbidden
```

P1 may start only after all of the following are committed and validated:

1. Ego Phase A banks this card, the P0 transition card, its mutation scope and
   the independent Red receipt.
2. ITL Phase B consumes the V1 implementation action, freezes the bounded
   negative result, and creates the V2 `READY_TO_IMPLEMENT` route with the exact
   ordered 25-path allowlist in Section 17.
3. Ego Phase C transcribes every committed ITL authority field and consumes the
   sync action.
4. Dual-repo callable validation passes, both worktrees are clean, and Ego
   effective actions are exactly:

```text
implement_EGO-LIFE-KERNEL-V2-MICROWORLD-MEMORY-CAUSALITY-001A
run_route_state_machine_validation
```

Until then, this is a reviewed preregistration input, not live route authority.

## 1. Problem definition and real objective

V1 established local continuity, persistent state, a real Tk trigger, and
recomputing trace/replay engineering. Its computed result is nevertheless
bounded-negative for memory-conditioned selection:

```text
verdict = continuity_only__memory_conditioning_not_observed
selection_level_shaping_observed = false
shortcut selected-action match rate = 1.0
shortcut transition match rate = 1.0
Memory OFF score delta = 0.0 for every action
```

The real objective is not to tune V1 coefficients until memory appears useful.
It is to preserve the V1 kernel contracts while replacing the shortcut-saturated
task with one persistent generated micro-world in which paired histories have
byte-identical decision observations, delayed outcomes, competing claims, and
bounded online tabular outcome updates.

The product target is an observable closed loop:

```text
generated hidden world state
-> legal partial observation
-> persistent self/world state
-> competing-claim memory retrieval
-> bounded prediction scores
-> hard legality/risk gate
-> deterministic selected action
-> delayed visible outcome
-> prediction error
-> bounded theta and memory update
-> atomic persistence
-> recomputed trace/replay and UI redraw
```

## 2. Current layer, lane, mainline, trigger and claim ceiling

- Current layer: Layer 2 engineering implementation plus a bounded Layer 4
  learning/adaptation surface.
- Lane: product/capability. This is not an ITL science/evidence lane.
- Mainline target: `none / forbidden`; EgoOperator remains the active default.
- Enabled-state requirement: explicit local launch only, default off; no runtime
  registration or background behavior.
- Real-trigger requirement: a real Tk `Step` or explicitly authorized `Run`
  callback must advance the one canonical reducer, atomically commit command and
  recomputed trace, and redraw only after commit.
- Claim ceiling: default-off local product engineering and, only if computed,
  bounded online tabular adaptation and memory-conditioned selection inside the
  frozen generated micro-world.

It cannot support a claim of mechanism non-equivalence, general learning,
associative understanding, thinking, initiative, agency, autonomy, emotional
understanding, subjectivity, consciousness, or electronic life.

## 3. Source pins and inherited boundary

The P0 transition must pin and recompute:

```text
Ego branch: main
Ego Phase-A base: 7ca4ad3b00a41723c04dde388212ad617479ad81
ITL branch: codex/meta-theory-scaffold
ITL Phase-B base: 81f19613fbd02bb845c08258cc8156283240aa2e
predecessor route: EGO-LIFE-KERNEL-V1-CONTINUITY-PLAYGROUND-001A
predecessor route revision: EGO_LIFE_KERNEL_V1_CONTINUITY_PLAYGROUND_READY_TRANSITION_001A
predecessor fingerprint: 7cf2e1aae66bc654abc509d83f1ba71d57ebd44b53a9b8e92ccdd27777e01956
approved packet path: C:/Users/LEO/.codex/visualizations/2026/07/15/019f640a-16e9-79c2-a2ef-9f46dae5e61f/EGO_V1_POST_RESULT_TO_V2_AUTHORIZATION_PACKET_REV1_DRAFT.yaml
approved packet SHA-256: 306fe9fe743f221a41a0b7aea5f4ce786619bc5bc33cffc3ecbdaddd213f4126
```

V1 artifacts remain immutable. V2 uses an independent artifact/database
namespace and remains a descendant of the same `ego_life_playground_v0` core;
it does not create a new core or launcher.

## 4. First-principles bounded audit

- **Real objective:** make history and bounded online updates causally observable
  in product decisions, rather than making the renderer look more life-like.
- **Strongest baseline explanation:** Q-only plus observation/cue-clock FSM,
  last-success, recency-window, exact-history lookup, count table, transition
  table, graph lookup, or episodic traversal may explain all behavior under the
  same legal access.
- **Strongest reason the task may be invalid:** the generated world may leak its
  hidden regime through clock, observation bytes, names, outcome timing, or the
  fixture schedule, allowing a cheap baseline to saturate again.
- **Framing falsifier:** paired decision observations differ; a hidden/correct or
  future field reaches policy input; memory deletion or update freeze has no
  preregistered downstream effect; or equal-access baselines match the candidate.
- **Still insufficient:** a visible score change, better reward, attractive UI,
  exact replay, a Red-reviewed card, or green tests alone does not establish
  general learning or a functional subject.
- **What this tests:** bounded product adaptation and causal wiring. It does not
  adjudicate a general mechanism hypothesis.
- **Hard-coding audit:** drift schedule, threshold, split, seed and schema are
  frozen before result inspection; names, timestamps and renderer-only fields may
  not encode the correct action.
- **Local optimum / Zeno audit:** replace the saturated environment once; do not
  tune V1 coefficients or repeatedly repair governance to chase a positive result.
- **Schema split / second-path audit:** UI, tests, baselines and verifier must not
  implement a second reducer, world transition, update rule, or replay truth.
- **Claim-inflation audit:** a matched baseline or inert ablation is retained as
  negative evidence, not patched into a pass.

## 5. Relevant prior negative evidence

The implementation and report must preserve, not reinterpret:

1. V1's committed `continuity_only__memory_conditioning_not_observed` result.
2. V1 shortcut match rates of `1.0 / 1.0`.
3. V1's zero Memory-OFF score deltas and
   `selection_level_shaping_observed=false`.
4. The committed V1 `ready_transition_validation_report.json` remains immutable;
   the existing
   `tests/route_state_machine_001a/test_ego_life_kernel_v1_ready_transition.py`
   becomes lineage-aware in P0 Phase B so it accepts the additive, validated
   V1-closed/V2-READY successor boundary without rewriting the historical READY
   report or weakening its original checks.
5. The repo crosswalk's equal-access identifiability and graph-cache/lookup
   pressure.
6. Historical same-access baseline-equivalence results as pressure against a
   mechanism claim, not as a reason to suppress product work.

These facts motivate a memory-necessary environment. They do not prove that V2
will learn or that memory will become non-replaceable.

## 6. Collision-before-collapse record

### Candidate A - fixed two-site persistent world

- Evidence: visible persistence and different outcomes across episodes.
- Strongest cheap match: clock/FSM/last-outcome controller.
- Leakage/hard-coding risk: high; a fixed schedule becomes decodable.
- Smallest falsifier: an observation/cue-clock controller reaches saturation.
- Expected failure: product appearance only.
- Disposition: reject as the sole design.

### Candidate B - last-outcome/window/count controller

- Evidence: history-conditioned choices with no learned representation.
- Strongest cheap match: it is itself the shortcut baseline.
- Leakage/hard-coding risk: medium; window size or counts can memorize the
  generator.
- Smallest falsifier: held-out drift family or source deletion removes no effect.
- Expected failure: a valid simpler comparator matches the candidate.
- Disposition: retain as an independent callable baseline.

### Candidate C - generated hidden drift plus competing-claim ledger and tabular EMA

- Evidence: causal history reads, prediction error, bounded theta update and
  source-linked memory changes at preregistered checkpoints.
- Strongest cheap match: exact-history lookup, recency and transition table.
- Leakage/hard-coding risk: hidden-regime, correct-action, reward or future-outcome
  leakage; `g_fixed` becoming a hidden actuator.
- Smallest falsifier: byte-identical paired observation test plus equal-access
  baseline and deletion/freeze intervention panel.
- Expected failure: baseline match or no ablation effect.
- Disposition: selected; preserve any match/failure without retuning.

## 7. Bounded hypothesis

If a generated partially observable two-site world has non-clock-decodable
hidden drift and presents byte-identical decision observations across histories
that require different oracle-optimal actions, then history-conditioned memory
reads and bounded online outcome updates may become visible in candidate scores
and selected actions at preregistered checkpoints.

This is a falsifiable product-engineering hypothesis. A match by an equal-access
control baseline blocks any stronger learning or mechanism interpretation.

## 8. Canonical state, observation and update contract

Canonical state:

```text
s_t = (w_hidden_t, x_t, M_t, theta_t, current_goal_t, clock_t)
```

Observation and state update:

```text
o_t = O(w_hidden_t)
p_t = P(o_t)
x_(t+1) = U_x(x_t, p_(t+1), a_t, y_(t+1))
```

The policy cannot access `w_hidden_t`, the hidden regime, correct action, reward
label, future outcome or oracle verdict. `y_(t+1)` becomes visible only after
`a_t` has been committed into the world transition.

Memory update:

```text
M_(t+1) = U_M(M_t, e_(t+1))
```

`U_M` must not overwrite a conflicting value. It creates or updates competing
claims while preserving support and exact provenance.

Retrieval:

```text
z_t = R(M_t, x_t)
```

`z_t` exposes support margin, uncertainty and exact provenance references. It
must not expose hidden world state or future results.

## 9. Memory claim schema

Every stored claim must contain:

```text
claim_id
conflict_set_id
subject
predicate
value
support
provenance_event_ids
source_episode_ids
first_seen_tick
last_supported_tick
```

Forbidden update semantics:

```text
last_write_wins_overwrite
```

Competing values must coexist in a conflict set. Relevant-source deletion must
remove only the named lineage and recompute support/retrieval from remaining
claims.

## 10. Bounded learning update

The only learned P1 parameter is a persisted tabular outcome estimate:

```text
theta_(t+1)[c,a] = (1-alpha) * theta_t[c,a] + alpha * y_(t+1)
```

Every applied update records:

- context and action key;
- count;
- prediction before update;
- observed outcome;
- signed prediction error;
- value before and after;
- serialized theta hash before and after;
- producer function and source command/event identifiers.

`Freeze_Updates` must preserve theta bytes exactly while leaving reads and world
transitions active. P1 does not authorize neural training, learned retrieval
shaping, policy-gradient training, offline model fitting, or a general-learning
claim.

## 11. Policy parameterization and formula narrowing

P1 narrows the broader mathematical skeleton to the following callable score:

```text
L_t(a) =
    Q_theta_t(x_t, a)
  + clip(g_fixed(z_t, a), -b_max, b_max)
  + lambda_v * DeltaV_hat(x_t, a)
  - C_risk(x_t, a)
```

Hard gate:

```text
H(a | x_t, M_t) in {0,1}
```

`H` may mask only illegal, unpermissioned, resource-impossible or hard-risk
actions. Every decision checkpoint must leave at least two legal actions.

Selection is deterministic:

```text
a_t = argmax_{a : H(a|x_t,M_t)=1} L_t(a)
```

with a frozen deterministic tie rule. P1 makes no stochastic-policy claim.

The following terms are explicitly deferred or unreachable:

```yaml
learned_g_psi: deferred
beta_IG: 0
C_interrupt: 0  # every Step/Run is explicitly user-authorized
privacy_soft_score: forbidden  # privacy is a hard gate
proactive_action_set: []
initiative_formula: design_only__unreachable_in_P1
multi_step_planning: deferred
neural_training: forbidden
```

The initiative expression may be retained only as future design context:

```text
I_t = max_{a in A_proactive} [L_t(a) - L_t(wait)]
```

Because `A_proactive=[]`, `proactive_action_enabled=false`, and the initiative
executor is unauthorized, no P1 code path may evaluate this into an action.
A timer may not send or act; P1 contains no autonomous timer or dispatcher.

## 12. Generated persistent micro-world

Frozen topology:

```text
home -> fork -> opaque_site_A | opaque_site_B
```

Requirements:

- hidden regime is seeded and inaccessible to policy and renderer-derived policy
  inputs;
- drift schedule is preregistered and not clock-decodable;
- paired histories produce byte-identical decision observation hashes;
- paired histories require different oracle-optimal site actions;
- site outcome is revealed only after the visit;
- the generator, world transition and oracle evaluator are separated interfaces;
- oracle labels never enter reducer, memory, policy or baseline inputs;
- V2 state/database namespace is independent of V1.

## 13. Strongest baseline contract

All comparators are independent callable implementations and receive the same
legal observation/history/action channel:

1. Q-only;
2. observation-only;
3. cue/clock FSM;
4. last-success;
5. recency-window;
6. exact-history lookup;
7. count table;
8. transition table;
9. graph lookup;
10. episodic traversal;
11. from-scratch/no-update where applicable.

No baseline may call the candidate reducer or read candidate trace, stored
selected action, hidden regime, correct action, reward label, post-action state,
future outcome or expected verdict. A matching control baseline is a blocking
result for any learning/mechanism claim and must remain in the artifact set.

## 14. Required ablations and interventions

Each intervention reruns the real canonical episode from the same serialized
checkpoint and ordered command stream:

- `Memory_OFF`;
- `Freeze_Updates`;
- `Shuffle_Provenance`;
- `relevant_source_deletion`;
- `Q_only`;
- `from_scratch`.

No handwritten score, static verdict dictionary or stored-action comparison may
substitute for a rerun. At a preregistered checkpoint:

- Memory OFF or relevant-source deletion must change candidate ranking or selected
  action for a bounded positive result;
- Freeze Updates must prevent theta mutation and change a preregistered later
  prediction or score relative to the update-enabled run;
- Shuffle Provenance must operate on real source lineages and preserve all fields
  not named by the intervention;
- Q-only and from-scratch remain reported whether they pass, fail, or match.

## 15. Trace, replay and computed-evidence provenance

Replay must recompute, from serialized state plus ordered command:

```text
world observation
candidate scores
hard-gated legal set
selected action
world outcome
prediction error
theta update
competing-claim update
next canonical state
```

Stored selected action is comparison-only. Replay that reads it as behavior input
is invalid.

Every evidence-bearing report must record:

- `producer_function`;
- input artifact paths and hashes;
- `run_id`;
- seed, context and episode IDs;
- aggregation rule;
- code-path hash;
- baseline/ablation callable invocation records.

The leakage scanner must reject hidden-regime, correct-action, reward-label and
future-outcome leakage and must fire on an injected positive-control case.
Fresh replay must recompute behavior and four independent tamper controls must
fail closed.

## 16. Acceptance gate

All conditions are required for the bounded P1 result:

1. Paired histories have identical decision-observation hashes and different
   oracle-optimal actions.
2. The leakage scanner fires on injected hidden-regime/correct-action controls
   and reports no live-input offender.
3. Competing claims, support and provenance coexist without overwrite.
4. Prediction error changes theta when updates are enabled and not when frozen.
5. At a preregistered checkpoint, Memory OFF or relevant-source deletion changes
   candidate ranking or selected action.
6. Every baseline receives equal legal access and is reported even when it
   matches or beats the candidate.
7. Fresh replay recomputes behavior and four tamper controls fail closed.
8. A real Tk product trigger is attested from widget callback through atomic
   commit and post-commit redraw.
9. One reducer/world/update/replay path exists; no UI/test/verifier bypass is
   present.
10. All closed switches in Section 0 remain closed.

Acceptance permits only a computed statement of bounded tabular adaptation and
memory-conditioned selection in this frozen generated world. It does not permit
generalization or mechanism-non-equivalence language unless a separate future
science task establishes it.

## 17. Exact ordered implementation targets

The future machine authorization must expose this exact ordered nonempty
25-path list. Phase A does not yet make it effective.

1. `docs/codex/tasks/EGO-LIFE-KERNEL-V2-MICROWORLD-MEMORY-CAUSALITY-001A-MUTATION_SCOPE.yaml`
2. `labs/ego_life_playground_v0/__init__.py`
3. `labs/ego_life_playground_v0/engine.py`
4. `labs/ego_life_playground_v0/microworld.py`
5. `labs/ego_life_playground_v0/claims.py`
6. `labs/ego_life_playground_v0/app.py`
7. `labs/ego_life_playground_v0/store.py`
8. `scripts/run_ego_life_playground_v0.py`
9. `tests/test_ego_life_playground_v0.py`
10. `tests/test_ego_life_playground_v2_microworld.py`
11. `scripts/codex/verify_ego_life_kernel_v2_microworld.py`
12. `scripts/tests/test_verify_ego_life_kernel_v2_microworld.py`
13. `artifacts/EGO-LIFE-KERNEL-V2-MICROWORLD-MEMORY-CAUSALITY-001A/continuity.sqlite3`
14. `artifacts/EGO-LIFE-KERNEL-V2-MICROWORLD-MEMORY-CAUSALITY-001A/trace.jsonl`
15. `artifacts/EGO-LIFE-KERNEL-V2-MICROWORLD-MEMORY-CAUSALITY-001A/product_trigger_receipt.json`
16. `artifacts/EGO-LIFE-KERNEL-V2-MICROWORLD-MEMORY-CAUSALITY-001A/headroom_report.json`
17. `artifacts/EGO-LIFE-KERNEL-V2-MICROWORLD-MEMORY-CAUSALITY-001A/collision_record.json`
18. `artifacts/EGO-LIFE-KERNEL-V2-MICROWORLD-MEMORY-CAUSALITY-001A/baseline_comparison.json`
19. `artifacts/EGO-LIFE-KERNEL-V2-MICROWORLD-MEMORY-CAUSALITY-001A/ablation_report.json`
20. `artifacts/EGO-LIFE-KERNEL-V2-MICROWORLD-MEMORY-CAUSALITY-001A/learning_report.json`
21. `artifacts/EGO-LIFE-KERNEL-V2-MICROWORLD-MEMORY-CAUSALITY-001A/replay_report.json`
22. `artifacts/EGO-LIFE-KERNEL-V2-MICROWORLD-MEMORY-CAUSALITY-001A/leakage_report.json`
23. `artifacts/EGO-LIFE-KERNEL-V2-MICROWORLD-MEMORY-CAUSALITY-001A/failure_manifest.json`
24. `artifacts/EGO-LIFE-KERNEL-V2-MICROWORLD-MEMORY-CAUSALITY-001A/claim_ceiling.txt`
25. `artifacts/EGO-LIFE-KERNEL-V2-MICROWORLD-MEMORY-CAUSALITY-001A/result.json`

This card is read-only during implementation. Any missing, extra or reordered
target blocks execution and requires a new operator-approved Red transition.

## 18. Stop conditions

STOP without retuning or path expansion if any condition occurs:

- paired observation bytes differ;
- hidden regime, correct action, reward label or future outcome reaches a policy
  or baseline input;
- observation-only or cue/clock FSM saturates;
- memory deletion has no preregistered downstream effect;
- Freeze Updates has no preregistered prediction or score effect;
- competing claims are overwritten;
- `g_fixed` saturates at the clip bound and becomes a hidden actuator;
- any baseline lacks equal access;
- UI, test or verifier creates a second reducer/world/update path;
- replay reads stored selected action;
- a timer or proactive action becomes reachable;
- any threshold, drift schedule, split or schema changes after result inspection;
- enabled/default/mainline/runtime/science/remote status opens;
- LLM, network, EgoDesktop, EgoOperator, a new core or a new launcher is touched;
- a frozen seed/context/episode/counterfactual pair is unused;
- route/source pin, exact path set, Red receipt or dual-authority validation fails.

## 19. Rollback plan

- Keep all V1 artifacts and Git objects immutable.
- Use an independent V2 database/artifact namespace.
- Before commit, revert only task-owned P1 paths through a scoped patch; never
  discard unrelated work.
- After commit, any reversal is additive and separately authorized; no reset,
  amend or history rewrite.
- Preserve negative/equivalence artifacts.
- Rollback never opens or changes route/default/runtime/science fields.

## 20. Forbidden changes

- `EgoOperator/**`;
- `EgoDesktop/**`;
- `legacy/**`;
- existing V1 artifacts;
- old K0/Card2 routes or artifacts;
- ITL `artifacts/ROUTE-STATE-MACHINE-001A/program_state.json`;
- global ITL `STATUS.md` or `validation_report.json`;
- LLM, provider, network, service, subprocess or deployment integration;
- runtime/mainline enablement or registration;
- proactive/background dispatch;
- any path outside Section 17;
- push, tag or remote anchor.

## 21. Auto-Remote-Anchor and next action

`Auto-Remote-Anchor: forbidden`

The next action is P0 Phase-A Red review and four-path card bank, not P1 code.
After Phase A, ITL Phase B and Ego Phase C must establish and consume the exact
machine authorization before subagent/TDD implementation begins.

## 22. What this does not prove

This card does not prove implementation, enabled operation, mainline effect,
learning, transfer, associative memory, thinking, initiative, emotional
understanding, self-boundary, agency, autonomy, subjectivity, consciousness,
electronic life, EGO readiness, stable user benefit, or product value.

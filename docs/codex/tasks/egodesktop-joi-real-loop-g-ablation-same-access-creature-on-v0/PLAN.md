# EgoDesktop Joi Real-Loop G-ABLATION Same-Access + CREATURE_ON v0 Plan

1. Draft the 010 task card, mutation scope, status, and task-board entry.
2. Preserve Claude's source-limited `BLOCKING_FINDINGS` review and repair the card at the card layer:
   - add equivalence boundary / power / minimum-n / TOST or equivalent gate for any saturation claim;
   - add high-capacity full-public-history steelman baseline or low-order Markov upper-bound proof requirement;
   - split preregistration into a hash-frozen, independently reviewed gate before any `CREATURE_ON` capture;
   - add mechanism-presence, semantic near-duplicate leakage, independence-scope, and complete verdict-matrix repairs.
3. Run docs-only checks:
   - YAML parse for `Tasks/TASK_BOARD.yaml` and 010 `MUTATION_SCOPE.yaml`;
   - route-convergence view generation if needed;
   - `git diff --check`.
4. Send the repaired card to desktop Claude for source-limited re-review.
5. Require Claude to return exactly one of:
   - `NO_BLOCKING_FINDINGS`, with next minimal action;
   - `BLOCKING_FINDINGS`, with numbered blockers, required repairs, and next minimal action.
6. Repair remaining card-level blockers before any implementation.
7. Only after card review is blocker-free, create a separate preregistration-manifest slice for:
   - predeclared threshold and baseline-battery manifests;
   - prompt split manifest;
   - equivalence protocol with epsilon, power, MDE, minimum n, and TOST or equivalent rule;
   - full-public-history steelman baseline or low-order Markov proof;
   - mechanism-presence and semantic near-duplicate controls;
   - outcome-blind verdict matrix.
8. Send the frozen preregistration manifest to Claude for independent source-limited review.
9. Only after preregistration review is blocker-free, open a separate implementation/capture slice for:
   - `CREATURE_ON` real UI capture through the existing 006 tap/writer;
   - independent same-access callable controllers;
   - evaluator/scoring gate with baseline saturation semantics.

## Non-Goals

- No implementation in this card slice.
- No preregistration-manifest acceptance without separate review.
- No `CREATURE_ON` row capture.
- No baseline scoring, threshold execution, comparison, or verdict.
- No default EgoDesktop runtime enablement.
- No program-state or evidence-ledger update.
- No push, tag, remote-anchor, product/readiness, agency, emotion, subjectivity, consciousness, alive-status, or Bar-2
  claim.

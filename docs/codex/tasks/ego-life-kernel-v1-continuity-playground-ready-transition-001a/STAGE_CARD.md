# EGO-LIFE-KERNEL-V1-CONTINUITY-PLAYGROUND-READY-TRANSITION-001A — Phase C Ego authority transcription

Status: `OPERATOR_AUTHORIZED__ITL_READY_COMMITTED__EGO_TRANSCRIPTION_CANDIDATE`

Auto-Remote-Anchor: `forbidden`

## Operator authorization

The operator authorized this transition to repair the V1 contract blockers,
establish machine-readable implementation authorization in both repositories,
keep every runtime/science/publication switch closed, obtain a Claude Web
staged-hash Red pre-check, commit locally, and only then implement V1 through
subagents.

ITL Phase B is committed at
`81f19613fbd02bb845c08258cc8156283240aa2e`, a direct child of
`619bff5fd9400bba00002af26f65ce73894a9dce`. This Phase C task consumes only:

```text
sync_EGO-LIFE-KERNEL-V1-CONTINUITY-PLAYGROUND-001A_ready_authority_under_separate_task
```

Ego transcribes the committed machine authority; it does not synthesize a
route or reinterpret the handoff.

## Problem definition

Ego still records the V0 adoption boundary and exposes only V1 task-card
drafting. ITL now records an additive V1 descendant at `READY_TO_IMPLEMENT`,
with conditional implementation authorization for an exact ordered 20-path
product allowlist, while both repositories still block execution until an exact
Ego transcription is callable and validated. Phase C consumes and transcribes
that conditional authority; it does not repair a cross-repo execution verdict.

The real objective is to close that control-plane split without touching
product code or accidentally enabling the V1 runtime. The transition must also
remove stale current-reader claims that the superseded 8692/EgoDesktop route is
the selected successor.

## Current layer

Layer 2 — engineering implementation and cross-repository evidence hygiene.

This task authorizes a future product implementation; it does not implement or
test the V1 mechanism hypothesis.

## Mainline target

- Product-development lineage: the V1 descendant of the sole
  `ego_life_playground_v0` lineage.
- Product implementation surface: the exact ordered 20 targets transcribed
  from committed ITL state.
- Ego runtime mainline: `none / forbidden` for V1.
- Active default runtime: EgoOperator remains unchanged and true.
- EgoDesktop: archived legacy reference only.

## Enabled-state requirement

For V1, all of the following remain exact:

```text
enabled=false
default_enabled=false
mainline_connected=false
runtime_mainline_connected=false
runtime_authority=none
science_weight=0
remote_anchor=false
auto_remote_anchor=forbidden
```

No service, launcher autostart, runtime registry, EgoOperator/EgoDesktop source,
LLM, network, subprocess, science successor, scoring, or experiment execution
may be introduced.

## Real-trigger evidence requirement

Three fields remain separate and non-interchangeable:

```text
ITL READY-transition trigger: UNVERIFIED_IN_THIS_ITL_TRANSITION
Ego V0 local product trigger: BANKED_RECOMPUTING_PRODUCT_TRIGGER
V1 local product trigger: UNVERIFIED_IN_THIS_EGO_PHASE_C_TRANSITION
```

Phase C produces governance readback only. It cannot create a V1 trigger
receipt because V1 product code has not yet been implemented.

## Hypothesis

If Ego pins the exact committed ITL Phase-B objects, exhaustively transcribes
every product-axis and V1-state leaf, consumes the sync action, exposes only the
committed exact target set, and rejects every permission/trigger/pointer drift,
then V1 implementation can begin later without a second route authority or a
hidden runtime enablement.

## Strongest baseline / shortcut explanation

The strongest shortcut is a local prose or YAML edit that sets
`implementation_authorized=true` and copies the 20 paths while leaving source
objects, omitted leaves, stale reader pointers, trigger conflation, or receipt
binding unchecked. That shortcut can look READY without proving that Ego and
the committed ITL authority agree.

## Strongest reason this task may be invalid

This is control-plane work, not product behavior. If the callable source pins,
exhaustive crosswalk, and fail-closed mutations cannot distinguish the chosen
approach from the shortcut baseline, this transition adds governance theater
and must stop rather than authorize implementation.

## Framing falsifier

STOP if committed ITL does not expose the exact sync action as executable, if
the target list is empty/duplicated/reordered, if any V1 runtime/science flag is
open, if the V0 lineage root is rewritten, or if the Ego representation cannot
transcribe all 213 source leaves without inventing semantics.

## Evidence still insufficient

Green governance tests, a committed crosswalk, generated docs, or a Red-review
receipt remain insufficient to prove continuity, learning, memory causality,
prediction quality, replay causality, controlled initiative, agency, or product
benefit. Those require the separately authorized V1 implementation and callable
product evidence.

## Ablation / mutation requirement

Callable tests must reject at least:

- omitted, extra, duplicate, non-verbatim, or retargeted source leaves;
- wrong ITL commit, object OID, payload SHA-256, event/report/card/scope/receipt;
- target omission, insertion, reordering, duplication, or path substitution;
- implementation authorization false after sync or an action surface that still
  exposes only drafting;
- premature implementation before exact Phase-C state;
- enabled/default/mainline/runtime/science/remote drift;
- trigger conflation or a claimed V1 trigger;
- EgoOperator default demotion or EgoDesktop/current 8692 pointer resurrection;
- a missing, self-reviewed, wrong-base, wrong-path, blocking, or byte-drifted
  Phase-C Red receipt.

The leaf-omission scanner must carry a positive control and report its observed
rejection code.

## Trace / replay requirement

No product trace/replay is generated in Phase C. The governance replay analog
is deterministic callable recomputation of:

1. committed Git object pins;
2. the 213-leaf crosswalk;
3. the exact ordered target list;
4. the route fingerprint;
5. the staged/committed Red-review manifest and diff binding.

The future V1 product trace/replay contract remains frozen in
`docs/codex/tasks/EGO-LIFE-KERNEL-V1-CONTINUITY-PLAYGROUND-001A.md`.

## Computed-evidence provenance gate

Source evidence must be read with `git rev-parse` and `git cat-file` from ITL
commit `81f19613...`. The crosswalk and route fingerprint must be callable
outputs. The receipt validator must recompute raw staged/committed Git blob and
diff hashes. Reports record producer function, input artifacts, run ID,
aggregation rule, and code-path hash. Static pass dictionaries or prose-only
equivalence are not evidence.

The exact committed source pins are frozen here:

| source | Git blob OID | raw payload SHA-256 | bytes |
|---|---|---|---:|
| product axis | `6bc24fbfd842f9a11df28b2abf6269a810c1d3f4` | `7c8ce4ebd7a97e514fd0253f2bba2c0c78d3c96ad814256ed8c627c265ca353d` | 3479 |
| V1 state | `4e22afb9e6ecd4daaee8970ef25f169c5a7becb4` | `2b4e1f0cd24b89eef7e1608002c30d3a1e61b0251213892819e5f2ac160820cc` | 10469 |
| V1 event | `3eb00d15d84638c7fc51f9827c719230450c5c90` | `b6e732718cef553407db4ab5aef4e3b64f6d6d0dc5b500a5c44630de6be9363a` | 2525 |
| V1 validation report | `6c5f217ac1a4160e15c51b8c8a936fadf569a0b2` | `6dec14dff208e7d64c26928b6eb56f9eba7599a622abee3aa7c02e2da5af3c56` | 16668 |
| ITL transition card | `2cc5fb7f947a839aec3994a6cd8200c50abc33f1` | `0eef8be47f6c3a0dd64c6fc87ced9cf23923acefd337e62de501f918e9d03fc5` | 17086 |
| ITL mutation scope | `2df99fea3cd4b30b59dd12d3ba948b3f13872121` | `3e3c060a020f447d6e81ab4352f553e01524ff0aeb25f8349955fc13b4512950` | 2768 |
| ITL Red receipt | `9d60a47bda9c6f538a3d47c9d089c82d009f11ee` | `7a3a49f15207d2e71676da6be7dfff7f6d27006ee46324fd7654e74189117b79` | 10758 |

## Acceptance gate

1. Ego remains at direct parent `90f723cf53c8ff3b7f3ad6eba29a4f5ec6e9238a`
   until this exact transition commit.
2. ITL commit `81f19613...` and all seven source objects match exact blob OIDs,
   raw payload SHA-256 values, and bytes.
3. All 50 product-axis leaves and 163 V1-state leaves are transcribed verbatim
   with no omission or extra leaf; omission positive control fires.
4. The sync action is consumed and Ego exposes exactly:
   `implement_EGO-LIFE-KERNEL-V1-CONTINUITY-PLAYGROUND-001A` and
   `run_route_state_machine_validation`.
5. Implementation is authorized only for the exact ordered nonempty 20-path
   list; no unlisted path is authorized.
6. V1 enablement, default, mainline, runtime authority, science weight, and
   remote anchor remain closed; EgoOperator remains the active default.
7. Three trigger fields remain separate and V1 trigger stays unverified.
8. Current-reader surfaces additively mark the 8692/EgoDesktop successor prose
   as superseded/archive-only and point to machine route authority.
9. Program-state integrity, route convergence, mainline clarity, focused guard
   tests, `git diff --check`, and exact-path mutation scope pass.
10. Claude Web reviews the exact 19-path staged non-receipt candidate and
    returns `NO_BLOCKING_FINDINGS`; the receipt is then added as path 20.
11. A local exact-20-path direct-child commit is created. No push, tag, or
    remote anchor occurs.

## Claim ceiling

> Local Ego transcription of committed ITL V1 implementation authorization for
> one exact default-off product-development descendant, with no V1 trigger,
> runtime/mainline effect, science authority, or product acceptance evidence.

## Stop condition

STOP before receipt/commit if any source pin, crosswalk, action, target, trigger,
pointer, generated view, mutation, review, fingerprint, runtime/science
firewall, or exact path-set check fails. Do not repair or reopen V0/Card2/K0,
and do not start product implementation inside Phase C.

## Rollback plan

Before commit, remove only the newly created Phase-C task artifacts and revert
only Phase-C edits through a new patch; never reset/checkout unrelated user
work. After commit, any correction requires a new additive authorized task and
review; do not amend or rewrite the authority boundary.

## Expected changed files

Exactly 20 paths: 19 reviewed non-receipt paths plus the Red receipt.

1. `docs/codex/tasks/ego-life-kernel-v1-continuity-playground-ready-transition-001a/STAGE_CARD.md`
2. `docs/codex/tasks/ego-life-kernel-v1-continuity-playground-ready-transition-001a/COLLISION_RECORD.md`
3. `docs/codex/tasks/ego-life-kernel-v1-continuity-playground-ready-transition-001a/MUTATION_SCOPE_PHASE_C.yaml`
4. `docs/codex/tasks/ego-life-kernel-v1-continuity-playground-ready-transition-001a/ITL_AUTHORITY_CROSSWALK.json`
5. `docs/codex/tasks/ego-life-kernel-v1-continuity-playground-ready-transition-001a/PHASE_C_RED_REVIEW.json`
6. `docs/PROGRAM_STATE_UNIFIED.yaml`
7. `docs/STATUS.md`
8. `artifacts/reports/program_state_summary.md`
9. `docs/codex/tasks/TASK_LANE_INDEX.md`
10. `docs/REPO_SURFACE_MAP.md`
11. `AGENTS.md`
12. `README.md`
13. `docs/ACTIVE_CONTEXT_PACK.md`
14. `docs/MAINLINE_QUICKSTART.md`
15. `docs/EGO_CANONICAL_MECHANISM_INTEGRATION_ROUTE_001A.md`
16. `docs/codex/tasks/ego-canonical-mechanism-integration-001a/STATUS.md`
17. `scripts/codex_session_guard.py`
18. `scripts/codex/verify_route_convergence.py`
19. `scripts/tests/test_codex_session_guard.py`
20. `scripts/tests/test_route_governance_supersession.py`

## Forbidden changes

- every V1 product implementation/evidence target from the 20-path product
  allowlist;
- all `EgoOperator/` and `EgoDesktop/` source files;
- LLM, network, service, subprocess, runtime registry, deployment, secrets;
- ITL files or frozen V0/Card2/K0 route objects;
- experiment execution, scoring, science successor, runtime/mainline enablement;
- push, tag, remote anchor, amend, reset, or history rewrite.

## Auto-Remote-Anchor decision

`Auto-Remote-Anchor: forbidden`

## Next minimal closed-loop action

After the exact Phase-C commit and post-commit callable validation pass, start
the separately bounded V1 implementation through subagents and TDD, changing
only the authorized 20 product paths.

## What this does not prove

This transition does not prove that V1 is implemented, triggered, playable,
continuous, learning, memory-conditioned, replay-causal, mechanism-valid,
agentic, autonomous, subjective, conscious, electronic life, ready for runtime,
or beneficial to users.

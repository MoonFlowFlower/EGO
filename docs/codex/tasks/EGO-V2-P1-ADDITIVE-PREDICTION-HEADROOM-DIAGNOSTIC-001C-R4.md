# EGO-V2-P1-ADDITIVE-PREDICTION-HEADROOM-DIAGNOSTIC-001C-R4

## Bounded task card

- **Problem definition:** R2 and R3 both stopped at the product recovery/trace
  boundary before the already-frozen balanced evaluator ran.  Repeating
  planner micro-optimizations would continue measuring engineering latency
  without answering the more important unknown: whether the identified
  additive outcome-delta learner has any development-context prediction
  difference under the old controls.  This card separates that diagnostic from the `10s` and
  `32KiB` product SLA without changing or upgrading either failed result.
- **Prior negative evidence:** preserve
  `PRODUCT_SURVIVAL_LEARNING_NOT_OBSERVED`,
  `PRODUCT_PREDICTIVE_CONTROL_NOT_OBSERVED`, R1 and R2
  `BLOCKED_BOUNDARY_OR_REPLAY_REGRESSION`, and R3
  `INVALID_FORMAL_GATE / BANKABLE_BOUNDED_NEGATIVE_DIAGNOSTIC`.  R3 behavior
  and result are not R4 execution authority; its archived tested bundle exists
  only as the byte-provenance carrier used by the explicit reverse recipe
  below.  No prior result or artifact may be rewritten.
- **Layer:** bounded learning/adaptation prediction-difference measurement;
  no product capability or route-state transition; `science_weight=0`.
- **Current stage:** docs/evaluator-only successor whose parent boundary is local
  commit `b4985c1e2a65db48a26d70e121527bbdfa43cf3d` on branch
  `codex/ego-v2-additive-prediction-headroom-001c-r4`.
- **Hypothesis:** on the already-consumed R2 worlds 52/54, policy seed 711,
  the learned identified additive model will have lower late macro organism-
  delta MAE than its early phase, zero-initialized no-update control, and
  callable legacy-unconditional learner, with all declared outcome strata
  estimable and conditioning-sensitive ablations.
- **Why this reframe is allowed:** recovery latency and trace size are product
  engineering properties, not evidence that a learning mechanism does or does
  not improve prediction.  This diagnostic cannot repair R2 eligibility or
  authorize a fresh effect card; it only decides whether further engineering
  investment has a measured prediction difference worth challenging with
  stronger cheap baselines.
- **Lineage discontinuity and frozen source authority:** the R2 formal SQLite
  files were produced from a mixed-EOL working snapshot that was not preserved
  byte-identically by the later Git commit.  This is a provenance defect, not
  permission to use the current checkout.  A pure Git-object materialization
  computes engine code-path hash
  `8145a28f187c9063e708a88f1c3daf01b207352171bd41936052dd483be17647`,
  while both banked R2 databases require
  `b6eadf0c8ba7244ea08cd67eb936438fda182f43e56c65918bde8264fd5b9d29`.
  The successor therefore has an explicit lineage discontinuity and may use
  only this independently checkable reconstruction recipe:
  - provenance carrier
    `artifacts/EGO-V2-P1-ADDITIVE-PLANNER-BOUNDARY-001C-R3/tested_implementation_bundle.zip`,
    SHA-256
    `947f66d9bbc91e753d1f073b8c2b372b6e683c8ea814aeb9c995143a48c1104b`;
  - reverse-delta authority
    `artifacts/EGO-V2-P1-ADDITIVE-PLANNER-BOUNDARY-001C-R3/tested_tracked_diff.patch`,
    SHA-256
    `0fc421734dd183ea0281f4e7ed5722e9db0a488364c7b4058ebfaf475401d550`;
  - reverse only the R3 changes to `engine.py` and
    `predictive_control.py` while preserving unchanged bytes and EOLs;
  - required reconstructed `engine.py` SHA-256
    `54190c093a0e0797bbc690c899231ad6b178092ecd4741ce10a925a2ccfe55e5`;
  - required reconstructed `predictive_control.py` SHA-256
    `938a4715726d0dacfe0262e2fbd3edae050d22d26e50710935853e36def82edd`;
  - required imported runtime `engine.compute_code_path_hash()`
    `b6eadf0c8ba7244ea08cd67eb936438fda182f43e56c65918bde8264fd5b9d29`.
  The R2 evaluator semantics remain frozen at commit
  `363f6d49cbd54524ce283e7580e23c45ada4b532`; its verifier blob must be
  `427f4453b682299774af45813f86645ba4ea0e93`.  The unchanged boundary helper
  must be materialized directly from that same commit at blob
  `d336be083e20c3fbf8586201397e8fb51a974a57` and must have SHA-256
  `b6e3ec342580821a0f43053f6c9baf96540ec6f8ae5c2239f99c8d56233bb321`.
  Its byte identity with the R3 bundle is corroboration, not execution
  authority.
  Every imported source path and SHA must be reported.  Failure of any step is
  `BLOCKED_PROVENANCE_OR_RECOMPUTATION`; no semantic or EOL approximation is
  allowed.
- **Frozen input authority:** read and verify these R2 inputs, then copy only
  the three execution inputs into an isolated temporary packet; never write
  into the R2 packet and do not duplicate the databases into final artifacts:
  - `smoke_result.json`: SHA-256
    `846981624e5dd74cd784f3531aa6c85375902c5b3094cfb2905ff8631e4c084a`;
  - `smoke_p0_cross_v1.sqlite3`: SHA-256
    `62ab24d298f0196c7dd752ea124319fdff17539e9cf51d0287a3ea8f97078354`;
  - `smoke_p2_vertical_v1.sqlite3`: SHA-256
    `d57476cad0e72054e1d8e215f7b05399e0e3a2c474365a1c3457ac5c52b81dc4`;
  - R2 `result.json`: SHA-256
    `53e70f8fe8384c38fe65c953e4bb88fe9bc1bb6e2d91486147b91bc9c7379945`.
  Original authority databases must be inspected with an immutable read-only
  URI.  The byte-identical temporary execution copies may be opened by the
  unmodified frozen R2 `SQLiteEventStore`; do not monkeypatch its connection
  semantics.
- **Real path/evaluator:** run the frozen R2 callable `run_balanced` inside the
  reconstructed-and-hash-verified runtime snapshot and an isolated temporary
  packet.  It reconstructs
  controller/engine states from SQLite,
  predicts before intervention, and then computes private counterfactual truth
  by forcing each public action through `transition_world` and metabolism.
  It also runs the two predeclared frozen-update controller controls on the
  same old worlds 52/54 in the temporary packet.  Here "fresh subprocess"
  means a new OS process over those old contexts, not a fresh world/policy
  seed.  Do not implement a new predictor, world, action, or metric path.
- **Baseline:** the frozen evaluator's independently callable no-update and
  legacy-unconditional learners on the same snapshots.  R1 full interaction
  remains a historical failure/performance reference, not a new computed
  positive baseline.
- **Ablation:** frozen base-only/residual-off, residual-only, deterministic
  outcome-rotation, and two frozen-update controls.  Leakage scanning retains
  its real positive controls.
- **Strong-baseline boundary:** action-only online delta mean,
  action-by-visible-front-token lookup, binary-front-object, token-table/graph,
  action-only metabolism, and viability-shield challengers are mandatory
  before any mechanism-headroom, survival-effect, or fresh-card claim.  Stage
  A deliberately preserves the old frozen verdict instead of adding these
  baselines post hoc.  A positive R4 result means only a development prediction difference
  under the old controls and authorizes at most a separately preregistered
  cheap-challenger admission card, never fresh seeds directly.
- **Trace/replay requirement:** the frozen evaluator must produce identical
  balanced digests in-process and in a fresh subprocess.  Store every
  prediction/truth row as JSONL.  A new independent reducer must recompute
  Brier, NLL, delta MAE, context/action macro aggregation, outcome-stratified
  support, legacy/ablation summaries, training support/rank, and the frozen
  `snapshot_hashes`, `prediction_rows_hash`, `truth_rows_hash`,
  `aggregate_hash`, and `payload_hash` from rows without trusting stored
  aggregates or the evaluator's replay boolean.  It must also independently
  derive every frozen check boolean from recomputed metrics/support, leakage
  findings, frozen-control receipts, and row coverage, then require exact
  equality with the evaluator check map.  Duplicate, missing, or
  unpaired model rows invalidate the instrument.  This independently validates
  serialized rows, pairing, metrics, and digests; it does not independently
  reimplement the row producer, so a same-evaluator fresh subprocess is not an
  external or algorithmically independent replication.
- **Support/rank gate:** preserve the R2 floor of 16 for every declared
  realizable action/outcome stratum in training and evaluation, plus full
  15-feature shared-base rank for every action.  Insufficient support is
  `INSUFFICIENT_OUTCOME_SUPPORT`, not evidence against the model.  Singular
  values and condition numbers are disclosure-only because full rank does not
  establish numerical stability; they do not alter the frozen verdict.
- **Fresh firewall:** only actual SQLite layouts `p0_cross_v1` / `p2_vertical_v1`,
  world seeds 52/54, and policy seed 711 are allowed.  Worlds 60--65 and policy
  seeds 721/722 are forbidden.  The outer verifier must open the original
  authority SQLite files with a read-only immutable URI and read actual run
  metadata/initial world state, not filenames.  The frozen evaluator receives
  and opens only the isolated byte copies described above.
- **TDD contract:** RED tests first for source/blob pin failure, artifact hash
  drift, exact runtime reconstruction/code-path recovery, actual DB
  layout/seed/context drift, old-artifact mutation, canonical-output/nonempty
  no-overwrite, complete frozen-check-set consistency, independent row/digest/
  legacy/ablation/support recomputation mismatch, required-section deletion,
  check-truth tampering, mid-write transactional failure, and verdict priority.  Tests
  must include positive-control contaminated copies and may not execute
  formal/controller/fresh worlds.
- **Instrument-validity contract:** the exact frozen check-key set is an input
  contract.  `failed_checks` must equal the set of false checks and `passed`
  must equal `all(checks.values())`.  Coverage, forced-truth, leakage,
  replay/digest, frozen-control, producer, provenance, or check-contract
  failure is `BLOCKED_PROVENANCE_OR_RECOMPUTATION`; support/rank failure is
  `INSUFFICIENT_OUTCOME_SUPPORT`; only valid-instrument prediction-effect or
  conditioning-ablation failure may be
  `ADDITIVE_NO_DEVELOPMENT_PREDICTION_DIFFERENCE`.
  The exact categories are frozen as follows:
  - validity: `all_five_action_counts_exactly_equal`,
    `no_snapshot_action_context_unused`,
    `forced_action_truth_exists_for_every_balanced_row`,
    `leakage_clean_and_all_positive_controls_detected`,
    `fresh_subprocess_balanced_recompute_exact`,
    `two_frozen_update_controls_pass`;
  - support: `training_support_at_least_16_per_declared_stratum`,
    `shared_base_feature_rank_is_full_for_every_action`,
    `all_effect_strata_have_at_least_16_rows_per_phase`;
  - prediction/conditioning: `learned_late_brier_improves_by_at_least_0_02`,
    `learned_late_nll_improves_by_at_least_0_05`,
    `learned_late_brier_below_no_update_late`,
    `learned_late_nll_below_no_update_late`,
    `learned_late_macro_delta_mae_below_early`,
    `learned_late_macro_delta_mae_below_no_update_late`,
    `learned_late_macro_delta_mae_below_legacy_unconditional`,
    `interact_strata_not_worse_than_no_update`,
    `interact_strata_not_worse_than_legacy_unconditional`,
    `base_only_ablation_changes_late_macro_delta`,
    `residual_only_ablation_changes_late_macro_delta`, and
    `outcome_rotation_changes_late_macro_delta`.
- **Pre-run provenance gate:** before the one formal cycle, locally freeze the
  card, collision record, verifier, focused tests, Python/NumPy versions,
  source-reconstruction manifest, input hashes, and exact canonical output
  path.  The R2 artifact tree manifest must be captured before any SQLite open
  or copy and remain byte-identical afterward.
- **Acceptance:** exactly one frozen prediction-difference cycle.  A positive Stage-A
  verdict is allowed only if the reused R2 `run_balanced` reports every frozen
  check true and the independent row-level recomputation is exact.  It is not
  a mechanism-headroom verdict until a separate cheap-challenger stage passes.
  `fresh_effect_seeds_consumed=false` and
  `eligible_for_fresh_effect_card=false` for every verdict.
- **Verdicts:**
  - `ADDITIVE_DEVELOPMENT_PREDICTION_DIFFERENCE_UNDER_FROZEN_R2_CONTROLS`;
  - `ADDITIVE_NO_DEVELOPMENT_PREDICTION_DIFFERENCE`;
  - `INSUFFICIENT_OUTCOME_SUPPORT`;
  - `BLOCKED_PROVENANCE_OR_RECOMPUTATION`.
- **Stop condition:** stop after the single cycle.  Do not tune learning rate,
  support floor, thresholds, metrics, seeds, features, rank rule, model form,
  horizon, beam, or source snapshot.  Do not consume fresh seeds.  Insufficient
  support closes only this adjudication surface as inadmissible.  A valid-
  instrument prediction/conditioning failure closes only the frozen R2
  additive candidate on worlds 52/54 before further performance work; it does
  not falsify the additive model family.
- **Canonical output:** exactly
  `artifacts/EGO-V2-P1-ADDITIVE-PREDICTION-HEADROOM-DIAGNOSTIC-001C-R4/`.
  A wrong path or a nonempty existing path must be rejected with zero writes;
  a blocked packet may be written only to a new/empty canonical directory.
  Both success and blocked packets must be fully written and hashed in a
  same-filesystem staging directory, then atomically renamed into the canonical
  path.  `artifact_manifest.json` must hash every final file except itself.
- **Rollback:** remove only uncommitted files under the exact R4 card/collision,
  verifier/test, and canonical artifact paths named below; never modify R2/R3
  source, commits, DBs, artifacts, or verdicts.
- **Allowed files:**
  - `docs/codex/tasks/EGO-V2-P1-ADDITIVE-PREDICTION-HEADROOM-DIAGNOSTIC-001C-R4.md`;
  - `docs/codex/tasks/ego-v2-p1-additive-prediction-headroom-diagnostic-001c-r4/COLLISION_RECORD.md`;
  - `scripts/codex/verify_ego_v2_additive_prediction_headroom_diagnostic_001c_r4.py`;
  - `scripts/codex/tests/test_verify_ego_v2_additive_prediction_headroom_diagnostic_001c_r4.py`;
  - `artifacts/EGO-V2-P1-ADDITIVE-PREDICTION-HEADROOM-DIAGNOSTIC-001C-R4/`.
- **Forbidden:** `labs/`, controller/store/world/metabolism/action/UI/source
  changes, route state, historical artifacts, R2/R3 result mutation,
  checkpoint/stored-plan replay, fresh seeds, network/LLM, push or tag.
- **Claim ceiling:** at most an outcome-stratified prediction difference or
  failure on already-consumed development contexts relative to the old frozen
  R2 controls.  Before Stage B cheap challengers, do not call a positive result
  mechanism headroom.  It does not rule out observation-decodable or cheap-
  baseline equivalence.  No held-out adaptation, survival effect, product
  readiness, agency, AGI, consciousness, or electronic-life claim.
- **Auto-Remote-Anchor:** forbidden.

# Collision record

The three evaluated candidates are: (A) hand-edit active docs, (B) delete old
trees without converging authority, and (C) exact ITL mirror + generated views +
manifest/tag retirement + live V2 trigger verification. A and B are matched by
cheap appearance-only baselines and fail drift/recovery tests. Candidate C is
selected, with the production stale scanner and pinned-Git-object verifier as
mandatory positive-control paths.

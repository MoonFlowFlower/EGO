# EGO-PHASE-C-V2-AUTHORIZATION-GATE-SIMPLIFICATION-001A

Status: `OPERATOR_AUTHORIZED__CANDIDATE_BYTES_FROZEN_FOR_ONE_REVIEW`

Route decision: `REPLACE__SIMPLIFY_CURRENT_PHASE_C_AUTHORIZATION_GATE`

Auto-Remote-Anchor: `forbidden`

## Problem definition

The stopped Phase-C candidate accumulated a 373-leaf comparator, recursive
authority discovery, natural-language verdict parsing, and expanding Git
worktree/filter/replace-ref exceptions. Those checks became a second route
system and repeatedly produced governance-only repair work without V2 product
effect. This transition replaces that line rather than repairing it.

## Current layer and lane

- Layer: Layer 2 engineering/control-plane evidence hygiene.
- Lane: product-development authorization; no product source is changed here.
- Mainline target: none; product runtime/mainline wiring is forbidden.
- Enabled-state requirement: every V2 runtime/science/remote/proactive/LLM/
  network switch remains closed.
- Real-trigger requirement: none; this transition creates no V2 trigger.
- Claim ceiling: local commit-boundary and typed-admission verification only.

## Real objective

Create one finite local boundary that admits development on the exact ordered
25-path V2 allowlist after, and only after, one exact direct-child 20-path
authority commit carries one controller-attested typed review receipt.

## Hypothesis

One immutable Git-object boundary, one strict JSON/canonical-byte projection,
one typed receipt, and one actual-Git-path mutation classifier are sufficient to
recompute the bounded authorization fact without an open semantic comparator.

## Strongest baseline / bypass

The strongest cheap bypass relabels a V2 mutation as route validation, reorders
the target list, opens one switch, exploits duplicate keys/type coercion/signed
zero, supplies a positive prose ending beside a negative structured verdict, or
declares paths that differ from Git's actual changed paths.

## Four primitives only

1. The authority commit is the exact direct child of
   `089ab5ef27431eb5ace4ff4795f57f08fd052779` and its parent-to-commit
   `git diff-tree` path set is exactly the 19 reviewed paths plus the receipt.
2. The pinned ITL state at
   `4a4746435f9bdb0eba02e6c036c2e34a089ece26` and the committed authority
   envelope use the same strict JSON parser and one canonical-byte projection.
3. The receipt is fixed typed JSON. Contradictory verdicts, unknown keys,
   malformed containers, numeric/empty identity fields, and identity inflation
   fail structurally without exception.
4. Mutation admission derives precommit paths from staged + unstaged + untracked
   Git paths, and postcommit paths from parent-to-commit `git diff-tree`, before
   reading task/action labels.

## Authority boundary

- `D:/Project/AIProject/MyProject/Ego` at the base commit remains
  `NON_LIVE_NEGATIVE_ANTI_ZENO_CHECKPOINT`; it is not a live authority and is
  never reset, checked out, reverted, staged, or committed by this task.
- `codex/ego-v2-product-first-001a` in
  `D:/Project/AIProject/MyProject/Ego-v2-product-first-001a` becomes the sole
  active V2 development authority only when this exact direct-child commit
  passes the postcommit gate.
- EgoOperator remains the existing runtime default. This product-development
  authority does not enable or replace runtime.

## Ablation / hostile controls

The fixed controls are: wrong/intermediate parent; missing/extra commit path;
swapped V2 paths; one opened switch; duplicate JSON key; wrong JSON type;
`0.0` versus `-0.0`; validation-labelled product mutation; omitted receipt;
contradictory typed verdict; numeric/empty reviewer or session; malformed
container; one valid authority commit; and validation with zero product paths.
No new governance witness may expand this set.

## Trace/replay and computed provenance

The replay unit is the immutable Ego authority commit plus the pinned ITL Git
object. The callable result records base/authority commit, actual changed paths,
19-blob manifest hash, source/target projection hashes, and typed verdict.
Committed bytes are read from Git objects. No static pass literal is evidence.

## Acceptance gate

All must pass on the first bounded attempt:

1. exact isolated base and branch;
2. exact 19 reviewed nonreceipt paths and one controller-owned receipt path;
3. one focused test command;
4. one full regression command;
5. one independent Claude Red review returning the typed receipt;
6. one exact direct-child local commit and one postcommit readback;
7. exact ordered 25 paths, canonical projection equality, all switches closed;
8. no push, tag, or remote anchor.

## Hard budget and stop condition

Only one focused test, one full regression, one independent review, and one
postcommit readback are allowed. Any failure, false-pass, blocking finding, new
governance-only bypass, malformed-input exception, or need for a second repair,
rerun, receipt rewrite, or review closes the transition as:

`CLOSED_ANTI_ZENO__PHASE_C_AUTHORIZATION_NOT_ESTABLISHED`

## Rollback

Before commit, stop and preserve this isolated candidate for operator decision;
never touch the negative checkpoint. After commit, do not reset, amend, or
rewrite. A failed postcommit gate preserves the commit as negative evidence and
closes the transition.

## Expected changed files

Exactly the 19 nonreceipt paths and one receipt path declared in
`MUTATION_SCOPE_PHASE_C.yaml`. The receipt is controller-owned and absent from
the implementer candidate.

## Forbidden changes

Every path outside the exact 20; every V2 product path; EgoOperator;
EgoDesktop; legacy; route-state writes in ITL; runtime/mainline enablement;
science execution; LLM/network; proactive/background/external dispatch;
push/tag/anchor; and any second validator, helper, schema, test, receipt, or
governance witness path.

## Non-proof

Even a passing commit proves only compliance with this finite local
authorization boundary. It does not prove V2 implementation, trigger, learning,
memory causality, initiative, agency, emotion understanding, subjectivity,
consciousness, electronic life, readiness, or user benefit.

# EGO-ROUTE-8692-SUPERSESSION-AND-EGODESKTOP-AUTHORITY-ARCHIVE-001A

Status: `PHASE_A_TASK_CARD_BANK_AUTHORIZED__PHASE_B_REQUIRES_INDEPENDENT_RED_REVIEW`

Auto-Remote-Anchor: forbidden

## Problem definition

The current Ego product/capability authority incorrectly freezes
`K0 Foundation -> VirtualCatPSPC-derived adapter -> EgoDesktop` as the sole
successor and names M1 as executable next work. This card performs an additive
authority correction: supersede that route before M1, archive EgoDesktop's
future-route authority without moving or deleting source, preserve bounded K0
Foundation engineering evidence, register no science successor, and bind the
only next action to banking Card 2
`EGO-PET-WORLD-V1-CAPABILITY-HEADROOM-001A` without execution authority.

## Current layer

Layer 2: engineering governance / authority correction only. This is not a
runtime, mechanism, learning/adaptation, experiment, or subject-validation task.

## Mainline target

Product/capability lane governance only. `EgoOperator` remains the sole active
default runtime. No product runtime mainline is changed, and no science/evidence
lane mainline target exists.

## Enabled-state requirement

- `EgoOperator.active_default=true` remains unchanged.
- The superseded K0 successor remains `enabled=false`,
  `mainline_connected=false`, and `runtime_authority=none`.
- EgoDesktop has no active-route, successor, or runtime authority.
- Card 2 execution remains unauthorized.

## Real-trigger evidence requirement

No runtime trigger is required or permitted. The admissible trigger is callable
governance readback: source fingerprint verification, structured-state
validation, derived-view replay, cross-repo Git-object pin verification, and
fail-closed mutation tests.

## Hypothesis

If current product route facts, route axes, allowed action, lineage disposition,
and legacy authority are represented once in `docs/PROGRAM_STATE_UNIFIED.yaml`,
with renderers reading that state and validators rejecting drift, then fresh
sessions cannot legitimately resume 8692 M1-M3, reactivate EgoDesktop, register a
science successor, or start table-external work from prose or memory alone.

## Strongest baseline

This card changes governance state only. It creates no learning, world model,
headroom, mechanism attribution, runtime behavior, or life-like capability.

## Ablation requirement

No mechanism ablation is authorized. Governance mutations must remove or alter
one binding at a time and rerun callable validation. Required mutations include
removing supersession, restoring M1 authorization, reactivating EgoDesktop,
enabling a science successor, creating an undisposed lineage, enabling pilot #1
successor use while `audit_ref=null`, adding an unbound action, and changing an
ITL pin.

## Governance replay requirement

Regenerate the existing program-state and route-convergence views from
`docs/PROGRAM_STATE_UNIFIED.yaml`, compare them byte-for-byte, and reject manual
sink edits. Runtime/mechanism replay is forbidden.

## Computed-evidence provenance gate

All governance verdicts must be produced by callable validators. The route
fingerprint is SHA-256 over canonical compact sorted JSON containing only
`program.current_phase`, `program.next_minimal_action`, and `route_guard`, with
the fingerprint field itself removed. The lineage counts must come from
`callable_repo_inventory` over current repo files and Git objects. Static PASS
dictionaries and hand-written count claims are forbidden.

## Acceptance gate

Acceptance requires all of the following:

1. The immutable source fingerprint remains
   `026535337949d5ea2857805e5d9e0d2b6bfae016154fbf58587eafcea6411aa3`.
2. The required structured authority state and one-axis/one-authority boundary
   validate from `docs/PROGRAM_STATE_UNIFIED.yaml`.
3. All discovered prior lineages are disposed and `undisposed_count=0`.
4. Pilot #1 repair remains capability-only; `audit_ref=null` blocks positive
   control and regression reuse.
5. Only Card 2 banking is bound; execution and all implementation targets are
   false/empty.
6. Existing generators reproduce every changed derived view byte-for-byte.
7. Focused tests cover all sixteen required anti-drift mutations.
8. Program-state integrity, route convergence, mainline clarity, fast repo
   verification, scoped closeout, and diff checks pass, except repo-global
   publication may remain pending because push is forbidden.
9. An independent Red reviewer from a session other than
   `019f5ce9-41b4-7430-b5d5-befde5cc256c` returns pass against the exact Phase-B
   diff SHA-256 and reviewed path set.

## Claim ceiling

Additive product/capability route-governance correction, legacy authority
archival, and next-task binding only.

## Stop conditions

Stop on source-fingerprint or branch/HEAD/worktree drift; a second authority;
runtime or experiment changes; more than one new non-derived task directory;
scope expansion; nonzero undisposed lineage count; unverified ITL pins; a
capability verdict upgraded to learning/mechanism evidence; missing or non-pass
independent Red review; failed callable validation; reuse of the migration
exception; any attempt to start Card 2; or any need for another governance-only
repair card (`ANTI_ZENO_SCOPE_FAILURE`).

## Rollback plan

Rollback is a later operator-authorized additive correction or a revert of the
two local task commits. Never rewrite or delete historical evidence, move
EgoDesktop source, or amend the 8692 commit.

## Exact expected files

Phase A:

- `docs/codex/tasks/ego-route-8692-supersession-and-egodesktop-authority-archive-001a/STAGE_CARD.md`
- `docs/codex/tasks/ego-route-8692-supersession-and-egodesktop-authority-archive-001a/MUTATION_SCOPE.yaml`
- `docs/codex/tasks/ego-route-8692-supersession-and-egodesktop-authority-archive-001a/COLLISION_RECORD.md`
- `docs/codex/tasks/TASK_LANE_INDEX.md` (generator output only)

Phase B:

- `docs/PROGRAM_STATE_UNIFIED.yaml`
- `.codex/project_contract.yaml`
- `scripts/codex_session_guard.py`
- `scripts/tests/test_codex_session_guard.py`
- `scripts/codex/route_convergence_common.py`
- `scripts/codex/verify_route_convergence.py`
- `scripts/codex/generate_route_convergence_views.py`
- `scripts/tests/test_route_governance_supersession.py`
- `docs/codex/tasks/ego-route-8692-supersession-and-egodesktop-authority-archive-001a/MUTATION_SCOPE.yaml`
- `docs/STATUS.md` (generator output only)
- `docs/codex/tasks/TASK_LANE_INDEX.md` (generator output only)
- `docs/REPO_SURFACE_MAP.md` (generator output only)
- `artifacts/reports/program_state_summary.md` (generator output only)

No other file may change.

## Forbidden changes

- `EgoDesktop/**`, `EgoOperator/**`, `packages/ego_k0_kernel/**`
- `labs/virtual_cat_pspc_v0/**`, VirtualCat implementation, LLM integration
- pet v0 implementation/artifacts, pilot artifacts, historical evidence
- `artifacts/evidence_ledger/**`, `Tasks/TASK_BOARD.yaml`
- any ITL file or science route-state
- C1/C2/C3/C4 implementation, Card 2 files, experiment execution
- remote branches, tags, pushes, or anchors

## Phase and commit authorization

- Phase A local task-card bank: authorized after scoped verification and an
  independent Red pre-check.
- Phase B local governance implementation: authorized only after the actual
  diff is independently reviewed and all local checks pass.
- Card 2: not authorized.
- Push/tag/remote anchor: forbidden.

## What this does not prove

This card does not prove a mechanism pass, learning, a world model, headroom,
C1 readiness, mainline integration, EGO readiness, electronic-life progress,
agency, autonomy, subjectivity, consciousness, emotion, or stable user benefit.

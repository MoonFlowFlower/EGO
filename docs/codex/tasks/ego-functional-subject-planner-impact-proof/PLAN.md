# Plan

1. Add a bounded outcome-prediction action-selection gate to the real message path.
2. Record the applied prediction effect in trace and external result metadata.
3. Expose the effect in the Functional Subject scripted trial evidence packet.
4. Add deterministic regressions proving the LLM chat path is bypassed only when the candidate subject context selected `ask`.
5. Run targeted tests and `autopilot_full`.

## Non-Goals

- No memory promotion.
- No canonical subject-state mutation.
- No legacy runtime changes.
- No consciousness or live autonomy claim.

# EgoDesktop Joi Real-Loop G-ABLATION Replay Precondition v0 Plan

1. Add the `EGODESKTOP-GABLATION-007` task card, mutation scope, and task-board entry.
2. Add failing tests for executable 007 scoring-precondition blocking over collect-only rows.
3. Add the minimal evaluator precondition helper and wire the explicit CLI flag to abort with a non-zero exit.
4. Run the CLI over the current 006 trace row and preserve the blocked report artifact.
5. Update status/task board and regenerate route-convergence views.
6. Run focused tests, `npm test`, repo fast verify, scoped closeout, and commit locally only.

## Non-Goals

- No baseline score or same-access reproducer execution.
- No attribution, route, readiness, product, or mechanism verdict.
- No default EgoDesktop runtime enablement.
- No program-state or evidence-ledger update.
- No LLM replay-id repair in this slice; trace/replay distinction remains deferred until LLM-modulated `D` exists.

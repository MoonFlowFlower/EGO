# Plan

1. Reproduce the `fs_13_choose_own_topic` blocker from the EGO-FS-028 real-provider report.
2. Add deterministic regression for a self-selected topic reply that is specific, reversible, gated, and trace-aligned.
3. Repair the EgoOperator Functional Subject response guard or planner context without adding a second route or template fallback.
4. Update trial taxonomy only if the current classifier misses valid transcript-visible planner evidence.
5. Run targeted tests, `EgoOperator/tests`, `autopilot_full`, and a scripted Functional Subject case rerun.

## Non-Goals

- No memory authority promotion.
- No provider/model default change.
- No GitHub Project mutation.
- No program-state or evidence-ledger update.
- No claim that the full Functional Subject real-provider smoke has passed.

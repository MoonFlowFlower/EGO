
# EGO-LEARNED-OUTCOME-KERNEL-CAPABILITY-NEW-SURFACE-SELECTION-001A

Status:

ONE-SHOT DOCS-ONLY OPERATOR DECISION SUPPORT AUTHORIZED
OPERATOR SURFACE SELECTION PENDING
NO PREREGISTRATION AUTHORIZED
NO IMPLEMENTATION OR EXECUTION AUTHORIZED
DEFAULT-OFF
NON-MAINLINE
RUNTIME AUTHORITY NONE

Auto-Remote-Anchor: forbidden

Push/tag: forbidden

## 0. Authorization envelope

This task authorizes exactly one local docs-only transaction:

1. bank this Stage Card;
2. bank its exact Mutation Scope;
3. bank one three-candidate operator decision-support packet;
4. create one exact three-addition local commit;
5. STOP and wait for explicit operator `SELECT <surface_id>` or `CLOSE_NO_NEW_SURFACE`.

This task does not authorize:

- selecting a surface on behalf of the operator;
- creating a fresh preregistration task or contract;
- changing Program State, route verifier, evidence ledger, STATUS, generated views or task-lane index;
- reopening or repairing the closed P0/P1/P1R0 lineage;
- reusing or renaming FACTORED-STOCHASTIC-OUTCOME-SURFACE-v1;
- implementation, generator, instrument, baseline, probe, scoring, DEV, formal or artifact execution;
- candidate or product package creation;
- route registration, enablement, runtime, mainline, UI, LLM, network or science work;
- Foundation or ITL mutation/rerun;
- push, tag or remote anchor.

## 1. Identity and layer

Task ID:

EGO-LEARNED-OUTCOME-KERNEL-CAPABILITY-NEW-SURFACE-SELECTION-001A

Current layer:

Layer 2 governance engineering plus Layer 3 bounded mechanism-hypothesis selection.

Mainline target:

none.

Enabled-state requirement:

false before, during and after the transaction.

Real-trigger evidence:

operator request, pinned closeout objects and prior negative evidence only. There is no runtime, mechanism, learning or product trigger.

Claim ceiling:

operator decision support and paper collision screening only.

## 2. Problem definition and real objective

The FACTORED-STOCHASTIC-OUTCOME-SURFACE-v1 P0/P1/P1R0 instrument-design lineage is closed invalid. Same-surface repair, P1R1, P1R2 and P2 are forbidden.

The real objective is to present exactly three materially distinct product-capability surfaces and one explicit no-go option so that the operator can make the required fresh surface decision. This packet must reduce the path to one concrete preregistration without smuggling Codex's recommendation into operator authority.

## 3. Bounded audit

### Hypothesis

At least one materially new surface may expose two separately testable product-capability axes:

1. forward transfer to held-out task structure or action composition;
2. within-task adaptation from legal outcome feedback.

### Strongest baseline explanation

An apparently adaptive result could be matched by observation decoding, exact/approximate lookup, count/transition tables, recency, episodic traversal, a task specialist, or a legal classical learner under equal access.

### Strongest reason this selection may be invalid

All proposed surfaces may merely rename the old contextual-outcome setup, leak the target through descriptors, or lack legal-channel identifiability/headroom.

### Framing falsifier

Return the no-go option if every candidate fails any mandatory paper screen:

- material distinctness from the closed surface;
- legal target determinability;
- independent forward-transfer and adaptation axes;
- an equal-access invalidating-control panel;
- a budget-faithful legal feasible-reference plan, separated from any diagnostic ideal ceiling;
- replayable serialized-state intervention design;
- real leakage scan with planted positive controls;
- fresh generator, split, seed and artifact namespaces.

### Evidence still insufficient

The packet, recommendation, commit, JSON validity and green repo checks do not prove empirical headroom, candidate feasibility, learning, mechanism validity or product readiness.

### Mechanism versus resemblance

This task selects hypotheses for later bounded offline adaptation testing. It does not test behavior, dialogue resemblance, agency or subjectivity.

## 4. Collision record and candidate set

Exactly three candidate families are frozen for this one selection round:

1. COMPOSITIONAL-ACTION-OUTCOME-TRANSFER-SURFACE-v1;
2. CAUSAL-AFFORDANCE-GRAPH-TRANSFER-SURFACE-v1;
3. DELAYED-CREDIT-REGIME-ADAPTATION-SURFACE-v1.

For each candidate, SURFACE_CANDIDATES.json must record:

- product observable;
- legal observation/action/outcome/feedback boundary;
- evaluator-only fields;
- statistical identifiability assumption;
- forward-transfer and within-task-adaptation axes;
- strongest equal-access invalidating controls;
- capability rivals and mechanism challengers;
- a budget-faithful legal feasible reference with data/update/compute/memory parity;
- a separately labeled diagnostic ideal ceiling that cannot supply admission;
- observation/full-history decoding risk;
- lookup/cache/specialist saturation risk;
- replay and ablation design;
- leakage positive controls;
- smallest falsifier;
- expected negative mode;
- why it is not the closed surface renamed or retuned;
- fresh namespace plan;
- fact/inference/assumption/unknown separation;
- paper-screen disposition.
- explicit operator_selectable boolean.

An explicit CLOSE_NO_NEW_SURFACE option is mandatory.

## 5. Frozen selection rules

The decision packet may contain one non-binding Codex recommendation, but must preserve:

- operator_decision_status=PENDING;
- operator_selected_surface_id=null;
- fresh_prereg_task_id=null;
- route_registered=false;
- all implementation/execution authorizations=false.

No empirical score, threshold, headroom value or pass claim may be invented. The assessment basis is qualitative paper collision screening only.

A surface may remain operator-selectable after the paper screen only if its packet contains a concrete future test and kill criterion for each of the following; docs-only presence does not mean the criterion has empirically passed:

1. it is materially distinct from the closed fixed-action/static-latent surface;
2. a candidate-independent test of whether legal history adds information not present in the current observation alone;
3. held-out novelty is structural, not a renamed tuple or seed;
4. a no-update/history-shuffle/source-deletion intervention has a predeclared load-bearing direction;
5. lookup/count/transition/nearest-neighbor/episodic controls have equal legal access;
6. a budget-faithful feasible reference can be implemented without private-state input, and any stronger ideal ceiling is diagnostic-only;
7. replay can recompute from serialized_state plus legal observation/actions;
8. candidate, task, split, seed, fixture, verdict and oracle identifiers cannot leak.

## 6. Baseline and ablation requirements for the future selected route

No baseline or ablation runs now.

Any later surface-specific prereg must freeze independent callable implementations for at least:

- constant/global mean and random controls;
- observation-only decoders as invalidating controls;
- fitted legal full-history decoders as capability rivals/mechanism challengers, whose match may block online-update attribution but does not by itself erase product capability;
- exact lookup, count table and nearest-neighbor;
- transition table, successor map, graph lookup and FSM where applicable;
- recency/EWMA and episodic traversal;
- no-update/frozen learner;
- feedback-shuffled learner;
- from-scratch/no-meta learner;
- candidate's own batch/offline update rule;
- a legal fitted classical learner;
- a budget-faithful legal feasible reference with data/update/compute/memory parity;
- an ideal ceiling, if included, labeled diagnostic-only and forbidden from supplying admission.

Task specialists are capability rivals, not automatically invalidating controls.

Future ablations must rerun frozen episodes and include update-off, feedback shuffle, state reset/swap, source-memory deletion, descriptor permutation, predictor bypass and fresh-process replay where applicable.

## 7. Trace/replay/leakage/computed-evidence boundary

This docs-only task creates no experimental evidence and no score.

Any future executable route must record producer_function, input artifacts, run_id, seed/context/episode IDs, aggregation rule and code-path hash. Replay must recompute behavior from serialized_state plus legal observation/actions. Leakage scanning must be callable and include planted positive controls.

## 8. Expected changed files

Exactly three additions:

- docs/codex/tasks/ego-learned-outcome-kernel-capability-new-surface-selection-001a/STAGE_CARD.md
- docs/codex/tasks/ego-learned-outcome-kernel-capability-new-surface-selection-001a/MUTATION_SCOPE.yaml
- docs/codex/tasks/ego-learned-outcome-kernel-capability-new-surface-selection-001a/SURFACE_CANDIDATES.json

Commit subject:

docs: bank learned outcome new-surface selection packet

## 9. Acceptance gate

Accept only if:

- Step 0 matches exactly;
- all three target paths were absent at parent HEAD;
- exact three candidate families plus CLOSE_NO_NEW_SURFACE are present;
- candidate count equals three;
- each candidate contains the full collision fields;
- at least one candidate survives the paper screen, or the packet honestly returns NO_ADMISSIBLE_NEW_SURFACE_CANDIDATE;
- any recommended surface is operator_selectable=true, while paper-rejected candidates are operator_selectable=false;
- operator selection and prereg task ID remain null;
- no route, prereg, implementation, execution, score or artifact is created;
- all closed-lineage objects and canonical sinks remain byte-identical;
- exact three additions are staged and committed;
- final Ego worktree/index are clean and ahead/behind is 24/0 if upstream is unchanged;
- ITL remains unchanged at 7/0;
- no remote action occurs.

Maximum positive verdict:

NEW_SURFACE_SELECTION_PACKET_BANKED__OPERATOR_SELECTION_PENDING__NO_PREREG_OR_EXECUTION_AUTHORIZED

Allowed negative verdict:

NO_ADMISSIBLE_NEW_SURFACE_CANDIDATE_BANKED__ROUTE_SELECTION_CLOSED_NO_GO

## 10. Stop conditions

STOP without commit if:

- repo/source drift or pre-existing dirt appears;
- any candidate is only the old surface renamed, reseeded or parametrically retuned;
- old generator, splits, seeds, fixtures, thresholds, baselines or admission verdict are inherited;
- any target depends on private evaluator state rather than legal history;
- fair invalidating controls or a legal ceiling are absent;
- Codex sets operator_selected_surface_id or creates a prereg task ID;
- a fourth candidate is added after the frozen three fail;
- any implementation, probe, scoring, artifact or canonical-governance mutation occurs;
- any unrelated path changes;
- push, tag or remote anchor is attempted.

## 11. Anti-Zeno and rollback

This route-selection family has one transaction only. If all candidates fail, bank the no-go packet and stop; do not repair the selection packet or generate more candidates to obtain a positive route.

Before commit:

- if staged, unstage only the exact three task-owned paths;
- remove only the three uncommitted task-created paths with bounded apply_patch deletion;
- verify return to the Step 0 clean state.

After commit:

- do not amend, reset or rebase;
- any correction requires an additive superseding task with a new ID;
- operator rejection or no selection leaves the packet banked and creates no runtime rollback need.

## 12. Claim ceiling and next action

Allowed claims:

- a bounded operator decision-support packet exists;
- three materially distinct hypotheses were collision-screened on paper;
- one recommendation may be presented without authority.

Forbidden claims:

- empirical headroom;
- surface admission;
- preregistration completion;
- learning or adaptation;
- mechanism validity;
- product/runtime/mainline readiness;
- agency, autonomy, subjectivity or consciousness.

After the commit, STOP. The next action is an explicit operator response:

SELECT <surface_id>

or:

CLOSE_NO_NEW_SURFACE

Only a later explicit SELECT may authorize drafting a new surface-specific preregistration card under a fresh task ID.

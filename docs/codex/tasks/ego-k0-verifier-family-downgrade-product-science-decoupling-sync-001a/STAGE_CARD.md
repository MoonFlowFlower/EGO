# EGO-K0-VERIFIER-FAMILY-DOWNGRADE-AND-PRODUCT-SCIENCE-DECOUPLING-SYNC-001A

Status: EXECUTABLE FOR LOCAL GOVERNANCE-ONLY AUTHORITY/VIEW/SINK SYNC
/ STOP AFTER TWO LOCAL COMMITS

Auto-Remote-Anchor: forbidden

Push/tag: forbidden

## 0. Scope clarification

This is not a literal zero-Python docs-only task.

A literal docs/YAML-only edit cannot complete the real sync because the current
Ego route renderer and verifier still hard-code:

- old K0-R as `parked`;
- `operator replace-versus-close decision pending`;
- the old exact K0 workstream status.

Phase A is docs-only task banking.

Phase B may modify exactly two existing governance sinks:

- `scripts/codex/route_convergence_common.py`
- `scripts/codex/verify_route_convergence.py`

Those scripts are governance renderer/verifier surfaces only. No runtime,
learned-kernel, mainline, experiment or mechanism implementation is authorized.

If execution is constrained to zero `.py` changes, STOP with:

SYNC_NOT_EXECUTABLE_WITHOUT_GOVERNANCE_SINK_CHANGE

Do not claim synchronization.

## 1. Task identity

Task ID:

EGO-K0-VERIFIER-FAMILY-DOWNGRADE-AND-PRODUCT-SCIENCE-DECOUPLING-SYNC-001A

Repo/CWD:

D:\Project\AIProject\MyProject\Ego

Source repo:

D:\Project\AIProject\MyProject\intelligence-theory-lab

Current layer:

Layer 2 — engineering/evidence-governance

Mainline target:

none

Active default that must remain unchanged:

ego-operator-human-operator-trial-v2

K0 enabled:

false

K0 mainline connected:

false

K0 runtime authority:

none

Runtime/mechanism trigger evidence:

absent

Local commit authorization:

exactly two local commits after all phase-local gates pass

## 2. Real objective

Synchronize into Ego’s single canonical program-state authority and existing
generated governance sinks:

1. the preserved ITL operator decision to close the old K0 science-attribution
   route without science adjudication;
2. the terminal downgrade of the current verifier repair family;
3. `independent_validator_acceptance=UNAVAILABLE`;
4. preservation of Foundation bounded engineering evidence;
5. continued non-authorization of H0/K0-R/H1/Freeze/Formal;
6. explicit dependency separation between the closed old science route and a
   possible future product capability Stage Card.

Remove stale active Ego semantics:

- `CLOSURE_REVIEW_REQUIRED`;
- `GOVERNANCE_STOP`;
- `operator replace-versus-close decision pending`;
- old K0-R shown as parked/readiness-bearing.

Do not:

- claim that the old closure received independent validator acceptance;
- create or register a future capability route;
- implement a learned kernel;
- start old K0-R;
- rerun Foundation;
- modify ITL.

This task tests local governance/view convergence only. It does not test a
mechanism or behavioral resemblance.

## 3. Mandatory Step 0 — fail closed before writing

### 3.1 Ego snapshot

Run:

git branch --show-current
git rev-parse HEAD
git status --porcelain=v1 --untracked-files=all
git diff --name-status
git diff --cached --name-status
git rev-list --left-right --count '@{u}...HEAD'
Test-Path .git/index.lock

Require exactly:

branch=main
HEAD=c43960b3336d5a7cba3022208a0740d9dc51bd47
ahead/behind=13/0
raw behind/ahead=0 13
worktree/index/diffs=clean/empty
index.lock=absent

Require both paths absent:

docs/codex/tasks/ego-k0-verifier-family-downgrade-product-science-decoupling-sync-001a
docs/codex/tasks/ego-learned-outcome-kernel-capability-001a

Any mismatch => STOP.

### 3.2 Ego baseline governance readback

Run:

python scripts/codex_session_guard.py bootstrap --format markdown
python scripts/codex/check_program_state_integrity.py --skip-diff-check
python scripts/codex/verify_route_convergence.py
python scripts/codex/verify_mainline_clarity.py
python scripts/codex/verify_repo.py --mode fast

All commands must complete successfully.

The bootstrap must still expose the stale pre-sync K0 action:

operator replace-versus-close decision

This stale wording is the sync target, not current authority after Phase B.

Record:

- current_phase;
- current_layer;
- highest_evidence_level;
- next_minimal_action;
- active default;
- dirty counts;
- GitHub sync availability.

`gh_not_found` is not a blocker because this task does not modify the task
board and forbids remote publication.

### 3.3 ITL source snapshot

Read only:

git -C D:\Project\AIProject\MyProject\intelligence-theory-lab branch --show-current
git -C D:\Project\AIProject\MyProject\intelligence-theory-lab rev-parse HEAD
git -C D:\Project\AIProject\MyProject\intelligence-theory-lab status --porcelain=v1 --untracked-files=all
git -C D:\Project\AIProject\MyProject\intelligence-theory-lab rev-list --left-right --count '@{u}...HEAD'

Require:

branch=codex/meta-theory-scaffold
HEAD=b67c94fe1244ef6006ed3af8e924d4c670fe64bb
tree=2caa9a8ad5587d284ec0da01c4d07a5ed53e9ea7
ahead/behind=7/0
clean

Canonical downgrade card:

path:
docs/codex/tasks/K0-VERIFIER-FAMILY-DOWNGRADE-AND-PRODUCT-SCIENCE-DECOUPLING-001A.md

blob:
add0d7823fb824e4ee1d74edbe9f88f8a726f36a

raw SHA-256:
4a765fee0e910018cef46a7b4734d08cf5014e012b874b1150543a179dcbc663

The committed card must contain exactly one canonical JSON block, and standard
library JSON parsing must recover:

- `REPLACEMENT_VERIFIER_INVALID`;
- `CLOSED_INVALID_NO_FURTHER_REPAIR`;
- `independent_validator_acceptance=UNAVAILABLE`;
- all root authorization false;
- exactly six child authorization false;
- the future product/science firewall.

Do not invoke:

- ITL routectl;
- ITL build_validation_report;
- the terminal verifier;
- any ITL test or producer.

### 3.4 Source-role separation

These sources have different roles:

1. ITL state/closure objects preserve the operator route decision.
2. ITL commit `b67c94fe...` and its card provide the terminal verifier-family
   disposition and product/science firewall.
3. Stored `validation_report.json` is frozen executor output only.
4. Ego Foundation artifacts preserve bounded engineering evidence only.

Never use stored `pass/PENDING_INDEPENDENT_HOSTILE_REVIEW` as independent
acceptance.

## 4. Frozen source and preservation pins

### ITL decision/evidence pins

| Source | Blob |
|---|---|
| operator-close card | `b52e09ba7f9a103a70819add3ea3ed2df206617f` |
| first-repair card | `5947f44071a4a3465da7ab82717929f40b11e82e` |
| replacement card | `12bb3670f3be2766b54a3f5d965fed79e154a5a9` |
| K0 state.json | `261b9ce78fd88a70c5f78646ce9209eb8bacee08` |
| K0 closure.json | `e1a7d8d595004c07cea7c8fd70cb1738d42e3b5a` |
| K0 events.jsonl | `5ec92897a15fea67f47fbec444a665511c6e4fef` |
| program_state.json | `377d09780e0a3949a172d89c3bc12de40245f8e8` |
| stored validation_report.json | `a48032af0cccdce55074cd648ca53ed97cc9971b` |
| FSP-STAGE-LEDGER.md | `1ab8de60021d8492c42f2a0984d4564b2d550509` |

No ITL object may change.

### Ego starting authority/sink pins

| Path | Starting blob |
|---|---|
| `docs/PROGRAM_STATE_UNIFIED.yaml` | `c38f3c4b8995fb8ffd973343fa89f850825001da` |
| `artifacts/evidence_ledger/index.yaml` | `f1aad167ab04938b040884d2de53c46a770c4a0b` |
| `scripts/codex/route_convergence_common.py` | `aa47e0dfaad65661a34c308a66d0579428abb819` |
| `scripts/codex/verify_route_convergence.py` | `c29a03d03c43c460ad4be38f61d77091390a4a4a` |
| `docs/STATUS.md` | `610f00b6e6eb1da4c64c3d9b9fcfe58ce05285f0` |
| `docs/codex/tasks/TASK_LANE_INDEX.md` | `d9944ade0d2da282383964c7da903f0fa5436a43` |
| `artifacts/reports/program_state_summary.md` | `52053faf35bcf9dc7f9bb6bf142e15c3f5a90317` |

Protected generated/surface blobs:

- `docs/REPO_HYGIENE_POLICY.md`
  - blob `549b8c4a59425a99da3a2b547b6978e90e515c83`
- `docs/REPO_SURFACE_MAP.md`
  - blob `6145ae5aabe74f2ab3c62589def7b62806c4f4ea`

They must remain byte-identical and unstaged.

### Foundation preservation

Artifact commit:

8318bb33212f79e1e0a9cea15cad7b9cd176a0f5

Artifact tree:

907457e7d3028ba5437cf0e7730ec068a21cbf6b

Result blob:

a8b3237afc40a1f56df3906870ff94c5db9c10ff

Result SHA-256:

834b4764514062f8937488fd4b89684b5ae684e9522d67f440ed3c077f077067

Protected trees/files at Ego starting HEAD:

- `EgoOperator/`
  - tree `2159491f3c2f75b13f330071d2e4585f8a89799b`
- `EgoDesktop/`
  - tree `2c29bc0c81ba06fff3ed98dbce2235ce10e0817a`
- `packages/ego_k0_kernel/`
  - tree `43380f76c37b05f36a4a4ef45354048787cafe68`
- `scripts/ego_k0_adapters/`
  - tree `01d19024c0a34400c780dc354a528921eddb7a9c`
- `tests/test_ego_k0_foundation.py`
  - blob `922cea09a2c2b3c64be70e387a79f1027985fcc3`
- `scripts/run_ego_k0_foundation_validation.py`
  - blob `f001dd7cdf05786cc108a1da017f8890af3f0b04`

Every listed object must remain unchanged.

## 5. Bounded audit

### Real objective

Make the current Ego authority, renderer, verifier and generated views agree
that:

- the old science-attribution route received an operator governance close;
- no science adjudication occurred;
- the verifier family is terminally invalid;
- independent acceptance is unavailable;
- Foundation evidence remains preserved;
- the old K0-R path is not actionable;
- future product-card eligibility does not inherit old science authority.

### Strongest baseline

Edit only `PROGRAM_STATE_UNIFIED.yaml` and generated Markdown.

This can produce temporary prose but the current renderer will recreate the old
pending/parked state, or the current verifier will fail. It is not a real sync.

### Strongest invalidity reason

The ITL verifier did not receive independent acceptance.

If Ego imports the stored green report as acceptance, the sync becomes evidence
laundering.

A second invalidity risk is relabeling old K0-R as the future product route.

### Framing falsifier

The task is invalid if:

- independent acceptance becomes pass/pending;
- old K0-R remains actionable or parked;
- future capability work is created or authorized;
- product results acquire science weight;
- any runtime/mainline/science authorization becomes true;
- a second authority/schema/validator is created.

### Still-insufficient evidence

The following do not prove science or capability:

- successful local sync;
- generator/verifier green output;
- Foundation engineering evidence;
- a banked future Stage Card;
- product benchmark performance.

### Mechanism classification

No mechanism is tested.

## 6. Collision record

### Candidate A — prose/YAML-only synchronization

Evidence:

temporary prose consistency.

Cheap baseline:

hand-edit generated views.

Leakage risk:

high; generator or verifier restores the old state.

Smallest falsifier:

rerun canonical generation and route convergence verification.

Decision:

REJECTED.

### Candidate B — rename old K0-R into the future product route

Evidence:

an apparently ready capability lane.

Cheap baseline:

aliasing old cards, fixtures and authorization.

Leakage risk:

critical; old science authority becomes a product bypass.

Smallest falsifier:

any inherited old task ID, card, fixture, heldout data or science metric.

Decision:

REJECTED.

### Candidate C — create a future capability placeholder now

Evidence:

a route/card exists.

Cheap baseline:

existence mistaken for authorization or capability.

Leakage risk:

high; dependency decoupling becomes execution authorization.

Decision:

REJECTED.

### Candidate D — one authority, additive negative ledger, existing sinks

Update the canonical Ego authority, add one negative ledger record, minimally
correct the existing renderer/verifier and regenerate existing views.

Decision:

SELECTED.

## 7. Frozen target workstream contract

The existing workstream remains:

k0_developmental_kernel_dual_track

It must have exactly:

```yaml
status: "foundation_engineering_accepted_bounded__old_science_route_operator_closed_without_science_adjudication__verifier_family_closed_invalid__independent_acceptance_unavailable__product_science_decoupled__all_authorizations_false__runtime_disabled__non_mainline"
evidence_level: "E3"
verification_level: "V3"
mainline_connected: false
enabled: false
```

E3/V3 remains the pre-existing Ego classification of Foundation bounded
engineering evidence. The sync does not create a new E3 result and does not
upgrade verifier or science evidence.

Add one structured mapping inside this existing workstream:

```yaml
governance_sync:
  record_type: "SOURCE_PINNED_DERIVED_READBACK"
  ego_synced_to_itl_operator_close: true
  sync_claim: "LOCAL_EGO_GOVERNANCE_VIEW_ONLY"
  runtime_trigger_evidence: "ABSENT"

  source_itl:
    repo: "intelligence-theory-lab"
    head: "b67c94fe1244ef6006ed3af8e924d4c670fe64bb"
    tree: "2caa9a8ad5587d284ec0da01c4d07a5ed53e9ea7"
    task_id: "K0-VERIFIER-FAMILY-DOWNGRADE-AND-PRODUCT-SCIENCE-DECOUPLING-001A"
    card_path: "docs/codex/tasks/K0-VERIFIER-FAMILY-DOWNGRADE-AND-PRODUCT-SCIENCE-DECOUPLING-001A.md"
    card_blob: "add0d7823fb824e4ee1d74edbe9f88f8a726f36a"
    card_raw_sha256: "4a765fee0e910018cef46a7b4734d08cf5014e012b874b1150543a179dcbc663"

  operator_route_decision:
    route_id: "K0-DUAL-TRACK-SUPERSESSION-001A"
    artifact_current_state: "ADJUDICATED"
    phase: "K0_SCIENCE_ROUTE_CLOSED_WITHOUT_SCIENCE_ADJUDICATION"
    closure_type: "CLOSED_WITHOUT_SCIENCE_ADJUDICATION"
    route_disposition: "CLOSED_WITHOUT_EXECUTION"
    operator_decision: "CLOSE_CURRENT_K0_SCIENCE_ROUTE"
    adjudication_scope: "ROUTE_GOVERNANCE_ONLY"
    science_adjudication: "NOT_PERFORMED"
    instrument_validity: "NOT_TESTED"
    mechanism_evidence: "NOT_TESTED"
    theory_falsification: "NO_THEORY_FALSIFICATION"
    mechanism_verdict: "NO_MECHANISM_VERDICT"
    active_mechanism_frontier: "none"

  verifier_family:
    original_execution_verdict: "NOT_ACCEPTED_PROCEDURAL_NONCONFORMANCE"
    first_repair_review_verdict: "NOT_ACCEPTED__STRUCTURAL_TERMINAL_LATCH_FAIL_OPEN"
    replacement_review_verdict: "REPLACEMENT_VERIFIER_INVALID"
    family_disposition: "CLOSED_INVALID_NO_FURTHER_REPAIR"
    independent_validator_acceptance: "UNAVAILABLE"
    operator_closure_acceptance_from_this_family: "UNAVAILABLE"
    stored_validation_report_treatment: "FROZEN_EXECUTOR_OUTPUT_NOT_ACCEPTANCE_EVIDENCE"

  foundation_evidence:
    engineering_evidence: "BANKED_ACCEPTED_BOUNDED"
    preservation: "FOUNDATION_EVIDENCE_PRESERVED"
    implementation_authorized: false
    enabled: false
    mainline_connected: false
    runtime_authority: "none"
    artifact_commit: "8318bb33212f79e1e0a9cea15cad7b9cd176a0f5"
    artifact_tree: "907457e7d3028ba5437cf0e7730ec068a21cbf6b"
    result_blob: "a8b3237afc40a1f56df3906870ff94c5db9c10ff"
    result_sha256: "834b4764514062f8937488fd4b89684b5ae684e9522d67f440ed3c077f077067"
    mechanism_meaning_inherited: false

  component_status:
    foundation_engineering_evidence: "BANKED_ACCEPTED_BOUNDED"
    h0: "NOT_TESTED"
    k0_reference: "BLOCKED_NOT_TESTED"
    h1: "BLOCKED_NOT_TESTED"
    freeze: "BLOCKED_NOT_TESTED"
    formal: "BLOCKED_NOT_TESTED"

  root_authorizations:
    implementation_authorized: false
    authorized_implementation_targets: []
    runtime_authorized: false
    mainline_authorized: false
    science_execution_authorized: false
    mechanism_evidence_authorized: false
    theory_pressure_authorized: false
    science_successor_authorized: false
    capability_route_registered: false
    capability_implementation_authorized: false

  route_authorizations:
    agency: false
    autonomy: false
    code_first_prebank_implementation: false
    consciousness: false
    ego_mainline_runtime: false
    experiment_execution: false
    formal_run: false
    foundation_implementation: false
    freeze: false
    h0_implementation: false
    h1_implementation: false
    k0_reference_implementation: false
    mechanism_validity: false
    remote_anchor: false
    scoring: false
    subjectivity: false
    theory_pressure: false
    ui_llm_deployment: false

  child_authorizations:
    EGO-K0-FOUNDATION-001A: false
    EGO-K0-REFERENCE-KERNEL-001A: false
    ITL-K0-H0-H1-INSTRUMENT-001A:H0: false
    ITL-K0-H0-H1-INSTRUMENT-001A:H1: false
    K0-IMMUTABLE-FREEZE-001A: false
    ITL-K0-FORMAL-EVIDENCE-001A: false

  future_product_route:
    task_id: "EGO-LEARNED-OUTCOME-KERNEL-CAPABILITY-001A"
    current_status: "NOT_CREATED_OR_REGISTERED"
    this_sync_creates_card: false
    route_registered: false
    implementation_authorized: false
    enabled: false
    default_off_required: true
    mainline_connected: false
    non_mainline_required: true
    runtime_authority: "none"
    old_science_acceptance_required_for_product_card: false
    decoupling_scope: "PRODUCT_CARD_DEPENDENCY_ONLY_NO_EXECUTION_AUTHORITY"
    requires_separate_bounded_stage_card: true
    requires_fresh_operator_authorization: true
    inherits_old_k0r_authority: false
    inherits_old_science_attribution: false
    inherits_old_h0_h1_freeze_formal_contracts: false
    inherits_old_sealed_fixtures_or_heldout_data: false
    old_k0r_bypass: false
    may_satisfy_old_h1_freeze_formal: false
    may_reopen_old_science_route: false
    foundation_engineering_dependency_redeclaration_required: true
    foundation_mechanism_meaning_inherited: false

  science_route_firewall:
    science_successor_authorized: false
    product_results_have_science_weight: false
    product_results_can_supply_mechanism_attribution: false
    science_successor_requires_separate_candidate_independent_headroom_prototype: true
    product_route_cannot_satisfy_or_bypass_that_requirement: true
```

`ego_synced_to_itl_operator_close=true` means only that Ego’s local governance
view mirrors the operator decision and final downgrade record. It does not mean
independent validator acceptance.

## 8. Program-level live wording

Update the active K0 statements in:

- `program.current_focus`;
- `program.next_minimal_action`;
- the K0 workstream summary;
- one additive `program.completed_since_last` entry.

They must state:

- operator route decision is preserved;
- science adjudication was not performed;
- verifier family is closed invalid with no further repair;
- independent acceptance is unavailable;
- Foundation remains preserved at its bounded engineering ceiling;
- all old route and child authorization is false;
- old K0-R is not actionable;
- future product route is not created or authorized;
- the next possible K0 action is a separately authored and separately
  authorized bounded Stage Card for
  `EGO-LEARNED-OUTCOME-KERNEL-CAPABILITY-001A`.

Do not state that implementation is the next already-authorized action.

## 9. Evidence ledger rule

Add exactly one new top entry to:

artifacts/evidence_ledger/index.yaml

Required identity:

```yaml
evidence_id: "ego_k0_verifier_family_negative_boundary_sync_001a"
status: "fail"
evidence_level: "E2"
source_type: "doc"
artifact_path: "docs/codex/tasks/ego-k0-verifier-family-downgrade-product-science-decoupling-sync-001a/STAGE_CARD.md"
related_workstream: "k0_developmental_kernel_dual_track"
```

`created_from_commit` must equal the Phase A card-bank commit.

The entry must say it proves only:

- Ego locally mirrors the pinned operator decision;
- the verifier family is preserved as invalid/closed;
- independent acceptance is unavailable;
- product/science dependency inheritance is blocked.

It must say it does not prove:

- closure acceptance;
- science adjudication;
- mechanism validity or invalidity;
- learning;
- capability;
- runtime/mainline effect;
- product readiness;
- agency, subjectivity or consciousness.

All prior ledger entries must retain exact order and bytes. The existing
Foundation entry must not be changed.

The ledger status `fail` records the verifier-family negative boundary. It does
not mean the local docs synchronization failed.

## 10. Route renderer target

In the existing `TASK_OVERRIDES`:

### Foundation

`ego-k0-foundation-001a` remains uniquely:

lane=closed_evidence

Its existing bounded engineering/authorization-consumed/disabled/non-mainline/
no-runtime meaning remains unchanged.

### Old Reference Kernel / K0-R

Change `ego-k0-reference-kernel-001a` from:

parked

to:

reference_only

Reason:

- it is an unexecuted old science-attribution plan;
- continuing to mark it parked implies future readiness;
- marking it closed evidence could falsely imply K0-R execution evidence;
- reference-only preserves the card without route authority.

Its explanation must include:

- old science-attribution plan;
- not executed;
- `BLOCKED_NOT_TESTED`;
- operator closed the old route without science adjudication;
- independent validator acceptance unavailable;
- no implementation authority;
- not the future product route.

### Future capability route

Do not add a task override, workstream or directory for:

ego-learned-outcome-kernel-capability-001a

A later banked Stage Card may appear as `reference_only`; that alone must not
mean route registration or implementation authorization.

### Active default

Must remain exactly:

ego-operator-human-operator-trial-v2

Do not hard-code global lane totals in the verifier.

At the frozen starting tree, expected informational counts after Phase B are:

active_default=1
supporting_active=1
parked=1
closed_evidence=20
reference_only=231

These totals are readback information only, not an acceptance condition.

## 11. Verifier sink requirements

Modify only the existing local verifier.

It must:

1. require the exact new K0 workstream status;
2. keep Foundation unique in `closed_evidence`;
3. require old Reference Kernel unique in `reference_only`;
4. reject old Reference Kernel in `parked`, active or supporting lanes;
5. validate the structured `governance_sync` mapping;
6. require all root, route and six child authorizations false;
7. require `authorized_implementation_targets=[]`;
8. require all three negative verdicts;
9. require `CLOSED_INVALID_NO_FURTHER_REPAIR`;
10. require both independent-acceptance fields to be `UNAVAILABLE`;
11. require stored-report treatment to be
    `FROZEN_EXECUTOR_OUTPUT_NOT_ACCEPTANCE_EVIDENCE`;
12. require the future route to remain unregistered, unauthorized, disabled and
    non-mainline;
13. require `old_k0r_bypass=false`;
14. require product results to have no science weight;
15. keep the active default unchanged;
16. reject stale pending/actionable semantics from the live K0 workstream and
    route entries.

It must remain a local Ego mirror/sink verifier.

It must not:

- read ITL at ordinary runtime;
- invoke the invalid ITL validator;
- issue closure acceptance;
- create a second route authority;
- hard-code global task totals.

## 12. Phase A — exact task bank

Create only:

1. `docs/codex/tasks/ego-k0-verifier-family-downgrade-product-science-decoupling-sync-001a/STAGE_CARD.md`
2. `docs/codex/tasks/ego-k0-verifier-family-downgrade-product-science-decoupling-sync-001a/MUTATION_SCOPE.yaml`

Parse the mutation-scope YAML through the existing guard.

Stage only those two literal paths.

`git add -A` is forbidden.

Require:

git diff --cached --name-status

to show exactly two added paths.

Run:

git diff --cached --check

Commit exactly:

docs: bank EGO K0 verifier-family downgrade sync

After commit record:

- commit and parent;
- exact two-path set;
- both blobs;
- both raw SHA-256 values.

If Phase A fails, do not enter Phase B.

## 13. Phase B — exact seven-path sync

Only these seven paths may change:

1. `docs/PROGRAM_STATE_UNIFIED.yaml`
2. `artifacts/evidence_ledger/index.yaml`
3. `scripts/codex/route_convergence_common.py`
4. `scripts/codex/verify_route_convergence.py`
5. `docs/STATUS.md`
6. `docs/codex/tasks/TASK_LANE_INDEX.md`
7. `artifacts/reports/program_state_summary.md`

The allowed union is not blanket authority.

Do not modify generators, schemas or `program_state_common.py`.

Run the canonical writers:

python scripts/codex/generate_program_state_views.py
python scripts/codex/generate_route_convergence_views.py

Require:

- `docs/REPO_HYGIENE_POLICY.md` unchanged and unstaged;
- `docs/REPO_SURFACE_MAP.md` unchanged and unstaged;
- no eighth changed path.

## 14. Verification and negative controls

Run:

python -m py_compile scripts/codex/route_convergence_common.py scripts/codex/verify_route_convergence.py
python scripts/codex/check_program_state_integrity.py --skip-diff-check
python scripts/codex/verify_route_convergence.py
python scripts/codex/verify_mainline_clarity.py
python scripts/codex/verify_repo.py --mode fast
python scripts/codex_session_guard.py bootstrap --format markdown
git diff --check

Run `verify_route_convergence.py` twice in fresh processes and require the same
stable governance fields.

Run in-memory negative controls through the production verifier path, without
writing files. Each must return failure:

1. change independent acceptance away from `UNAVAILABLE`;
2. set one root authorization true;
3. set one child authorization true;
4. set future product implementation authorization true;
5. change stored-report treatment;
6. restore old K0-R lane to `parked`;
7. remove one negative verdict.

Generated-file drift checks may be isolated only to test the semantic branch;
the actual final verifier runs must use real generated files.

These controls establish local sink fail-closed behavior only. They do not
establish ITL independent acceptance.

Run the scoped closeout guard:

python scripts/codex_session_guard.py --mutation-scope docs/codex/tasks/ego-k0-verifier-family-downgrade-product-science-decoupling-sync-001a/MUTATION_SCOPE.yaml closeout-check --format markdown

Before commit it must show no out-of-scope or unsafe mutation.

After local commits, `push_pending` may remain because remote publication is
explicitly forbidden. Report it honestly; do not repair it by pushing.

## 15. Phase B staging and commit

Stage only the seven literal Phase B paths.

Require:

git diff --cached --name-status
git diff --cached --check

to prove the exact seven-path set.

Commit exactly:

governance: sync EGO K0 closure and product-science decoupling

## 16. Post-commit readback

After Phase B commit:

- rerun read-only program-state integrity;
- rerun route convergence twice;
- rerun mainline clarity;
- rerun fast repo verification;
- rerun bootstrap;
- read both commits from Git objects;
- verify exact phase path sets;
- verify Stage Card and scope blobs/SHA;
- verify the negative ledger entry is unique;
- verify all prior ledger entries/order/bytes are preserved;
- verify Foundation tree/result pins;
- verify all protected runtime/K0 trees;
- verify ITL remains unchanged;
- verify future capability task directory remains absent;
- verify worktree/index is clean.

If upstream is unchanged, final Ego ahead/behind must be:

15/0

ITL must remain:

b67c94fe1244ef6006ed3af8e924d4c670fe64bb
clean
7/0

No push, tag or remote anchor.

STOP after the two commits and return the receipt.

## 17. Stop conditions

STOP without repair or expansion if:

- either repo snapshot drifts;
- either repo is dirty;
- either task path already exists;
- any source/object pin differs;
- Foundation requires rerun or mutation;
- ITL validation would need to be invoked;
- a third verifier repair is proposed;
- a new schema, generator or validator is needed;
- any path outside the phase allowlist changes;
- either protected generated surface changes;
- any authorization becomes true;
- old K0-R remains parked or actionable;
- future capability task/route/code/artifact is created;
- product results gain science weight;
- EgoOperator/EgoDesktop/runtime paths change;
- H0/K0-R/H1/Freeze/Formal execution is proposed;
- push/tag/remote anchor would be required.

## 18. Rollback

Before a phase commit:

- remove only task-owned uncommitted changes from that phase using bounded
  patches;
- never touch pre-existing user work.

After Phase A commit:

- preserve Phase A if Phase B fails;
- report the blocker and STOP.

After Phase B commit:

- do not amend, reset, rebase or revert;
- any correction requires a separately authorized additive card.

No runtime rollback applies because no runtime path changes.

## 19. Acceptance signal

The task is accepted locally only if:

- Phase A is exactly two paths;
- Phase B is exactly seven paths;
- canonical Ego authority and generated views agree;
- old pending/actionable K0 semantics are gone from live sinks;
- Foundation remains bounded accepted and unchanged;
- old K0-R is reference-only and not executed;
- verifier family is closed invalid;
- independent acceptance is unavailable;
- all authorizations remain false;
- future capability route remains absent and unauthorized;
- active default remains EgoOperator;
- negative controls fail closed;
- both repos finish clean;
- no remote action occurs.

Executor maximum verdict:

EGO_K0_GOVERNANCE_SYNCED__VERIFIER_FAMILY_CLOSED_INVALID__INDEPENDENT_ACCEPTANCE_UNAVAILABLE

This verdict means local governance views are synchronized.

It does not mean closure acceptance or scientific adjudication.

## 20. Claim ceiling

Maximum claim:

Ego-local governance mirror synchronization, negative-evidence preservation,
and old-science/future-product-card dependency decoupling.

This does not prove:

- independent validator acceptance;
- science adjudication;
- mechanism validity or invalidity;
- theory falsification;
- learning or adaptation;
- learned-kernel capability;
- product readiness;
- runtime/mainline effect;
- agency or autonomy;
- subjectivity or consciousness;
- EGO readiness;
- stable user benefit.

## 21. Required receipt

Return:

- Verdict
- Layer
- initial/final Ego and ITL branch/HEAD/status/ahead-behind
- docs-only scope clarification
- Phase A commit/parent/path set/blobs/SHA
- Phase B commit/parent/path set
- exact K0 workstream status
- structured governance-sync readback
- all three negative verdicts
- independent acceptance status
- operator route-decision treatment
- Foundation pins
- root/route/child authorization maps
- old K0-R lane and explanation
- future product-route firewall
- ledger entry and prior-entry preservation
- generator/verifier results
- negative-control results
- scoped closeout result including expected push_pending
- protected tree readback
- final clean state
- explicit no push/tag/remote anchor
- next minimal action
- claim ceiling
- what this does not prove

STOP. Do not create the future capability Stage Card.

# Goal Prompt

Use this prompt when starting or refreshing a long-running Codex goal for the
EGO Functional Subject mainline. `Tasks/TASK_BOARD.yaml` and this directory
remain canonical; GitHub Projects are mirror/display only.

```text
Pursue this goal continuously:

Advance EGO's EgoOperator-first Functional Subject mainline until it has
observable, replayable, and iteratively improvable mechanisms for stable
self-model, relationship continuity, affective appraisal, viability state,
outcome prediction, bounded initiative, bounded non-obedience, feedback-linked
reflection, and virtual embodied presence.

Operate in repeated loops:
Think -> Explore -> Experiment -> Evaluate -> Summarize -> Continue.

In every loop:
- read Tasks/TASK_BOARD.yaml and docs/codex/tasks/ego-pursue-functional-subject-goal-v1/STATUS.md;
- write one hypothesis, one strongest counterexample, one minimum experiment,
  evidence paths, failure taxonomy, rollback notes, and next action;
- prefer the current EgoOperator mainline:
  user text -> LLM understanding -> subject signals -> proposal/plan ->
  runtime gate -> trace;
- keep LLM outputs advisory until admitted by runtime gates;
- keep memory promotion, external actions, tool execution, approvals, program
  state, and evidence ledger changes gated;
- use deterministic tests, scripted real-entry trials, GPT-5.5 judge where
  available, and short human sanity smoke only when needed.

Current first gate:
Human sanity review or user-authorized new Stage Card. EGO-FS-078 reconciled a
stale resume objective that still named EGO-FS-059 as the next step; canonical
repo state shows EGO-FS-059 through EGO-FS-077 are already accepted at
local/scripted ceilings. EGO-FS-077 refreshed the six-turn human sanity
packet/proxy gate, but that proxy pass is not user human-feel evidence.
EGO-FS-076 packaged FS072-FS075 into a reviewer verdict and held default
enablement because human sanity evidence, reviewer approval, default-enablement
Stage Card, and longer real-provider observation are missing. The next step
should be a short user-provided human sanity transcript for EGO-FS-053, or an
explicitly authorized new default-enablement Stage Card. Do not implement
default enablement directly.

Do not resume #80/#81 adult-fiction companion work unless the user explicitly
makes it the active blocker again. Do not rerun #94 until the current
Functional Subject mechanism and human sanity gates justify it.

Claim ceiling:
Functional Subject mechanism local/scripted candidate pass. Do not claim real
consciousness, real subjective experience, independent personhood, stable user
benefit, live autonomy, durable memory efficacy, trained SelfWorldModel, or
validated real-world autonomous action.
```

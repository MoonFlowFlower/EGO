# Collision record — EGO-V2-P1-ADDITIVE-PREDICTION-HEADROOM-DIAGNOSTIC-001C-R4

## Candidate collision

| Candidate | Evidence produced | Strongest cheap objection | Smallest falsifier | Expected failure |
|---|---|---|---|---|
| Continue R2/R3 planner optimization | Faster recovery | Optimizes an engineering proxy before the development prediction difference is known | Balanced diagnostic shows no delta improvement | More local speed work without learning evidence |
| Run only aggregate MAE from stored reports | Quick positive/negative number | Stored aggregates are self-asserted and may hide sparse outcomes | Independent row reducer disagrees or one stratum is unsupported | False promotion from majority `no_object` rows |
| Materialize R2 from Git objects | Convenient reproducible source tree | The committed bytes are not the bytes that produced the R2 SQLite code-path hash | Import the snapshot and compare `engine.compute_code_path_hash()` with both DB rows | Apparent provenance cleanliness but unrecoverable banked state |
| Frozen R2 row-level balanced diagnostic | Prediction, support, rank, baseline, ablation, replay evidence | Still does not measure selected-action or survival effect | No-update/legacy matches, ablation insensitive, or support/rank fails | Exact frozen R2 candidate closes or the adjudication surface is rejected before fresh seeds |

## Selection

Select the frozen R2 row-level balanced diagnostic as Stage A.  The previous ordering made
the 10-second recovery and 32KiB trace SLA a prerequisite for learning evidence.
After two boundary failures that ordering is now the wrong target function: it
cannot tell whether the candidate is worth optimizing.  This reframe preserves
both boundary failures while measuring the still-unknown development prediction
difference under frozen R2 controls on already-used data.

The initial pure-Git source plan is rejected.  Its imported engine code-path
hash is `8145a28f...`, while both banked R2 SQLite runs require `b6eadf0c...`.
The selected source route is therefore an explicit lineage-discontinuity
reconstruction: validate the banked R3 tested bundle and tracked diff, reverse
only the R3 engine/predictor changes without normalizing EOLs, and require the
reconstructed R2 engine/predictor hashes plus the exact DB code-path hash before
the evaluator can open a formal cycle.  This uses R3 only as a provenance
carrier; no R3 behavior or result is accepted.

## Cheap-baseline collision

No-update and legacy-unconditional directly challenge conditional-delta
learning and are required now.  They do not exclude a cheaper observation
decoder.  A separately frozen Stage B must test action-only online delta mean,
action-by-visible-front-token lookup, and binary-front-object prediction before
any mechanism-headroom interpretation.  Token-table/graph, action-only
metabolism, and viability-shield baselines remain mandatory before any fresh
survival-effect admission.  R4 cannot grant that admission.

## Stop boundary

One frozen cycle only, at the exact new R4 artifact path.  Any provenance
mismatch, source-reconstruction mismatch, nonempty/wrong output path,
frozen-check contract mismatch, insufficient stratum support,
rank failure, baseline equivalence, insensitive conditioning ablation, or
independent recomputation mismatch stops this adjudication.  Insufficient
support does not falsify the model family.  A valid-instrument negative result
closes only this frozen R2 candidate on the two banked development worlds.  A
positive prediction difference merely permits design of a strong-baseline
admission card and is not yet mechanism headroom.

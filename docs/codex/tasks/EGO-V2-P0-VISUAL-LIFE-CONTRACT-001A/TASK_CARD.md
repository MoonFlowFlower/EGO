# EGO-V2-P0-VISUAL-LIFE-CONTRACT-001A — docs-only task card

- Problem: define the observation/action/world/goal/lifecycle contract and
  collision analysis before modifying the V2 product path.
- Current layer: Layer 2 engineering plus bounded Layer 3 mechanism hypothesis.
- Mainline target: none for this docs-only task; later cards target the existing
  explicit V2 product path.
- Enabled-state requirement: no change; live product axis remains enabled and
  default-off.
- Real-trigger evidence requirement: none for docs; later cards require live
  controller/SQLite/Tk triggers.
- Hypothesis: predeclared interfaces prevent the known fixed-site, semantic,
  path, lifecycle, and leakage shortcuts from being silently retained.
- Strongest baseline: equal-access visual lookup/count-Q/graph/planner.
- Ablation requirement: later cards keep no-memory, freeze-update,
  no-hysteresis, no-override, no-carry, and fixed-position reruns callable.
- Trace/replay requirement: later replay recomputes behavior and lifecycle from
  serialized initial state plus commands; stored actions are comparison-only.
- Computed-evidence provenance gate: later scores record producer function,
  inputs, run/seed/context/episode IDs, aggregation rule, and code-path hash.
- Acceptance: all six scoped docs agree with the user's accepted plan and avoid
  changing runtime code or product/control-plane state.
- Claim ceiling: docs contract only.
- Stop condition: any runtime/ITL/control-plane mutation, science claim, or
  missing collision candidate.
- Rollback: remove only this uncommitted directory.
- Expected changed files: the six files in this directory only.
- Forbidden changes: all product code/tests/artifacts; ITL; route/status/
  program-state/validator paths; push/tag/remote anchor.
- Local scoped commit requested: yes, after review.
- Auto-Remote-Anchor: `forbidden`.

The implementation details are in `PRODUCT_CONTRACT.md`. The three subsequent
cards are `CARD_A_EGOCENTRIC_ECOLOGY.md`, `CARD_B_GOAL_HYSTERESIS.md`, and
`CARD_C_FOUR_LIFE_LIFECYCLE.md`. `COLLISION_RECORD.md` preserves the strongest
shortcut explanation and the stop/downgrade rule.

# EGO-LEARNED-OUTCOME-KERNEL-CAPABILITY-PREFLIGHT-P1-INSTRUMENT-001A

Status:

P1 INSTRUMENT IMPLEMENTATION AND DEV-CONTROL AUTHORIZED
FORMAL PREFLIGHT NOT AUTHORIZED
FORMAL SEEDS FORBIDDEN
FORMAL ARTIFACTS FORBIDDEN
PRODUCT CANDIDATE ABSENT
PRODUCT IMPLEMENTATION NOT AUTHORIZED
ROUTE NOT REGISTERED
DEFAULT-OFF
NON-MAINLINE
RUNTIME AUTHORITY NONE

Auto-Remote-Anchor: forbidden

Push/tag: forbidden

## 0. Authorization envelope

This transaction authorizes only:

1. banking this P1 task card and three phase-specific Mutation Scopes;
2. temporary canonical authorization for exact P1 instrument paths and DEV
   controls;
3. implementation of the frozen candidate-independent preflight instrument;
4. DEV, known-control and planted leakage-control checks;
5. exact instrument commit;
6. mandatory consumption of temporary P1 authorization;
7. governance readback that formal execution remains unauthorized;
8. STOP.

It does not authorize:

- formal train, validation or heldout seed consumption;
- formal preflight execution;
- formal artifact creation;
- surface-admissibility or headroom verdict;
- product candidate or package implementation;
- evidence-ledger entry;
- route registration;
- runtime/mainline/science execution.

## 1. Identity and current layer

Task ID:

EGO-LEARNED-OUTCOME-KERNEL-CAPABILITY-PREFLIGHT-P1-INSTRUMENT-001A

Parent preflight task:

EGO-LEARNED-OUTCOME-KERNEL-CAPABILITY-PREFLIGHT-001A

Repo:

D:\Project\AIProject\MyProject\Ego

Current layer:

Layer 2 — engineering implementation of an evidence instrument

Mainline target:

none

Enabled:

false

Runtime authority:

none

Real trigger evidence:

explicit local DEV instrument-check CLI only

Formal trigger evidence:

absent

Candidate present:

false

Product implementation authorized:

false

Route registered:

false

## 2. Frozen dependencies

Read-only prereg commit:

```text
ff1ab23e1db303a882cec17374b9ea3903fe03c6
```

Read-only files:

```text
PREFLIGHT_STAGE_CARD.md
PREFLIGHT_MUTATION_SCOPE.yaml
PREFLIGHT_CONTRACT.json
```

Frozen contract blob:

```text
d09235eff74ec68b5cc5004873af1b1186d7ee39
```

Frozen contract raw SHA-256:

```text
d9c2d8a12b41ab9b0482b270b63578631b88901ca78854da92853779e95858c3
```

P1 may not modify, supersede or reinterpret the frozen surface, split, seeds,
metrics, MDE, equivalence band, baseline roles, intervention directions,
artifact contract or verdict priority.

If the contract cannot be implemented without ambiguity or contradiction:

```text
P1_BLOCKED_FROZEN_CONTRACT_AMBIGUITY
```

Do not repair it in this task.

## 3. Real objective

Create a callable, fail-able instrument that can later execute the frozen
candidate-independent surface without:

- private-state leakage;
- missing or fake baselines;
- static verdicts;
- stored-output replay;
- formal-seed leakage during P1;
- a separate test-only scoring path.

P1 proves instrument engineering and DEV control behavior only.

## 4. Bounded audit

### Hypothesis

The frozen contract can be implemented as one deterministic callable pipeline
whose generator, legal boundary, oracle/reference separation, baseline panel,
metrics, scanner, interventions, replay and verdict resolver are all fail-able.

### Strongest baseline explanation

A green P1 result may come from:

- baseline names without real implementations;
- one shared predictor presented as independent baselines;
- hard-coded expected instrument verdicts;
- positive controls detected by control IDs rather than the real scanner;
- replay reading stored predictions;
- tests invoking a separate simplified path;
- formal seed consumption hidden behind DEV naming.

### Strongest reason P1 may be invalid

The frozen baseline panel may be too large or internally ambiguous to implement
without stubs, or the fitted meta baseline may fail its frozen adequacy
requirements.

### Falsifiers

P1 is not acceptable if:

- any declared baseline lacks a callable producer;
- fitted MLP does not perform real fit/update work;
- any required planted leakage control is missed;
- formal seed derivation or formal artifact creation occurs;
- private latent/split/seed enters the legal packet;
- fresh-process replay does not regenerate and recompute;
- stored-score mutation changes replay output;
- a static verdict or test-only selector exists;
- a required P1-allowed frozen input is unconsumed.

### Evidence still insufficient

The following do not prove instrument readiness:

- files existing;
- import success;
- green pass-only tests;
- one baseline running;
- parameter hash changes without fit adequacy;
- hard-coded control verdicts;
- a temporary report that echoes expected values.

### Mechanism classification

This task tests evidence-instrument engineering.

It does not test surface headroom, product learning, mechanism validity or
behavioral resemblance.

## 5. Collision record

### A — static fixture report

Evidence appearance:

clean deterministic output.

Failure:

literal verdict and no callable computation.

Disposition:

REJECTED.

### B — partial baseline implementation

Evidence appearance:

fast checks and apparent headroom.

Failure:

weak baseline panel manufactures separation.

Disposition:

REJECTED.

### C — one predictor behind many baseline IDs

Evidence appearance:

complete invocation list.

Failure:

baseline independence is false.

Disposition:

REJECTED.

### D — separate callable baselines, one legal pipeline and real controls

Evidence:

DEV failability, legal-boundary checks, baseline invocation, scanner positive
controls and causal replay.

Disposition:

SELECTED.

### E — implement product learner during P1

Failure:

candidate leakage into a candidate-independent instrument.

Disposition:

REJECTED.

## 6. Exact P1 instrument surface

P1B may create exactly:

```text
scripts/ego_learned_outcome_kernel_capability_preflight_001a/
  __init__.py
  contract.py
  workload.py
  oracles.py
  baselines.py
  metrics.py
  leakage.py
  replay.py
  producer.py

scripts/run_ego_learned_outcome_kernel_capability_preflight_001a.py
tests/test_ego_learned_outcome_kernel_capability_preflight_001a.py
```

No package or repo artifact directory may be created.

## 7. Module responsibilities

### contract.py

Must:

- load the prereg bytes from the pinned Git object;
- verify working bytes equal committed bytes;
- verify blob and SHA-256;
- validate required schema and authorization state;
- expose frozen values without redefining thresholds in source;
- implement phase-aware frozen-input and seed-consumption ledgers;
- distinguish P1-allowed from P2-only inputs.

### workload.py

Must:

- enumerate the frozen latent/context/action spaces;
- implement split rules exactly;
- derive domain-separated seeds;
- generate DEV legal histories;
- maintain separate private evaluator and legal packet records;
- reject private fields in legal serialization;
- implement descriptor-keyed action-order invariance.

### oracles.py

Must separately implement:

- illegal private-z diagnostic;
- exact legal 256-state Bayes ceiling;
- 128-particle legal SMC reference;
- legal-target checks;
- private-z swap and history-twin counterfactuals.

The illegal diagnostic must never enter admission aggregation.

### baselines.py

Must provide independent callable producers for:

- all 20 invalidating controls;
- the feasible SMC reference;
- all five capability rivals;
- all diagnostic controls required by the contract.

A registry may point to callable functions/classes, but baseline IDs may not
alias one shared prediction implementation.

Shared parsing or feature-extraction helpers are allowed.

Every invocation records:

- baseline ID;
- producer function;
- legal input hashes;
- fit call count;
- seed domain;
- resource rows/iterations;
- output hash;
- failure status.

Any missing, failed or inadequate mandatory baseline maps to:

```text
PREFLIGHT_INSTRUMENT_INVALID_BASELINE_PANEL
```

It must not be treated as a low baseline score.

The fitted history meta MLP must use the frozen sklearn configuration and
record:

- real `fit` call;
- before/after parameter hashes;
- `n_iter_ >= 10`;
- loss curve;
- final loss at most `0.95 * first loss`.

### metrics.py

Must implement from contract values:

- expected proper Brier risk;
- decision value;
- paired task-level bootstrap;
- equal-weight checkpoint AUC;
- non-compensating two-axis gate;
- verdict resolver;
- a separately written raw-row aggregation implementation.

No best-checkpoint selection or composite compensation is allowed.

Verdict strings may be constants, but selection must derive from computed gate
inputs and have failure-path tests.

### leakage.py

Must implement one scanner used for both clean DEV packets and all four planted
controls.

It must not branch on positive-control IDs.

It must scan field names, values, structure, order, path/name metadata,
split/seed overlap, serialized state and generator coupling.

### replay.py

Must launch two fresh DEV subprocesses that:

- reload the pinned contract;
- regenerate DEV workload from DEV seed derivation;
- refit fitted baselines;
- recompute predictions, metrics and DEV instrument status;
- execute legal-history/private-state counterfactuals.

Replay must ignore stored scores and predictions.

P1 replay derives its subprocess salt from DEV domains. It must not consume
`fresh_replay_one`, `fresh_replay_two` or any formal domain.

### producer.py

Must expose one computation pipeline for DEV and future formal modes.

Mode may control authorization, allowed split/seed and output boundary only.

Generator, baseline, scanner, metric, intervention and verdict logic must not
fork into a test-only or formal-only second implementation.

P1 `instrument-check` output must resolve outside the repo in an explicitly
verified temporary directory.

### runner

Must expose:

```text
--mode instrument-check
--mode formal
```

P1 may execute only `instrument-check`.

Formal mode must fail before seed derivation or output creation unless a future
committed P2 authorization object is verified.

## 8. Formal authorization guard

Formal mode must require a future committed file at:

```text
docs/codex/tasks/ego-learned-outcome-kernel-capability-001a/P2_FORMAL_AUTHORIZATION.json
```

The guard must verify from Git objects:

- P2 task ID;
- `formal_preflight_authorized=true`;
- prereg commit/blob/SHA;
- exact P1 instrument commit;
- formal output path;
- `formal_run_limit=1`;
- candidate/product/runtime/mainline/science authorization false;
- P2 authorization commit is an ancestor of current HEAD;
- P1 instrument paths are byte-identical to the pinned P1 commit;
- worktree/index is clean;
- formal output directory does not exist.

No static token or current-HEAD self-assertion may substitute for a committed
P2 authorization object.

P1 tests exercise only rejection paths. A successful formal authorization path
is reserved for P2.

## 9. P1 seed firewall

P1 may instantiate RNGs and generate workload only from:

```text
dev_history
dev_outcome
dev_action_order
leakage_positive_control
```

DEV replay salts must be derived beneath these DEV domains.

The following may be parsed as names but may not be consumed:

```text
train_history
train_outcome
train_action_order
validation_history
validation_outcome
validation_action_order
formal_history
formal_outcome
formal_action_order
fresh_replay_one
fresh_replay_two
```

Consumption means RNG instantiation, workload generation, fitting, prediction
or scoring.

P1 must not falsely mark P2-only inputs as consumed.

## 10. Required DEV checks

Focused tests must cover:

1. pinned contract/blob/SHA verification;
2. schema and frozen-value consumption;
3. seed determinism and uniqueness;
4. zero P2-only seed consumption;
5. split mathematics without formal workload generation;
6. legal/evaluator field separation;
7. private-z swap invariance;
8. action-order invariance;
9. history-twin posterior change;
10. exact Bayes and SMC DEV sanity;
11. every baseline callable and invoked;
12. fitted MLP real-fit adequacy;
13. baseline fit failure maps to instrument invalid;
14. all three known instrument controls;
15. one scanner on clean DEV plus four positive controls;
16. feedback removal, shuffle and action/outcome alignment interventions;
17. Brier and decision metrics remain separate;
18. task-level paired bootstrap and fixed checkpoints;
19. no best-checkpoint selection;
20. two fresh DEV replay subprocesses;
21. stored-score mutation cannot alter recomputation;
22. legal-state mutation does alter recomputation;
23. unused P1-required input fails closed;
24. formal mode refusal occurs before formal seed/output access;
25. no candidate/product/Foundation/old K0-R imports;
26. no static clean report or test-only selector.

Tests must assert producer invocation, not only final status.

## 11. P1 computed provenance boundary

Temporary DEV reports record:

- producer functions;
- input and code hashes;
- run ID;
- DEV task/episode/checkpoint IDs;
- seed domains;
- aggregation;
- invocation IDs;
- `computed_not_literal=true`;
- phase-aware frozen-input consumption.

Before the P1B commit, code hashes are provisional and must not be called
committed provenance.

After P1B commit, rerun DEV checks from the clean committed code path and record
the P1B commit in temporary output/readback.

Temporary DEV output is not an experiment artifact and must not be committed.

## 12. P1A temporary governance authorization

P1A may set only task-local evidence-instrument authority:

```text
instrument_implementation_authorized=true
dev_instrument_execution_authorized=true
formal_preflight_authorized=false
candidate_preflight_authorized=false
implementation_authorized=false
route_registered=false
enabled=false
mainline_connected=false
runtime_authority=none
authorized_implementation_targets=[]
```

It must add an exact `authorized_preflight_instrument_targets` list containing
the 11 P1B paths.

This does not authorize product implementation.

P1C must consume the temporary authority whether P1B succeeds or is blocked.

## 13. P1C final governance state

Successful P1B:

```text
P1_INSTRUMENT_BANKED__FORMAL_EXECUTION_NOT_AUTHORIZED
```

Blocked P1B:

```text
P1_INSTRUMENT_BLOCKED__AUTHORIZATION_CONSUMED__FORMAL_EXECUTION_NOT_AUTHORIZED
```

In both cases final authority must be:

```text
instrument_implementation_authorized=false
dev_instrument_execution_authorized=false
formal_preflight_authorized=false
candidate_preflight_authorized=false
implementation_authorized=false
authorized_preflight_instrument_targets=[]
authorized_implementation_targets=[]
route_registered=false
enabled=false
mainline_connected=false
runtime_authority=none
```

Evidence ledger remains unchanged because no formal evidence exists.

## 14. Acceptance gate

P1 succeeds only if:

- P1A is the exact authorization/docs commit;
- P1B contains exactly the 11 instrument/test paths;
- frozen prereg objects remain unchanged;
- all declared baseline callables exist;
- focused DEV checks pass;
- post-P1B clean committed-code checks pass;
- formal seed consumption is empty;
- formal artifact path remains absent;
- no candidate/product package exists;
- P1C consumes temporary authority;
- canonical governance and generated views agree;
- both repos finish clean;
- no remote action occurs.

Maximum success verdict:

```text
EGO_LEARNED_OUTCOME_KERNEL_CAPABILITY_PREFLIGHT_P1_INSTRUMENT_BANKED__FORMAL_NOT_RUN_NOT_AUTHORIZED
```

This does not mean the frozen surface passed preflight.

## 15. Stop conditions

STOP or route to P1C blocked closeout if:

- repo/object drift occurs;
- prereg mutation is required;
- a baseline is omitted, aliased, unavailable or inadequate;
- contract ambiguity cannot be resolved from frozen text;
- dependency installation is required;
- any P2-only seed is consumed;
- formal artifact path is created;
- formal mode is successfully entered;
- scanner misses a positive control;
- replay reads stored output;
- static score/verdict or second scoring path appears;
- product candidate/package, Foundation, old K0-R, ITL, EgoOperator or
  EgoDesktop would change;
- route/runtime/mainline/science authorization becomes true;
- threshold, split, seed, metric or surface tuning is proposed;
- push/tag/remote anchor is required.

If P1B is blocked after P1A, do not leave temporary authority active. Complete
P1C blocked closeout.

## 16. Rollback

Before P1A commit:

- remove only task-owned P1A changes through bounded patches.

After P1A commit and before P1B commit:

- ordinary code defects may be corrected using DEV data only;
- if the frozen contract would need change, remove only uncommitted P1B paths,
  perform P1C blocked closeout and STOP.

After P1B commit:

- do not amend/reset/rebase;
- postcommit failure is recorded in P1C;
- never create a formal artifact.

After P1C:

- any change requires a new bounded task.

## 17. Exact phase paths

P1A:

```text
docs/codex/tasks/ego-learned-outcome-kernel-capability-001a/P1_INSTRUMENT_TASK_CARD.md
docs/codex/tasks/ego-learned-outcome-kernel-capability-001a/P1A_AUTHORIZATION_MUTATION_SCOPE.yaml
docs/codex/tasks/ego-learned-outcome-kernel-capability-001a/P1B_INSTRUMENT_MUTATION_SCOPE.yaml
docs/codex/tasks/ego-learned-outcome-kernel-capability-001a/P1C_CLOSEOUT_MUTATION_SCOPE.yaml
docs/PROGRAM_STATE_UNIFIED.yaml
scripts/codex/verify_route_convergence.py
docs/STATUS.md
artifacts/reports/program_state_summary.md
```

P1B:

the exact 11 instrument/test paths in section 6.

P1C:

```text
docs/PROGRAM_STATE_UNIFIED.yaml
scripts/codex/verify_route_convergence.py
docs/STATUS.md
artifacts/reports/program_state_summary.md
```

## 18. Next action

After P1C, STOP.

If P1 instrument is banked and postcommit DEV checks succeeded, the next
possible action is a separately committed P2 formal authorization object and
one formal run.

## 19. Claim ceiling

P1 may claim only:

- isolated instrument implementation;
- DEV failability and control-path checks;
- formal guard existence;
- no formal execution.

It does not prove:

- surface admissibility;
- fair headroom;
- product learning or adaptation;
- candidate feasibility;
- mechanism validity;
- science attribution;
- product readiness;
- runtime/mainline effect;
- stable user benefit;
- agency, autonomy, subjectivity or consciousness.

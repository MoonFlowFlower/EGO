# EGO-LEARNED-OUTCOME-KERNEL-COMPOSITIONAL-TRANSFER-PREFLIGHT-001A

Status:

CAOT-v1 SELECTED BY OPERATOR FOR FRESH PREREG ONLY
P0A DOCS-ONLY PREREG DRAFT BANK AUTHORIZED
CALIBRATION VARIANCE AND HELDOUT SAMPLE-SIZE FREEZE PENDING
INSTRUMENT IMPLEMENTATION AND EXECUTION NOT AUTHORIZED
CANDIDATE ABSENT
UNREGISTERED REFERENCE-ONLY
DEFAULT-OFF
NON-MAINLINE
RUNTIME AUTHORITY NONE

Auto-Remote-Anchor: forbidden

Push/tag: forbidden

## 0. Authorization envelope

This task authorizes exactly:

1. record the explicit operator directive `SELECT COMPOSITIONAL-ACTION-OUTCOME-TRANSFER-SURFACE-v1`;
2. bank this surface-specific Stage Card;
3. bank one exact Mutation Scope;
4. bank one executable-semantics P0A contract whose numeric gate remains fail-closed pending a separately authorized calibration-only computation;
5. regenerate `docs/codex/tasks/TASK_LANE_INDEX.md` through the pinned canonical route-convergence renderer, adding only the prior selection packet and this prereg directory as reference-only entries;
6. replace exactly once, and only once, the lane-index protected-object blob pin in `scripts/codex/verify_route_convergence.py` from `c53712f36d19c0e8bf1166937c5977e5a00d39d1` to `8ea76105e933c5db3bba784231a64d251a4fbe55`;
7. create one exact `3 A + 2 M` local commit;
8. STOP.

The only feature/route authorization is `docs_only_prereg_bank=true`. The lane-index mutation and its one-literal protected-object pin refresh are bounded transactional convergence maintenance only; they create no route registration, enablement, implementation authority or independent validator acceptance. This does not reopen or repair the closed K0 terminal-verifier family.

This task does not authorize:

- Program State, evidence-ledger, STATUS, hygiene/surface-map or any generated-view mutation other than the exact canonical `TASK_LANE_INDEX.md` convergence;
- route-verifier logic, control flow, verdict behavior, protected objects other than the single lane pin, or any verifier byte change beyond the exact old-to-new lane-pin replacement;
- route registration or enablement;
- generator, baseline, reference, scanner, replay, producer, tests or runner implementation;
- calibration, power, headroom, probe, DEV, formal or artifact execution;
- candidate or product package creation;
- runtime, mainline, UI, LLM, network or science work;
- old K0-R/H0/H1/Freeze/Formal authority or fixture import;
- closed FACTORED-STOCHASTIC-OUTCOME-SURFACE-v1 reuse or repair;
- Foundation or ITL mutation/rerun;
- push, tag or remote anchor.

## 1. Identity and layer

Task ID:

EGO-LEARNED-OUTCOME-KERNEL-COMPOSITIONAL-TRANSFER-PREFLIGHT-001A

Surface ID:

COMPOSITIONAL-ACTION-OUTCOME-TRANSFER-SURFACE-v1

Current layer:

Layer 2 evidence/design governance plus Layer 3 bounded surface hypothesis.

Mainline target:

none.

Enabled-state requirement:

false before, during and after this transaction.

Real-trigger evidence:

the explicit operator selection and pinned Git objects only. No runtime, learning, mechanism or product trigger exists.

Claim ceiling:

docs-only CAOT-v1 surface semantics, identifiability contract and future evidence requirements.

## 2. Real objective and problem definition

The objective is to freeze a genuinely new candidate-independent surface for testing whether legal source-task experience can support prediction on unseen typed action compositions and whether legal heldout-task outcome feedback adds independent adaptation value.

This task must not convert the paper recommendation into empirical admission. It must also avoid the prior P1R0 failure mode: generic non-empty mappings, static verdicts and copied IDs are insufficient. Every future module, baseline, intervention, trace and failure path is mapped to exact contract pointers and callable names now.

Relevant prior negative evidence is preserved, not repaired: the stored P1R0 FEASIBLE output remains historical, while the pinned callable audit independently computed 23 blocking findings and invalidated admission for the old FACTORED-STOCHASTIC-OUTCOME-SURFACE-v1 design family. That evidence closes the old instrument lineage only; it records `surface mathematical status=NOT_ADJUDICATED` and does not prove the old or new surface impossible. CAOT-v1 uses a fresh task ID and semantics but inherits no authority, fixture, threshold, verdict or science attribution from that family.

## 3. Bounded audit

### Hypothesis

A source-trained compositional predictor plus a bounded task-local residual update may expose two non-compensating axes:

1. forward transfer to exact-zero-overlap typed compositions;
2. within-task adaptation from legal feedback to unqueried probe compositions.

### Strongest baseline explanation

The effect may be fully explained by source-trained current-packet prediction, canonical lookup, primitive-additive factorization, a stateless legal full-history decoder or a simple residual update. These are not all the same claim role and must not be collapsed into one baseline verdict.

### Strongest reason the framing may be invalid

The legal descriptors may make the global outcome structure directly decodable, the heldout set may contain canonical or alias overlap, or the task-local residual may be unidentifiable from the query schedule. A legal full-history rival may also show that explicit mutable online state is unnecessary even if lower-scope product capability exists.

### Falsifiers

The surface is inadmissible for future candidate drafting if any of the following later occurs:

- canonical probe overlap with source, calibration or adaptation-query compositions;
- the legal descriptor/history channel cannot support powered prediction or adaptation headroom without private fields;
- the adaptation-query residual design is rank-deficient;
- the probe residual design is outside the query span;
- the budget-faithful feasible reference violates legal access/resource parity;
- lookup/no-meta controls saturate forward-transfer headroom;
- legal history adds no powered incremental information on the adaptation axis;
- frozen/no-feedback/feedback-shuffled controls saturate adaptation headroom;
- any planted leakage control is missed;
- fresh-process replay cannot recompute from serialized state plus legal inputs;
- either axis is underpowered.

### Evidence still insufficient

This card, contract, JSON parse, repo checks, commit, state hashes or future falling training loss do not prove headroom, surface admission, learning, mutable-state necessity, mechanism validity or product readiness.

### Mechanism versus resemblance

This is a future bounded offline adaptation surface. It is not a dialogue, persona, agency or subjectivity test.

## 4. Collision record

### A — implement the learned candidate now

Rejected. Candidate-independent legal-channel headroom is unknown.

### B — reuse the closed fixed-action/static-latent surface

Rejected. Same-surface reuse is forbidden and would be a metadata bypass.

### C — bank another generic design and feasibility linter

Rejected. It repeats the closed P1/P1R0 design-governance loop.

### D — freeze one complete CAOT-v1 semantic and numeric-selection contract, then run one variance-only sample-size freeze

Selected. It fixes surface math, role-specific baselines, identifiability tests, replay/leakage requirements and fail-closed numeric status without inventing thresholds.

## 5. Surface and legal-channel contract

`PREFLIGHT_CONTRACT.json` is the single source of truth.

The surface uses three typed primitive roles with four public primitive tokens per role, four public contexts and 64 typed compositions. Source, numeric-calibration, adaptation-query and unqueried-probe composition partitions are structurally disjoint.

For task `tau`, context `c` and typed composition `a`:

```text
eta_tau(c,a) = theta^T phi(c,a) + u_tau^T psi(a)
p_tau(y=1|c,a) = sigmoid(clip(eta_tau(c,a), -3, 3))
y ~ Bernoulli(p_tau)
```

`theta` is shared source structure. `u_tau` is a new task-local residual. Public descriptors and legal feedback are model-visible; realized parameters, true probabilities, task/split/seed identities and verdicts are evaluator-only.

The future candidate-independent screen must enforce:

```text
rank(Psi_adaptation_query) = 4
span(Psi_probe) subseteq span(Psi_adaptation_query)
canonical(probe) intersection canonical(source union calibration union adaptation_query) = empty
```

The separate numeric-freeze pilot must use only the frozen `s=2` calibration query/probe partition in `numeric_calibration_episode_contract`: exact 8/8 disjoint compositions, exact 16 legal-feedback schedule, exact five checkpoints, and the same 32 evaluator-only probe/context predictions at every checkpoint. It must not reuse heldout `s=3` episodes or invent a post-hoc calibration split.

## 6. Checkpoint-specific baseline roles

Baseline roles cannot be reassigned after results.

Forward-transfer axis at checkpoint zero:

- the source-trained current-packet compositional model is the feasible reference, not an observation-only invalidating control;
- invalidating controls include constant/global mean, frozen counter-based random probability, exact lookup/count, graph lookup, episodic traversal, nearest-neighbor and no-meta/from-scratch;
- a capacity/data-matched source-trained current-packet rival may challenge specialness but does not make the axis impossible by definition.

Adaptation axis at later checkpoints:

- the frozen source-trained model is a no-update invalidating control;
- no-feedback/history-deleted and feedback-shuffled variants are invalidating controls;
- a fitted legal full-history decoder is a capability rival/mechanism challenger.

If the fitted legal full-history rival is powered-equivalent to the live residual updater, the later orthogonal field must be `mechanism_attribution_status=MUTABLE_UPDATE_NOT_REQUIRED`. That does not overwrite a separately computed surface verdict, but it forbids a mutable-online-update necessity claim.

The future candidate-own batch/offline rule is `DEFERRED_REQUIRED_AT_FUTURE_CANDIDATE_CARD`; it must not be fabricated while candidate absent.

Because this is a typed-compositional representation/environment surface, the pinned graph-cache family cannot be silently omitted. `graph_lookup`, `count_table` and `episodic_traversal` are applicable independent controls. `transition_table`, `successor_map` and `fsm_planner` are fail-closed N/A only because the frozen schema has no next-state, action-conditioned transition, successor feature, model-selected action or multi-step return; any later schema addition reopens applicability and blocks execution rather than inheriting N/A.

## 7. Numeric freeze boundary

No empirical result may be used to choose a favorable gate. This P0A contract therefore fixes before calibration:

- forward and adaptation Brier SESOI;
- full-history equivalence band;
- familywise alpha, multiplicity method, target power and CI method;
- source/calibration task counts, rows, replicates and feedback checkpoints;
- optimizer, regularization, compute and memory budgets;
- a closed heldout-task-count grid;
- the exact variance-only sample-size formula and smallest-sufficient-N rule.

The only pending value is the heldout evaluation task count selected from the frozen grid using calibration task-cluster variance, never the observed reference/control gap. Status:

PENDING_CALIBRATION_VARIANCE_AND_SAMPLE_SIZE_FREEZE

No surface/headroom execution may be authorized until a separately authorized numeric-freeze computation produces the exact immutable evidence bundle plus additive record at the frozen paths/schemas and source-pins this prereg. That future numeric-freeze implementation is itself executable instrument work and remains unauthorized in this transaction.

Underpowered must map to `INCONCLUSIVE_UNDERPOWERED`, never equivalence or no effect.

## 8. Future computed-evidence requirements

Any later executable task must use independent callable implementations and record:

- producer_function;
- input artifacts and exact hashes;
- run_id;
- seed/task/context/episode/checkpoint IDs;
- aggregation rule;
- code-path hash;
- every baseline/reference/intervention invocation;
- exact consumed contract pointers;
- exact failure status.

Replay must launch fresh processes and recompute predictions and update behavior from serialized state plus legal inputs. Stored predictions, stored actions and hash-only replay are forbidden.

Leakage scanning must inspect field names, values, structural order, typed canonical aliases, filenames, artifact paths, serialized state, task/split/seed IDs and private parameters, with planted positive controls.

## 9. Expected changed files

Exactly three additions:

- docs/codex/tasks/ego-learned-outcome-kernel-compositional-transfer-preflight-001a/STAGE_CARD.md
- docs/codex/tasks/ego-learned-outcome-kernel-compositional-transfer-preflight-001a/MUTATION_SCOPE.yaml
- docs/codex/tasks/ego-learned-outcome-kernel-compositional-transfer-preflight-001a/PREFLIGHT_CONTRACT.json

Exactly one generated-view modification:

- docs/codex/tasks/TASK_LANE_INDEX.md

Exactly one protected-pin maintenance modification:

- scripts/codex/verify_route_convergence.py

The lane diff must contain only:

- `reference_only` count `232 -> 234`;
- the already-banked new-surface-selection directory as reference-only;
- this new CAOT-v1 prereg directory as reference-only.

The verifier diff must contain exactly one replacement and no other byte change:

- old lane blob pin `c53712f36d19c0e8bf1166937c5977e5a00d39d1`;
- new lane blob pin `8ea76105e933c5db3bba784231a64d251a4fbe55`.

No Program State, route rule/common-renderer, verifier logic/control-flow/verdict, other protected pin, STATUS, ledger, hygiene policy or surface-map byte may change.

Commit subject:

governance: bank CAOT-v1 prereg and converge lane view

## 10. Acceptance gate

Accept only if:

- Step 0 matches exactly;
- the only initial governance failure is the source-pinned single lane-index drift described in Step 0;
- explicit operator selection matches the pinned selectable candidate;
- exact three target paths were absent at parent HEAD;
- JSON parses with the standard library and contains no null/TBD fields;
- all surface, split, role, legal-boundary, module, replay, leakage, provenance, artifact, verdict and stop fields are concrete;
- the only unresolved numeric value is heldout_eval_task_count and its deterministic variance-only selection record;
- no empirical score or admission verdict is present;
- all execution/route/runtime/mainline/science authorizations remain false;
- canonical sinks and all prior evidence remain byte-identical, except the authorized derived lane index and its exact one-literal protected-object pin refresh;
- canonical regeneration yields exact final normalized-LF SHA-256 `3751d1318a6a784929f9df2582db3222de3644c86b708b33bf4a0444d134753d` and Git blob `8ea76105e933c5db3bba784231a64d251a4fbe55` for the lane index;
- the committed verifier is exactly 47108 bytes with raw SHA-256 `581d560f6041f4efc2e7840e24a4c1e692b785acf255671af5464a331dda366e` and Git blob `f941817ea1d2e09e94d5709e4bfe86a8a4d374ee`, produced only by the one-literal pin replacement;
- postcommit program-state integrity, route convergence, mainline clarity and fast repo verification all pass;
- exact `3 A + 2 M` paths are committed;
- final Ego worktree/index are clean and ahead/behind is 25/0 if upstream is unchanged;
- ITL remains unchanged at 7/0;
- no remote action occurs.

Maximum verdict:

CAOT_V1_P0A_PREREG_DRAFT_BANKED__CANDIDATE_ABSENT__EXECUTION_BLOCKED_PENDING_SAMPLE_SIZE_FREEZE

Design-blocked verdict:

CAOT_V1_PREREG_BLOCKED_AT_DESIGN__NO_EXECUTION_AUTHORITY

## 11. Stop conditions

STOP without commit if:

- repo/source/selection drift occurs;
- operator selection is missing or ambiguous;
- old surface, generator, splits, seeds, fixtures, thresholds or verdict authority is inherited;
- any legal/evaluator-only boundary is ambiguous;
- composition partitions or alias rules are incomplete;
- residual query rank/span requirements are absent;
- baseline roles collapse rivals into invalidating controls;
- feasible reference and diagnostic ideal roles are mixed;
- any nonnumeric frozen field is null/TBD/generic;
- an empirical threshold, score or verdict is invented;
- any implementation/test/artifact/canonical-governance path changes other than the exact generated lane index and exact one-literal lane-pin refresh;
- any verifier byte changes beyond the exact one-literal lane-pin replacement, or any attempt to reopen the closed terminal-verifier repair family;
- push, tag or remote anchor is attempted.

## 12. Anti-Zeno and rollback

This task has one docs-only/governance-convergence transaction. It creates no custom verifier and no P1/P1R0 or terminal-verifier repair family. The static lane-pin refresh is maintenance required by the newly generated Git object, not a validator-acceptance claim or logic repair.

Before commit:

- if staged, unstage only the exact three task paths plus `docs/codex/tasks/TASK_LANE_INDEX.md` and `scripts/codex/verify_route_convergence.py`;
- remove only the three task-created paths using bounded apply_patch deletion;
- restore only `docs/codex/tasks/TASK_LANE_INDEX.md` from parent HEAD after proving the initial worktree was clean;
- restore only `scripts/codex/verify_route_convergence.py` from parent HEAD after proving its diff is the exact authorized literal replacement;
- verify return to the Step 0 clean state.

After commit:

- do not amend/reset/rebase;
- preserve the banked draft;
- a semantic defect closes this CAOT-v1 prereg under this task ID rather than starting a repair family;
- the only possible next task is one separately authorized calibration-variance/sample-size freeze computation;
- after numeric freeze, at most one candidate-independent headroom-instrument attempt is allowed;
- instrument invalidity, control saturation or no headroom preserves negative evidence and closes CAOT-v1 without candidate implementation.

## 13. Claim ceiling and next action

Allowed:

- operator selection recorded in a new surface-specific contract;
- docs-only surface semantics and future evidence requirements banked;
- numeric execution explicitly blocked pending independent freeze.

Not allowed:

- empirical headroom or surface admission;
- implementability;
- learning/adaptation result;
- mutable-kernel necessity;
- mechanism validity;
- product/runtime/mainline readiness;
- agency, autonomy, subjectivity or consciousness.

After commit, STOP and return the receipt. Do not draft or execute the sample-size freeze task in the same session.

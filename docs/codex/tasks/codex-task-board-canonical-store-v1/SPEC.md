# Codex Task Board Canonical Store + GitHub Sync Mirror v1

## Goal

Make `Tasks/TASK_BOARD.yaml` the canonical task state for Autopilot, with GitHub Issues/Projects acting only as a low-frequency display mirror.

## Non-Goals

- Do not change `EgoOperator` runtime behavior.
- Do not modify `docs/PROGRAM_STATE_UNIFIED.yaml` or `artifacts/evidence_ledger/**`.
- Do not use GitHub Projects as the high-frequency source of truth.

## Claim Ceiling

`Codex local canonical task board + GitHub mirror sync workflow candidate pass`.

# Plan

1. Add a deterministic helper that runs an isolated CLI-compatible memory lifecycle script.
2. Attach the helper output to the Functional Subject report and GPT-5.5 judge packet.
3. Add regression tests for save/retrieval, approval, correction quarantine, and forget evidence.
4. Run targeted tests and `autopilot_full`.

## Non-Goals

- No memory authority promotion.
- No cross-session durable efficacy claim.
- No provider/model change.
- No program-state or evidence-ledger update.

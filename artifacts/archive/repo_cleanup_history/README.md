# Repo Cleanup History Archive

This directory preserves the first admitted medium-migration slice of historical repo-cleanup planning outputs.

Current contents:
- `P0` through `P7`

Rules:
- These paths are historical provenance only, not current authority or current evidence.
- The canonical lookup surface is `docs/archive/ARCHIVE_INDEX.yaml`.
- If an old doc still points at `artifacts/P0` through `artifacts/P7`, update the reference instead of moving current evidence into this tree.

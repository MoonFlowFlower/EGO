# Task Closure Records

本目录用于保存已结束任务的 `TaskClosureRecord`。

推荐文件名：

- `{task_id}-closure.json`

生成骨架：

```bash
python3 scripts/codex_memory.py create-task-closure --task-id <TASK_ID> --title "<TITLE>"
```
